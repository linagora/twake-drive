---
phase: 09-internationalization
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
  - src/modules/views/OnlyOffice/ScribeModal.jsx
autonomous: true
requirements:
  - I18N-01
  - I18N-03

must_haves:
  truths:
    - "No hardcoded French or English UI strings remain in any Scribe component"
    - "All Scribe components resolve display text via t() from useI18n"
    - "ScribePopover resolves loading message keys and error message keys via t()"
    - "Breadcrumbs in ScribePopover are built from translated labels"
    - "'Scribe' brand name and '(Ctrl+I)' shortcut remain as literal strings"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx"
      provides: "Translated tooltip text"
      contains: "useI18n"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx"
      provides: "Translated action labels and input placeholders"
      contains: "t(action.labelKey)"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx"
      provides: "Translated prompt placeholder"
      contains: "useI18n"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "Translated button labels"
      contains: "useI18n"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "t() resolution of loading keys, error keys, and breadcrumbs"
      contains: "useI18n"
    - path: "src/modules/views/OnlyOffice/ScribeModal.jsx"
      provides: "Translated modal labels"
      contains: "useI18n"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      via: "t(deriveLoadingMessage().key) and t(classifyScribeError().messageKey)"
      pattern: "t\\(.*\\.key\\)|t\\(.*\\.messageKey\\)"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/scribeActions.js"
      via: "t(action.labelKey) for display text"
      pattern: "t\\(.*labelKey\\)"
---

<objective>
Wire all 6 Scribe JSX components to use `t()` from `useI18n` for every user-facing string, replacing all hardcoded French and English text.

Purpose: Complete the i18n integration so that switching Cozy locale changes all Scribe UI text.
Output: 6 updated JSX components with zero hardcoded UI strings (except brand name "Scribe" and shortcut "(Ctrl+I)")
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-internationalization/09-CONTEXT.md
@.planning/phases/09-internationalization/09-RESEARCH.md
@.planning/phases/09-internationalization/09-01-SUMMARY.md

@src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx
@src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx
@src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx
@src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
@src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
@src/modules/views/OnlyOffice/ScribeModal.jsx

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes -->

From scribeActions.js (after Plan 01):
- SCRIBE_ACTIONS[].labelKey: string (i18n key like 'Scribe.menu.correct_grammar')
- SCRIBE_ACTIONS[].children[].labelKey: string | null (null for translate children that use native label)
- Translate children have: label (native lang name), labelKey: null
- Custom input child has: placeholderKey: 'Scribe.translate.other_language'
- buildTranslateChildren(accountLang) returns children with label + labelKey fields

From scribeAI.js (after Plan 01):
- deriveLoadingMessage(actionId, label) returns { key: string, params?: object }
- classifyScribeError(err) returns { messageKey: string, canRetry: boolean }

From locale JSON files:
- t('Scribe.button.text_ai') -> "Text AI" / "IA Texte" etc.
- t('Scribe.button.insert') -> "Insert" / "Insérer" etc.
- t('Scribe.button.replace') -> "Replace" / "Remplacer" etc.
- t('Scribe.button.retry') -> "Retry" / "Réessayer" etc.
- t('Scribe.button.cancel') -> "Cancel" / "Annuler" etc.
- t('Scribe.button.insert_after') -> "Insert After" / "Insérer après" etc.
- t('Scribe.modal.selected_text') -> "Selected text:" / "Texte sélectionné :" etc.
- t('Scribe.prompt.placeholder') -> "Help me write" / "Aidez-moi à écrire" etc.
- t('Scribe.translate.other_language') -> "Other language..." / "Autre langue..." etc.
- t('Scribe.translate.translating_to', { language }) -> "Translating to %{language}..."
- t(loadingResult.key, loadingResult.params) for loading messages
- t(errorResult.messageKey) for error messages
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ScribeFloatingButton, ScribePromptInput, ScribeResultPanel, and ScribeModal to use t()</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx, src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx, src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx, src/modules/views/OnlyOffice/ScribeModal.jsx</files>
  <action>
**ScribeFloatingButton.jsx:**
1. Add `import { useI18n } from 'twake-i18n'`
2. Add `const { t } = useI18n()` inside the component
3. Replace `"Text AI"` with `{t('Scribe.button.text_ai')}`
4. Keep `"(Ctrl+I)"` as literal (shortcut, not translated per user decision)
5. Keep `"Scribe"` as literal (brand name, not translated per user decision)

**ScribePromptInput.jsx:**
1. Add `import { useI18n } from 'twake-i18n'`
2. Add `const { t } = useI18n()` inside the forwardRef component
3. Replace `placeholder="Help me write"` with `placeholder={t('Scribe.prompt.placeholder')}`

**ScribeResultPanel.jsx:**
1. Add `import { useI18n } from 'twake-i18n'`
2. Add `const { t } = useI18n()` inside the component
3. Replace `label="Retry"` with `label={t('Scribe.button.retry')}`
4. Replace `label="Replace"` with `label={t('Scribe.button.replace')}`
5. Replace `label="Inserer"` with `label={t('Scribe.button.insert')}` (fixes the hardcoded French bug)

**ScribeModal.jsx:**
1. Add `import { useI18n } from 'twake-i18n'`
2. Add `const { t } = useI18n()` inside the component
3. Keep `title="Scribe"` as literal (brand name)
4. Replace `"Selected text:"` caption with `{t('Scribe.modal.selected_text')}`
5. Replace `label="Cancel"` with `label={t('Scribe.button.cancel')}`
6. Replace `label="Insert After"` with `label={t('Scribe.button.insert_after')}`
7. Replace `label="Replace"` with `label={t('Scribe.button.replace')}`
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && node -e "
const fs = require('fs');
const files = [
  'src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx',
  'src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx',
  'src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx',
  'src/modules/views/OnlyOffice/ScribeModal.jsx'
];
let ok = true;
for (const f of files) {
  const src = fs.readFileSync(f, 'utf8');
  if (!src.includes('useI18n')) { console.error('Missing useI18n in ' + f); ok = false; }
  // Check no hardcoded strings remain (except Scribe brand and Ctrl+I)
  if (f.includes('FloatingButton') && src.includes('\"Text AI\"')) { console.error('Hardcoded Text AI in FloatingButton'); ok = false; }
  if (f.includes('PromptInput') && src.includes('\"Help me write\"')) { console.error('Hardcoded placeholder in PromptInput'); ok = false; }
  if (f.includes('ResultPanel') && (src.includes('\"Inserer\"') || src.includes('label=\"Retry\"') || src.includes('label=\"Replace\"'))) { console.error('Hardcoded labels in ResultPanel'); ok = false; }
  if (f.includes('ScribeModal') && (src.includes('\"Cancel\"') || src.includes('\"Insert After\"') || src.includes('\"Selected text:\"'))) { console.error('Hardcoded labels in ScribeModal'); ok = false; }
}
if (ok) console.log('All 4 components use t() correctly');
else process.exit(1);
"</automated>
  </verify>
  <done>ScribeFloatingButton, ScribePromptInput, ScribeResultPanel, and ScribeModal all use t() for user-facing strings. No hardcoded English or French strings remain (except "Scribe" brand and "(Ctrl+I)" shortcut).</done>
</task>

<task type="auto">
  <name>Task 2: Wire ScribeActionMenu and ScribePopover to use t() with labelKey, loading keys, and error keys</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx, src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</files>
  <action>
**ScribeActionMenu.jsx:**
1. The component already imports `useI18n` (for `lang`). Destructure `t` as well: `const { t, lang } = useI18n()`
2. Replace all `action.label` references used for display with `t(action.labelKey)`:
   - `<ListItemText primary={action.label} />` -> `<ListItemText primary={t(action.labelKey)} />`
   - In child items: use `child.labelKey ? t(child.labelKey) : child.label` (translate children have labelKey: null, use native label directly)
   - `<ListItemText primary={child.label} />` -> `<ListItemText primary={child.labelKey ? t(child.labelKey) : child.label} />`
3. Replace `placeholder="Other language..."` with `placeholder={t(child.placeholderKey)}` for the custom input child (it has `placeholderKey: 'Scribe.translate.other_language'`)
4. Update the `onSelect` callbacks to pass the **translated** label for display purposes:
   - Direct action click: `onSelect(action.id, t(action.labelKey), t(action.labelKey))`
   - Child action click: `onSelect(child.id, child.labelKey ? t(child.labelKey) : child.label, t(action.labelKey) + ' > ' + (child.labelKey ? t(child.labelKey) : child.label))`
   - Note: For translate children, `label` is the native language name (already display-ready)
   - Custom language submit: `onSelect('translate-custom', trimmed, t(actions.find(a=>a.id==='translate').labelKey) + ' > ' + trimmed)` (breadcrumb uses translated "Translate" parent)
   - Prompt submit stays as-is (free-prompt uses user's typed text as label)
5. Update `selectAction` helper similarly to pass translated labels

**ScribePopover.jsx:**
1. Add `import { useI18n } from 'twake-i18n'`
2. Add `const { t } = useI18n()` inside the component
3. Update `handleActionSelect` to resolve loading message via t():
   - `const loadingInfo = deriveLoadingMessage(actionId, label)` (returns `{ key, params? }`)
   - `setLoadingMessage(loadingInfo.params ? t(loadingInfo.key, loadingInfo.params) : t(loadingInfo.key))`
4. Update the error handling in the catch block:
   - `const classified = classifyScribeError(err)` (returns `{ messageKey, canRetry }`)
   - `setResult({ text: '', breadcrumb, error: classified.messageKey ? t(classified.messageKey) : '', canRetry: classified.canRetry })`
5. The breadcrumb is now passed already-translated from ScribeActionMenu (via the onSelect callback), so no changes needed for breadcrumb display.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && node -e "
const fs = require('fs');
const menu = fs.readFileSync('src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx', 'utf8');
const popover = fs.readFileSync('src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx', 'utf8');
let ok = true;
// ScribeActionMenu should use t() with labelKey
if (!menu.includes('labelKey')) { console.error('ScribeActionMenu missing labelKey usage'); ok = false; }
if (menu.includes('\"Other language...\"')) { console.error('Hardcoded Other language... in ActionMenu'); ok = false; }
// ScribePopover should import useI18n and use t()
if (!popover.includes('useI18n')) { console.error('ScribePopover missing useI18n import'); ok = false; }
if (!popover.includes('messageKey')) { console.error('ScribePopover not using messageKey from classifyScribeError'); ok = false; }
if (ok) console.log('ScribeActionMenu and ScribePopover wired correctly');
else process.exit(1);
"</automated>
  </verify>
  <done>ScribeActionMenu renders translated labels via t(action.labelKey), builds translated breadcrumbs, and passes translated labels to onSelect. ScribePopover resolves loading and error i18n keys via t(). No hardcoded UI strings remain in either component.</done>
</task>

</tasks>

<verification>
- grep across all 6 Scribe JSX files for hardcoded English/French strings (should find only "Scribe" brand and "(Ctrl+I)")
- All components import and use useI18n
- ScribePopover correctly resolves { key, params } from deriveLoadingMessage via t()
- ScribePopover correctly resolves messageKey from classifyScribeError via t()
- Breadcrumbs display translated action labels
</verification>

<success_criteria>
- Zero hardcoded French or English UI strings in Scribe components (except "Scribe" brand name and "(Ctrl+I)" shortcut)
- All 6 components use `t()` from `useI18n` for user-facing text
- Loading messages, error messages, action labels, button labels, tooltips, and placeholders are all translated
- Changing locale would change all Scribe UI text
</success_criteria>

<output>
After completion, create `.planning/phases/09-internationalization/09-02-SUMMARY.md`
</output>
