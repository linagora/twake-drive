---
phase: 11-pipeline-de-conversion
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/scribeAI.js
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
autonomous: true
requirements: [CONV-01, CONV-02]

must_haves:
  truths:
    - "When HTML is available in the intent, the LLM receives Markdown-converted text instead of plain text"
    - "When HTML is NOT available, the LLM still receives plain text as before (backward compat)"
    - "The LLM response Markdown is stored for Phase 13 HTML reconversion"
    - "selectedHtml prop flows from View.jsx through ScribePopover to buildMessages"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      provides: "buildMessages uses htmlToMarkdown when extra.html is present"
      contains: "htmlToMarkdown"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "selectedHtml prop wired to buildMessages extra parameter"
      contains: "selectedHtml"
  key_links:
    - from: "ScribePopover.jsx"
      to: "scribeAI.js buildMessages"
      via: "extra.html parameter"
      pattern: "html:\\s*selectedHtml"
    - from: "scribeAI.js"
      to: "scribeConversion.js"
      via: "htmlToMarkdown import"
      pattern: "import.*htmlToMarkdown.*from.*scribeConversion"
---

<objective>
Wire the conversion module into the Scribe AI pipeline so HTML-formatted text is converted to Markdown before being sent to the LLM.

Purpose: Complete the integration so that when a user selects formatted text in OO and triggers Scribe, the LLM receives clean Markdown (preserving structure) instead of plain text. Also ensure the raw Markdown response is available for Phase 12 (preview) and Phase 13 (reconversion to HTML).

Output: Working pipeline where formatted OO text flows as Markdown to the LLM.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pipeline-de-conversion/11-RESEARCH.md
@.planning/phases/11-pipeline-de-conversion/11-01-SUMMARY.md

<interfaces>
<!-- From scribeConversion.js (created by Plan 01): -->
export function htmlToMarkdown(html: string): string
export function markdownToHtml(markdown: string): string

<!-- From ScribePopover.jsx (current props): -->
ScribePopover({ open, selectedText, onReplace, onInsert, onCancel })
// View.jsx already passes: selectedHtml={pendingIntent?.data?.html || ''}
// But ScribePopover does NOT yet accept or use selectedHtml

<!-- From scribeAI.js buildMessages (current signature): -->
export function buildMessages(actionId, selectedText, label, extra)
// extra is currently { language } for translate-custom, undefined otherwise
// Research says: extra?.html ? htmlToMarkdown(extra.html) : selectedText

<!-- From View.jsx line 153: -->
selectedHtml={pendingIntent?.data?.html || ''}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate htmlToMarkdown into buildMessages</name>
  <files>src/modules/views/OnlyOffice/Scribe/scribeAI.js</files>
  <action>
In `scribeAI.js`:

1. Add import at top:
   ```javascript
   import { htmlToMarkdown } from '@/modules/views/OnlyOffice/Scribe/scribeConversion'
   ```

2. In `buildMessages()`, add HTML-to-Markdown conversion at the start of the function body, BEFORE any prompt building:
   ```javascript
   // When HTML is available (v2.1 rich text pipeline), convert to Markdown for better LLM input
   const textForPrompt = extra?.html ? htmlToMarkdown(extra.html) : selectedText
   ```

3. Replace ALL occurrences of `selectedText` in the prompt templates with `textForPrompt`:
   - Line ~85 (free-prompt): `Text: ${selectedText}` -> `Text: ${textForPrompt}`
   - Line ~98 (fallback): `${selectedText}` -> `${textForPrompt}`
   - Line ~103 (template): `prompt.replace('{selectedText}', selectedText)` -> `prompt.replace('{selectedText}', textForPrompt)`

Do NOT change the function signature -- `selectedText` param stays as-is for backward compat.

4. Remove the temporary console.log in View.jsx if one exists for HTML verification (per 10-02 SUMMARY: "Temporary console.log for HTML verification added in View.jsx (to remove in Phase 11)").
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -n "htmlToMarkdown" src/modules/views/OnlyOffice/Scribe/scribeAI.js && grep -n "textForPrompt" src/modules/views/OnlyOffice/Scribe/scribeAI.js</automated>
  </verify>
  <done>buildMessages uses htmlToMarkdown(extra.html) when HTML available, falls back to plain selectedText otherwise</done>
</task>

<task type="auto">
  <name>Task 2: Wire selectedHtml through ScribePopover to buildMessages</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</files>
  <action>
In `ScribePopover.jsx`:

1. Add `selectedHtml` to the component's destructured props:
   ```javascript
   const ScribePopover = ({ open, selectedText, selectedHtml, onReplace, onInsert, onCancel }) => {
   ```

2. Add to PropTypes:
   ```javascript
   selectedHtml: PropTypes.string,
   ```
   (Not `.isRequired` -- backward compat, defaults to empty string from View.jsx)

3. In `handleActionSelect`, update the `extra` object construction to ALWAYS include `html` when `selectedHtml` is available:
   ```javascript
   const extra = {}
   if (actionId === 'translate-custom') {
     extra.language = label
   }
   if (selectedHtml) {
     extra.html = selectedHtml
   }
   const messages = buildMessages(actionId, selectedText, label, Object.keys(extra).length > 0 ? extra : undefined)
   ```

4. Add `selectedHtml` to the `handleActionSelect` useCallback dependency array.

5. Remove the temporary console.log from View.jsx if present (check for console.log referencing html/selectedHtml near the ScribePopover rendering).
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -n "selectedHtml" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx && grep -c "html:" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</automated>
  </verify>
  <done>selectedHtml prop accepted by ScribePopover, passed as extra.html to buildMessages. View.jsx already passes it (from Phase 10). Console.log cleanup done.</done>
</task>

</tasks>

<verification>
- `grep -rn "htmlToMarkdown" src/modules/views/OnlyOffice/Scribe/` shows import in scribeAI.js
- `grep -rn "selectedHtml" src/modules/views/OnlyOffice/Scribe/` shows prop in ScribePopover.jsx
- `grep -rn "textForPrompt" src/modules/views/OnlyOffice/Scribe/scribeAI.js` shows usage in all prompt paths
- `grep -rn "console.log" src/modules/views/OnlyOffice/View.jsx | grep -i html` returns nothing (cleanup done)
- Existing tests still pass: `npx jest src/modules/views/OnlyOffice/Scribe/ --no-cache`
</verification>

<success_criteria>
- When selectedHtml is present, LLM receives Markdown-converted text (not raw HTML, not plain text)
- When selectedHtml is empty/absent, LLM receives plain text as before (backward compat)
- No console.log artifacts from Phase 10 remain
- Pipeline: OO HTML -> normalizeOoHtml -> Turndown -> Markdown in prompt -> LLM -> Markdown response (ready for Phase 12/13)
</success_criteria>

<output>
After completion, create `.planning/phases/11-pipeline-de-conversion/11-02-SUMMARY.md`
</output>
