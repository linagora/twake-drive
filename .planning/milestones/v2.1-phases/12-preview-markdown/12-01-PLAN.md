---
phase: 12-preview-markdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
  - src/modules/views/OnlyOffice/Scribe/scribe.styl
  - jest.config.js
  - package.json
autonomous: true
requirements: [PREV-01, PREV-02]

must_haves:
  truths:
    - "Le panneau de resultat affiche le Markdown rendu (titres, gras, italique, listes, tableaux) au lieu du texte brut"
    - "Le rendu respecte le theme Scribe en mode clair (backgrounds clairs, texte sombre)"
    - "Le rendu respecte le theme Scribe en mode sombre (backgrounds sombres, texte clair)"
    - "Les tableaux et blocs de code larges scrollent horizontalement sans casser le layout"
    - "Les erreurs continuent a s'afficher en texte brut (pas de Markdown rendering pour les erreurs)"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx"
      provides: "Wrapper component around react-markdown with theme-aware styling"
      exports: ["MarkdownPreview"]
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "Result panel using MarkdownPreview for resultText display"
    - path: "src/modules/views/OnlyOffice/Scribe/scribe.styl"
      provides: "Updated max-width (800px), removed monospace/pre-wrap overrides"
    - path: "jest.config.js"
      provides: "transformIgnorePatterns updated for react-markdown ESM deps"
  key_links:
    - from: "ScribeResultPanel.jsx"
      to: "MarkdownPreview.jsx"
      via: "import and render MarkdownPreview with resultText as children"
      pattern: "<MarkdownPreview>.*resultText.*</MarkdownPreview>"
    - from: "MarkdownPreview.jsx"
      to: "react-markdown"
      via: "import ReactMarkdown, render with remarkGfm plugin"
      pattern: "import ReactMarkdown from 'react-markdown'"
    - from: "MarkdownPreview.jsx"
      to: "cozy-ui useTheme"
      via: "useTheme() for palette tokens in component overrides"
      pattern: "useTheme"
---

<objective>
Render LLM Markdown responses as formatted text in the Scribe result panel using react-markdown with GFM support, styled via MUI theme tokens for dark/light mode consistency.

Purpose: Users see formatted AI results (headings, bold, lists, tables, code) instead of raw Markdown syntax, making the output immediately readable.
Output: MarkdownPreview component, updated ScribeResultPanel, updated styles, Jest config for ESM compat.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-preview-markdown/12-RESEARCH.md

@src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
@src/modules/views/OnlyOffice/Scribe/scribe.styl

<interfaces>
<!-- Current ScribeResultPanel render target (line 106-114): -->
```jsx
<div
  className={styles['scribe-result-text']}
  style={{
    backgroundColor: theme.palette.action.hover,
    ...(error ? { color: theme.palette.error.main } : {})
  }}
>
  {error || resultText}
</div>
```

<!-- Current scribe-result-text styles: -->
```stylus
.scribe-result-text
  max-height min(500px, 70vh)
  min-height 48px
  overflow-y auto
  padding 12px
  border-radius 4px
  font-family monospace
  font-size 13px
  line-height 1.5
  white-space pre-wrap
  margin-bottom 12px
  transition max-height 200ms ease-out
```

<!-- Jest transformIgnorePatterns (jest.config.js line 64-67): -->
```javascript
transformIgnorePatterns: [
  'node_modules/(?!cozy-ui|cozy-harvest-lib|cozy-keys-lib|cozy-sharing|marked|)',
  'jest-runner'
],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps and create MarkdownPreview component</name>
  <files>package.json, src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx, jest.config.js</files>
  <action>
1. Install dependencies:
   ```
   yarn add react-markdown remark-gfm
   ```
   Verify `node_modules/react-markdown/package.json` shows v9+ (not the transitive v4.3.1).

2. Create `src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx`:
   - Import ReactMarkdown from 'react-markdown', remarkGfm from 'remark-gfm', useTheme from 'cozy-ui/transpiled/react/styles'
   - Component accepts `children` prop (the Markdown string)
   - Wrapper div with inline styles:
     - `fontSize: '0.85em'` (scaling coefficient per user decision)
     - `lineHeight: 1.5`
     - `color: theme.palette.text.primary`
     - `wordWrap: 'break-word'`, `overflowWrap: 'break-word'`
     - `whiteSpace: 'normal'` (override parent pre-wrap)
     - `fontFamily: 'inherit'` (override parent monospace)
   - ReactMarkdown with `remarkPlugins={[remarkGfm]}` and `children={children}`
   - `components` prop with theme-aware inline styles for:
     - `pre`: background `isDark ? theme.palette.grey[800] : theme.palette.grey[100]`, padding 12px, borderRadius 4px, overflowX auto, margin 8px 0
     - `code`: detect inline vs block. Inline code gets background (same grey tokens), padding 2px 4px, borderRadius 3px, fontSize 0.9em. Block code (inside pre) renders plain.
     - `table`: wrap in a div with overflowX auto. Table gets borderCollapse collapse, width 100%, fontSize 0.95em
     - `th`: border `1px solid ${theme.palette.divider}`, padding 6px 10px, background `isDark ? grey[800] : grey[50]`, textAlign left
     - `td`: border `1px solid ${theme.palette.divider}`, padding 6px 10px
     - `a`: color `theme.palette.primary.main`
     - `blockquote`: borderLeft `3px solid ${theme.palette.divider}`, paddingLeft 12px, marginLeft 0, color `theme.palette.text.secondary`
     - `p`, `h1`-`h6`, `ul`, `ol`: margin `4px 0` (compact spacing for result panel)
   - Export as named export `{ MarkdownPreview }`
   - Use isDark = `theme.palette.type === 'dark'` for dark mode detection

3. Update `jest.config.js` transformIgnorePatterns to include the ESM dependency tree:
   Change line 65 from:
   ```
   'node_modules/(?!cozy-ui|cozy-harvest-lib|cozy-keys-lib|cozy-sharing|marked|)',
   ```
   To:
   ```
   'node_modules/(?!cozy-ui|cozy-harvest-lib|cozy-keys-lib|cozy-sharing|marked|react-markdown|remark-gfm|unified|bail|devlop|is-plain-obj|trough|remark-.*|rehype-.*|hast-util-.*|mdast-util-.*|micromark.*|unist-.*|vfile|vfile-message|html-void-elements|zwitch|hastscript|web-namespaces|property-information|space-separated-tokens|comma-separated-tokens|decode-named-character-reference|character-entities|ccount|escape-string-regexp|markdown-table|longest-streak|)',
   ```
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && node -e "const pkg = require('./node_modules/react-markdown/package.json'); console.log('react-markdown version:', pkg.version); if (parseInt(pkg.version) < 9) { process.exit(1); }" && echo "MarkdownPreview exists:" && test -f src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx && echo "OK"</automated>
  </verify>
  <done>react-markdown v9+ installed, MarkdownPreview.jsx exists with theme-aware component overrides for pre/code/table/th/td/a/blockquote/headings, jest.config.js updated for ESM deps</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MarkdownPreview into ScribeResultPanel and update styles</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx, src/modules/views/OnlyOffice/Scribe/scribe.styl</files>
  <action>
1. Update `ScribeResultPanel.jsx`:
   - Add import: `import { MarkdownPreview } from '@/modules/views/OnlyOffice/Scribe/MarkdownPreview'`
   - Replace line 113 `{error || resultText}` with:
     ```jsx
     {error ? error : <MarkdownPreview>{resultText}</MarkdownPreview>}
     ```
     Error stays as plain text string (no Markdown rendering). resultText always goes through MarkdownPreview per user decision (no conditional fallback, no detection logic).

2. Update `scribe.styl`:
   - Change `.scribe-result-panel` max-width from `700px` to `800px` (per user decision, gives tables more room)
   - In `.scribe-result-text`, remove or override `font-family monospace` and `white-space pre-wrap` since MarkdownPreview handles these via inline styles. Replace with:
     ```
     font-family inherit
     white-space normal
     ```
     Keep all other properties (max-height, min-height, overflow-y, padding, border-radius, font-size, line-height, margin-bottom, transition).

3. Verify the build compiles:
   ```
   yarn build
   ```
   Or at minimum check that the module resolves without import errors.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -q "MarkdownPreview" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx && grep -q "800px" src/modules/views/OnlyOffice/Scribe/scribe.styl && grep -q "white-space normal" src/modules/views/OnlyOffice/Scribe/scribe.styl && echo "All checks passed"</automated>
  </verify>
  <done>ScribeResultPanel renders resultText through MarkdownPreview, errors stay plain text, max-width increased to 800px, monospace/pre-wrap overrides removed from stylus</done>
</task>

</tasks>

<verification>
1. `yarn test -- --testPathPattern=Scribe --passWithNoTests` -- existing tests still pass with updated Jest config
2. Visual verification (in-browser): Open Scribe, trigger an AI action on formatted text. Result panel should show rendered Markdown (bold, headings, lists) instead of raw syntax.
3. Dark mode check: Toggle theme, verify code blocks and table headers use appropriate dark backgrounds.
4. Wide table test: If LLM returns a table, it should scroll horizontally within the result panel without breaking layout.
</verification>

<success_criteria>
- MarkdownPreview component exists and renders react-markdown with remark-gfm
- ScribeResultPanel uses MarkdownPreview for all non-error results
- Theme tokens used for all color-dependent elements (code bg, table borders, links, blockquotes)
- No white-space/font-family conflicts between stylus and Markdown rendering
- Jest tests pass with updated transformIgnorePatterns
- max-width increased to 800px
</success_criteria>

<output>
After completion, create `.planning/phases/12-preview-markdown/12-01-SUMMARY.md`
</output>
