---
phase: v3.2-03-cablage-document-complet-strategie-taille
plan: 04
type: execute
wave: 3
depends_on: ["v3.2-03-02", "v3.2-03-03"]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js
autonomous: true
requirements: [CTX-LLM-01, CTX-LLM-04]
must_haves:
  truths:
    - "The composition spec proves the document path: includeDocument injects an ephemeral [Full document] block into the CURRENT turn only (never stored on the message, never replayed in history)"
    - "The any-combination matrix (document × selection × discussion) composes deterministically without breaking the v3.1 contract"
    - "Budget truncation sets documentNotice='truncated'; extraction error/timeout sets documentNotice='unavailable' with no doc block; default unlimited sets no notice"
    - "Determinism holds for a fixed document state (mock extractFullDocument returns fixed md): same transcript + same checkboxes + same doc state ⇒ byte-identical aiMessages"
    - "scribeAI.spec proves the includeDocument framing branch + combined doc+selection clause, and that all-false output is byte-identical (frozen blocks intact)"
    - "PROBE-01 (scribeProbe.spec.js), the v3.1 regression corpus (scribeResponse.corpus.spec.js), and the literal-audit (scribeLiteralAudit.spec.js) are GREEN with the document enabled"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx"
      provides: "document-path composition + any-combination + ephemeral + notice + determinism assertions"
      contains: "Full document"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js"
      provides: "includeDocument framing assertions + frozen-block byte-identity"
      contains: "includeDocument"
  key_links:
    - from: "compose spec mock extractFullDocument"
      to: "ScribeContext.sendMessage doc path"
      via: "injected setExtractFullDocument"
      pattern: "extractFullDocument"
    - from: "gate run"
      to: "PROBE-01 + corpus + literal-audit"
      via: "jest"
      pattern: "scribeProbe|corpus|LiteralAudit"
---

<objective>
Author the document-path regression/composition assertions and run the consolidated GREEN gate
that proves D-07 (frozen v3.1 contract) holds with the document enabled and that the three
sources compose in any combination.

Purpose: CTX-LLM-01 + CTX-LLM-04 must ship without regressing the v3.1 response contract. This
is the recognizable HARD-GATE-style plan (cf. v3.1-03 sonde gate, v3.2-02 verification gate).

Output: extended compose + scribeAI specs; a documented all-green run of compose, scribeAI,
corpus, probe, and literal-audit suites with the document path active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-PATTERNS.md
@.planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-VERIFICATION.md
@src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx

<interfaces>
<!-- Existing compose-spec harness (reuse verbatim): -->
<!--   captured[]  collects aiMessages via the mocked callScribeAIWithReask -->
<!--   real buildChatSystemPrompt / encodeSelectionForPrompt are kept (only callScribeAIWithReask is mocked) -->
<!--   driven through the real ScribeProvider + act() -->
<!-- Existing quadrants: A sel/disc on/off matrix; D-03 determinism; CTX-LLM-05 no-fragment-leak; D-03 live-read. -->

<!-- New surface from plan 02 (to drive in the spec): -->
<!--   ScribeContext exposes setExtractFullDocument(fn) — inject a mock returning a FIXED md (Pitfall 6) -->
<!--   includeDocument flag toggles the [Full document]…[End of full document] block on the CURRENT turn -->
<!--   userMessage.documentNotice = 'truncated' | 'unavailable' | undefined -->
<!--   budget via flag('drive.scribe.contextBudget') — mock cozy-flags to test truncation -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the composition spec for the document path, any-combination, notices, and determinism</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx (whole file — the mock harness :1-137, the quadrants :138-300)
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the document path delivered by plan 02: includeDocumentRef, applyBudget, doc block, documentNotice, setExtractFullDocument)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md (Pattern 3, Pattern 5, Pitfall 6)
  </read_first>
  <action>
    Reuse the existing harness (captured[] + mocked callScribeAIWithReask + real builders). Add a
    way to inject `setExtractFullDocument` from the test (render through ScribeProvider, grab the
    setter from context, set a mock returning a FIXED md). Mock `cozy-flags` `flag` so
    `drive.scribe.contextBudget` can be set per-test (default unset = unlimited). Add cases:
    (1) includeDocument ON, default budget: the CURRENT user turn's content begins with
    `[Full document]\n...\n[End of full document]`; no documentNotice; the stored user message
    content and any prior turn carry NO doc block (ephemeral — assert history replay is unchanged).
    (2) Any-combination matrix: document × selection × discussion (at least the doc+selection+disc
    all-ON case and doc-ON/others-OFF case) compose deterministically; the system prompt includes
    the includeDocument framing; recommended block order document → selection → user text on the
    current turn.
    (3) Budget exceeded (flag set below the fixed md length): the doc block is truncated head-keeping
    at a `\n\n` boundary and `documentNotice === 'truncated'` on the user message.
    (4) Extraction error/timeout (mock resolves `{ md:'', error:'timeout' }`):
    `documentNotice === 'unavailable'` and NO `[Full document]` block in the current turn.
    (5) Determinism (Pitfall 6): with the mock returning fixed md, two identical sends produce
    byte-identical aiMessages — assert and document the guarantee now reads "same transcript +
    same checkboxes + same document state ⇒ same aiMessages".
    Keep the existing quadrants passing (no edits that weaken them).
  </action>
  <verify>
    <automated>yarn test ScribeContext.compose.spec.jsx 2>&1 | tail -8</automated>
    <automated>grep -c "Full document\|documentNotice\|setExtractFullDocument\|contextBudget" src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx</automated>
  </verify>
  <acceptance_criteria>
    - New cases assert: ephemeral doc block on the current turn only; any-combination determinism; documentNotice='truncated' on budget overflow; documentNotice='unavailable' on error with no block; byte-identical aiMessages for fixed doc state.
    - History replay (prior turns) carries no doc block.
    - All existing quadrant tests still pass.
    - `yarn test ScribeContext.compose.spec.jsx` is GREEN.
  </acceptance_criteria>
  <done>The composition spec proves the document path, any-combination, never-silent notices, and determinism for fixed doc state.</done>
</task>

<task type="auto">
  <name>Task 2: Extend scribeAI.spec for the includeDocument framing + run the consolidated GREEN gate</name>
  <files>src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js (existing contextSourceFraming / buildChatSystemPrompt cases)
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (the grown contextSourceFraming from plan 02 Task 3 + frozen blocks)
    - .planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-VERIFICATION.md (the frozen-byte-identity expectation)
  </read_first>
  <action>
    Add scribeAI.spec cases: (a) `buildChatSystemPrompt(md, { includeDocument: true })` contains the
    document framing sentence; (b) `{ includeDocument: true, includeSelection: true }` additionally
    contains the combined selection-is-focus clause; (c) `buildChatSystemPrompt(md)` with no flags
    (or all false) is byte-identical to the pre-v3.2-03 output (assert it still starts with
    CHAT_PERSONA + contains RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT unchanged); (d) the framing
    contains NO literal "1./2./3." list and NO "mention/list the sources" instruction. Then run the
    consolidated gate: ScribeContext.compose.spec.jsx, scribeAI.spec.js, scribeResponse.corpus.spec.js,
    scribeProbe.spec.js, scribeLiteralAudit.spec.js — all must be GREEN with the document path
    active. Record the run + counts + the confirmation that the frozen artifacts
    (RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT/CHAT_PERSONA/markerPreservationClauses, scribeResponse.js,
    callScribeAIWithReask) are unmodified, in the SUMMARY.
  </action>
  <verify>
    <automated>yarn test scribeAI.spec.js 2>&1 | tail -6</automated>
    <automated>yarn test scribeResponse.corpus.spec.js scribeProbe.spec.js scribeLiteralAudit.spec.js 2>&1 | tail -8</automated>
    <automated>git diff --stat -- src/modules/views/OnlyOffice/Scribe/scribeResponse.js src/modules/views/OnlyOffice/Scribe/scribeAI.js | grep -E "RESPONSE_CONTRACT_CORE" ; echo "verify frozen-block edits absent in SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - scribeAI.spec asserts the includeDocument sentence, the combined doc+selection clause, the all-false byte-identity, and the no-numbered-list / no-meta-commentary guard.
    - All five suites (compose, scribeAI, corpus, probe, literal-audit) are GREEN.
    - SUMMARY records that frozen v3.1 artifacts are unmodified (D-07).
  </acceptance_criteria>
  <done>The full v3.2-03 change set passes the v3.1 regression + composition gate with the document enabled; D-07 confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → ScribeContext seam | Mocked extraction/flags drive the real composition seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.2-03-09 | Tampering/EoP | prompt-injection regression (document content breaking the JSON contract) | mitigate | PROBE-01 + corpus run GREEN with the document path active; frozen contract byte-identity asserted (D-07) |
| T-v3.2-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no registry interaction |
</threat_model>

<verification>
- Compose spec: document path, any-combination, ephemeral, notices, determinism — all GREEN.
- scribeAI spec: framing branch + frozen byte-identity — GREEN.
- corpus + probe + literal-audit — GREEN with document enabled.
- Frozen artifacts unmodified (D-07).
</verification>

<success_criteria>
With the document context enabled, the three sources compose deterministically in any combination, truncation/failure produce never-silent notices, and the v3.1 response contract (PROBE-01 + corpus) remains GREEN with frozen artifacts byte-identical.
</success_criteria>

<output>
Create `.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-04-SUMMARY.md` when done (record suite counts + D-07 frozen-artifact confirmation).
</output>
