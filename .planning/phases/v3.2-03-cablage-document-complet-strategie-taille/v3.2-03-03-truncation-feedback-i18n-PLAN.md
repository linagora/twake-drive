---
phase: v3.2-03-cablage-document-complet-strategie-taille
plan: 03
type: execute
wave: 2
depends_on: ["v3.2-03-02"]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx
  - src/locales/en.json
  - src/locales/fr.json
  - src/locales/de.json
  - src/locales/es.json
  - src/locales/it.json
autonomous: true
requirements: [CTX-LLM-04]
must_haves:
  truths:
    - "When a user turn has documentNotice='truncated', ChatMessageList renders a discreet single-line status notice in thread order immediately after that turn's UserBubble (info icon + muted text.secondary, role=status aria-live=polite, no red/accent) — never silent"
    - "When documentNotice='unavailable', ChatMessageList renders the existing ErrorBubble with the failure copy"
    - "With no truncation/failure (default unlimited), no notice is rendered (silent success is correct)"
    - "All new user-facing strings are i18n keys present in ALL FIVE locales (fr/en/de/es/it) and routed through t() — literal-audit + parity gates stay green"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx"
      provides: "documentNotice rendering after the user bubble (truncated notice row + unavailable ErrorBubble)"
      contains: "documentNotice"
    - path: "src/locales/en.json"
      provides: "Scribe.include.documentTruncated + documentUnavailable"
      contains: "documentTruncated"
  key_links:
    - from: "msg.documentNotice (set by plan 02)"
      to: "ChatMessageList render branch"
      via: "thread-order conditional after UserBubble"
      pattern: "documentNotice"
    - from: "notice text"
      to: "i18n"
      via: "t('Scribe.include.document*')"
      pattern: "Scribe.include.document"
---

<objective>
Render the never-silent truncation/extraction feedback per the approved UI-SPEC, and add the
i18n strings across all five locales.

Purpose: CTX-LLM-04 criterion #3 — the user is always told when the document context was
truncated or could not be included. Pure reuse of existing UI patterns (no new tokens).

Output: ChatMessageList renders documentNotice (discreet status row for 'truncated', ErrorBubble
for 'unavailable'); five locale files gain the new keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-UI-SPEC.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-PATTERNS.md

<interfaces>
<!-- documentNotice is set on the user message by plan 02 (sendMessage): -->
<!--   userMessage.documentNotice = 'truncated' | 'unavailable' | undefined -->

From src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx:
  component reads useScribe() → { messages, isLoading, currentSelection }  :284
  messages.map render branches (user :429-437; error :439-447; assistant …) :428-517
  loading-indicator row to CLONE for the truncation notice                  :519-534
  ErrorBubble component to REUSE for 'unavailable'                          :117-133

UI-SPEC copy (EN canonical):
  Scribe.include.documentTruncated  = "Document too long — only part of it was sent to the assistant."
  Scribe.include.documentUnavailable= "Couldn't read the document — your message was sent without it."
  (optional, off by default) Scribe.loading.reading_document = "Reading the document…"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render documentNotice after the triggering user bubble in ChatMessageList</name>
  <files>src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx (:117-133 ErrorBubble, :282-285 component/hooks, :428-447 user/error map branches, :519-534 loading-indicator row)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-UI-SPEC.md (DEC-UI-01, DEC-UI-03, DEC-UI-04, Component Inventory, Interaction Contract, A11y)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-PATTERNS.md (ChatMessageList section: Analog A/B/C)
  </read_first>
  <action>
    In the `messages.map` user branch (:429-437), after returning the `UserBubble`, also render
    documentNotice for that turn when present. Because a map callback returns a single node, return
    a Fragment containing the `UserBubble` plus the conditional notice (keep stable keys, e.g.
    `${msg.id}-notice`). For `msg.documentNotice === 'truncated'`: render a discreet
    `<div role="status" aria-live="polite">` cloned from the loading-indicator row (:519-534) —
    `alignSelf:'flex-start'; display:'flex'; alignItems:'center'; gap:8; padding:'8px 12px';
    opacity:0.7; fontSize:13` — containing a 14px info/warning cozy-ui `Icon` with
    `currentColor` = `theme.palette.text.secondary` (NO red, NO accent) followed by a `<span>`
    with `t('Scribe.include.documentTruncated')`. For `msg.documentNotice === 'unavailable'`:
    render the existing `ErrorBubble` with `content={t('Scribe.include.documentUnavailable')}`,
    `theme`, `t`. Resolve all colors via the existing `theme` (DEC-UI-04, no new hex). The icon is
    decorative (text carries meaning — no aria-label needed). Do NOT add a badge to the Inclure
    zone and do NOT add a separate pending spinner (DEC-UI-02 reuse the existing loading row).
  </action>
  <verify>
    <automated>grep -c "documentNotice\|Scribe.include.documentTruncated\|Scribe.include.documentUnavailable\|role=\"status\"" src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx</automated>
    <automated>yarn test ChatMessageList.render.spec.jsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 'truncated' renders a `role="status" aria-live="polite"` discreet row (info Icon at 14px text.secondary + `t('Scribe.include.documentTruncated')`), styled as a clone of the loading row, in thread order right after the triggering UserBubble.
    - 'unavailable' renders `ErrorBubble` with `t('Scribe.include.documentUnavailable')`.
    - No notice renders when documentNotice is absent.
    - No hard-coded user-facing strings (all via `t()`); no new hex colors; no Inclure-zone badge; no new pending spinner.
    - Existing ChatMessageList render spec passes.
  </acceptance_criteria>
  <done>Truncation and extraction-failure are surfaced discreetly and never silently, per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Add the document-notice i18n keys to all five locales</name>
  <files>src/locales/en.json, src/locales/fr.json, src/locales/de.json, src/locales/es.json, src/locales/it.json</files>
  <read_first>
    - src/locales/en.json (Scribe.include group :795-800; Scribe.loading group :817-827)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-UI-SPEC.md (§ i18n Keys — full 5-locale table)
  </read_first>
  <action>
    Add `documentTruncated` and `documentUnavailable` under the `Scribe.include` group in ALL FIVE
    locale files, using the UI-SPEC i18n table verbatim (planner may refine wording but MUST keep
    5-locale parity). Optionally add `reading_document` under `Scribe.loading` (off by default —
    only if Task 1 / plan 02 opted into the doc-aware loading caption; otherwise omit to avoid an
    unused key). EN canonical strings: documentTruncated = "Document too long — only part of it was
    sent to the assistant."; documentUnavailable = "Couldn't read the document — your message was
    sent without it." Use the fr/de/es/it translations from the UI-SPEC table. Keep valid JSON.
  </action>
  <verify>
    <automated>for f in en fr de es it; do node -e "JSON.parse(require('fs').readFileSync('src/locales/$f.json','utf8'))" && grep -c "documentTruncated\|documentUnavailable" src/locales/$f.json; done</automated>
    <automated>yarn test scribeLiteralAudit.spec.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `Scribe.include.documentTruncated` and `Scribe.include.documentUnavailable` exist in all five locale files (grep returns ≥2 per file).
    - All five JSON files parse.
    - `scribeLiteralAudit.spec.js` passes (no missing keys / no hard-coded strings).
  </acceptance_criteria>
  <done>The two notice strings are localized across fr/en/de/es/it with parity preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentNotice (host state) → DOM render | An enum flag controls which notice renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.2-03-07 | Information disclosure | truncation notice copy | accept | Copy is cut-point-agnostic ("only part of it") and contains no document content; no PII leak |
| T-v3.2-03-08 | Tampering | documentNotice rendered as text/icon only | accept | Notice value is a fixed enum from plan 02 ('truncated'/'unavailable'); only mapped to a `t()` key, never rendered as raw HTML |
| T-v3.2-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no registry interaction |
</threat_model>

<verification>
- Truncated → discreet status row after the user bubble; unavailable → ErrorBubble; absent → nothing.
- All notice text via t(); five-locale parity; literal-audit green.
</verification>

<success_criteria>
The user always sees a discreet, localized notice when the document context was truncated or could not be included, with zero new design tokens and zero hard-coded strings.
</success_criteria>

<output>
Create `.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-03-SUMMARY.md` when done.
</output>
