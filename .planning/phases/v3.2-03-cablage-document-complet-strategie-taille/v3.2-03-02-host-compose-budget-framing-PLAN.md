---
phase: v3.2-03-cablage-document-complet-strategie-taille
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/View.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeAI.js
autonomous: true
requirements: [CTX-LLM-01, CTX-LLM-04]
must_haves:
  truths:
    - "View.jsx exposes extractFullDocument() that broadcasts a 'cozy-bridge:extract-document' request, awaits the correlated 'cozy-bridge:document-extracted' reply by reqId, and resolves { md, error } (with a timeout fallback to error:'timeout')"
    - "extractFullDocument is injected into ScribeContext via a setExtractFullDocument setter, mirroring setPanelActions"
    - "sendMessage reads includeDocument LIVE via includeDocumentRef (mirroring includeSelectionRef/includeDiscussionRef), awaits extraction when checked, and PREPENDS a [Full document]…[End of full document] block to the CURRENT user turn only — never stored on the message nor in history (D-05 ephemeral)"
    - "A configurable budget (cozy-flags 'drive.scribe.contextBudget', default null = unlimited) is the SOLE truncation point — the plugin returns the whole document uncut, so applyBudget truncates head-keeping at a block boundary only when a budget is configured AND exceeded; the user message carries documentNotice = 'truncated' | 'unavailable' (never the document markdown). Under the default unlimited budget nothing is ever cut → no notice → never silent (D-03)"
    - "contextSourceFraming grows with ONE short includeDocument sentence (+ one combined doc+selection focus clause); RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT/CHAT_PERSONA/markerPreservationClauses stay byte-identical"
    - "The three sources (document/discussion/selection) compose in any combination; the v3.1 response contract and callScribeAIWithReask are unchanged"
  artifacts:
    - path: "src/modules/views/OnlyOffice/View.jsx"
      provides: "extractFullDocument round-trip + injection into ScribeContext"
      contains: "cozy-bridge:extract-document"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "includeDocumentRef + extractFullDocumentRef + setExtractFullDocument + budget/truncation + ephemeral doc block + documentNotice"
      contains: "includeDocumentRef"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      provides: "contextSourceFraming includeDocument branch + buildChatSystemPrompt includeDocument flag"
      contains: "includeDocument"
  key_links:
    - from: "ScribeContext.sendMessage"
      to: "extractFullDocument (View.jsx) via extractFullDocumentRef"
      via: "await injected setter"
      pattern: "extractFullDocumentRef"
    - from: "ScribeContext.sendMessage"
      to: "current user turn aiMessages content"
      via: "prepended [Full document] block, ephemeral"
      pattern: "Full document"
    - from: "budget"
      to: "documentNotice on user message"
      via: "applyBudget truncation flag"
      pattern: "documentNotice"
---

<objective>
Wire the full-document context end-to-end on the host side: request extraction from the plugin
at send time, apply a configurable size budget, inject the document as an ephemeral block at the
existing composition seam, and grow the enumerated prompt frame — all without touching the frozen
v3.1 contract.

Purpose: CTX-LLM-01 (inject the docx) + CTX-LLM-04 (controlled size + truncation). Reuses the
v3.2-02 seam (live refs, flag-parameterized buildChatSystemPrompt) by design.

Output: View.jsx gains the extraction round-trip and injects it; ScribeContext gains
includeDocumentRef, the budget/truncation logic, the ephemeral doc block, and the documentNotice
flag; scribeAI grows contextSourceFraming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-CONTEXT.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md
@.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-PATTERNS.md
@.planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-VERIFICATION.md

<interfaces>
<!-- Host↔plugin message contract (shared with plan 01 — keep these strings IDENTICAL). -->
<!-- Request (host → plugin):  { type: 'cozy-bridge:extract-document', reqId: <string> } -->
<!-- Reply  (plugin → host):   { type: 'cozy-bridge:document-extracted', reqId: <string>, md: <string>, error: <string|null> } -->
<!-- NOTE: the reply carries NO truncation field. Plan 01 always returns the whole document uncut -->
<!-- (D-02 default = send everything). applyBudget below is the SOLE truncation/signalling point. -->

From src/modules/views/OnlyOffice/View.jsx (analogs to mirror):
  broadcastToFrames(msg)                 :86-102   // walks nested frames, postMessage('*')
  plugin-ready correlated listener        :121-132  // window message listener idiom + cleanup
  setPanelActions injection               :48,:239-242  // useEffect setter + cleanup return
  flag('drive.scribe.enabled')            :42       // numeric/string cozy-flags precedent

From src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx:
  includeDocument state                    :65       // already exists, default false
  includeDiscussionRef/includeSelectionRef :86-89    // mirror pattern (UPDATE the :84-85 comment)
  sendMessage seam (async)                 :161-244  // read live refs :166-169; selection block :219-227; flag call :206-209
  setPanelActions setter                   :152-154
  provider value/deps                      :346-397  // includeDocument already surfaced :359-360

From src/modules/views/OnlyOffice/Scribe/scribeAI.js:
  contextSourceFraming({includeSelection, includeDiscussion}) :144-156   // grow with includeDocument
  buildChatSystemPrompt(selectionMd, flags)                   :172-181   // thread includeDocument into flags
  FROZEN — DO NOT EDIT: RESPONSE_CONTRACT_CORE, CARDINALITY_CHAT, CHAT_PERSONA, markerPreservationClauses
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extractFullDocument round-trip in View.jsx + inject into ScribeContext</name>
  <files>src/modules/views/OnlyOffice/View.jsx, src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/View.jsx (:40-48 scribe wiring, :86-132 broadcastToFrames + listeners, :237-242 setPanelActions injection)
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (:152-154 setPanelActions setter, :346-397 provider value/deps)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md (Pattern 2, Pitfall 2, Pitfall 3, Open Question 3)
  </read_first>
  <action>
    In View.jsx add `const extractFullDocument = useCallback(() => new Promise(resolve => {...}), [broadcastToFrames])`
    that: generates a `reqId` (`crypto.randomUUID?.()` or `String(Date.now())`), registers a
    one-shot `window` message listener filtering `e.data.type === 'cozy-bridge:document-extracted'
    && e.data.reqId === reqId` that removes itself and resolves `{ md: e.data.md || '', error:
    e.data.error || null }`, broadcasts `{ type: 'cozy-bridge:extract-document', reqId }` via
    broadcastToFrames, and sets a timeout (~8s) that removes the listener and resolves
    `{ md: '', error: 'timeout' }` so a dead/missing plugin yields the DEC-UI-03 'unavailable'
    notice instead of hanging (Pitfall 2). Use the dedicated message type, NOT cozy-bridge:intent
    (Pitfall 3 — bypasses the 1 MB cap). In ScribeContext add a `setExtractFullDocument` setter
    (mirror setPanelActions) that stores the fn into an `extractFullDocumentRef`; surface
    `setExtractFullDocument` on the provider value/deps. In View.jsx inject it with a
    `useEffect(() => { if (!setExtractFullDocument) return; setExtractFullDocument(extractFullDocument);
    return () => setExtractFullDocument(null) }, [setExtractFullDocument, extractFullDocument])`,
    exactly mirroring the setPanelActions effect at :239-242.
  </action>
  <verify>
    <automated>grep -c "cozy-bridge:extract-document" src/modules/views/OnlyOffice/View.jsx</automated>
    <automated>grep -c "setExtractFullDocument" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</automated>
    <automated>yarn test ScribeContext.compose.spec.jsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - View.jsx broadcasts `'cozy-bridge:extract-document'` and resolves on `'cozy-bridge:document-extracted'` correlated by `reqId`; a timeout resolves `{ md:'', error:'timeout' }`.
    - The promise listener is removed on both success and timeout (no leak).
    - ScribeContext exposes `setExtractFullDocument` (in value + deps) and stores into `extractFullDocumentRef`.
    - View.jsx injects/cleans up extractFullDocument mirroring the setPanelActions effect.
    - The existing `ScribeContext.compose.spec.jsx` still passes (no regression).
  </acceptance_criteria>
  <done>The host can ask the plugin for the full document and receive its markdown as a Promise, injected into ScribeContext for the seam to await.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: includeDocumentRef + budget/truncation + ephemeral doc block + documentNotice in sendMessage</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <behavior>
    - When includeDocument is live-checked, sendMessage awaits extractFullDocumentRef.current() before assembling aiMessages.
    - The plugin always returns the WHOLE document uncut (plan 01, D-02); applyBudget is the SOLE truncation point on the host — there is no source-side truncation to react to.
    - With no budget configured (flag null/undefined): the full extracted md is prepended to the CURRENT user turn; documentNotice is unset. Nothing was cut anywhere ⇒ not silent (this is the D-02 default path).
    - With a budget configured and exceeded: md is truncated head-keeping at the last \n\n block boundary; documentNotice = 'truncated'.
    - On extraction error/timeout: no doc block is added; documentNotice = 'unavailable'; the message is still sent.
    - The document block is NEVER written to userMessage.content nor to any prior turn (ephemeral, D-05); prior turns replay without a doc block.
    - documentNotice is stored on the user message object (not the document markdown).
  </behavior>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (:81-89 ref-mirror block + the :84-85 "intentionally NOT mirrored" comment, :161-244 sendMessage, :219-227 selection block composition, :206-209 buildChatSystemPrompt call)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md (Pattern 3, Pattern 5, Pitfall 6)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-UI-SPEC.md (DEC-UI-01, DEC-UI-03, Interaction Contract)
  </read_first>
  <action>
    Add `import flag from 'cozy-flags'`. Mirror the live-ref pattern: add `includeDocumentRef`
    (= includeDocument) next to includeDiscussionRef/includeSelectionRef and UPDATE the :84-85
    comment that says includeDocument is "intentionally NOT mirrored" to reflect that it is now
    mirrored for v3.2-03. Add `extractFullDocumentRef` (set by Task 1's setter). In sendMessage:
    read `var includeDocument = includeDocumentRef.current` (live). When `includeDocument &&
    extractFullDocumentRef.current`, `await extractFullDocumentRef.current()` BEFORE building
    aiMessages (sendMessage is already async; user bubble + setIsLoading(true) already happen first
    so the existing "Scribe is thinking…" indicator covers latency — DEC-UI-02). The plugin reply
    is the whole document uncut (shape `{ md, error }`, no truncation field — plan 01); so the host
    `applyBudget` is the ONLY truncation point. Implement a pure helper `applyBudget(md)`:
    `const budget = flag('drive.scribe.contextBudget')`; if budget is a positive number AND
    `md.length > budget`, keep the head up to the last `\n\n` at/under budget and set
    `truncated=true`; else return md untruncated. Decide documentNotice: error/timeout →
    `'unavailable'` (no doc block); truncated → `'truncated'`; else none (default unlimited budget ⇒
    nothing cut ⇒ no notice ⇒ never silent, D-03). Set `userMessage.documentNotice = documentNotice`
    (store the NOTICE, never the markdown — Pattern 3, D-05). Prepend
    `[Full document]\n${docMd}\n[End of full document]\n\n` to the CURRENT turn's content ONLY
    (recommended order: document → selection → user text); the prior-turn map at :219-227 stays
    unchanged so history never carries a doc block. Add `includeDocument` to the
    `buildChatSystemPrompt(currentSelectionMd, { includeSelection, includeDiscussion, includeDocument })`
    flags. Keep sendMessage deps `[client, t]` unchanged (live reads via refs). Document the
    budget strategy (key `drive.scribe.contextBudget`, char unit, default unlimited, head-keeping
    block-boundary cut, host-side as the sole truncation point) in the SUMMARY for CTX-LLM-04.
  </action>
  <verify>
    <automated>grep -c "includeDocumentRef\|applyBudget\|documentNotice\|drive.scribe.contextBudget\|Full document" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</automated>
    <automated>yarn test ScribeContext.compose.spec.jsx 2>&1 | tail -5</automated>
    <automated>grep -c "intentionally NOT mirrored" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx | grep -q '^0$' && echo "comment updated"</automated>
  </verify>
  <acceptance_criteria>
    - `includeDocumentRef` mirrors includeDocument; the stale "NOT mirrored" comment is updated.
    - `import flag from 'cozy-flags'` present; budget read via `flag('drive.scribe.contextBudget')`; `null`/`undefined` ⇒ unlimited (full doc sent uncut, no notice).
    - applyBudget is the sole truncation point (host-side); the plugin reply has no truncation field to read.
    - Truncation is head-keeping at a `\n\n` boundary and sets documentNotice='truncated'; error/timeout sets documentNotice='unavailable' with no doc block.
    - The doc block is prepended to the current turn only and is NOT stored on userMessage.content nor replayed in history.
    - buildChatSystemPrompt receives `includeDocument` in its flags; sendMessage deps remain `[client, t]`.
    - Existing compose spec passes.
  </acceptance_criteria>
  <done>sendMessage injects a fresh, budget-bounded, ephemeral document block per send and records a never-silent documentNotice when truncated or unavailable; under the default unlimited budget the full document is sent uncut with no notice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grow contextSourceFraming with the includeDocument branch (append-only)</name>
  <files>src/modules/views/OnlyOffice/Scribe/scribeAI.js</files>
  <behavior>
    - contextSourceFraming with includeDocument:true appends ONE short, leading-space-prefixed English sentence framing the full document as reference ("use what is relevant, ignore the rest").
    - When BOTH includeDocument and includeSelection are true, ONE additional combined clause states the selection is the user's focus within that document (D-04).
    - With all flags false the output is '' (byte-identical to pre-change), so existing call sites stay byte-identical.
    - RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT/CHAT_PERSONA/markerPreservationClauses are unchanged; no literal "1./2./3." list; no instruction to mention/list sources.
  </behavior>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (:130-156 contextSourceFraming + its JSDoc seed note, :158-181 buildChatSystemPrompt)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-RESEARCH.md (Pattern 4, Pitfall — Mistral-Small meta-commentary)
    - .planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-PATTERNS.md (scribeAI.js section + FROZEN warning)
  </read_first>
  <action>
    Add `includeDocument` to the `contextSourceFraming({ includeSelection, includeDiscussion,
    includeDocument })` destructure and append ONE short leading-space-prefixed English sentence
    when includeDocument is true (e.g. " The full document is provided below for reference; use
    what is relevant to the request and ignore the rest."). Add ONE combined clause appended only
    when BOTH includeDocument && includeSelection are true (e.g. " The selection marks the user's
    current focus within that document."). Update the JSDoc to note the seed has grown into the
    enumerated frame (v3.2-03). Add `includeDocument` to buildChatSystemPrompt's flags JSDoc. Do
    NOT edit RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT/CHAT_PERSONA/markerPreservationClauses. Do NOT
    pass document markdown through markerPreservationClauses (read-only context). Do NOT build a
    literal numbered list nor instruct the model to mention sources.
  </action>
  <verify>
    <automated>grep -c "includeDocument" src/modules/views/OnlyOffice/Scribe/scribeAI.js</automated>
    <automated>yarn test scribeAI.spec.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - contextSourceFraming returns the includeDocument sentence only when the flag is true; returns the combined focus clause only when both doc+selection true.
    - With all flags false/absent, contextSourceFraming returns '' (byte-identical regression).
    - No edits to RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT/CHAT_PERSONA/markerPreservationClauses (frozen).
    - No literal "1./2./3." list and no "tell the user which sources you used" instruction.
    - `scribeAI.spec.js` passes.
  </acceptance_criteria>
  <done>The enumerated frame informs the model of the document source append-only, contract intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin → host postMessage (`cozy-bridge:document-extracted`) | Untrusted reply enters host React state |
| document markdown → LLM prompt | Untrusted document text becomes prompt content (prompt-injection surface) |
| cozy-flags config → truncation budget | Config value governs payload size |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.2-03-04 | Tampering/EoP | malicious instructions embedded in the docx injected via the [Full document] block | mitigate | Frozen hard-gated RESPONSE_CONTRACT_CORE + callScribeAIWithReask re-ask + contextual fallback (D-07, untouched); the document is framed as reference ("use what is relevant, ignore the rest"), never as instructions; re-validated by PROBE-01 + corpus in plan 04 |
| T-v3.2-03-05 | Spoofing | host one-shot listener for `cozy-bridge:document-extracted` | mitigate | Match both `type` AND the exact `reqId`; remove listener on resolve/timeout; same-page nested-iframe trust model |
| T-v3.2-03-06 | DoS | oversized doc per send (D-05 no cache) | mitigate | `applyBudget` is the sole truncation point and bounds size when configured; per D-02 the default is explicitly unlimited (the user opts in), so the plugin sends the whole document uncut and the host never silently truncates; dedicated channel (no 1 MB intent path); timeout resolves rather than hangs the send |
| T-v3.2-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (cozy-flags already a dependency); no registry interaction |
</threat_model>

<verification>
- extractFullDocument resolves { md, error } via reqId correlation with a timeout fallback.
- includeDocumentRef mirrors live state; budget default unlimited; applyBudget is the sole truncation point; truncation head-keeping at block boundary; documentNotice never silent (default unlimited ⇒ uncut, no notice).
- Doc block ephemeral (not stored / not in history); frozen scribeAI blocks byte-identical.
- compose + scribeAI specs pass.
</verification>

<success_criteria>
Checking « document » and sending injects a fresh full-document markdown block into the current turn's prompt; under the default unlimited budget the whole document is sent uncut with no notice, and when a budget is configured and exceeded applyBudget truncates and sets a never-silent documentNotice (extraction failure → 'unavailable'); composes with discussion+selection in any combination, and leaves the v3.1 contract intact.
</success_criteria>

<output>
Create `.planning/phases/v3.2-03-cablage-document-complet-strategie-taille/v3.2-03-02-SUMMARY.md` when done (document the CTX-LLM-04 size strategy: key, unit, default, cut point, host-side as the sole truncation point).
</output>
</content>
