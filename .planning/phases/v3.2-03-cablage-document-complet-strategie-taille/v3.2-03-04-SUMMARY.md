---
phase: v3.2-03-cablage-document-complet-strategie-taille
plan: 04
subsystem: ui
tags: [scribe, onlyoffice, regression-gate, prompt-composition, determinism, jest, D-07]

# Dependency graph
requires:
  - phase: v3.2-03-02
    provides: "host compose seam — ephemeral [Full document] block, applyBudget truncation, documentNotice, includeDocument framing in buildChatSystemPrompt"
  - phase: v3.2-03-03
    provides: "documentNotice rendering + truncated/unavailable i18n across five locales"
provides:
  - "any-combination composition assertions (document × selection × discussion) proving deterministic composition without breaking the v3.1 contract"
  - "document-aware determinism guarantee restated for a FIXED document state (same transcript + same checkboxes + same doc state ⇒ byte-identical aiMessages)"
  - "D-07 frozen-contract byte-identity gate in scribeAI.spec (all-false output starts with CHAT_PERSONA, frozen RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT contiguous)"
  - "consolidated GREEN gate run: compose + scribeAI + corpus + probe + literal-audit all pass with the document path active"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Regression-gate plan: extend existing spec suites with combination + determinism assertions, then run a consolidated multi-suite GREEN gate (cf. v3.1-03 sonde gate, v3.2-02 verification gate)"
    - "Object-identity current-turn detection (m === userMessage) keeps the document block ephemeral — verified by the all-ON / prior-turn assertions"

key-files:
  created: []
  modified:
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx
    - src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js

key-decisions:
  - "Plan 02 had already authored the bulk of the document-path compose + scribeAI assertions; this plan ADDED only the gaps the regression gate explicitly requires (any-combination all-ON + doc-aware determinism + the D-07 startsWith byte-identity anchor) rather than duplicating existing cases"
  - "D-07 verified two ways: (a) git diff shows this plan touches NO source files (only the two spec files), so scribeResponse.js / scribeAI.js / the frozen contract strings are byte-identical; (b) an explicit byte-identity assertion in scribeAI.spec pins the all-false chat prompt to the documented baseline"

requirements-completed: [CTX-LLM-01, CTX-LLM-04]

# Metrics
duration: ~20min
completed: 2026-06-26
---

# Phase v3.2-03 Plan 04: Regression + Composition Gate Summary

**The v3.2-03 document context now passes the v3.1 regression + composition gate: the three sources (document × selection × discussion) compose deterministically in any combination, the document path adds no nondeterminism for a fixed document state, truncation/failure keep their never-silent notices, and the frozen v3.1 contract (CHAT_PERSONA + RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT, scribeResponse.js, callScribeAIWithReask) is byte-identical (D-07) — proven by an all-green run of all five suites with the document enabled.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-06-26
- **Tasks:** 2
- **Files modified:** 2 (both spec files — no source changes)

## Accomplishments

- **Compose spec (`ScribeContext.compose.spec.jsx`):** added the any-combination matrix + document-aware determinism describe block (3 new cases):
  - **doc + selection + discussion ALL ON** — full history (system + prior user + prior assistant + current user); the system prompt carries all three source-framing sentences + the combined doc+selection focus clause; the current turn orders `document → selection → user text`; the prior user turn replays ITS selection block but carries NO `[Full document]` block (D-05 ephemeral); the frozen `RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT` block stays contiguous.
  - **doc ON / selection & discussion OFF** — a single current turn only (no history), doc framing only (no selection framing, no combined focus clause), doc block precedes the user text.
  - **document-aware determinism** — with the mock extractor returning a fixed md, two identical sends produce byte-identical `aiMessages`; the guarantee now reads "same transcript + same checkboxes + same document state ⇒ byte-identical aiMessages".
- **scribeAI spec (`scribeAI.spec.js`):** added the **D-07 frozen-contract byte-identity gate** — the all-false chat prompt explicitly `startsWith(CHAT_PERSONA)`, keeps `RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT` contiguous, and is byte-identical to the documented pre-v3.2-03 baseline.
- **Consolidated GREEN gate run** with the document path active — all five suites pass (see counts below).

## Consolidated Gate — suite counts (document path active)

| Suite | Result |
|-------|--------|
| `ScribeContext.compose.spec.jsx` | 17 / 17 |
| `scribeAI.spec.js` | 34 / 34 |
| `scribeResponse.corpus.spec.js` (v3.1 regression corpus) | 41 / 41 |
| `scribeProbe.spec.js` (PROBE-01) | 46 / 46 |
| `scribeLiteralAudit.spec.js` (literal audit) | 4 / 4 |
| **Total** | **142 / 142** |

Single combined run: `Test Suites: 5 passed, 5 total · Tests: 142 passed, 142 total`.

## D-07 — frozen v3.1 artifacts unmodified

- `git diff --stat e99935d73 HEAD` for `scribeResponse.js` and `scribeAI.js` (source) is **EMPTY** — this plan touched **no source files**, only the two spec files. The frozen artifacts — `RESPONSE_CONTRACT_CORE`, `CARDINALITY_CHAT`, `CHAT_PERSONA`, `markerPreservationClauses`, `scribeResponse.js`, and `callScribeAIWithReask` — are therefore byte-identical.
- An explicit assertion (`scribeAI.spec.js` › D-07 case) additionally pins the all-false chat prompt to the documented baseline string, so any future drift in the frozen blocks fails the gate.
- Tampering threat **T-v3.2-03-09** (prompt-injection / document content breaking the JSON contract) is mitigated: PROBE-01 (46/46) + the v3.1 corpus (41/41) are GREEN with the document path active.

## Task Commits

1. **Task 1: any-combination matrix + document-aware determinism (compose spec)** — `0fb7e5907` (test)
2. **Task 2: D-07 frozen-contract byte-identity gate (scribeAI spec) + consolidated gate run** — `afaf4e1ee` (test)

_Plan metadata commit (this SUMMARY) follows._

## Files Created/Modified

- `src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx` — +95 lines: the `Any-combination matrix + document-aware determinism (v3.2-03-04)` describe block (3 cases). Existing 14 quadrant + full-document cases preserved verbatim.
- `src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js` — +27 lines: the D-07 frozen-contract byte-identity case appended to the `v3.2-03 includeDocument framing` block. All existing cases preserved.

## Decisions Made

- **Pre-existing coverage honored, not duplicated.** Plan 02 (commits `5405e184a`, `fd261d6c8`) had already authored the full-document path compose cases (default/budget/error/ephemeral/live-ref) and the includeDocument framing cases (sentence / combined clause / byte-identity / no-numbered-list). This plan added only the regression-gate gaps those didn't cover: the **all-ON** combination, an **doc-ON/others-OFF** combination, **document-aware determinism**, and the explicit **D-07 startsWith** anchor.
- **D-07 verified structurally + behaviorally.** The git-diff structural check (no source touched) is the strongest proof; the byte-identity spec assertion is the executable guard that keeps it from regressing later.

## Deviations from Plan

### Pre-existing coverage (not a fix, documented for traceability)

**1. [scope] The spec assertions the plan describes were largely already present**
- **Found during:** Task 1 + Task 2 (reading the target spec files)
- **Detail:** The plan was authored expecting these document-path spec bodies to be added here; Plan 02 had already authored most of them (the compose full-document path block and the scribeAI includeDocument framing block). Per the "extend, do not overwrite" instruction, every existing test was preserved and only the genuinely-missing regression-gate assertions (all-combination matrix, document-aware determinism, the D-07 startsWith anchor) were added.
- **Impact:** No duplication; the consolidated gate still runs and proves the required guarantees. No source changes were needed because the host compose path (Plan 02) and the notice rendering (Plan 03) were already complete and green.

**Total deviations:** 0 auto-fixed bugs. 1 documented scope note (pre-existing coverage).

## Known Stubs

None — test-only plan; no placeholder data or unwired components introduced.

## Threat Flags

None — this plan modifies only test files; it introduces no new endpoints, auth paths, file-access patterns, or schema changes. It strengthens the mitigation for the already-registered T-v3.2-03-09.

## Self-Check: PASSED

- Files present: `ScribeContext.compose.spec.jsx`, `scribeAI.spec.js` — both FOUND.
- Commits present: `0fb7e5907`, `afaf4e1ee` — both in `git log`.
- Gate: all five suites GREEN with the document path active (142/142). D-07 confirmed (no source diff vs base + explicit byte-identity assertion).

## Notes on test tooling (worktree)

The worktree has no `node_modules`; per Plan 02's documented approach, `node_modules` was symlinked from the main checkout (gitignored, not committed) and jest was run with a worktree-local config (`jest.worktree.config.js`, gitignored, not committed) that drops the `@swc-contrib/mut-cjs-exports` swc plugin (which fails through a symlinked `node_modules`). The Scribe spec mocks do not depend on that plugin. CI in the main checkout runs the full config.

---
*Phase: v3.2-03-cablage-document-complet-strategie-taille*
*Completed: 2026-06-26*
