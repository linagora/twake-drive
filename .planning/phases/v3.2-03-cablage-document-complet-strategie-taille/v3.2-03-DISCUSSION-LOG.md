# Phase v3.2-03 — Discussion Log

**Date:** 2026-06-25
**Mode:** discuss-phase interactif (default), orchestration manuelle (slug vX.Y-NN).
**Areas sélectionnées par l'utilisateur :** Politique de troncature · Retour « tronqué » (UX) · Doc + sélection ensemble · Fraîcheur d'extraction.

---

## Axe 1 — Politique de troncature

**Q (initiale) :** « Quand le docx dépasse le budget, que fait Scribe ? » — options : tronquer+prévenir / refuser+inviter / repli texte-brut+prévenir.
**Échange :** l'utilisateur a d'abord demandé à clarifier, puis a questionné **comment je connais la taille max acceptable du LLM**. Réponse : je ne la connais pas ; ça dépend de la variante Mistral-Small servie par cozy-stack (32k vs 128k), des limites cozy-stack, du budget *utilisable* (< fenêtre nominale, fragilité contrat sur petit modèle), et de la place réservée à la réponse. Aucun budget tokens dans le code (juste cap 1 Mo octets).
**Décision utilisateur :** « le LLM n'est pas connu, c'est configurable. On prévoit la limitation **sur la base d'une config**, mais **par défaut, sans cette conf, on envoie systématiquement tout**. Et on passe à la suite. »
→ **D-02** (config-driven, défaut illimité, troncature signalée si budget configuré dépassé ; seuil/unité/coupe non figés). Repli « Retour tronqué » → **D-03** (indicateur discret, jamais silencieux, forme = discrétion).

## Axe 2 — Retour « tronqué » (UX)
**Résolu indirectement** par la décision de l'axe 1 : la troncature ne survient qu'au cas budget-configuré-dépassé. Capté en **D-03** (indicateur discret + i18n, style SelectionChip, jamais silencieux ; forme exacte = discrétion planner). Non re-drillé (l'utilisateur a demandé d'avancer).

## Axe 3 — Document + sélection cochés ensemble
**Q :** comment traite-t-on la sélection (déjà dans le doc) ? — options : les deux indépendamment / doc⇒sélection=pointeur / indépendance totale.
**Décision :** **« Les deux, indépendamment »** → **D-04** (doc complet + bloc [Selected text…] ; le cadre explique que la sélection = focus).

## Axe 4 — Fraîcheur d'extraction
**Q :** quand extrait-on le doc ? — options : à chaque envoi (frais) / extraction unique+cache / tu décides.
**Décision :** **« À chaque envoi (frais) »** → **D-05** (extraction fraîche à chaque envoi ; cache écarté).

## Clôture
**Q :** prêt pour le CONTEXT / cadrer le wording du frame / explorer d'autres zones.
**Décision :** **« Prêt pour le CONTEXT »**. Le wording du cadre énuméré (D-06) reste à la discrétion du planner ; direction verrouillée depuis v3.2-02.

---

## Deferred / discrétion notés
- Cache d'extraction + invalidation (écarté, reconsidérer si latence).
- Tokenizer exact (hors scope, estimation heuristique).
- Cap transport 1 Mo bridge : contrainte technique à gérer (chunk/borne), pas une décision utilisateur.
