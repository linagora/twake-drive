---
phase: v3.1-02-prompt-plumbing
plan: 03
type: execute
wave: 3
depends_on: [v3.1-02-01-inline-contract-prompt, v3.1-02-02-chat-seam-model-render]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
autonomous: true
requirements: [INLINE-01, INLINE-02]
must_haves:
  decisions: [D-08, D-09, D-11, D-13]
  truths:
    - "handleActionSelect routes the raw LLM response through parseScribeResponse(raw, { surface: 'popover' }) before any display or storage"
    - "The parsed response is normalized to exactly one insertable fragment: 2+ frags join with \\n\\n, 0 frags promote discussion, 1 frag used directly"
    - "The popover shows and inserts only the normalized fragment; discussion is not rendered"
    - "transformCellMarkersForPreview is applied to the normalized fragment text (replacing the raw response)"
    - "Insert/Replace operate on the normalized fragment text"
    - "Each inline exchange mirrors the full parsed turn into shared chat history via addMessage (user breadcrumb + assistant { content: discussion, fragments, fellBack })"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "Inline parse seam, single-fragment normalization, normalized-fragment display/insert, full-turn mirroring"
      contains: "parseScribeResponse"
  key_links:
    - from: "ScribePopover.handleActionSelect"
      to: "parseScribeResponse"
      via: "call with surface popover before display/storage"
      pattern: "parseScribeResponse"
    - from: "ScribePopover normalized fragment"
      to: "transformCellMarkersForPreview"
      via: "preview transform applied to normalized fragment"
      pattern: "transformCellMarkersForPreview"
    - from: "ScribePopover.handleActionSelect"
      to: "addMessage"
      via: "mirror full parsed turn (discussion + fragments + fellBack) into chat history"
      pattern: "addMessage"
---

<objective>
Wire the v3.1-01 contract through the INLINE popover surface, completing the
no-regression plumbing. In handleActionSelect:
1. D-13: route the raw LLM response through `parseScribeResponse(raw, { surface:
   'popover' })` BEFORE any display or storage.
2. D-08 (INLINE-01): normalize the parsed result to exactly one insertable unit —
   2+ fragments join with `\n\n`; 0 fragments promote `discussion`; 1 fragment used
   directly. Re-ask on invalid parse is deferred to v3.1-05.
3. D-09 (INLINE-01): show/insert ONLY the normalized fragment. discussion is NOT rendered
   in the popover. transformCellMarkersForPreview and the rawResult/reinjection path now
   operate on the normalized fragment text (replacing today's raw response).
4. D-11 (INLINE-02): mirror the FULL parsed turn into shared chat history via addMessage —
   the user breadcrumb turn plus an assistant turn shaped like plan 02's model
   ({ content: discussion, discussion, fragments, fellBack }) so the chat render-time
   compose helper renders inline turns identically to chat-native turns.

Purpose: ROADMAP criteria 1 (inline parses + enforces single fragment), 2 (inline call
site parses before display), 5 (inline exchange mirrored into shared history). Completes
the inline half of the zero-regression proof before the v3.1-03 probe.
Output: Modified ScribePopover.jsx.

NO new rendering: the popover still shows a single insertable blob and Insert/Replace
still produce insertable text. On the parse fallback (surface 'popover' yields fragments
== [raw]), normalization yields raw as the single fragment, so Insert/Replace produce the
same insertable text as today — byte-identical fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-02-prompt-plumbing/v3.1-02-CONTEXT.md

@src/modules/views/OnlyOffice/Scribe/scribeResponse.js

<interfaces>
<!-- Public surface this plan consumes. Source: scribeResponse.js (v3.1-01). Use directly. -->

parseScribeResponse(raw, { surface }) returns:
  { discussion, fragments, valid, fellBack, warnings, raw }
  - surface 'popover': fallback yields { discussion: '', fragments: [raw], fellBack: true }
  - On valid parse: fragments may be 0, 1, or N entries; discussion may be present.
  - NEVER throws.
</interfaces>

<existing_code>
<!-- Current inline path. Source: ScribePopover.jsx -->

handleActionSelect (async):
  - messages = buildMessages(actionId, selectedText, label, extra)
  - const text = await callScribeAI(client, messages, { signal })
  - const { displayMd, warning } = transformCellMarkersForPreview(text, enrichedMd)  <- D-09 target: operate on normalized fragment
  - setRawResult(text)                  <- D-09 target: rawResult becomes normalized fragment
  - setCellWarning(warning)
  - setResult({ text: displayMd, breadcrumb, error: '', canRetry: false }); setStep('result')
  - Mirror block (D-11 target):
      addMessage({ id, role: 'user', content: breadcrumb, timestamp })
      addMessage({ id, role: 'assistant', content: text, timestamp })   <- replace with full parsed turn

handleReplace -> onReplace(rawResult || result.text)
handleInsert  -> onInsert(rawResult || result.text)
  (Both stay correct once rawResult holds the normalized fragment.)

addMessage comes from useScribe(); the extended assistant model shape
({ content: discussion, discussion, fragments, fellBack }) is defined in plan 02.
The test-markdown dev bypass (no LLM) and the error/abort branches are out of scope —
do not route them through parseScribeResponse.
</existing_code>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline parse seam + single-fragment normalization + normalized-fragment display/insert</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (handleActionSelect success branch, rawResult/result.text usage, transformCellMarkersForPreview call, handleReplace/handleInsert)
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js (parseScribeResponse popover fallback shape; discussion/fragments semantics)
    - src/modules/views/OnlyOffice/Scribe/tableCellMarkers.js (transformCellMarkersForPreview signature — confirm it takes the response text + enrichedMd)
  </read_first>
  <action>
    In ScribePopover.jsx handleActionSelect, after `const text = await callScribeAI(...)`:
    (a) D-13: import parseScribeResponse from scribeResponse and call
    `parseScribeResponse(text, { surface: 'popover' })` BEFORE any setResult / setRawResult /
    transformCellMarkersForPreview / addMessage.
    (b) D-08: compute a single normalized fragment string from the parsed result:
    if `fragments.length >= 2` join them with `\n\n`; if `fragments.length === 0` use
    `discussion` (promote discussion to the fragment); if exactly 1, use `fragments[0]`.
    Naming of the normalization is Claude's discretion; keep it inline/local.
    (c) D-09: feed the NORMALIZED fragment (not the raw `text`) into
    transformCellMarkersForPreview, and call setRawResult with the normalized fragment so
    Insert/Replace (handleReplace/handleInsert via rawResult) operate on it. discussion is
    NOT rendered in the popover — do not surface discussion in setResult. Keep the
    cellWarning and result.text (displayMd) wiring otherwise unchanged.
    Do not change the test-markdown bypass, the error branch, or the abort logic. Do not
    route the error/abort paths through parseScribeResponse.
  </action>
  <verify>
    <automated>grep -n "parseScribeResponse\|surface: 'popover'\|transformCellMarkersForPreview\|setRawResult" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx | grep -v "^[0-9]*: *//"; node --check src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</automated>
  </verify>
  <acceptance_criteria>
    - ScribePopover.jsx imports parseScribeResponse and calls it with { surface: 'popover' } in handleActionSelect before any setResult/setRawResult/transformCellMarkersForPreview/addMessage (assert ordering by reading the success branch).
    - A normalization step produces one fragment string covering all three cases (>=2 join with \n\n, 0 promote discussion, 1 direct).
    - transformCellMarkersForPreview receives the normalized fragment, and setRawResult is called with the normalized fragment (not the raw response).
    - discussion is not passed to setResult / not rendered in the popover.
    - handleReplace/handleInsert still resolve to rawResult (now the normalized fragment).
    - The fallback path (fragments == [raw]) yields raw as the single fragment, so insertable text is unchanged from today.
    - `node --check src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` exits 0.
  </acceptance_criteria>
  <done>Inline parses with surface 'popover', normalizes to exactly one fragment, and shows/inserts only that fragment via the existing preview+reinjection path.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the full parsed inline turn into shared chat history</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (the addMessage mirror block in handleActionSelect success branch)
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the extended assistant message model shape defined in plan 02: content == discussion + discussion + fragments + fellBack)
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx (compose helper from plan 02 — confirm what fields it reads so mirrored turns render identically)
  </read_first>
  <action>
    In ScribePopover.jsx, update the success-branch mirror block (D-11) so the assistant
    turn pushed via addMessage matches plan 02's extended model: keep the user breadcrumb
    turn ({ role: 'user', content: breadcrumb }) and replace the verbatim assistant turn
    ({ role: 'assistant', content: text }) with the FULL parsed turn from this exchange —
    `content: parsed.discussion`, plus `discussion: parsed.discussion`, `fragments:
    parsed.fragments`, `fellBack: parsed.fellBack`. Reuse the same `parsed` object computed
    in task 1 (do not call parseScribeResponse a second time). This makes the chat
    render-time compose helper render inline turns identically to chat-native turns, and
    lets v3.1-04 cards light up for inline turns too. Leave the error-branch mirror
    (role 'error') unchanged. Do not store the normalized single-fragment blob as the
    mirrored assistant content — the mirror carries the unnormalized discussion + fragments
    so chat shows the full turn; only the popover uses the normalized single fragment.
  </action>
  <verify>
    <automated>grep -n "addMessage\|fragments\|fellBack\|discussion" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx | grep -v "^[0-9]*: *//"; node --check src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</automated>
  </verify>
  <acceptance_criteria>
    - The success-branch assistant addMessage call carries content == parsed.discussion plus discussion, fragments, fellBack (matching plan 02's model).
    - The user breadcrumb addMessage turn is preserved.
    - parsed is reused from task 1 (no second parseScribeResponse call in the success branch).
    - The error-branch mirror (role 'error') is unchanged.
    - Mirrored assistant content is the discussion channel, not the normalized single-fragment blob.
    - `node --check src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` exits 0.
  </acceptance_criteria>
  <done>Each inline exchange mirrors the full parsed turn (breadcrumb + discussion/fragments/fellBack) into shared chat history, rendering identically to chat-native turns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cozy-stack /ai response -> handleActionSelect | Untrusted LLM text parsed before display/insert |
| normalized fragment -> OO document (onInsert/onReplace) | Insertable content crosses into the document via existing reinjection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-02-03 | Tampering/DoS (malformed or oversized LLM JSON) | handleActionSelect parse seam | mitigate | Raw response passes through parseScribeResponse (never throws, DoS-bounded, prototype-pollution safe — v3.1-01). On failure the popover fallback yields raw as the single fragment, preserving today's behavior. |
| T-v3.1-02-03b | Tampering (rich-marker corruption on reinjection) | transformCellMarkersForPreview + onInsert/onReplace | accept | Reinjection pipeline is unchanged; it now runs on the normalized fragment instead of the raw response, but the fragment is byte-identical to today on the fallback path. No new reinjection logic introduced. |

No high-severity threats introduced. No new auth, network endpoint, or dependency. The
only new behavior is routing already-received text through the tested parser and
selecting one normalized fragment.
</threat_model>

<verification>
- `node --check src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` passes.
- handleActionSelect parses with surface 'popover' before display/storage.
- Normalization covers >=2 / 0 / 1 fragment cases to exactly one insertable string.
- Popover shows/inserts only the normalized fragment; discussion not rendered.
- Mirror pushes the full parsed turn matching plan 02's model.
- Existing contract tests still green: `env NODE_ENV=test jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js`.
</verification>

<success_criteria>
Maps to ROADMAP v3.1-02 criterion 1 (inline imposes exactly 1 fragment, discussion not
rendered), criterion 2 (inline call site parses before display), criterion 5 (inline
exchange mirrored into shared chat history and appears in chat). Completes INLINE-01 and
INLINE-02.
</success_criteria>

<output>
Create `.planning/phases/v3.1-02-prompt-plumbing/v3.1-02-03-SUMMARY.md` when done.
</output>
