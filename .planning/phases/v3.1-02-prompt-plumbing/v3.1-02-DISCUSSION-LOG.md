# Phase v3.1-02: Prompt + plumbing - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-16
**Phase:** v3.1-02-prompt-plumbing
**Areas discussed:** Contract prompt design, Interim chat display, Inline cardinality fallback, Message model + mirroring

---

## Contract prompt design

### Prompt explicitness
| Option | Description | Selected |
|--------|-------------|----------|
| Terse rules + 1 example | Prose contract + one worked example with `{{fragment:N}}` | ✓ |
| Terse rules, no example | Prose only, shortest, riskier for generic model | |
| Few-shot (2-3 examples) | Multiple worked examples, most robust, heaviest | |

### Marker-preservation rules placement
| Option | Description | Selected |
|--------|-------------|----------|
| Keep conditional, scope to fragments | Only append when marker present; reword to apply inside fragments | ✓ |
| Always include in contract block | Unconditional on both surfaces | |
| You decide | Defer to planning | |

### Cardinality expression
| Option | Description | Selected |
|--------|-------------|----------|
| Shared block + surface line | One contract + per-surface sentence (inline=1, chat=0..N) | ✓ |
| Two fully separate prompts | Independent inline/chat instructions | |

**Notes:** Model is a generic LLM behind the cozy-stack proxy — no native json_object mode in v3.1, so a concrete example anchors JSON shape. Prompt quality directly gates the v3.1-03 probe.

---

## Interim chat display

### Display strategy before cards exist
| Option | Description | Selected |
|--------|-------------|----------|
| Compose discussion + inline fragments | Replace `{{fragment:N}}` with `fragments[N]`, append unreferenced; throwaway helper | ✓ |
| Discussion only, markers stripped | Fragment content vanishes until v3.1-04 (regression) | |
| Raw passthrough this phase | Display raw text, parse only for model fields | |

### Fallback handling
| Option | Description | Selected |
|--------|-------------|----------|
| Falls out naturally | fallback gives discussion==raw, fragments==[] → renders raw | ✓ |
| You decide | Confirm during planning | |

### Where composition lives (reconciling criterion 3)
| Option | Description | Selected |
|--------|-------------|----------|
| Render-time helper, content stays discussion | content == discussion pure; compose at render | ✓ |
| Store composed in content | content = composed blob; amend criterion 3 | |

**Notes:** Crux of the phase. Render-time composition preserves `content == discussion` literally and keeps multi-turn serialization (which reads the discussion field) clean. The helper is intentionally temporary — deleted when v3.1-04 fragment cards land.

---

## Inline cardinality fallback

### 2+ fragments on a valid inline response
| Option | Description | Selected |
|--------|-------------|----------|
| Join fragments | Concatenate with `\n\n` into one insertable unit | ✓ |
| Use fragment[0] only | Drop the rest (content loss) | |
| Fall back to raw | Treat as parse failure | |

### 0 fragments on a valid inline response
| Option | Description | Selected |
|--------|-------------|----------|
| Promote discussion to the fragment | Use discussion as the single insertable fragment | ✓ |
| Fall back to raw | Raw becomes the fragment | |
| You decide | Defer to planning | |

**Notes:** Re-ask is deferred to v3.1-05; these are interim normalization policies that preserve today's "whole response is insertable" behavior with no content loss.

---

## Message model + mirroring

### Inline turn shape mirrored into chat history
| Option | Description | Selected |
|--------|-------------|----------|
| Mirror full parsed turn | `{content: discussion, fragments, fellBack}` — renders like chat-native, cards light up in v3.1-04 | ✓ |
| Mirror fragment as content only | Simplest, but inline turns diverge / no cards later | |
| You decide | Defer to planning | |

### Empty-discussion mirrored turn appearance
| Option | Description | Selected |
|--------|-------------|----------|
| Compose helper handles it | Shows just the fragment; no empty bubble | ✓ |
| Synthesize a label | Generate label from breadcrumb (needs i18n) | |

### Multi-turn LLM serialization
| Option | Description | Selected |
|--------|-------------|----------|
| Use serializeAssistantTurnForHistory | Reuse v3.1-01 helper (discussion + truncated note) | ✓ |
| Send discussion field only | Drop the fragment note | |

**Notes:** Honors criterion 4 with already-tested v3.1-01 code; never re-injects raw fragment bodies into the LLM context.

---

## Claude's Discretion

- Exact wording/whitespace of contract instruction blocks and the worked example (within D-01/D-02/D-03 shape).
- Naming/location of the throwaway render-time compose helper.
- Whether stored `content` retains or strips `{{fragment:N}}` markers (invisible after composition either way).

## Deferred Ideas

None — discussion stayed within phase scope. Fragment cards, keyboard nav, dev probe, re-ask, i18n, regression corpus, and per-fragment rich reinjection were all correctly recognized as v3.1-03/04/05 scope.
