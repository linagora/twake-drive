---
phase: v3.1-02-prompt-plumbing
verified: 2026-06-17T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase v3.1-02: Prompt + Plumbing Verification Report

**Phase Goal:** Les deux surfaces (chat + inline) emettent le contrat et le parsent a la reception, sans nouveau rendu : la `discussion` sert de contenu affiche, le comportement reste fonctionnellement equivalent a aujourd'hui, prouvant l'absence de regression.
**Verified:** 2026-06-17
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | System prompts emit the contract: chat = 0..N fragments (free discussion), inline = exactly 1 fragment (discussion not rendered) | VERIFIED | `scribeAI.js` line 111: `' Return **exactly ONE** fragment.'` appended to `RESPONSE_CONTRACT_BLOCK` in `buildMessages`. `ScribeContext.jsx` lines 25-31: `CHAT_SYSTEM_PROMPT` declares `"discussion"` and `"fragments"` with explicit `0..N` cardinality. |
| 2 | Both call sites pass the response through the v3.1-01 parse seam before any display | VERIFIED | `ScribeContext.sendMessage` line 157: `parseScribeResponse(responseText, { surface: 'chat' })` called before `setMessages` at line 161. `ScribePopover.handleActionSelect` line 149: `parseScribeResponse(text, { surface: 'popover' })` before `transformCellMarkersForPreview` (line 166), `setRawResult` (line 167), and `addMessage` (lines 179-188). |
| 3 | Extended message model `{discussion, fragments, fellBack}` added, `content == discussion` invariant preserved, existing UI unchanged | VERIFIED | `ScribeContext.jsx` lines 162-171: stored message sets `content: parsed.discussion`, plus `discussion`, `fragments`, `fellBack`. `ChatMessageList.jsx` line 243: `MessageActions` still reads `msg.content` (unchanged). `composeAssistantDisplay` operates at render time only — stored content is never mutated. |
| 4 | Multi-turn history serializes only discussion + compact fragment note (no raw fragment bodies) | VERIFIED | `ScribeContext.jsx` lines 139-147: assistant-turn mapping calls `serializeAssistantTurnForHistory({ discussion: m.discussion ?? m.content, fragments: m.fragments })` — old verbatim `content: m.content` path for assistant turns is gone; backward-compat via `m.discussion ?? m.content` fallback for pre-extended messages. |
| 5 | Each inline exchange (prompt + discussion + fragment) is mirrored into shared chat history and appears in the chat panel | VERIFIED | `ScribePopover.jsx` lines 178-188: success branch pushes user breadcrumb turn + full parsed assistant turn `{ content: parsed.discussion, discussion: parsed.discussion, fragments: parsed.fragments, fellBack: parsed.fellBack }` via `addMessage` — the normalized single-fragment blob is NOT used for the mirror. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/scribeAI.js` | Contract-emitting inline system prompt + reworded conditional marker rules | VERIFIED | `RESPONSE_CONTRACT_BLOCK` const at lines 44-51 contains the terse contract + one worked example (`{{fragment:0}}`). Inline cardinality sentence `Return **exactly ONE** fragment.` at line 111. Three conditional guards (TABLE/CELL, footnote, REF) all reworded to `Inside the fragment string(s) (never in \`discussion\`)` at lines 113, 116, 119. All three remain gated on `extra?.enrichedMd` — counted 3 such guards. |
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` | Chat contract prompt, chat parse seam, extended model, history serialization | VERIFIED | Both `parseScribeResponse` and `serializeAssistantTurnForHistory` imported (lines 9-11). `CHAT_SYSTEM_PROMPT` includes `discussion`, `fragments`, and `0..N` cardinality (lines 22-31). Parse called at line 157 before `setMessages` at 161. Extended model at lines 162-171. History serialization at line 142. |
| `src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx` | Throwaway render-time compose helper (discussion + fragments) | VERIFIED | `extractChannelMarkers` imported from `scribeResponse` (line 11). `composeAssistantDisplay` helper at lines 32-65 annotated as THROWAWAY/v3.1-04. Handles D-06 (empty fragments → return discussion), D-07 (empty discussion + fragments → return fragment text), unreferenced fragments appended. Render path at lines 235-239 uses the helper for messages with contract fields, falls back to `msg.content` for legacy. |
| `src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` | Inline parse seam, single-fragment normalization, normalized display/insert, full-turn mirroring | VERIFIED | `parseScribeResponse` imported (line 17). Called at line 149 before all downstream calls. Normalization at lines 154-161 covers all 3 cases (>=2 join `\n\n`, 0 promote discussion, 1 direct). `transformCellMarkersForPreview` receives normalized fragment at line 166. `setRawResult(normalizedFragment)` at line 167. Mirror pushes `parsed.discussion` as content (not the normalized fragment) at lines 179-188. Single `parseScribeResponse` call — no double-parse. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `buildMessages` systemPrefix | user-role message content | `systemBase` + `RESPONSE_CONTRACT_BLOCK` + cardinality sentence, prepended as prefix | VERIFIED | All 3 return statements in `buildMessages` use `systemPrefix` prefix on `role: 'user'` content. No `role: 'system'` in `scribeAI.js`. |
| `ScribeContext.sendMessage` | `parseScribeResponse` | import + call with `surface: 'chat'` before setMessages | VERIFIED | Line 157 before line 161. |
| `ScribeContext.aiMessages` assistant map | `serializeAssistantTurnForHistory` | assistant-turn content serialization | VERIFIED | Lines 139-147: `if (m.role === 'assistant')` branch calls serializer with `m.discussion ?? m.content` fallback. |
| `ChatMessageList.AssistantBubble` | `composeAssistantDisplay` helper | render-time composition | VERIFIED | Lines 235-242: `hasContractFields` guard, `composeAssistantDisplay` called for extended-model messages, `msg.content` fallback for legacy. |
| `ScribePopover.handleActionSelect` | `parseScribeResponse` | call with `surface: 'popover'` before display/storage | VERIFIED | Line 149, before lines 166-188. |
| `ScribePopover` normalized fragment | `transformCellMarkersForPreview` | preview transform on normalized fragment | VERIFIED | Line 166: `transformCellMarkersForPreview(normalizedFragment, enrichedMd)`. |
| `ScribePopover.handleActionSelect` | `addMessage` | full parsed turn mirrored (discussion + fragments + fellBack) | VERIFIED | Lines 178-188: assistant turn uses `parsed.discussion`, `parsed.fragments`, `parsed.fellBack` — not the normalized blob. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `ChatMessageList.jsx` — `displayContent` | `msg.discussion`, `msg.fragments` | Set in `ScribeContext.sendMessage` via `parseScribeResponse(responseText, { surface: 'chat' })` → `setMessages` with extended model | Yes — populated from live LLM response after parse | FLOWING |
| `ScribePopover.jsx` — `displayMd` (shown in result panel) | `normalizedFragment` | Derived from `parsed.fragments` / `parsed.discussion` after `parseScribeResponse(text, { surface: 'popover' })` | Yes — populated from live LLM response | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| scribeResponse contract tests pass | `env NODE_ENV=test npx jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js` | 38 tests passed, 0 failed, 0.464s | PASS |
| `scribeAI.js` syntax valid | `node --check src/modules/views/OnlyOffice/Scribe/scribeAI.js` | exit 0 | PASS |
| No `role: 'system'` introduced in `buildMessages` | `grep "role.*system" scribeAI.js` | Only in JSDoc comment, no `{ role: 'system' }` object | PASS |
| Conditional marker guards unchanged (3 total) | `grep -cE "extra\?\.enrichedMd &&" scribeAI.js` | 3 | PASS |
| Single `{{fragment:0}}` worked example in prompt body | `grep -n "{{fragment" scribeAI.js` | 1 concrete example at line 50; others in JSDoc/prose (lines 40, 48, 49) | PASS |
| `parseScribeResponse` called once in `ScribePopover` | `grep -n "parseScribeResponse" ScribePopover.jsx` | 2 lines: 1 import (line 17), 1 call (line 149) | PASS |
| `content == discussion` invariant in stored chat message | `ScribeContext.jsx` lines 162-171 | `content: parsed.discussion` + `discussion: parsed.discussion` | PASS |
| Chat fallback no-regression: `composeAssistantDisplay(raw, [])` returns `raw` unchanged | Code inspection: `if (frags.length === 0) return disc` at line 37 of ChatMessageList | Returns `disc` (= `raw`) byte-identically | PASS |
| Popover fallback no-regression: `parseScribeResponse(raw, { surface: 'popover' })` → `fragments: [raw]`, normalization picks `fragments[0]` = `raw` | `scribeResponse.js` `buildFallback` + `ScribePopover.jsx` normalization | `fragments.length === 1` branch: `normalizedFragment = parsed.fragments[0]` = `raw` | PASS |

### Probe Execution

Step 7c: SKIPPED — no `probe-*.sh` files declared for this phase; phase is a prompt/wiring-only change with no runnable probe entry points in `scripts/`.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INLINE-01 | Plans 01, 03 | Inline surface emits exactly-1-fragment contract and normalizes response to a single insertable fragment before display/insert | SATISFIED | `scribeAI.js`: `RESPONSE_CONTRACT_BLOCK` + `' Return **exactly ONE** fragment.'` in `buildMessages`. `ScribePopover.jsx` lines 149-168: parse → normalize (3-case logic) → `transformCellMarkersForPreview(normalizedFragment)` → `setRawResult(normalizedFragment)`. discussion NOT passed to `setResult`. |
| INLINE-02 | Plans 02, 03 | Both call sites route LLM response through `parseScribeResponse`; extended model with content==discussion + fragments + fellBack; multi-turn history via `serializeAssistantTurnForHistory`; inline exchanges mirrored into shared chat history | SATISFIED | `ScribeContext.jsx` lines 9-11, 142-147, 157-171. `ScribePopover.jsx` lines 17, 149, 178-188. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ChatMessageList.jsx` | 16-17 (JSDoc) | Annotated "THROWAWAY" on `composeAssistantDisplay` | Info | Intentional — matches plan spec (D-04/D-07); slated for removal in v3.1-04. Not a regression concern. |

No TBD/FIXME/XXX debt markers found in any file modified by this phase.

### Human Verification Required

None. All truths are verifiable via static code analysis and the automated test suite.

### Gaps Summary

No gaps. All 5 phase success criteria are met by the live code:

1. Both system prompts emit the contract with correct cardinality (chat 0..N, inline exactly 1).
2. Both call sites route LLM responses through `parseScribeResponse` before display/storage.
3. Extended message model preserves `content == discussion` while adding structured fields; existing UI is undisturbed.
4. Multi-turn history uses `serializeAssistantTurnForHistory` — no raw fragment bodies injected into LLM context.
5. Inline exchanges are mirrored into shared chat history with the full parsed turn shape (not the normalized blob), enabling consistent rendering when the chat panel is opened.

The zero-regression proof holds: chat fallback (`fragments == []`) renders `discussion` unchanged; popover fallback (`fragments == [raw]`) normalizes to `raw` as the single insertable fragment — byte-identical to pre-phase behavior in both cases. 38/38 contract tests pass.

---

_Verified: 2026-06-17_
_Verifier: Claude (gsd-verifier)_
