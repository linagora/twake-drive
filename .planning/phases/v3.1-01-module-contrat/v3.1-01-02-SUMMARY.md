---
phase: v3.1-01-module-contrat
plan: 02
subsystem: api
tags: [llm, markers, mcp, json-schema, regex, scribe]

# Dependency graph
requires:
  - phase: v3.1-01-01
    provides: parseScribeResponse core + result shape in scribeResponse.js
provides:
  - FRAGMENT_MARKER_RE strict {{fragment:N}} grammar (never collides with {{REF:scribe-ref-N:…}})
  - extractChannelMarkers(text, channel) generic ordered-hit scanner (forward-compat for {{action:N}})
  - serializeAssistantTurnForHistory(parsed) multi-turn history serializer (discussion + truncated compact note)
  - SCRIBE_OUTPUT_SCHEMA documented JSON-Schema artifact + SCRIBE_OUTPUT_SCHEMA.md
  - Non-fatal {{fragment:N}} cross-check wired into parseScribeResponse (fragment-marker-out-of-range / fragment-not-referenced)
affects: [v3.1-02 prompt plumbing, v3.1-04 chat/popover render, future {{action:N}} channel]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Channel-anchored linear marker regex (literal channel name + ':' + \\d+ + '}}'), never greedy .+?"
    - "Generic per-call channel marker scanner reused across marker families"
    - "Multi-turn history = discussion + truncated English fragment note (ARCHITECTURE §2 Option B), not i18n"

key-files:
  created:
    - src/modules/views/OnlyOffice/Scribe/SCRIBE_OUTPUT_SCHEMA.md
  modified:
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js

key-decisions:
  - "FRAGMENT_MARKER_RE is the documented source-of-truth; extractChannelMarkers builds fresh per-call regexes to avoid stateful global-regex lastIndex bugs"
  - "Cross-check warnings are non-fatal: never flip a parsed success to throw, never change fellBack"
  - "HISTORY_FRAGMENT_TRUNCATE = 160 chars; serializer never emits full fragment bodies or raw contract JSON"
  - "SCRIBE_OUTPUT_SCHEMA is documentation-only; runtime validation stays hand-rolled"

patterns-established:
  - "{{fragment:N}} 0-indexed; index N resolves to fragments[N]"
  - "{{fragment:N}} provably never matches {{REF:scribe-ref-N:…}} (CONTRACT-04), proven by dedicated tests"

requirements-completed: [CONTRACT-01, CONTRACT-04]

# Metrics
duration: ~15min
completed: 2026-06-16
---

# Phase v3.1-01 (Plan 02): Markers + MCP + Schema Summary

**Fragment marker grammar (`{{fragment:N}}`, REF-collision-proof), generic `extractChannelMarkers`, multi-turn `serializeAssistantTurnForHistory`, and the documented `SCRIBE_OUTPUT_SCHEMA` artifact — completing the contract module's public surface.**

## Performance

- **Duration:** ~15 min (executed inline by orchestrator, same as plan 01)
- **Completed:** 2026-06-16
- **Tasks:** 3
- **Files modified:** 1 created, 2 modified

## Accomplishments
- `FRAGMENT_MARKER_RE` + `extractChannelMarkers(text, channel)`: strict, linear, read-only, 0-indexed marker scanner that provably never matches `{{REF:scribe-ref-N:…}}` cross-ref markers (dedicated green tests).
- Non-fatal `{{fragment:N}}` cross-check wired into `parseScribeResponse` — `fragment-marker-out-of-range` and `fragment-not-referenced` warnings, never throwing, `fellBack` unchanged (completes ROADMAP criterion 1).
- `serializeAssistantTurnForHistory(parsed)`: discussion + truncated compact English note; never leaks full fragment bodies or raw JSON.
- `SCRIBE_OUTPUT_SCHEMA` exported (documentation artifact) + `SCRIBE_OUTPUT_SCHEMA.md` documenting the contract, the 0-indexed marker convention, and the REF non-collision rule.

## Task Commits

Each task committed atomically (TDD RED → GREEN):

1. **Task 1: failing spec for markers + REF preservation + cross-check** — `f9769ce0d` (test)
2. **Task 2: FRAGMENT_MARKER_RE + extractChannelMarkers + cross-check wiring** — `ee3f9dc1d` (feat)
3. **Task 3: serializeAssistantTurnForHistory + SCRIBE_OUTPUT_SCHEMA artifact** — `45fde0017` (feat)

## Files Created/Modified
- `src/modules/views/OnlyOffice/Scribe/scribeResponse.js` — marker grammar, scanner, serializer, schema, cross-check wiring
- `src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js` — extended to 38 tests (markers, REF preservation, cross-check, serialize, schema)
- `src/modules/views/OnlyOffice/Scribe/SCRIBE_OUTPUT_SCHEMA.md` — human-readable contract doc

## Decisions Made
- Executed inline (same rationale as plan 01: transient API 529 killed subagent dispatches).
- `extractChannelMarkers` builds a fresh per-call `RegExp` (channel name regex-escaped) rather than reusing the global `FRAGMENT_MARKER_RE`, avoiding stateful `lastIndex` bugs and supporting a future `{{action:N}}` channel.

## Deviations from Plan
None — plan executed as written (inline execution mechanism aside).

## Issues Encountered
None during execution. (The pre-existing SDK milestone-prefix limitation noted in plan 01 still applies to ROADMAP/STATE auto-updates — handled manually at phase close.)

## Verification
- `env NODE_ENV='test' npx jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js` → 38/38 pass.
- Dedicated REF-preservation tests green: `{{REF:scribe-ref-N:…}}` never matched as a fragment marker, preserved verbatim through parse.
- Purity grep: no React/network/require imports. `git diff package.json` empty.

## Next Phase Readiness
- The contract module (`scribeResponse.js`) is feature-complete for v3.1-01: parse + validate + fallback + markers + MCP helpers + schema. Ready for v3.1-02 (prompt plumbing / dev probe) to consume `parseScribeResponse` at the `callScribeAI` seam.

---
*Phase: v3.1-01-module-contrat*
*Completed: 2026-06-16*
