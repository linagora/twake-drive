---
phase: 13-reinjection-et-integrite-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/View.jsx
  - plugins/onlyoffice-scribe/scripts/code.js
autonomous: false
requirements:
  - REINJ-01
  - REINJ-02
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04

must_haves:
  truths:
    - "L'action Replace reinjecte du HTML formate dans OO via PasteHtml -- gras, italique, titres et listes sont preserves"
    - "L'action Insert After insere du contenu HTML formate apres la selection sans ecraser le texte existant"
    - "Le formatage inline (gras, italique) survit au cycle complet: OO -> LLM -> OO"
    - "Les blocs structurels (titres, listes a puces, listes numerotees) survivent au cycle complet"
    - "Les tableaux, liens et blocs de code survivent au cycle complet"
    - "Si le champ html est absent, PasteText est utilise comme fallback (backward compat)"
  artifacts:
    - path: "src/modules/views/OnlyOffice/View.jsx"
      provides: "MD-to-HTML conversion before sending response to plugin"
      contains: "markdownToHtml"
    - path: "plugins/onlyoffice-scribe/scripts/code.js"
      provides: "PasteHtml-based reinsertion with fallback to PasteText"
      contains: "PasteHtml"
  key_links:
    - from: "src/modules/views/OnlyOffice/View.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/scribeConversion.js"
      via: "import markdownToHtml"
      pattern: "import.*markdownToHtml.*scribeConversion"
    - from: "src/modules/views/OnlyOffice/View.jsx"
      to: "plugins/onlyoffice-scribe/scripts/code.js"
      via: "respond() sends { text, html } via postMessage"
      pattern: "data:\\s*\\{.*text.*html"
    - from: "plugins/onlyoffice-scribe/scripts/code.js"
      to: "OO API"
      via: "PasteHtml method call"
      pattern: "executeMethod.*PasteHtml"
---

<objective>
Wire the "Replace" and "Insert After" buttons to send formatted HTML (not plain text) back to the OO plugin, which calls PasteHtml instead of PasteText. This completes the rich text round-trip pipeline: OO selection -> HTML extraction -> Markdown conversion -> LLM -> Markdown response -> HTML reconversion -> PasteHtml reinsertion.

Purpose: Complete the v2.1 Formatage Riche milestone by making formatting survive the full cycle.
Output: Working rich text reinsertion for Replace and Insert After actions with plain text fallback.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reinjection-et-integrite-pipeline/13-RESEARCH.md

<interfaces>
<!-- Key functions and contracts the executor needs -->

From src/modules/views/OnlyOffice/Scribe/scribeConversion.js:
```javascript
export function markdownToHtml(markdown)
// Converts Markdown string to HTML string via marked.parse()
// Example: markdownToHtml("**bold**") => "<p><strong>bold</strong></p>\n"
```

From src/modules/views/OnlyOffice/View.jsx (current state):
```javascript
const handleReplace = useCallback(
  text => {
    respond({ status: 'ok', action: 'replace', data: { text } })
    setTimeout(focusEditor, 100)
  },
  [respond, focusEditor]
)

const handleInsert = useCallback(
  text => {
    respond({ status: 'ok', action: 'insert', data: { text } })
    setTimeout(focusEditor, 100)
  },
  [respond, focusEditor]
)
```

From plugins/onlyoffice-scribe/scripts/code.js (current state):
```javascript
// ES5 only -- no arrow functions, no const/let
var lastSelectedHtml = "";  // stores class-stripped HTML from init()

function handleIntentResponse(msg) {
  if (msg.action === "replace") {
    window.Asc.plugin.executeMethod("PasteText", [msg.data.text]);
  } else if (msg.action === "insert") {
    insertAfterWithText(msg.data.text);
  } else if (msg.action === "cancel") {
    log("Intent cancelled");
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HTML conversion to View.jsx response handlers</name>
  <files>src/modules/views/OnlyOffice/View.jsx</files>
  <action>
    1. Add import at top of file:
       `import { markdownToHtml } from '@/modules/views/OnlyOffice/Scribe/scribeConversion'`

    2. Update `handleReplace` callback: before calling respond(), convert the Markdown text to HTML via `markdownToHtml(text)`, then send both fields in the response payload:
       `respond({ status: 'ok', action: 'replace', data: { text, html } })`
       The `text` field is kept for backward compatibility and plain-text fallback.

    3. Update `handleInsert` callback with the same pattern:
       `const html = markdownToHtml(text)`
       `respond({ status: 'ok', action: 'insert', data: { text, html } })`

    Do NOT modify handleCancel, triggerScribe, focusEditor, or any other code in View.jsx.
    Do NOT modify scribeConversion.js -- markdownToHtml already exists and works.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -n "markdownToHtml" src/modules/views/OnlyOffice/View.jsx | head -5 && grep -n "data:.*html" src/modules/views/OnlyOffice/View.jsx | head -5</automated>
  </verify>
  <done>handleReplace and handleInsert both call markdownToHtml() and include html field in response payload alongside text field</done>
</task>

<task type="auto">
  <name>Task 2: Update plugin to use PasteHtml with fallback and add insertAfterWithHtml</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js</files>
  <action>
    IMPORTANT: All code must be ES5 syntax (no arrow functions, no const/let, no template literals).

    1. Update `handleIntentResponse` function to check for `msg.data.html` field and use PasteHtml when present, falling back to existing PasteText/insertAfterWithText behavior:

       For "replace" action:
       - If `msg.data && msg.data.html`: call `window.Asc.plugin.executeMethod("PasteHtml", [msg.data.html])`
       - Else: call `window.Asc.plugin.executeMethod("PasteText", [msg.data.text])` (existing behavior)

       For "insert" action:
       - If `msg.data && msg.data.html`: call new `insertAfterWithHtml(msg.data.html)`
       - Else: call existing `insertAfterWithText(msg.data.text)`

       Add log() calls distinguishing HTML vs plain text paths.

    2. Add new function `insertAfterWithHtml(newHtml)` near the existing `insertAfterWithText`:
       - Concatenates `lastSelectedHtml + "<p>&nbsp;</p>" + newHtml`
       - Calls `window.Asc.plugin.executeMethod("PasteHtml", [combined])`
       - This replaces the selection with original content + separator + new content, effectively inserting after.
       - `lastSelectedHtml` is already stored from init() and contains the class-stripped HTML.

    Do NOT modify init(), castIntent(), buildEditIntentData(), selection polling, or any other functions.
    Do NOT change the insertAfterWithText function -- keep it as fallback.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -n "PasteHtml" plugins/onlyoffice-scribe/scripts/code.js | head -5 && grep -n "insertAfterWithHtml" plugins/onlyoffice-scribe/scripts/code.js | head -5</automated>
  </verify>
  <done>handleIntentResponse uses PasteHtml when html field present and falls back to PasteText/insertAfterWithText when absent. insertAfterWithHtml function concatenates original HTML + separator + new HTML and calls PasteHtml.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify rich text round-trip in browser</name>
  <action>Human verifies the full rich text reinsertion pipeline works in the browser.</action>
  <what-built>Rich text reinsertion pipeline: Replace and Insert After buttons now send formatted HTML to the OO plugin, which uses PasteHtml to preserve formatting through the full cycle.</what-built>
  <how-to-verify>
    Before testing, clear plugin cache:
    rm -f plugins/onlyoffice-scribe/**/*.gz plugins/onlyoffice-scribe/*.gz

    Restart OO container:
    ./scripts/oo-dev-setup.sh

    Then test in the browser:

    1. REPLACE with inline formatting:
       - Select text containing bold and italic in OO
       - Click Scribe button, choose an action (e.g. "Improve")
       - Click "Replace"
       - Verify: replaced text preserves bold/italic formatting from the AI response

    2. REPLACE with structural elements:
       - Select a paragraph, use Scribe with a prompt like "Convert to a bulleted list"
       - Click "Replace"
       - Verify: bulleted list appears with proper formatting in OO

    3. INSERT AFTER:
       - Select some text, use Scribe to generate additional content
       - Click "Insert After"
       - Verify: original text is preserved WITH its formatting, new content appears after with formatting

    4. PLAIN TEXT FALLBACK (optional):
       - Open browser console, confirm no errors during Replace/Insert operations
       - Check [Scribe] logs show "HTML" path being used

    5. TABLES AND CODE (if LLM returns them):
       - Try a prompt that generates a table or code block
       - Verify formatting survives reinsertion

    Note: Ordered lists may show all items as "1." due to known OO bug #79263. This is acceptable for v2.1.
  </how-to-verify>
  <resume-signal>Type "approved" if rich text reinsertion works, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- grep confirms markdownToHtml import and html field in View.jsx response payloads
- grep confirms PasteHtml calls in plugin code.js
- grep confirms insertAfterWithHtml function exists in plugin code.js
- Human verification confirms end-to-end rich text round-trip works in browser
</verification>

<success_criteria>
- Replace action sends { text, html } and plugin uses PasteHtml -- formatted text appears in OO
- Insert After action concatenates original HTML + new HTML via PasteHtml -- original formatting preserved
- Plain text fallback works when html field is absent
- Inline formatting (bold, italic) survives the full cycle
- Structural elements (headings, lists, paragraphs) survive the full cycle
- Tables, links, and code blocks survive the full cycle
</success_criteria>

<output>
After completion, create `.planning/phases/13-reinjection-et-integrite-pipeline/13-01-SUMMARY.md`
</output>
