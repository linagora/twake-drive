---
phase: 24.1-table-round-trip-rearchitecture-clone-insertcontent
plan: 02
subsystem: ui
tags: [onlyoffice, tables, clone, insertcontent, callcommand, es5]

requires:
  - phase: 24.1-table-round-trip-rearchitecture-clone-insertcontent
    plan: 01
    provides: "[TABLE:N] wrapper markers in extraction output"
provides:
  - "Clone+InsertContent table round-trip architecture replacing in-place modification"
  - "Mixed [ApiParagraph, ApiTable] content arrays through InsertContent"
  - "Both Replace and Insert modes for table content"
affects: [table-reinjection, multi-table, future-table-features]

tech-stack:
  added: []
  patterns: ["Clone table via Copy() before InsertContent, modify clone cells, push into content[]"]

key-files:
  created: []
  modified:
    - plugins/onlyoffice-scribe/scripts/code.js

key-decisions:
  - "Table clones stored in object keyed by TABLE index (not array) for sparse access in content loop"
  - "Placeholder tokens (__SCRIBE_TABLE_N__) survive marked.lexer as paragraph text for detection in content building loop"

patterns-established:
  - "Pre-cache pattern: Clone originals before InsertContent (same as image pre-cache pattern)"
  - "Placeholder substitution: special text tokens in markdown -> detected in content loop -> replaced with cloned objects"

requirements-completed: []

duration: 8min
completed: 2026-03-23
---

# Phase 24.1 Plan 02: Clone+InsertContent Table Architecture Summary

**Table round-trip rearchitected: clone via Copy(), modify clone cells, push into mixed content[] for InsertContent -- both Replace and Insert modes work**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-23T13:26:13Z
- **Completed:** 2026-03-23T13:34:16Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Replaced in-place table cell reinjection with clone+InsertContent architecture
- TABLE:N blocks parsed in plugin scope, cells pre-flattened, passed as parsedTables via Asc.scope
- Tables cloned via Copy() before InsertContent destroys the selection, clone cells modified with LLM runs + source font
- Content building loop detects __SCRIBE_TABLE_N__ placeholders and pushes clones into content[]
- InsertContent receives mixed [ApiParagraph, ApiTable] arrays -- no tableRoundTripDone guard
- Both Replace and Insert modes now work naturally for table content

## Task Commits

Each task was committed atomically:

1. **Task 1: Parse TABLE:N blocks in plugin scope and prepare table data for callCommand** - `84d327d55` (feat)
2. **Task 2: Clone tables + InsertContent in callCommand, remove in-place path** - `5cff40628` (feat)

## Files Created/Modified
- `plugins/onlyoffice-scribe/scripts/code.js` - TABLE:N block parsing, placeholder tokens, clone+InsertContent architecture, removed in-place path and guard

## Decisions Made
- Placeholder tokens (__SCRIBE_TABLE_N__) chosen over HTML comments because they survive marked.lexer as simple paragraph text
- Table clones stored in object keyed by index (not array) for direct lookup in content loop
- Backward compat: bare CELL markers without TABLE wrappers auto-wrapped in TABLE:0

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Clone+InsertContent architecture complete -- tables flow through the same InsertContent path as paragraphs
- Multi-table support is structurally ready (parsedTables array, tableClones object)
- Post-injection selection may need adjustment for mixed content (tables have no text length contribution)

---
*Phase: 24.1-table-round-trip-rearchitecture-clone-insertcontent*
*Completed: 2026-03-23*
