---
phase: v3.2-02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeAI.js
  - src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx
autonomous: true
requirements: [CTX-LLM-02, CTX-LLM-03, CTX-LLM-05]
must_haves:
  truths:
    - "CTX-LLM-03 (selection ON): « sélection » checked ⇒ the current turn's selection is sent — the current user message carries the inline [Selected text from document]…[End of selected text] block (encoded via the existing encodeSelectionForPrompt) AND buildChatSystemPrompt is called WITH the selection markdown (marker-preservation clauses present when markers exist)"
    - "CTX-LLM-03 / D-04 (selection OFF): « sélection » unchecked ⇒ EVERYTHING about the current selection is removed for the current turn — NO [Selected text…] block on the current user message AND buildChatSystemPrompt is called WITHOUT selectionMd (no marker-preservation clauses). The LLM sees nothing of the current selection."
    - "CTX-LLM-02 / D-01 (discussion OFF, the NEW default): « discussion » unchecked ⇒ the aiMessages array contains ONLY system prompt + the current turn (current user message, with its selection block iff « sélection » checked); ZERO prior user/assistant turns are mapped in"
    - "CTX-LLM-02 / D-01 (discussion ON): « discussion » checked ⇒ full prior history is included exactly as v3.1 did — prior user turns replay their inline [Selected text…] block AS-IS, prior assistant turns are serialized via serializeAssistantTurnForHistory (discussion-only + truncated fragment note, never raw fragments)"
    - "D-02 (selection gate scope): « sélection » gates ONLY the current turn. Past turns' selections are part of history and are replayed unchanged whenever « discussion » is checked — unchecking « sélection » never strips a past turn's [Selected text…] block"
    - "D-03 / CTX-LLM-05 determinism: composition is a pure function of (transcript, live checkbox values) — same transcript + same checkbox values ⇒ byte-identical aiMessages array, asserted by a deterministic unit test (no network, no model)"
    - "D-03 live-read (no stale closure): the include booleans are read LIVE at send time (via refs mirroring the existing messagesRef.current pattern, or correct useCallback deps) so a checkbox toggled between renders is honored on the very next send — no stale-closure regression"
    - "D-05 framing seed: buildChatSystemPrompt emits a SHORT, conditional English announcement of which context sources are actually included this turn (selection / discussion), driven by per-source flags. It does NOT build the full enumerated v3.2-03 frame, does NOT incite meta-commentary, and does NOT perturb the discussion/fragments JSON contract"
    - "CTX-LLM-05 contract intact: injected context NEVER leaks into fragments; the discussion/fragments separation, the single corrective re-ask (callScribeAIWithReask), and the contextual fallback all stay byte-for-byte unchanged. scribeResponse.js / callScribeAIWithReask are NOT modified."
    - "Regression GREEN: yarn test for scribeAI.spec.js, scribeResponse.corpus.spec.js, scribeResponse.spec.js and scribeProbe.spec.js all pass with the wiring in place (no contract drift, no fragment-leak)"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "Gated prompt composition at the sendMessage seam: discussion gate on the history filter/map, selection gate on the current turn's [Selected text…] block + the selectionMd passed to buildChatSystemPrompt, include booleans read live via refs, D-05 source flags forwarded to buildChatSystemPrompt"
      contains: "includeDiscussion"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      provides: "buildChatSystemPrompt extended with a short, conditional D-05 context-source announcement driven by explicit per-source flags; marker clauses still keyed off selectionMd; RESPONSE_CONTRACT_CORE / CARDINALITY_CHAT / CHAT_PERSONA unchanged"
      contains: "buildChatSystemPrompt"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx"
      provides: "Deterministic composition test: mocks callScribeAIWithReask, captures the aiMessages array, asserts the four gate matrices (sel on/off × discussion on/off), the D-02 past-selection-replay invariant, byte-identical determinism on repeat, and the no-fragment-leak / contract-untouched guard"
      contains: "aiMessages"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js"
      provides: "Extended unit coverage for the D-05 conditional framing in buildChatSystemPrompt (announcement present/absent per source flag; contract core + cardinality unchanged; no meta-comment incitement)"
      contains: "buildChatSystemPrompt"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      via: "buildChatSystemPrompt called with selectionMd gated by includeSelection AND the per-source D-05 flags (includeSelection/includeDiscussion read live)"
      pattern: "buildChatSystemPrompt"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      to: "the history filter/map in sendMessage"
      via: "discussion gate skips prior turns when includeDiscussion is false (current turn only)"
      pattern: "includeDiscussion"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      to: "live checkbox values"
      via: "refs mirroring messagesRef.current so toggled booleans are honored at send (no stale closure)"
      pattern: "Ref.current"
---

<objective>
Wire the two cheap in-memory contexts — conversation history and the current selection — into the LLM prompt at the single composition seam `ScribeContext.sendMessage`, gated by the « discussion » and « sélection » include checkboxes posed (state-only) in v3.2-01. Reuse the existing encoders (`encodeSelectionForPrompt`, the inline `[Selected text from document]…[End of selected text]` block, `serializeAssistantTurnForHistory`). Keep the native multi-turn array and the v3.1 `discussion`/`fragments` response contract byte-for-byte intact (D-00 = gate the existing structure, no redesign).

Purpose: Make the include checkboxes actually shape the prompt — deterministically — without touching the hard-gated v3.1 contract, re-ask, or fallback. Establish the composition point as a clean, extensible seam so v3.2-03 can branch in the full document later without a rewrite.

Output: A gated, deterministic prompt-composition seam in `ScribeContext.sendMessage`; a short conditional D-05 framing line in `buildChatSystemPrompt`; and an explicit PROBE-01 + v3.1-regression + determinism verification gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/REQUIREMENTS.md
@.planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-CONTEXT.md
@.planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-CONTEXT.md

@src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
@src/modules/views/OnlyOffice/Scribe/scribeAI.js
@src/modules/views/OnlyOffice/Scribe/scribeResponse.js
@src/modules/views/OnlyOffice/Scribe/scribeProbe.js
@src/modules/views/OnlyOffice/Scribe/ChatInput.jsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

ScribeContext.jsx (the seam):
- include booleans (useState, l.65-67): `includeDocument` (false), `includeDiscussion` (false), `includeSelection` (true). Already exposed on the provider value.
- `messagesRef` (l.78-79): `const messagesRef = useRef(messages); messagesRef.current = messages` — the EXISTING live-read pattern that avoids a stale closure in the `sendMessage` useCallback (deps `[client, t]`). Mirror this for the include booleans.
- `sendMessage = useCallback(async (text, selectionContext) => {…}, [client, t])` (l.151-305). The SEAM comment is at l.164. Current assembly (l.168-213):
  - `currentMessages = [...messagesRef.current, userMessage]`
  - `const { selectionMd: currentSelectionMd } = encodeSelectionForPrompt({ enrichedMd: selectionContext?.markdown, text: selectionContext?.text })`
  - `aiMessages = [ { role:'system', content: buildChatSystemPrompt(currentSelectionMd) }, ...currentMessages.filter(error).filter(user|assistant).map(…) ]`
  - the user-with-selection branch builds the inline `[Selected text from document]\n${selectionMd}\n[End of selected text]\n\n${m.content}` block (l.188-196)
  - the assistant branch calls `serializeAssistantTurnForHistory({ discussion: m.discussion ?? m.content, fragments: m.fragments })` (l.202-209)
- After assembly: `callScribeAIWithReask(client, aiMessages, { surface: 'chat' })` — DO NOT change this call or its result handling.

scribeAI.js:
- `encodeSelectionForPrompt(sel) → { selectionMd, markerClauses }` (l.122) — single source of truth for selection encoding. Pass nothing / empty to get `selectionMd === ''`.
- `buildChatSystemPrompt(selectionMd)` (l.137): currently `CHAT_PERSONA + '\n\n' + RESPONSE_CONTRACT_CORE + CARDINALITY_CHAT + markerPreservationClauses(selectionMd)`. Calling with `''`/`undefined` already drops the marker clauses (D-04 mechanism).
- `RESPONSE_CONTRACT_CORE`, `CARDINALITY_CHAT`, `CHAT_PERSONA` (l.50-84) — the hard-gated v3.1 contract strings. DO NOT modify them.
- `callScribeAIWithReask` (l.343) and `markerPreservationClauses` (l.95) — DO NOT modify.

scribeResponse.js:
- `serializeAssistantTurnForHistory({ discussion, fragments }) → string` (l.357): discussion + a truncated fragment note; never raw fragment bodies, never raw contract JSON. DO NOT modify.

Test infra: Jest. Run with `yarn test <file>`. Existing relevant specs:
- `scribeAI.spec.js` — prompt-composition contract (asserts core/cardinality strings)
- `scribeResponse.corpus.spec.js` — the v3.1 malformed-response regression corpus
- `scribeResponse.spec.js` — parser unit tests
- `scribeProbe.spec.js` — PROBE-01 conformance metrics engine
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate selection (current turn) + discussion (history) at the sendMessage seam, reading checkboxes live</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the whole sendMessage seam l.151-305; the include booleans l.65-67; the existing messagesRef live-read pattern l.78-79)
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (encodeSelectionForPrompt l.122, buildChatSystemPrompt l.137)
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js (serializeAssistantTurnForHistory l.357 — confirm it is unchanged and discussion-only)
    - .planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-CONTEXT.md (D-01, D-02, D-03, D-04 — the locked behaviors)
  </read_first>
  <action>
Modify ONLY `sendMessage` (and add the ref plumbing above it). Do NOT touch the response handling (callScribeAIWithReask call, parsed result, probe/log/setMessages) — those stay byte-for-byte.

(a) Live-read plumbing (D-03, no stale-closure regression): add refs for the include booleans mirroring the EXISTING `messagesRef.current = messages` pattern at l.78-79 — e.g. an `includeDiscussionRef` and `includeSelectionRef` whose `.current` is assigned on every render. Inside `sendMessage` read the booleans from `*.current`, NOT from the closed-over `includeDiscussion`/`includeSelection` (the callback deps are `[client, t]` and MUST stay that way to avoid re-creating the callback / changing identity). `includeDocument` is out of scope for this phase — do not read or branch on it.

(b) Selection gate — current turn ONLY (D-02, D-04): introduce a local `selectionIncluded = includeSelectionRef.current && !!selectionContext`. When `selectionIncluded` is false: (1) build the current user message content WITHOUT the `[Selected text from document]…[End of selected text]` block (plain `userMessage.content`), and (2) compute `currentSelectionMd` as `''` so `buildChatSystemPrompt` receives no selection markdown (no marker-preservation clauses — the D-04 mechanism). When true: keep the existing behavior exactly (encode via encodeSelectionForPrompt, build the inline block, pass the real selectionMd). IMPORTANT: the current turn's `userMessage` object stored in `setMessages` keeps `.selection` AS SENT — i.e. set `userMessage.selection = selectionIncluded ? selectionContext : null` so the transcript is the faithful registry of what was actually sent (D-03), and so a later history replay of THIS turn reflects what was sent. Do not attach a selection that wasn't included.

(c) Discussion gate — prior turns (D-01): when `includeDiscussionRef.current` is false, the mapped history must contain ONLY the current turn — i.e. map over just `[userMessage]` (current turn) instead of `[...messagesRef.current, userMessage]`. When true, keep the existing full `currentMessages` history map verbatim (prior user turns replay their inline [Selected text…] block AS-IS via their stored `.selection`; prior assistant turns via serializeAssistantTurnForHistory). CRITICAL (D-02): the discussion gate decides how many turns are mapped; it MUST NOT alter how a PAST turn is rendered — past turns replay exactly as v3.1 did. Do not let the current-turn selection gate leak into the past-turn mapping.

(d) Keep the system message first, then the mapped turns. The map's two branches (user-with-selection, assistant-via-serializer, plain-user fallback) stay structurally identical to today; only the INPUT array (current-turn-only vs full history) and the current turn's selection-block presence change.

No fenced code in this plan — name the identifiers and the exact gate conditions; the executor writes the JSX/JS.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && yarn test src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - sendMessage useCallback deps remain `[client, t]` (NOT widened to include the booleans); the booleans are read via `*.current` refs assigned each render.
    - With « sélection » OFF: the current user message content has NO `[Selected text from document]` substring AND `buildChatSystemPrompt` is invoked with `''` (verifiable in Task 3's compose spec).
    - With « sélection » ON: current user message contains the inline `[Selected text from document]…[End of selected text]` block and buildChatSystemPrompt receives the real selectionMd.
    - With « discussion » OFF: the assembled aiMessages has exactly 2 entries (system + current user turn) regardless of how many prior turns exist in messages.
    - With « discussion » ON: prior turns are present and a prior user turn with a stored `.selection` still shows its `[Selected text…]` block (D-02 replay), independent of the current « sélection » value.
    - The stored current `userMessage.selection` equals `selectionContext` when included, `null` when not (faithful registry, D-03).
    - callScribeAIWithReask call, parsed handling, logScribeExchange, recordProbeSample, and setMessages(assistant) are byte-for-byte unchanged vs the read_first file.
    - scribeResponse.corpus.spec.js and scribeProbe.spec.js stay GREEN.
  </acceptance_criteria>
  <done>Selection and discussion gates wired at the seam, booleans read live via refs, response handling untouched; corpus + probe specs pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the D-05 conditional context-source framing line to buildChatSystemPrompt</name>
  <files>src/modules/views/OnlyOffice/Scribe/scribeAI.js, src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (buildChatSystemPrompt l.137, CHAT_PERSONA/RESPONSE_CONTRACT_CORE/CARDINALITY_CHAT l.50-84, markerPreservationClauses l.95 — do NOT modify the contract strings)
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the system-message line that calls buildChatSystemPrompt, as gated in Task 1)
    - .planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-CONTEXT.md (D-05 — the LIGHT additive framing; the explicit "this is a SEED only" boundary)
  </read_first>
  <action>
Extend `buildChatSystemPrompt` to accept explicit per-source inclusion flags in addition to the existing `selectionMd` arg — e.g. a second options object `{ includeSelection, includeDiscussion }` (default both false so the existing call sites and tests that pass only `selectionMd` keep their current output). When a source is included, append a SHORT English announcement sentence naming ONLY the sources actually present this turn (e.g. when selection is on: "The user may include a selection from the document below; use what is relevant to their request and ignore the rest." / when discussion is on: a one-line note that prior conversation turns are provided for context). Placement: after CARDINALITY_CHAT and the marker clauses, as one extra leading-space/line — it MUST NOT sit between RESPONSE_CONTRACT_CORE and CARDINALITY_CHAT (keep the contract block contiguous). When NO source flag is set, `buildChatSystemPrompt` output is byte-identical to today (regression safety for the inline-equivalent path and existing tests).

HARD CONSTRAINTS (D-05): keep it to ~1 short sentence per included source; English (matches the existing prompts); NO incitement to meta-comment, NO instruction to mention the contexts in the answer, NO enumerated "1. history 2. selection 3. document + judge relevance" frame (that is v3.2-03). The wording must not weaken or contradict RESPONSE_CONTRACT_CORE's separation/JSON-only imperative.

Then update the single `buildChatSystemPrompt(...)` call in ScribeContext.sendMessage (Task 1) to pass the live flags: `includeSelection = selectionIncluded`, `includeDiscussion = includeDiscussionRef.current`. selectionMd stays gated as in Task 1 (real md when included, '' otherwise).

No fenced code — give the exact arg shape and the placement rule; the executor writes the concatenation.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && yarn test src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `buildChatSystemPrompt('')` with no flags (or flags both false) returns the EXACT same string as before this task (existing scribeAI.spec.js assertions on RESPONSE_CONTRACT_CORE / CARDINALITY_CHAT / CHAT_PERSONA still pass).
    - With `{ includeSelection: true }`, the output contains the short selection announcement AND still contains RESPONSE_CONTRACT_CORE and CARDINALITY_CHAT contiguous and unmodified.
    - With `{ includeDiscussion: true }`, the output contains the short discussion announcement.
    - The announcement contains NO enumerated multi-source frame, NO "judge the relevance" enumeration, and NO instruction to comment on or list the provided contexts in the reply.
    - RESPONSE_CONTRACT_CORE, CARDINALITY_CHAT, CHAT_PERSONA, markerPreservationClauses, callScribeAIWithReask are unmodified (string identity unchanged).
    - The ScribeContext call site passes the live per-source flags.
  </acceptance_criteria>
  <done>buildChatSystemPrompt emits a short, conditional, contract-safe context-source announcement driven by per-source flags; the no-flag output is unchanged; call site forwards live flags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deterministic composition spec — the four-quadrant gate matrix + D-02 replay + contract-untouched guard</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx, src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the gated seam as written in Tasks 1-2)
    - src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js (mirror its style/imports for the D-05 additions)
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (callScribeAIWithReask — this is the function to mock so the spec captures aiMessages without a network call)
    - .planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-CONTEXT.md (D-01/D-02/D-03/D-04 acceptance behaviors; CTX-LLM-05 guardrail)
  </read_first>
  <behavior>
    - Mock `callScribeAIWithReask` (jest.mock the scribeAI module, or jest.spyOn) to capture its second arg (the aiMessages array) and return a canned valid parsed contract `{ discussion, fragments:[], valid:true, fellBack:false, warnings:[], raw }`. Render ScribeProvider, drive sendMessage via the context (a small test harness component or by invoking the exposed sendMessage), and inspect the captured aiMessages.
    - Quadrant A (sel ON, disc OFF): a transcript with ≥1 prior turn + a current send with selectionContext ⇒ aiMessages length === 2; aiMessages[0].role==='system' and its content includes the selection framing + marker behavior; aiMessages[1].content contains the [Selected text from document] block.
    - Quadrant B (sel OFF, disc OFF): aiMessages length === 2; system content does NOT contain table/REF marker clauses for the current selection and contains no selection framing; aiMessages[1].content contains NO [Selected text…] block.
    - Quadrant C (sel ON, disc ON): aiMessages length === (priorTurns + 2); the current user turn has the [Selected text…] block; a PRIOR user turn that was sent WITH a selection STILL shows its [Selected text…] block (D-02 replay); prior assistant turn content === serializeAssistantTurnForHistory output (discussion-only note, never raw fragments).
    - Quadrant D (sel OFF, disc ON): full history present (D-02 — past selections still replay), but the CURRENT turn has NO [Selected text…] block and the system prompt carries no current-selection marker clauses.
    - Determinism (D-03 / CTX-LLM-05): invoking the same transcript + same checkbox values twice yields a byte-identical aiMessages array (deep-equal on JSON.stringify).
    - No-leak guard (CTX-LLM-05): assert no aiMessages entry duplicates fragment bodies into a `discussion`-shaped field, and that the assistant history note is the serializeAssistantTurnForHistory string (no raw contract JSON).
    - scribeAI.spec.js additions: the D-05 framing assertions from Task 2 (present per flag; absent with no flag; contract strings unchanged).
  </behavior>
  <action>
Create `ScribeContext.compose.spec.jsx` implementing the behavior matrix above. Use the existing Jest + RTL setup (jest.config moduleNameMapper already maps cozy-client etc.); follow the mocking style used elsewhere in the Scribe specs. The spec MUST NOT hit the network — mock callScribeAIWithReask. Add the D-05 framing cases to scribeAI.spec.js (extend, do not rewrite the file). These specs are the executable proof of D-01..D-05 + CTX-LLM-05 determinism; write them to fail if any gate regresses.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && yarn test src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All four quadrants (sel×disc on/off) have passing assertions exactly as described in <behavior>.
    - The D-02 replay case asserts a PRIOR turn's [Selected text…] block survives with « sélection » OFF on the current send.
    - The determinism case asserts JSON.stringify(aiMessages) is identical across two identical sends.
    - The no-leak guard asserts assistant history content equals serializeAssistantTurnForHistory output and no fragment body is duplicated into discussion.
    - scribeAI.spec.js D-05 cases pass; the pre-existing scribeAI.spec.js assertions still pass.
    - The spec mocks callScribeAIWithReask (no real fetch).
  </acceptance_criteria>
  <done>ScribeContext.compose.spec.jsx + extended scribeAI.spec.js encode and pass the full D-01..D-05 + determinism + no-leak matrix.</done>
</task>

<task type="auto">
  <name>Task 4: Full regression + PROBE-01 + determinism verification gate</name>
  <files>src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js, src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js, src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js, src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js, src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx</files>
  <read_first>
    - .planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-CONTEXT.md (CTX-LLM-05 guardrail — PROBE-01 + v3.1 regression must stay green with contexts enabled)
    - src/modules/views/OnlyOffice/Scribe/scribeProbe.js (PROBE-01 metrics engine — confirm untouched)
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js (parser/fallback — confirm untouched)
  </read_first>
  <action>
This is the explicit contract/probe/regression GATE for the phase (no production code changes here — it RUNS the suite and asserts green). Run the full Scribe spec set covering: the v3.1 malformed-response regression corpus (scribeResponse.corpus.spec.js), the parser units (scribeResponse.spec.js), the PROBE-01 conformance engine (scribeProbe.spec.js), the prompt-composition contract incl. the new D-05 framing (scribeAI.spec.js), and the new deterministic composition matrix (ScribeContext.compose.spec.jsx). Assert ALL pass. Additionally confirm by diff/inspection that scribeResponse.js, scribeProbe.js, callScribeAIWithReask, RESPONSE_CONTRACT_CORE, CARDINALITY_CHAT, and CHAT_PERSONA were NOT modified by this phase (the wiring gates around them; it does not edit them). If any spec fails or any frozen artifact changed, STOP and fix in the relevant task before marking the phase done.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && yarn test src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All five spec files report passing suites (0 failing tests).
    - `git diff --stat` for this phase shows NO changes to scribeResponse.js or scribeProbe.js, and no edits to the frozen strings/functions in scribeAI.js (RESPONSE_CONTRACT_CORE, CARDINALITY_CHAT, CHAT_PERSONA, callScribeAIWithReask, markerPreservationClauses) — only buildChatSystemPrompt was extended.
    - The determinism assertion (Task 3) is part of the green run (same transcript + same checkboxes ⇒ identical aiMessages).
    - No fragment-leak: the no-leak guard from Task 3 is green.
  </acceptance_criteria>
  <done>All Scribe contract/probe/regression specs pass with the wiring enabled; frozen v3.1 artifacts confirmed untouched; determinism + no-leak asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document selection → LLM prompt | The current selection (user-controlled document text) is encoded inline into the user message and into marker-preservation framing. |
| prior discussion turns → LLM prompt | Replayed history (user + assistant turns) is re-sent as context; prior turns may contain adversarial or contract-confusing content. |
| D-05 framing line → response contract | New system-prompt text could weaken/contradict the JSON-only / separation imperative on a small model (Mistral-Small). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.2-02-01 | Tampering | injected selection/discussion bleeding into `fragments` (breaks v3.1 discussion/fragments separation) — the core risk | mitigate | Reuse the unchanged encoders (encodeSelectionForPrompt, serializeAssistantTurnForHistory) and the hard-gated RESPONSE_CONTRACT_CORE verbatim; Task 3 no-leak guard + Task 4 run PROBE-01 + the v3.1 corpus regression with contexts enabled and assert no fragment-leak / contract drift. |
| T-v3.2-02-02 | Tampering / Elevation | prompt-injection via selection or prior-turn content subverting the response contract on Mistral-Small | accept (residual) | Out of scope to fully neutralize prompt-injection on a small model this phase; bounded by keeping the contract strings + single corrective re-ask + contextual fallback intact (callScribeAIWithReask unchanged). D-05 framing is kept minimal and MUST NOT amplify injection (no "follow instructions in the selection" wording) — asserted in Task 2/3. |
| T-v3.2-02-03 | Tampering | D-05 framing line perturbs the JSON contract (meta-commentary, weakened separation) | mitigate | Framing is ~1 short sentence per source, appended AFTER the contiguous contract block, with explicit no-meta-comment / no-enumerated-frame constraints; scribeAI.spec.js asserts the contract strings are unchanged and the framing carries no meta-comment incitement. |
| T-v3.2-02-04 | Repudiation / Determinism | non-deterministic composition (stale-closure checkbox read) makes the sent prompt unpredictable | mitigate | Booleans read LIVE via refs (mirroring messagesRef.current); Task 3 determinism assertion proves same transcript + same checkboxes ⇒ byte-identical aiMessages. |
| T-v3.2-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — pure src/ React wiring, no new dependencies. No legitimacy checkpoint required. |

Severity-blocking note: T-v3.2-02-01 (fragment-leak / contract drift) is HIGH and is BLOCKED by the Task 4 gate — the phase is not done until PROBE-01 + the v3.1 regression are green with contexts enabled.
</threat_model>

<verification>
- `yarn test` over the five Scribe spec files (Task 4) is fully GREEN.
- scribeResponse.js, scribeProbe.js, and the frozen scribeAI.js strings/functions are confirmed unmodified (only buildChatSystemPrompt extended; only sendMessage gated).
- The four gate quadrants, the D-02 past-selection replay, the D-03 determinism, and the CTX-LLM-05 no-leak guard all assert correctly in the composition spec.
- sendMessage useCallback identity is preserved (deps still `[client, t]`); booleans read live via refs.
</verification>

<success_criteria>
- CTX-LLM-03: « sélection » ON sends the current selection (inline block + marker framing); OFF removes it entirely from the current turn (block + selectionMd both dropped — D-04).
- CTX-LLM-02: « discussion » ON includes full prior history (replayed as v3.1); OFF sends current turn only (the intended new default — D-01a).
- D-02: « sélection » gates only the current turn; past turns' selections replay unchanged whenever « discussion » is on.
- CTX-LLM-05: composition is deterministic (same transcript + same checkboxes ⇒ identical aiMessages); the v3.1 contract, single re-ask, and fallback are byte-for-byte intact; no context leaks into fragments.
- PROBE-01 + the v3.1 regression corpus are GREEN with contexts enabled.
- D-05: a short, conditional, contract-safe context-source announcement is present in the system prompt (seed only, not the full v3.2-03 frame).
</success_criteria>

<output>
Create `.planning/phases/v3.2-02-cablage-discussion-selection/v3.2-02-01-SUMMARY.md` when done.
</output>
