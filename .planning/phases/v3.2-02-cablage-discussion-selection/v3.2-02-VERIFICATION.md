---
phase: v3.2-02-cablage-discussion-selection
verified: 2026-06-25T17:00:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase v3.2-02: Câblage discussion + sélection — Verification Report

**Phase Goal:** Quand l'utilisateur coche « discussion » et/ou « sélection », ces contextes sont réellement injectés dans le prompt envoyé au LLM, de façon déterministe, sans altérer le contrat de réponse structurée v3.1 (séparation discussion/fragments) ni le repli/re-ask.

**Verified:** 2026-06-25T17:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | « sélection » ON ⇒ current selection in prompt (inline block + marker clauses); OFF ⇒ no selection block and buildChatSystemPrompt receives '' | ✓ VERIFIED | `selectionIncluded = includeSelectionRef.current && !!selectionContext`; `currentSelectionMd = selectionIncluded ? encodedSelectionMd : ''` (ScribeContext.jsx:169,195). Quadrant A/B spec assertions pass (160/160). |
| 2 | « discussion » ON ⇒ full prior history mapped; OFF ⇒ current turn only | ✓ VERIFIED | `turnsToMap = includeDiscussion ? [...messagesRef.current, userMessage] : [userMessage]` (ScribeContext.jsx:200-202). Quadrant C/D spec assertions pass. |
| 3 | Composition is deterministic: same transcript + same checkbox values ⇒ byte-identical aiMessages | ✓ VERIFIED | D-03 spec asserts `JSON.stringify(aiMessages)` identical across two sends. 160/160 green. |
| 4 | v3.1 response contract intact: injected context never leaks into fragments; discussion/fragments separation, single re-ask, and contextual fallback byte-for-byte unchanged | ✓ VERIFIED | scribeResponse.js and scribeProbe.js unmodified (git diff vs base 23eb4f1de shows zero changes). callScribeAIWithReask / REASK_CORRECTION_NUDGE / RESPONSE_CONTRACT_CORE / CARDINALITY_CHAT / CHAT_PERSONA all byte-identical to baseline. No-leak guard spec passes. |
| 5 | PROBE-01 + existing regression gates stay green with contexts enabled | ✓ VERIFIED | 160/160 across all five spec files: scribeResponse.corpus.spec.js, scribeResponse.spec.js, scribeProbe.spec.js, scribeAI.spec.js, ScribeContext.compose.spec.jsx — confirmed by independent run. |

**Score:** 5/5 success criteria verified (10/10 plan must-haves below)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` | Gated seam: discussion gate on history, selection gate on current turn, live-read via refs, D-05 flags forwarded | ✓ VERIFIED | Contains `includeDiscussionRef` / `includeSelectionRef` refs (lines 86-89), `turnsToMap` gate (line 200), `selectionIncluded` gate (line 169), `currentSelectionMd` gating (line 195), `buildChatSystemPrompt` call with live flags (lines 206-209). sendMessage deps remain `[client, t]` (line 343). |
| `src/modules/views/OnlyOffice/Scribe/scribeAI.js` | buildChatSystemPrompt extended with D-05 framing; RESPONSE_CONTRACT_CORE / CARDINALITY_CHAT / CHAT_PERSONA unchanged | ✓ VERIFIED | New private `contextSourceFraming` helper (lines 144-156); `buildChatSystemPrompt` gains optional `flags` arg (line 172); framing appended after marker clauses (line 179). All frozen strings unchanged vs base 23eb4f1de. |
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.compose.spec.jsx` | Four-quadrant gate matrix, D-02 replay, D-03 determinism, CTX-LLM-05 no-leak guard | ✓ VERIFIED | File created (297 lines). Covers all four quadrants (A/B/C/D), D-02 past-selection replay in Quadrant D, D-03 determinism + live-read/stale-closure assertion, CTX-LLM-05 no-fragment-leak guard. Mocks callScribeAIWithReask to capture aiMessages; no network. |
| `src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js` | D-05 framing assertions: present/absent per flag; contract strings unchanged | ✓ VERIFIED | 6 new cases under `describe('D-05 conditional context-source framing')` asserting: baseline byte-identity with no flags; selection announcement present when flag set; discussion announcement present when flag set; both flags together; no enumerated frame / no meta-comment incitement; contract strings unchanged. All existing assertions still pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ScribeContext.jsx | scribeAI.js buildChatSystemPrompt | Called with `(currentSelectionMd, { includeSelection: selectionIncluded, includeDiscussion })` at lines 206-209 | ✓ WIRED | Live flags forwarded; selectionMd is `''` when selection excluded (D-04), real md when included. |
| ScribeContext.jsx | history filter/map | `turnsToMap = includeDiscussion ? [...messagesRef.current, userMessage] : [userMessage]` at line 200 | ✓ WIRED | Discussion gate decides the input array; the map's rendering of each turn is structurally unchanged (D-02 preserved). |
| ScribeContext.jsx | live checkbox values via refs | `includeDiscussionRef.current` / `includeSelectionRef.current` read inside sendMessage; refs assigned each render (lines 86-89) | ✓ WIRED | Mirrors `messagesRef.current` pattern exactly. sendMessage callback identity preserved (deps `[client, t]`). No-stale-closure spec asserts toggle between sends is honored on next send with same callback reference. |

### Data-Flow Trace (Level 4)

All artifacts operate on already-in-memory data (messages array + include booleans). No external data source: encodeSelectionForPrompt receives selectionContext passed synchronously by the caller; messagesRef.current reflects the current React state. No hollow prop or static-return risk. Trace complete.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All five gate-spec files pass | `yarn test …5 spec files…` | 160 passed, 0 failed | ✓ PASS |
| Frozen artifacts unchanged | `git diff 23eb4f1de HEAD -- scribeResponse.js scribeProbe.js` | Empty diff | ✓ PASS |
| Only buildChatSystemPrompt extended in scribeAI.js | `git diff 23eb4f1de HEAD -- scribeAI.js` (additions only) | Only `contextSourceFraming` + `flags` arg added; all frozen strings/functions untouched | ✓ PASS |

### Probe Execution

Step 7c: PROBE-01 is a runtime dev-mode probe (not a shell script), verified via `scribeProbe.spec.js` in the gate suite. 160/160 passing confirms the probe engine itself is correct and unmodified. The PROBE-01 HARD GATE from v3.1-03 applies to a real-model run (not to this phase's unit tests). No `probe-*.sh` shell probes exist for this phase.

### Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| CTX-LLM-02 | v3.2-02 | « discussion » checked ⇒ history included | ✓ SATISFIED | Discussion gate in `turnsToMap` (ScribeContext.jsx:200); Quadrant C spec (prior turns in aiMessages when discussion ON); Quadrant A/B spec (only current turn when OFF). |
| CTX-LLM-03 | v3.2-02 | « sélection » checked ⇒ current selection in prompt | ✓ SATISFIED | `selectionIncluded` gate (ScribeContext.jsx:169); `currentSelectionMd` gate (line 195); inline block and marker clauses dropped when OFF. Quadrant A/B spec. |
| CTX-LLM-05 | v3.2-02 | Composition deterministic, v3.1 contract intact | ✓ SATISFIED | D-03 determinism spec; no-leak guard spec; scribeResponse.js / scribeProbe.js unmodified; callScribeAIWithReask unchanged; RESPONSE_CONTRACT_CORE / CARDINALITY_CHAT byte-identical. |

All three phase-claimed requirements fully satisfied. CTX-LLM-01 and CTX-LLM-04 are correctly mapped to v3.2-03 (not claimed by this phase) and not verified here.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| ScribeContext.jsx | 172, 315 | `id: Date.now()` / `Date.now() + 1` collision risk | ℹ️ Info | Pre-existing (predates v3.2-02); noted in code review as IN-02; no correctness impact on prompt assembly; out of scope for this phase. |

No `TBD`, `FIXME`, or `XXX` markers found in any file modified by this phase.

### Human Verification Required

None. All must-haves are verifiable programmatically. The phase goal is entirely about prompt assembly determinism and contract preservation — observable via the test suite and code inspection.

---

## Must-Haves Trace (Plan Frontmatter)

All 10 plan must-have truths verified against actual code:

1. **CTX-LLM-03 (selection ON)** — `selectionIncluded = includeSelectionRef.current && !!selectionContext`; when true, `buildChatSystemPrompt(encodedSelectionMd, { includeSelection: true, ... })` passes real md + flag; user message gets the `[Selected text from document]...` block (ScribeContext.jsx:169,195,206,219-226). VERIFIED.

2. **CTX-LLM-03 / D-04 (selection OFF)** — when `selectionIncluded` is false: `currentSelectionMd = ''`, `buildChatSystemPrompt('', { includeSelection: false, ... })` carries no marker clauses and no framing; user message is bare `text`. `userMessage.selection = null`. Quadrant B spec asserts. VERIFIED.

3. **CTX-LLM-02 / D-01 (discussion OFF)** — `turnsToMap = [userMessage]` only; aiMessages = `[system, current user]`, length 2. Quadrant A/B specs assert length === 2 with prior turns seeded. VERIFIED.

4. **CTX-LLM-02 / D-01 (discussion ON)** — `turnsToMap = [...messagesRef.current, userMessage]`; prior user turns replay their stored `.selection` block AS-IS (map branch `m.role === 'user' && m.selection`); prior assistant turns serialized via `serializeAssistantTurnForHistory`. Quadrant C spec asserts all prior turn entries. VERIFIED.

5. **D-02 (selection gate scope)** — `selectionIncluded` guards only the CURRENT turn (the new userMessage). Past turns come from `messagesRef.current` and render via their stored `.selection` (set at send time, independent of current checkbox). Quadrant D spec asserts prior user turn still shows `[Selected text]` block when current selection is OFF. VERIFIED.

6. **D-03 / CTX-LLM-05 determinism** — Booleans read live via refs (same pattern as messagesRef). sendMessage deps locked to `[client, t]`. Determinism spec asserts byte-identical aiMessages across two identical sends. Stale-closure spec asserts callback identity preserved after toggle. VERIFIED.

7. **D-03 live-read (no stale closure)** — refs assigned each render (`includeDiscussionRef.current = includeDiscussion`, `includeSelectionRef.current = includeSelection` at lines 87,89). sendMessage reads `*.current` at invocation time. Stale-closure spec confirms: toggle discussion between sends, second send includes prior turns WITHOUT re-creating the callback. VERIFIED.

8. **D-05 framing seed** — `contextSourceFraming` private function appends ~1 short English sentence per included source AFTER `CARDINALITY_CHAT + markerPreservationClauses` (scribeAI.js:179). No enumerated frame, no meta-comment incitement (spec asserts). With no flags: byte-identical to baseline (regression spec asserts). VERIFIED.

9. **CTX-LLM-05 contract intact** — scribeResponse.js: zero diff. scribeProbe.js: zero diff. callScribeAIWithReask, REASK_CORRECTION_NUDGE: zero diff. RESPONSE_CONTRACT_CORE, CARDINALITY_CHAT, CHAT_PERSONA: zero diff (only comments and the `buildChatSystemPrompt` signature changed in scribeAI.js). No-leak guard spec: raw fragment body `'frag body that must not leak verbatim'` never appears verbatim as any entry's `.content`. VERIFIED.

10. **Regression GREEN** — 160/160 tests: scribeAI.spec.js, scribeResponse.corpus.spec.js, scribeResponse.spec.js, scribeProbe.spec.js, ScribeContext.compose.spec.jsx. Confirmed by independent verifier run. VERIFIED.

---

## Gaps Summary

No gaps. All success criteria and plan must-haves are met with direct code evidence. The gate suite is green at 160/160 with no regressions. All frozen v3.1 artifacts are byte-identical to base 23eb4f1de. Code review warnings WR-01 and WR-02 (dev/probe data divergence) were fixed in the orchestrator fix commit `b1b11bb5f` before the phase was submitted for verification; `inputMd` now uses `currentSelectionMd` and dev panel fields are gated on `selectionIncluded`.

---

_Verified: 2026-06-25T17:00:00Z_
_Verifier: Claude (gsd-verifier)_
