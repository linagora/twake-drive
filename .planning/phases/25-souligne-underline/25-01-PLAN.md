---
phase: 25-souligne-underline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/onlyoffice-scribe/scripts/code.js
  - src/modules/views/OnlyOffice/Scribe/scribeAI.js
  - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeConversion.js
  - src/modules/views/OnlyOffice/Scribe/scribeConversion.spec.js
autonomous: false
requirements:
  - FMT-01

must_haves:
  truths:
    - "Underlined text in OO selection appears as <u>text</u> in extracted markdown"
    - "After LLM processing, underlined text is reinjected into OO with underline formatting via Builder API"
    - "Underline coexists with bold, italic, strikethrough — a bold+underlined run stays bold+underlined after round-trip"
    - "The markdown preview panel renders <u> tags as underlined text"
  artifacts:
    - path: "plugins/onlyoffice-scribe/scripts/code.js"
      provides: "Underline extraction (GetUnderline) and injection (SetUnderline), marked extension for <u> token parsing"
      contains: "GetUnderline"
    - path: "src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx"
      provides: "rehype-raw plugin for rendering <u> tags in preview"
      contains: "rehypeRaw"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeAI.js"
      provides: "System prompt mentioning <u> tag preservation"
      contains: "<u>"
  key_links:
    - from: "plugins/onlyoffice-scribe/scripts/code.js (extraction)"
      to: "plugins/onlyoffice-scribe/scripts/code.js (injection)"
      via: "<u>text</u> markdown markers round-trip through LLM"
      pattern: "<u>.*</u>"
    - from: "plugins/onlyoffice-scribe/scripts/code.js (flattenTokens)"
      to: "plugins/onlyoffice-scribe/scripts/code.js (addRunsToParagraph)"
      via: "underline flag on run objects"
      pattern: "underline.*SetUnderline"
---

<objective>
Add underline (souligne) support to the full Scribe round-trip pipeline: extraction from OO, markdown representation as `<u>` tags, LLM preservation, Builder API reinjection, and preview rendering.

Purpose: Complete inline formatting support — underline is the last missing inline style after bold, italic, and strikethrough.
Output: Underlined text survives the full round-trip (OO -> markdown -> LLM -> markdown -> OO) with formatting preserved.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@plugins/onlyoffice-scribe/scripts/code.js
@src/modules/views/OnlyOffice/Scribe/scribeAI.js
@src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx
@src/modules/views/OnlyOffice/Scribe/scribeConversion.js
@src/modules/views/OnlyOffice/Scribe/scribeConversion.spec.js

<interfaces>
<!-- Existing inline formatting pattern in code.js — underline follows the same pattern -->

From code.js getRunFlags() (line ~1358):
```javascript
function getRunFlags(tp) {
  var isBold = tp ? tp.GetBold() : false;
  var isItalic = tp ? tp.GetItalic() : false;
  var isStrike = tp ? tp.GetStrikeout() : false;
  // ... font detection ...
  return { bold: !!isBold, italic: !!isItalic, strikethrough: !!isStrike, code: isCode };
}
```

From code.js buildMarkdownFromParts() (line ~1257):
```javascript
// Each part: { text, bold, italic, strikethrough, code, raw, link }
function buildMarkdownFromParts(parts) {
  var curBold = false, curItalic = false, curStrike = false, curCode = false;
  // transitionTo(wantBold, wantItalic, wantStrike, wantCode) handles open/close markers
}
```

From code.js flattenInline() (line ~97):
```javascript
function flattenInline(tokens, parentBold, parentItalic, parentStrikethrough, parentCode, parentLink) {
  // tok.type === "strong" -> bold, "em" -> italic, "del" -> strikethrough
  // Need to add: "underline" custom token type -> underline flag
}
```

From code.js addRunsToParagraph() (line ~640):
```javascript
if (run.bold) r.SetBold(true);
if (run.italic) r.SetItalic(true);
if (run.strikethrough) r.SetStrikeout(true);
// Need to add: if (run.underline) r.SetUnderline(true);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Underline extraction, markdown emission, injection, and marked extension in code.js</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js</files>
  <read_first>
    - plugins/onlyoffice-scribe/scripts/code.js (full file — understand getRunFlags, formatRun, buildMarkdownFromParts, flattenTokens/flattenInline, addRunsToParagraph, makeHyperlink)
  </read_first>
  <action>
    Add underline support to all 4 pipeline stages in code.js. Follow the exact same pattern as bold/italic/strikethrough at each stage. All code must be ES5 (no const/let, no arrow functions).

    **Stage 1 — Extraction (getRunFlags + formatRun):**
    1. In `getRunFlags()` (~line 1358): Add `var isUnderline = tp ? tp.GetUnderline() : false;` and include `underline: !!isUnderline` in the returned object.
    2. In `formatRun()` (~line 1228): Add `var isUnderline = tp ? tp.GetUnderline() : false;` after isStrike. After all markdown wrapping, if isUnderline: `escaped = "<u>" + escaped + "</u>";` (underline wraps outermost since markdown has no underline syntax — use HTML `<u>` tags).

    **Stage 2 — Markdown emission (buildMarkdownFromParts):**
    3. Add `curUnderline = false` to the state variables (~line 1259).
    4. In `closeAll()`: Before the existing closing markers block, add underline close: `if (curUnderline) { result += "</u>"; curUnderline = false; }`. Underline closes FIRST (outermost) — it wraps around bold/italic/strike. The trailing whitespace extraction must happen before ALL closes including underline.
    5. In `transitionTo(wantBold, wantItalic, wantStrike, wantCode)`: Add `wantUnderline` as a 5th parameter. Add `(curUnderline && !wantUnderline)` to the needsClose check. Close underline first (before code/strike/bold/italic closes): `if (curUnderline && !wantUnderline) { result += "</u>"; curUnderline = false; }`. Open underline last (after italic/bold/strike/code opens): `if (wantUnderline && !curUnderline) { result += "<u>"; curUnderline = true; }`. Actually — underline must open BEFORE other markers (since `<u>**text**</u>` is the desired nesting) and close AFTER. So: open `<u>` first, close `</u>` last.
       Correction on nesting order: `<u>` is HTML and wraps around markdown syntax. So the emission order should be:
       - Open: `<u>` first, then `*` (italic), then `**` (bold), then `~~` (strike), then `` ` `` (code)
       - Close: `` ` `` first, then `~~`, then `**`, then `*`, then `</u>` last
       This means in `transitionTo`: open underline BEFORE italic, close underline AFTER italic.
    6. In the main loop (~line 1317): Add `var wantUnderline = !!part.underline;` and pass it to `transitionTo`.

    **Stage 3 — Annotated parts propagation:**
    7. In `paragraphToMarkdown()` — everywhere an annotated part is pushed, add `underline: flags.underline` (or `hFlags.underline` for hyperlinks). This includes:
       - Hyperlink parts (~line 1399, 1401): add `underline: hFlags.underline`
       - Normal text run (~line 1447): add `underline: flags.underline`
       - Image interleave text parts (~lines 1432, 1440): add `underline: flags.underline`

    **Stage 4 — Injection (flattenInline + addRunsToParagraph + makeHyperlink):**
    8. Add a **marked inline extension** for `<u>` tags. Before `flattenTokens` function definition, register a custom extension on `window.marked`:
       ```javascript
       // Register <u> underline extension for marked.lexer
       if (window.marked && window.marked.use) {
         window.marked.use({
           extensions: [{
             name: "underline",
             level: "inline",
             start: function(src) {
               return src.indexOf("<u>");
             },
             tokenizer: function(src) {
               var match = src.match(/^<u>([\s\S]*?)<\/u>/);
               if (match) {
                 return {
                   type: "underline",
                   raw: match[0],
                   text: match[1],
                   tokens: []
                 };
               }
             },
             childTokens: ["tokens"],
             renderer: function(token) { return "<u>" + token.text + "</u>"; }
           }]
         });
       }
       ```
       NOTE: The `childTokens: ["tokens"]` tells marked to recursively tokenize the inner content into `token.tokens`, so nested formatting like `<u>**bold**</u>` produces proper child tokens.

    9. In `flattenInline()` (~line 97): Add parameter `parentUnderline` (7th param after parentLink). Update ALL recursive calls to pass it through:
       - `tok.type === "text"`: add `underline: !!parentUnderline` to the pushed run
       - `tok.type === "strong"`: pass `parentUnderline` through
       - `tok.type === "em"`: pass `parentUnderline` through
       - `tok.type === "del"`: pass `parentUnderline` through
       - `tok.type === "codespan"`: add `underline: !!parentUnderline`
       - `tok.type === "link"`: pass `parentUnderline` through
       - `tok.type === "image"`: no change needed (images don't have underline)
       - `tok.type === "underline"` (NEW): `runs = runs.concat(flattenInline(tok.tokens, parentBold, parentItalic, parentStrikethrough, parentCode, parentLink, true));`
       - Fallback with `tok.tokens`: pass `parentUnderline` through
       - Fallback with `tok.text`: add `underline: !!parentUnderline`

    10. Update ALL call sites of `flattenInline` to pass the 7th `parentUnderline` argument (default `false`):
        - In `flattenList` (~line 149): add `false` as 7th arg
        - In main `flattenTokens` loop — paragraph (~line 161), heading (~line 184), unknown block (~line 235): add `false` as 7th arg
        - In table header/rows (~lines 213, 222): add `false` as 7th arg

    11. In `addRunsToParagraph()` (~line 640): After `if (run.strikethrough) r.SetStrikeout(true);` add `if (run.underline) r.SetUnderline(true);`

    12. In `makeHyperlink()` (~line 614): Add `run.underline` to the formatting check condition, and inside the loop add `if (run.underline) linkRun.SetUnderline(true);`

    **Important ES5 constraint:** No arrow functions, no const/let, no template literals. Use var, function expressions, string concatenation.
  </action>
  <acceptance_criteria>
    - grep "GetUnderline" plugins/onlyoffice-scribe/scripts/code.js returns at least 2 matches (getRunFlags + formatRun)
    - grep "SetUnderline" plugins/onlyoffice-scribe/scripts/code.js returns at least 2 matches (addRunsToParagraph + makeHyperlink)
    - grep "curUnderline" plugins/onlyoffice-scribe/scripts/code.js returns at least 3 matches (state var + closeAll + transitionTo)
    - grep 'name.*underline' plugins/onlyoffice-scribe/scripts/code.js returns at least 1 match (marked extension)
    - grep "parentUnderline" plugins/onlyoffice-scribe/scripts/code.js returns at least 5 matches
    - No const/let/arrow functions in code.js (ES5 only)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -c "GetUnderline" plugins/onlyoffice-scribe/scripts/code.js && grep -c "SetUnderline" plugins/onlyoffice-scribe/scripts/code.js && grep -c "curUnderline" plugins/onlyoffice-scribe/scripts/code.js && grep -c "parentUnderline" plugins/onlyoffice-scribe/scripts/code.js && echo "--- ES5 check ---" && ! grep -n "=>" plugins/onlyoffice-scribe/scripts/code.js && ! grep -nP "^\s*(const|let)\s" plugins/onlyoffice-scribe/scripts/code.js && echo "ES5 OK"</automated>
  </verify>
  <done>
    - GetUnderline() read in extraction, underline flag propagated through annotated parts
    - buildMarkdownFromParts emits <u>...</u> tags with proper nesting around markdown syntax
    - marked extension tokenizes <u>...</u> into underline tokens with child token parsing
    - flattenInline handles underline token type, propagates underline flag to runs
    - SetUnderline(true) called in addRunsToParagraph and makeHyperlink
    - All code is ES5 compliant
  </done>
</task>

<task type="auto">
  <name>Task 2: System prompt, preview rendering, and fallback path updates</name>
  <files>
    src/modules/views/OnlyOffice/Scribe/scribeAI.js,
    src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx,
    src/modules/views/OnlyOffice/Scribe/scribeConversion.js,
    src/modules/views/OnlyOffice/Scribe/scribeConversion.spec.js
  </files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeAI.js (SYSTEM_PROMPT constant)
    - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx (full file)
    - src/modules/views/OnlyOffice/Scribe/scribeConversion.js (normalizeHtml, createTurndownService)
    - src/modules/views/OnlyOffice/Scribe/scribeConversion.spec.js (existing tests)
    - package.json (check if rehype-raw already installed)
  </read_first>
  <action>
    **1. System prompt update (scribeAI.js):**
    Update SYSTEM_PROMPT to mention `<u>` tag preservation alongside markdown formatting:
    Change from:
    `'You are a writing assistant. Return only the transformed text, no explanations or commentary. Preserve any Markdown formatting (bold, italic, lists, headings) present in the input. Respond in the same language as the input text.'`
    To:
    `'You are a writing assistant. Return only the transformed text, no explanations or commentary. Preserve any Markdown formatting (bold, italic, lists, headings) and HTML tags like <u>...</u> present in the input. Respond in the same language as the input text.'`

    **2. MarkdownPreview update:**
    Install rehype-raw: `yarn add rehype-raw` (or npm install). Then in MarkdownPreview.jsx:
    - Add import: `import rehypeRaw from 'rehype-raw'`
    - Add `rehypePlugins={[rehypeRaw]}` to the `<Markdown>` component (alongside existing `remarkPlugins={[remarkGfm]}`)
    This allows raw HTML `<u>` tags in the markdown to render as underlined text in the preview panel.

    **3. Turndown underline passthrough (scribeConversion.js):**
    In `createTurndownService()`, add a custom rule to preserve `<u>` tags through Turndown conversion (Turndown by default strips unknown HTML tags). Add after the existing rules:
    ```javascript
    td.addRule('underline', {
      filter: 'u',
      replacement(content) {
        return '<u>' + content + '</u>'
      }
    })
    ```
    This ensures that if HTML-based extraction path is used (fallback), underline survives the htmlToMarkdown conversion.

    **4. Tests (scribeConversion.spec.js):**
    Add test cases:
    - `htmlToMarkdown` with `<u>underlined</u>` input produces `<u>underlined</u>` output
    - `htmlToMarkdown` with `<u><strong>bold underlined</strong></u>` preserves both
    - `normalizeHtml` merges adjacent `<u>` tags (already in mergeableTags, but add a test to confirm)
  </action>
  <acceptance_criteria>
    - grep "<u>" src/modules/views/OnlyOffice/Scribe/scribeAI.js returns at least 1 match
    - grep "rehypeRaw" src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx returns at least 1 match
    - grep "rehype-raw" src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx returns at least 1 match (import)
    - grep "underline" src/modules/views/OnlyOffice/Scribe/scribeConversion.js returns at least 1 match
    - grep "underline" src/modules/views/OnlyOffice/Scribe/scribeConversion.spec.js returns at least 1 match
    - yarn test scribeConversion passes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -c "rehypeRaw" src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx && grep -c "<u>" src/modules/views/OnlyOffice/Scribe/scribeAI.js && grep -c "underline" src/modules/views/OnlyOffice/Scribe/scribeConversion.js && yarn jest --testPathPattern=scribeConversion --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    - System prompt instructs LLM to preserve <u> tags
    - MarkdownPreview renders <u> tags as underlined text via rehype-raw
    - Turndown preserves <u> tags through HTML-to-markdown conversion
    - Tests pass for underline in htmlToMarkdown and normalizeHtml
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify underline round-trip in OO</name>
  <files>none</files>
  <action>Restart OO container via ./scripts/oo-dev-setup.sh, then present the verification steps below to the user for manual testing of the underline round-trip.</action>
  <what-built>Complete underline round-trip: extraction from OO, markdown with &lt;u&gt; tags, LLM preservation, Builder API reinjection with underline formatting, and preview rendering.</what-built>
  <how-to-verify>
    1. Restart OO container: `./scripts/oo-dev-setup.sh`
    2. Open a document in OnlyOffice editor
    3. Type some text and apply underline formatting (Ctrl+U) to part of it
    4. Also apply bold+underline to another part, and italic+underline to another
    5. Select the formatted text and trigger Scribe (Ctrl+Shift+I)
    6. Choose any action (e.g., "Corriger la grammaire")
    7. Verify in the preview panel: underlined text should appear underlined
    8. Click "Replace" — the reinjected text should have underline formatting preserved in OO
    9. Verify bold+underline and italic+underline combinations are preserved
    10. Test "Insert after" as well to ensure underline works in insert mode
  </how-to-verify>
  <verify>User confirms underline round-trip works correctly</verify>
  <done>User types "approved" — underline formatting survives full round-trip in OO</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Underlined text in OO selection is extracted as `<u>text</u>` in the enriched markdown
- The LLM system prompt instructs preservation of `<u>` tags
- marked.lexer with the custom extension tokenizes `<u>text</u>` into underline tokens
- flattenInline produces runs with `underline: true` flag
- Builder API applies SetUnderline(true) on injection
- MarkdownPreview renders underlined text in the preview panel
- Bold+underline, italic+underline, strikethrough+underline combinations all survive round-trip
- No regressions in existing bold/italic/strikethrough formatting
</verification>

<success_criteria>
- Underlined text round-trips through Scribe without losing formatting
- Combined formatting (bold+underline, italic+underline) preserved
- Preview panel shows underlined text
- All existing tests pass (no regressions)
- ES5 compliance maintained in plugin code
</success_criteria>

<output>
After completion, create `.planning/phases/25-souligne-underline/25-01-SUMMARY.md`
</output>
