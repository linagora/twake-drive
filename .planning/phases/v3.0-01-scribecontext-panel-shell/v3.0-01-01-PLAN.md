---
phase: v3.0-01-scribecontext-panel-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx
  - src/modules/views/OnlyOffice/index.jsx
  - src/modules/views/OnlyOffice/View.jsx
  - src/locales/en.json
  - src/locales/fr.json
autonomous: true
requirements: [PANEL-01, PANEL-02, PANEL-04]

must_haves:
  truths:
    - "A 400px side panel appears to the right of the OO editor when isPanelOpen is true"
    - "The OO editor iframe shrinks horizontally to make room for the panel"
    - "The panel has a branded header with Scribe icon, title, and close X button"
    - "Clicking the close button closes the panel and the editor expands back"
    - "The panel body shows a centered Scribe icon with tagline placeholder"
    - "Panel state is persisted in localStorage and restored on page load"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "ScribeProvider + useScribe hook with isPanelOpen, togglePanel, openPanel, closePanel"
      exports: ["ScribeProvider", "useScribe"]
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx"
      provides: "Panel shell with header and placeholder body"
      exports: ["ScribePanel", "PANEL_WIDTH"]
    - path: "src/modules/views/OnlyOffice/index.jsx"
      provides: "ScribeProvider wrapping Editor"
    - path: "src/modules/views/OnlyOffice/View.jsx"
      provides: "ScribePanel rendered as flex sibling, onlyOfficeEditor with flex properties"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      to: "localStorage"
      via: "useState initializer + useEffect sync"
      pattern: "localStorage\\.(get|set)Item.*scribe-panel"
    - from: "src/modules/views/OnlyOffice/View.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx"
      via: "conditional render in flex container"
      pattern: "isPanelOpen.*ScribePanel"
    - from: "src/modules/views/OnlyOffice/View.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      via: "useScribe hook"
      pattern: "useScribe"
---

<objective>
Create ScribeContext provider and ScribePanel shell, wire them into View.jsx so the panel renders as a flex sibling to the OO editor and the editor resizes correctly.

Purpose: Delivers the core panel infrastructure -- state management, panel component, and layout integration. This is the foundation all subsequent v3.0 phases build on.
Output: Working side panel that can be opened/closed (initially controlled via React DevTools or localStorage), OO editor resizes properly.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.0-01-scribecontext-panel-shell/v3.0-01-CONTEXT.md
@.planning/phases/v3.0-01-scribecontext-panel-shell/v3.0-01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/modules/views/OnlyOffice/index.jsx:
```jsx
// ScribeProvider must wrap <Editor /> inside <OnlyOfficeProvider>
<OnlyOfficeProvider ...>
  <Editor />    // <-- ScribeProvider wraps this
  <Outlet />
</OnlyOfficeProvider>
```

From src/modules/views/OnlyOffice/View.jsx:
```jsx
// Current flex container -- ScribePanel renders as sibling here
<div className="u-flex u-flex-grow-1">
  <div id="onlyOfficeEditor" />        // needs flex: '1 1 auto', minWidth: 0
  <OnlyOfficeAIAssistantPanel />       // existing 30% panel (leave untouched)
</div>

// ScribePopover and ScribeFloatingButton rendered below the flex container
// View receives: { id, apiUrl, docEditorConfig } props
// Uses: flag('drive.scribe.enabled'), useCozyBridge(allowedOrigins)
```

From src/modules/views/OnlyOffice/useCozyBridge.js:
```js
export function useCozyBridge(allowedOrigins)
// Returns: { pendingIntent, showScribeButton, respond }
```

From src/locales/en.json (Scribe key structure):
```json
{
  "Scribe": {
    "menu": { ... },
    "button": { "text_ai": "Text AI", ... },
    ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScribeContext provider and ScribePanel shell</name>
  <files>
    src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx,
    src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx,
    src/locales/en.json,
    src/locales/fr.json
  </files>
  <action>
Create `ScribeContext.jsx` following the OnlyOfficeProvider pattern:
- `createContext()` for ScribeContext
- `ScribeProvider` component with:
  - `isPanelOpen` state, initialized from `localStorage.getItem('scribe-panel-open') === 'true'` (wrapped in try/catch, default false)
  - `useEffect` to persist `isPanelOpen` to localStorage on change (try/catch)
  - `togglePanel`, `openPanel`, `closePanel` callbacks (useCallback)
  - `useMemo` for the context value object
- `useScribe` hook: `useContext(ScribeContext)`
- Export both `ScribeProvider` and `useScribe`

Create `ScribePanel.jsx`:
- Import Paper, Typography, IconButton, Icon from cozy-ui transpiled paths
- Import useTheme from cozy-ui styles, useI18n from twake-i18n, useScribe from ScribeContext
- `PANEL_WIDTH = 400` constant (exported)
- Theme detection: `const isDark = (theme.palette.type || theme.palette.mode) === 'dark'`
- Always-rendered approach for slide animation: the panel container is always in the DOM when `isPanelOpen` is true. Use inline styles (not stylus -- portal pattern established in codebase).
- Panel structure:
  - Outer div: `width: PANEL_WIDTH`, `flexShrink: 0`, `display: flex`, `flexDirection: column`, `height: '100%'`, `boxShadow` (theme-aware: darker shadow for dark mode), `background: theme.palette.background.paper`, `overflow: 'hidden'`, `transition: 'width 200ms ease-out'` (slide-in animation per user decision)
  - Header div: flex row, `alignItems: center`, `padding: '12px 16px'`, `borderBottom: 1px solid ${theme.palette.divider}`. Contains:
    - Scribe sparkle SVG icon (20x20, same purple #7C3AED as ScribeFloatingButton)
    - Typography variant="h6" with "Scribe" text, `marginLeft: 8`, `flex: 1`
    - IconButton size="small" with close icon (use `Icon icon="cross"` -- if that doesn't work, fall back to inline SVG X). onClick calls `closePanel()`.
  - Body div: `flex: 1`, centered content (flexbox center both axes), `padding: 24`, `opacity: 0.6`. Contains:
    - Larger Scribe sparkle SVG (48x48, same path data)
    - Typography variant="body2" color="textSecondary" with `t('Scribe.panel.tagline')`, marginTop: 16

Add i18n keys to locales:
- `en.json`: Add `"panel": { "tagline": "Your writing assistant" }` inside the existing `"Scribe"` object
- `fr.json`: Add `"panel": { "tagline": "Votre assistant d'ecriture" }` inside the existing `"Scribe"` object
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && node -e "const fs=require('fs'); const ctx=fs.readFileSync('src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx','utf8'); const pnl=fs.readFileSync('src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx','utf8'); const ok = ctx.includes('ScribeProvider') && ctx.includes('useScribe') && ctx.includes('localStorage') && pnl.includes('PANEL_WIDTH') && pnl.includes('closePanel') && pnl.includes('tagline') && pnl.includes('transition'); if(!ok) throw new Error('Missing exports or key patterns'); console.log('OK: ScribeContext + ScribePanel verified')"</automated>
  </verify>
  <done>ScribeContext.jsx exports ScribeProvider and useScribe with localStorage persistence. ScribePanel.jsx exports ScribePanel and PANEL_WIDTH with branded header, close button, placeholder body, and ~200ms slide-in transition. i18n keys added for en and fr.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ScribeProvider into index.jsx and ScribePanel into View.jsx</name>
  <files>
    src/modules/views/OnlyOffice/index.jsx,
    src/modules/views/OnlyOffice/View.jsx
  </files>
  <action>
In `index.jsx`:
- Import `ScribeProvider` from `@/modules/views/OnlyOffice/Scribe/ScribeContext`
- Wrap `<Editor />` and `<Outlet />` with `<ScribeProvider>` inside `<OnlyOfficeProvider>`:
  ```
  <OnlyOfficeProvider ...>
    <ScribeProvider>
      <Editor />
      <Outlet />
    </ScribeProvider>
  </OnlyOfficeProvider>
  ```

In `View.jsx`:
- Import `useScribe` from `@/modules/views/OnlyOffice/Scribe/ScribeContext`
- Import `ScribePanel` from `@/modules/views/OnlyOffice/Scribe/ScribePanel`
- Call `const { isPanelOpen } = useScribe()` in the View component
- Add inline style to the `#onlyOfficeEditor` div: `style={{ flex: '1 1 auto', minWidth: 0 }}`. This is CRITICAL for the flex shrinking to work -- without `minWidth: 0`, the div won't shrink below its content size.
- After `<OnlyOfficeAIAssistantPanel />` in the flex container, add: `{isScribeEnabled && isPanelOpen && <ScribePanel />}`
- The existing ScribeFloatingButton and ScribePopover remain unchanged in this plan. Coexistence logic (hiding floating button when panel is open) is handled in plan 02.

IMPORTANT: Do NOT change any ScribeFloatingButton or ScribePopover logic in this task. Plan 02 handles that.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && node -e "const fs=require('fs'); const idx=fs.readFileSync('src/modules/views/OnlyOffice/index.jsx','utf8'); const view=fs.readFileSync('src/modules/views/OnlyOffice/View.jsx','utf8'); const ok = idx.includes('ScribeProvider') && view.includes('useScribe') && view.includes('ScribePanel') && view.includes('minWidth') && view.includes('isPanelOpen'); if(!ok) throw new Error('Missing wiring'); console.log('OK: index.jsx + View.jsx wired')"</automated>
  </verify>
  <done>ScribeProvider wraps Editor in index.jsx. View.jsx renders ScribePanel as flex sibling when isPanelOpen is true. onlyOfficeEditor div has flex: '1 1 auto' and minWidth: 0 for proper shrinking.</done>
</task>

</tasks>

<verification>
1. Set `localStorage.setItem('scribe-panel-open', 'true')` in browser console, reload the OO editor page with `drive.scribe.enabled` flag on
2. A 400px panel should appear on the right with Scribe header and placeholder content
3. The OO editor should be narrower (visibly pushed left)
4. Click the X button in the panel header -- panel closes, editor expands back
5. Reload -- panel should reopen (localStorage persisted)
6. Set localStorage to 'false', reload -- panel should not appear
</verification>

<success_criteria>
- ScribePanel renders as a 400px flex sibling to the right of the OO editor
- OO editor iframe shrinks when panel is open and expands when closed
- Close button in panel header closes the panel
- Panel state persists in localStorage across reloads
- Panel has ~200ms slide-in transition animation
- Existing ScribeFloatingButton and ScribePopover remain functional (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/v3.0-01-scribecontext-panel-shell/v3.0-01-01-SUMMARY.md`
</output>
