---
phase: v3.1-07-i18n-5-locales
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/de.json
  - src/locales/es.json
  - src/locales/it.json
  - src/locales/fr.json
  - src/locales/en.json
  - src/locales/scribeI18nParity.spec.js
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "The Scribe.* key set is byte-for-byte identical across fr, en, de, es, it (the flattened-key-parity test is the proof)"
    - "de/es/it each gained the 7 previously-missing keys (panel.tagline, chat.typing, chat.error_prefix, chat.welcome, chat.selection_context, button.open_panel, button.copy) with native-quality translations, not English copies"
    - "Two new keys introduced for the extraction workstream (Scribe.panel.description, Scribe.result.table_partial_insert_unavailable) exist in ALL 5 target locales, translated"
    - "No existing Scribe.* translation value was altered (only additions)"
  artifacts:
    - path: "src/locales/scribeI18nParity.spec.js"
      provides: "Flattened Scribe.* key-set equality gate across fr/en/de/es/it + non-empty-value assertion"
      contains: "describe('Scribe i18n parity"
    - path: "src/locales/de.json"
      provides: "Scribe.panel + Scribe.chat sub-objects + button.open_panel/copy + panel.description + result.table_partial_insert_unavailable"
      contains: "\"tagline\""
    - path: "src/locales/es.json"
      provides: "same 7 missing keys + 2 new keys, Spanish"
      contains: "\"tagline\""
    - path: "src/locales/it.json"
      provides: "same 7 missing keys + 2 new keys, Italian"
      contains: "\"tagline\""
  key_links:
    - from: "src/locales/scribeI18nParity.spec.js"
      to: "src/locales/{fr,en,de,es,it}.json"
      via: "import each JSON, flatten Scribe.*, assert identical key sets"
      pattern: "Scribe i18n parity"
---

<objective>
Bring de/es/it to full Scribe.* parity with the fr/en reference (the 7 missing keys), introduce the two new keys the extraction workstream (plan 02) will wire (panel.description, result.table_partial_insert_unavailable) into ALL 5 target locales, and lock criterion 1 behind a deterministic flattened-key-set parity Jest test (I18N-01).

Purpose: This is the single source-of-truth plan for locale JSON. It owns every locale file so plan 02 only wires `t()` calls — no JSON merge race on the shared `Scribe.button.copy` key.
Output: de/es/it + new keys translated in all 5 locales; `scribeI18nParity.spec.js` proving Scribe.* key-set equality across fr/en/de/es/it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-07-i18n-5-locales/v3.1-07-CONTEXT.md

<interfaces>
<!-- Reference values and exact gaps. Extracted from the live locale files; use directly, no exploration. -->

Reference locales (fr.json, en.json) — Scribe.* namespace: 44 keys each, full parity (verified zero diff).

The 7 keys MISSING from de.json / es.json / it.json (identical gap across all three) — reference values:
  Scribe.panel.tagline          fr "Votre assistant d'ecriture"            en "Your writing assistant"
  Scribe.chat.typing            fr "Scribe reflechit..."                   en "Scribe is thinking..."
  Scribe.chat.error_prefix      fr "Erreur"                                en "Error"
  Scribe.chat.welcome           fr "Posez-moi une question sur votre document"  en "Ask me anything about your document"
  Scribe.chat.selection_context fr "Texte selectionne"                     en "Selected text"
  Scribe.button.open_panel      fr "Panneau Scribe"                        en "Scribe Panel"
  Scribe.button.copy            fr "Copier"                                en "Copy"

In de/es/it the `panel` and `chat` sub-objects do NOT exist yet — they must be created. `button` exists (has insert/replace/etc.) and gains open_panel + copy.

Existing target-locale tone anchors (do NOT invent a different register):
  de  button.insert "Einfügen"   button.replace "Ersetzen"   prompt.placeholder "Hilf mir beim Schreiben"
  es  button.insert "Insertar"   button.replace "Reemplazar"
  it  button.insert "Inserisci"  button.replace "Sostituisci"

Locale registry: src/locales/index.js imports each JSON and exports `locales`. The parity test imports the JSON files directly (no twake-i18n needed) — `import fr from './fr.json'` etc.

Interpolation convention (cozy-ui / twake-i18n): `%{var}` (see existing Scribe.translate.translating_to "...%{language}..."). None of the 9 keys in this plan take interpolation.
</interfaces>
</context>

<design_decision>
TWO new keys are introduced here (consumed by plan 02's extraction work) so that plan 02 touches ONLY .jsx and never the locale JSON — this keeps files_modified disjoint and removes the Scribe.button.copy merge race the CONTEXT flagged:

  Scribe.panel.description  — the result-panel info button label/title (replaces hard-coded "Description du panneau" / "Description").
  Scribe.result.table_partial_insert_unavailable — the disabled-Insert tooltip (replaces hard-coded "Insertion non disponible pour une selection partielle de tableau").

The hard-coded "Copier le contenu" / "Copier" (ScribeResultPanel CopyButton) is NOT a new key — it REUSES the existing Scribe.button.copy, which this plan is already adding to de/es/it. So after this plan, all keys plan 02 needs exist in all 5 locales.

Placement convention: add `tagline` under a new/existing `panel` object; `typing/error_prefix/welcome/selection_context` under a new `chat` object; `open_panel/copy` under the existing `button` object; `description` under `panel`; `table_partial_insert_unavailable` under a new `result` object. Mirror this structure across all 5 files so the flattened key sets are identical.
</design_decision>

<tasks>

<task type="auto">
  <name>Task 1: Add the 7 missing keys (de/es/it) + 2 new keys (all 5 locales), translated</name>
  <read_first>
    - src/locales/fr.json (Scribe block — exact reference structure/values for the 7 keys)
    - src/locales/en.json (Scribe block — English reference values)
    - src/locales/de.json, src/locales/es.json, src/locales/it.json (current Scribe block — confirm panel/chat absent, button present, observe existing tone)
    - .planning/phases/v3.1-07-i18n-5-locales/v3.1-07-CONTEXT.md (locked: native-quality translations, reuse button.copy)
  </read_first>
  <action>
    For de.json, es.json, it.json: add the 7 missing Scribe.* keys with NATIVE-quality translations (not English copies, not literal calques) matching each locale's existing Scribe tone (anchors in <interfaces>). Create the `panel` object (`tagline`), create the `chat` object (`typing`, `error_prefix`, `welcome`, `selection_context`), and add `open_panel` + `copy` to the existing `button` object.
    Suggested translations (use these unless a more idiomatic native phrasing is warranted — never fall back to English):
      de: panel.tagline "Ihr Schreibassistent" · chat.typing "Scribe denkt nach..." · chat.error_prefix "Fehler" · chat.welcome "Fragen Sie mich etwas über Ihr Dokument" · chat.selection_context "Ausgewählter Text" · button.open_panel "Scribe-Panel" · button.copy "Kopieren"
      es: panel.tagline "Tu asistente de escritura" · chat.typing "Scribe está pensando..." · chat.error_prefix "Error" · chat.welcome "Pregúntame lo que quieras sobre tu documento" · chat.selection_context "Texto seleccionado" · button.open_panel "Panel de Scribe" · button.copy "Copiar"
      it: panel.tagline "Il tuo assistente di scrittura" · chat.typing "Scribe sta pensando..." · chat.error_prefix "Errore" · chat.welcome "Chiedimi qualsiasi cosa sul tuo documento" · chat.selection_context "Testo selezionato" · button.open_panel "Pannello Scribe" · button.copy "Copia"
    Then add the TWO new keys to ALL 5 locales (fr, en, de, es, it) — create a `panel.description` and a `result.table_partial_insert_unavailable`:
      Scribe.panel.description:
        fr "Description du panneau" · en "Panel description" · de "Panel-Beschreibung" · es "Descripción del panel" · it "Descrizione del pannello"
      Scribe.result.table_partial_insert_unavailable:
        fr "Insertion non disponible pour une sélection partielle de tableau" · en "Insertion unavailable for a partial table selection" · de "Einfügen für eine partielle Tabellenauswahl nicht verfügbar" · es "Inserción no disponible para una selección parcial de tabla" · it "Inserimento non disponibile per una selezione parziale di tabella"
    Do NOT alter any existing Scribe.* value. Keep each JSON valid (2-space indent, matching the file's existing style, trailing-comma-free). Do NOT touch non-target locales (ar/ja/ko/nl/nl_NL/pl/ru/vi/zh_*).
  </action>
  <verify>
    <automated>node -e "['fr','en','de','es','it'].forEach(l=>{const o=require('./src/locales/'+l+'.json').Scribe;const need=['panel.tagline','panel.description','chat.typing','chat.error_prefix','chat.welcome','chat.selection_context','button.open_panel','button.copy','result.table_partial_insert_unavailable'];need.forEach(k=>{const v=k.split('.').reduce((a,p)=>a&&a[p],o);if(!v||typeof v!=='string'||!v.trim())throw new Error(l+' missing/empty '+k);});});console.log('OK all 9 keys present and non-empty in 5 locales')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 of fr/en/de/es/it parse as valid JSON (the node -e above does not throw on require).
    - Each of de/es/it now has Scribe.panel.tagline, Scribe.chat.{typing,error_prefix,welcome,selection_context}, Scribe.button.{open_panel,copy} — non-empty strings.
    - All 5 locales have Scribe.panel.description and Scribe.result.table_partial_insert_unavailable — non-empty strings.
    - de/es/it values for the 7 keys are NOT identical to the English reference values (native translation, not a copy) — spot-check chat.welcome and panel.tagline differ from en.
    - No pre-existing Scribe.* value changed: `git diff src/locales/{de,es,it}.json` shows only additions; `git diff src/locales/{fr,en}.json` shows only the 2 new keys added.
  </acceptance_criteria>
  <done>de/es/it reach 46 Scribe.* keys each (44 + 2 new), fr/en reach 46 too; all 9 target keys present and translated in every locale; no existing value mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Flattened Scribe.* key-set parity gate across the 5 target locales</name>
  <read_first>
    - src/locales/index.js (how locales are imported — the spec imports the JSON files directly, same path style)
    - src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js (existing jest conventions in this repo: describe/it, no special harness)
  </read_first>
  <action>
    Create src/locales/scribeI18nParity.spec.js. It imports fr, en, de, es, it JSON directly (`import fr from './fr.json'` ... or require — match repo module style). Implement a local `flatten(obj, prefix)` that returns the sorted list of dotted leaf keys (string leaves only) under the `Scribe` namespace for one locale.
    Assertions (the criterion-1 regression net):
      1. Reference: compute the fr Scribe key set as the expected set. For each of en/de/es/it, assert its flattened Scribe key set is DEEP-EQUAL to fr's (use a sorted-array equality or set-difference both directions). On mismatch the failure message must name the locale AND the missing/extra keys (compute and print the symmetric difference) so a future drift is self-explaining.
      2. Non-empty: for every locale in the 5, assert every Scribe leaf value is a non-empty trimmed string (guards against a key that exists but was left "").
    Keep it dependency-free (no new npm). Use only jest globals already available in the repo.
  </action>
  <verify>
    <automated>yarn jest src/locales/scribeI18nParity.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `yarn jest src/locales/scribeI18nParity.spec.js` exits 0 with the parity + non-empty assertions green for all 5 locales.
    - The test FAILS loudly if a key is removed: confirm by temporarily deleting one Scribe.chat key from de.json, re-running (expect red naming `de` + the missing key), then restoring — leave the file restored and green.
    - The spec imports/reads exactly the 5 target locale files (fr/en/de/es/it), not the 16-file set (non-target locales are explicitly out of scope per CONTEXT — they would falsely fail).
    - `grep -c "Scribe i18n parity" src/locales/scribeI18nParity.spec.js` >= 1.
  </acceptance_criteria>
  <done>scribeI18nParity.spec.js asserts identical flattened Scribe.* key sets + non-empty values across fr/en/de/es/it and is green; criterion 1 has a deterministic regression gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation JSON -> UI render | Locale values are rendered via t() into the Scribe surfaces; values are author-controlled (not user/LLM input), low trust risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-07-01 | Tampering | locale JSON edits silently mutating an existing value | mitigate | Task 1 acceptance asserts `git diff` shows additions only; the parity test's non-empty assertion + existing surface suites catch a broken/blanked value. |
| T-v3.1-07-02 | Information Disclosure | translation drift leaving a locale with a missing/English-fallback label | mitigate | scribeI18nParity.spec.js is a permanent regression net: any future key add to fr/en that is not mirrored to de/es/it fails CI, naming the gap. |
| T-v3.1-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (pure JSON edits + a dependency-free jest spec). No legitimacy gate required. |
</threat_model>

<verification>
- `yarn jest src/locales/scribeI18nParity.spec.js` green (key-set parity + non-empty across the 5 locales).
- `node -e` key-presence check (Task 1 verify) passes for all 9 target keys in all 5 locales.
- `git diff src/locales/` confirms additive-only changes to de/es/it and only the 2 new keys in fr/en.
</verification>

<success_criteria>
- ROADMAP v3.1-07 criterion 1 met for the data layer: card labels + fallback/error messages (the 7 + 2 keys) exist and are natively translated across fr/en/de/es/it, locked behind a deterministic flattened-key-set parity Jest gate. (Wiring of the 2 new keys into the surfaces is plan 02.)
</success_criteria>

<output>
Create `.planning/phases/v3.1-07-i18n-5-locales/v3.1-07-01-SUMMARY.md` when done.
</output>
