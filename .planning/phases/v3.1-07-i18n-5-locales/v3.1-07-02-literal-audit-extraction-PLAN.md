---
phase: v3.1-07-i18n-5-locales
plan: 02
type: execute
wave: 2
depends_on: [v3.1-07-01]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js
autonomous: true
requirements: [I18N-01]
must_haves:
  truths:
    - "Every hard-coded user-facing FR string in the result/popover/chat Scribe surfaces is replaced by a t('Scribe.*') call"
    - "The disabled-Insert tooltip renders t('Scribe.result.table_partial_insert_unavailable') instead of the hard-coded French sentence"
    - "The result-panel info button uses t('Scribe.panel.description') (label + aria/title) instead of hard-coded 'Description du panneau'/'Description'"
    - "The copy affordance reuses t('Scribe.button.copy') instead of the hard-coded 'Copier'/'Copier le contenu'"
    - "A literal-audit test/gate fails if any of the three known hard-coded FR patterns reappears in the surface files"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js"
      provides: "Source-scanning regression gate: fails if a known hard-coded Scribe UI literal survives in the user surfaces"
      contains: "literal audit"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "All user-facing strings wired through t(); the 3 confirmed literals removed"
      contains: "t('Scribe.result.table_partial_insert_unavailable')"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      to: "Scribe.result.table_partial_insert_unavailable / Scribe.panel.description / Scribe.button.copy"
      via: "t() replaces hard-coded FR literals"
      pattern: "t\\('Scribe\\.(result|panel|button)\\."
    - from: "src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js"
      to: "ScribeResultPanel.jsx source text"
      via: "read file, assert forbidden literal patterns are absent"
      pattern: "Insertion non disponible"
---

<objective>
Run a full literal audit across the user-facing popover/chat/result Scribe surfaces and extract every remaining hard-coded user-facing string to a t('Scribe.*') call (the keys all exist after plan 01), then lock criterion 2 behind a source-scanning Jest gate that fails if any known hard-coded literal reappears (I18N-01).

Purpose: Deliver "zero hard-coded Scribe UI string in the popover/chat/result surfaces" — the success criterion 2 — with a permanent regression net.
Output: ScribeResultPanel.jsx fully wired through t(); scribeLiteralAudit.spec.js gating the surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-07-i18n-5-locales/v3.1-07-CONTEXT.md

<interfaces>
<!-- Audit findings + exact wiring targets. Extracted from the live code; use directly. -->

i18n hook (already imported in every surface): `import { useI18n } from 'twake-i18n'`; in-component `const { t } = useI18n()`; call `t('Scribe.some.key')`. Interpolation is `%{var}` (none needed here).

Keys available after plan 01 (do NOT add to locale JSON in this plan — it is plan 01's exclusive territory):
  Scribe.panel.description                      (new, all 5 locales)
  Scribe.result.table_partial_insert_unavailable (new, all 5 locales)
  Scribe.button.copy                            (existed in fr/en, added to de/es/it by plan 01)

CONFIRMED hard-coded USER-FACING literals in ScribeResultPanel.jsx (line numbers approximate — verify by reading):
  1. ~l.215-216  aria-label="Description du panneau"  title="Description"
       -> inside `DevPanelTitle` component (used by the result info toggle). Replace BOTH with t('Scribe.panel.description').
  2. ~l.269-270  aria-label="Copier le contenu"  title="Copier"
       -> inside `CopyButton` component. Replace BOTH with t('Scribe.button.copy').
       NOTE: CopyButton/DevPanelTitle are functional components — `const { t } = useI18n()` must be added inside each (they don't have it yet). Confirm by reading l.197 and l.254.
  3. ~l.1331  title={ insertDisabled ? 'Insertion non disponible pour une selection partielle de tableau' : undefined }
       -> inside the user-facing `ScribeResultPanel` component (l.988-1411), which ALREADY has `const { t } = useI18n()` at ~l.1009. Replace the literal with t('Scribe.result.table_partial_insert_unavailable'), keeping the ternary (undefined when enabled).

SCOPE BOUNDARY (locked by CONTEXT): the PROBE-01 dev panels (ParsedResponsePanel / ProbeMetricsPanel / DevPanelGrid / ScribeDevPanels, ~l.316-985, and the "Inspecteur dev (chat)" / "DEV — inspecteur detaille" strings in ChatMessageList) are DEVELOPER diagnostics gated behind isScribeDevMd() — they are NOT the user-facing popover/chat/result surfaces and are NOT in scope for criterion 2. Do NOT translate them. The three literals above are the only user-facing hard-coded strings: 1 & 2 surface via the result info/copy affordances the CONTEXT explicitly named; 3 is the disabled-Insert tooltip in the live result panel.

Surfaces audited and already clean (t()-only, no action needed — confirm during audit): FragmentCard.jsx, ScribePopover.jsx, ScribeActionMenu.jsx, MarkdownPreview.jsx, ChatMessageList.jsx (chat.welcome/typing/error_prefix already via t() at l.131/148/532), ScribeContext.jsx. scribeResponse.js buildFallback emits warning CODES (not rendered UI text) — leave as-is.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full literal audit + extract the 3 confirmed user-facing strings to t()</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (DevPanelTitle ~l.197-252, CopyButton ~l.254-291, ScribeResultPanel component ~l.988-1411 incl. the disabled-Insert tooltip ~l.1331 and the existing useI18n at ~l.1009)
    - src/modules/views/OnlyOffice/Scribe/{FragmentCard,ScribePopover,ScribeActionMenu,MarkdownPreview,ChatMessageList,ScribeContext}.jsx (confirm they are already t()-only for user-facing strings — see <interfaces>)
    - .planning/phases/v3.1-07-i18n-5-locales/v3.1-07-CONTEXT.md (locked scope: 3 strings + fuller audit; dev panels out of scope; reuse button.copy)
  </read_first>
  <action>
    First, AUDIT: grep each user surface for hard-coded user-facing literals (JSX text nodes, aria-label/title/placeholder/alt props with prose, not code constants/event names/className/test-id). If the audit surfaces a user-facing literal beyond the three below, STOP and report it (it would need a key from plan 01 that may not exist) — do NOT silently translate a string with no key, and do NOT expand scope into the dev panels.
    Then EXTRACT the three confirmed literals in ScribeResultPanel.jsx:
      1. In DevPanelTitle (~l.197): add `const { t } = useI18n()` at the top of the component body; replace aria-label="Description du panneau" and title="Description" both with `t('Scribe.panel.description')`.
      2. In CopyButton (~l.254): add `const { t } = useI18n()`; replace aria-label="Copier le contenu" and title="Copier" both with `t('Scribe.button.copy')`.
      3. In the ScribeResultPanel component (~l.1331): replace the hard-coded `'Insertion non disponible pour une selection partielle de tableau'` inside the title ternary with `t('Scribe.result.table_partial_insert_unavailable')`, preserving the `insertDisabled ? ... : undefined` shape. The component already has `t` in scope (~l.1009) — do not add a second useI18n().
    Do NOT modify the locale JSON files (plan 01 owns them; the keys already exist). Do NOT touch the dev-panel diagnostic French prose (ParsedResponsePanel/ProbeMetricsPanel/DevPanelGrid and the ChatMessageList "Inspecteur dev" strings) — out of scope per CONTEXT.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.spec.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Insertion non disponible" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` returns 0.
    - `grep -c "Description du panneau" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` returns 0; `grep -c "Copier le contenu" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` returns 0.
    - `grep -c "t('Scribe.result.table_partial_insert_unavailable')" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` >= 1; `t('Scribe.panel.description')` >= 1; the CopyButton path references `t('Scribe.button.copy')`.
    - DevPanelTitle and CopyButton each have `const { t } = useI18n()` in scope (grep both component bodies); ScribeResultPanel still has exactly one useI18n() (no duplicate added).
    - `yarn jest src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.spec.jsx` exits 0 (existing result-panel suite still green).
    - Audit note recorded in the SUMMARY: the user-facing surfaces beyond ScribeResultPanel were checked and are already t()-only; the dev-gated PROBE-01 panels are intentionally out of scope per CONTEXT.
  </acceptance_criteria>
  <done>The 3 confirmed user-facing literals render via t(); the fuller audit found no other in-scope user-facing literal; dev panels untouched; ResultPanel suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Literal-audit regression gate over the user surfaces</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js (repo jest conventions: plain describe/it, fs read allowed via node)
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (post-extraction — to confirm the forbidden patterns are gone)
  </read_first>
  <action>
    Create src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js. It reads the source TEXT of the user-facing surface files (fs.readFileSync, path resolved relative to __dirname): ScribeResultPanel.jsx, FragmentCard.jsx, ScribePopover.jsx, ScribeActionMenu.jsx, MarkdownPreview.jsx, ChatMessageList.jsx.
    Assertions (criterion-2 net):
      1. FORBIDDEN literals: assert each of these exact substrings is ABSENT from the concatenated surface sources (these are the known extracted FR literals — their reappearance means a regression): "Insertion non disponible pour une selection partielle de tableau", "Description du panneau", "Copier le contenu", "Copier"... — for the bare-word ones ("Copier", "Description") scope the check narrowly to aria-label="/title=" prop literals via a regex so the dev-panel French diagnostic prose (legitimately out of scope) does not trip the gate. Prefer asserting the full distinctive sentences plus a regex like /(?:aria-label|title)="(?:Copier|Description)[^"]*"/ that matches ONLY hard-coded prop literals, not t() calls.
      2. Document the scope: a top-of-file comment listing the surfaces in scope and explicitly noting the PROBE-01 dev panels are excluded per CONTEXT (so a future reader does not "fix" the gate to scan dev prose).
    Keep it dependency-free (node fs only, jest globals). The gate must FAIL if someone re-introduces a hard-coded user-facing prop literal in these surfaces.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `yarn jest src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js` exits 0 against the post-extraction sources.
    - The gate FAILS if a literal is re-introduced: temporarily re-add `title="Copier"` somewhere in ScribeResultPanel.jsx's user component, re-run (expect red), then revert — leave green.
    - The regex for bare words is scoped to aria-label="/title=" props (does NOT match `t('Scribe.button.copy')` nor the dev-panel French diagnostic prose) — confirm by running green WITH the dev panels' existing French strings still present in the file.
    - `grep -c "literal audit" src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js` >= 1.
    - Full Scribe suite sanity: `yarn jest src/modules/views/OnlyOffice/Scribe/` exits 0 except the known pre-existing red ScribeContainer "Drawer PaperProps fullscreen on mobile" (layout, untouched) — confirm it is the ONLY failure.
  </acceptance_criteria>
  <done>scribeLiteralAudit.spec.js scans the user surfaces, fails on any re-introduced hard-coded prop literal, passes against the extracted sources, and is scoped to exclude the out-of-scope dev panels; criterion 2 has a permanent gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code -> rendered UI | Hard-coded literals in surface JSX bypass i18n; the audit gate is the boundary check that all user-facing prose crosses through t(). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-07-03 | Information Disclosure | a future hard-coded FR literal leaking into a non-FR locale's UI | mitigate | scribeLiteralAudit.spec.js permanently fails if a known forbidden literal (or a hard-coded aria-label/title prop) reappears in the user surfaces. |
| T-v3.1-07-04 | Tampering | over-broad audit regex falsely flagging legitimate t() calls or dev-panel prose | mitigate | The bare-word check is scoped to aria-label="/title=" prop literals only; acceptance asserts green WITH dev-panel French prose still present, so the gate cannot creep into out-of-scope text. |
| T-v3.1-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (JSX edits + a node-fs jest spec, no new deps). No legitimacy gate required. |
</threat_model>

<verification>
- `yarn jest src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.spec.jsx` green (extraction did not break the result panel).
- `yarn jest src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js` green (no forbidden literal survives) and proven to fail on re-introduction.
- `grep -c` checks confirm the 3 confirmed literals are gone and replaced by t() calls.
- `yarn jest src/modules/views/OnlyOffice/Scribe/` green modulo the known pre-existing Drawer red.
</verification>

<success_criteria>
- ROADMAP v3.1-07 criterion 2 met: the literal audit shows NO hard-coded user-facing Scribe UI string in the popover/chat/result surfaces — the 3 confirmed FR literals are wired through t(), the fuller audit found no others in scope, and a permanent Jest gate prevents regression.
</success_criteria>

<output>
Create `.planning/phases/v3.1-07-i18n-5-locales/v3.1-07-02-SUMMARY.md` when done.
</output>
