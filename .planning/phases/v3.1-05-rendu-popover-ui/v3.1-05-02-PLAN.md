---
phase: v3.1-05-rendu-popover-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeActions.js
autonomous: false
requirements: [MENU-01]
must_haves:
  truths:
    - "D-04: The action menu shows, in order: Corriger la grammaire, Traduire ▸, Changer le ton ▸, Améliorer ▸, the free-prompt input as an integrated list entry, then an 'Ouvrir le panneau latéral' entry at the bottom"
    - "D-06: The standalone in-popover open-panel <button> (PanelIcon) is removed; the new menu entry calls the same onOpenPanel callback"
    - "D-06: The new open-panel entry reuses the existing i18n key Scribe.button.open_panel"
    - "D-05: Keyboard roving covers the actions, the prompt entry as an integrated list input (index n), and the open-panel entry (index n+1), wrapping consistently on desktop and mobile"
    - "D-07: The floating PanelIcon button (ScribeFloatingButton) is left unchanged"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx"
      provides: "Integrated prompt entry + open-panel ListItem routed to onOpenPanel; removed standalone PanelIcon button"
      contains: "onOpenPanel"
    - path: "src/modules/views/OnlyOffice/Scribe/scribeActions.js"
      provides: "Declarative action tree unchanged for LLM actions; open-panel entry NOT added to SCRIBE_ACTIONS (injected like DEV_MD_ACTION)"
      contains: "SCRIBE_ACTIONS"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx"
      to: "onOpenPanel"
      via: "the bottom 'Ouvrir le panneau latéral' ListItem onClick"
      pattern: "onOpenPanel"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx"
      to: "Scribe.button.open_panel"
      via: "ListItemText label for the open-panel entry"
      pattern: "Scribe.button.open_panel"
---

<objective>
Rework the popover action menu (MENU-01): make the free-prompt an integrated list entry, add an "Ouvrir le panneau latéral" entry at the bottom that replaces the removed in-popover open-panel icon button, and keep keyboard roving consistent. The floating PanelIcon button on the document is conserved unchanged (D-07).

Purpose: A single coherent menu surface — quick actions, then free prompt, then panel access — instead of a separate prompt card plus a floating icon. Order per D-04, validated in discussion.

Output: Updated `ScribeActionMenu.jsx` (relocated prompt entry + open-panel ListItem, removed icon button, extended roving) and `scribeActions.js` (open-panel injected like DEV_MD_ACTION, NOT into SCRIBE_ACTIONS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-CONTEXT.md
@.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx:
- l.21-29 `DEV_MD_ACTION` — the precedent for a non-LLM menu entry injected into `actions` (not in SCRIBE_ACTIONS).
- l.31-36 `PanelIcon` svg — reuse as the `<ListItemIcon>` for the open-panel entry.
- l.69-75 `actions = useMemo(...)` — builds SCRIBE_ACTIONS + optional DEV_MD_ACTION. The open-panel entry must NOT be an LLM action.
- l.77 `const PROMPT_INDEX = actions.length` — prompt sits AFTER all actions.
- l.96-103 `updateFocus(newIndex)` — `newIndex === PROMPT_INDEX` focuses prompt input, else focuses menu Paper.
- l.136-163 `handlePromptArrow(direction)` — 'down' currently wraps to index 0; must instead go to the new open-panel entry.
- l.168 `handleKeyDown` early-returns when `focusIndex === PROMPT_INDEX` (prompt owns its arrows).
- l.384-390 main-list arrow wrap: `% (actionCount + 1)` — grows to `% (actionCount + 2)` once open-panel is a roving target.
- l.433-480 the standalone `onOpenPanel && (<button><PanelIcon/>…</button>)` block + tooltip — REMOVE this (D-06).
- l.474 tooltip already uses `t('Scribe.button.open_panel')` — same key for the new ListItemText.
- l.488-562 mobile main-list/submenu branch; l.565-684 desktop `actions.map` ListItem render (item shape, first/last borderRadius l.583-584); l.688-707 the separate prompt `<Paper width:500>` hosting `<ScribePromptInput>` (relocate into the list per D-05).
- ListItem render shape (l.579-600): `<ListItem button selected={…} onClick={…}><ListItemIcon><Icon icon={…}/></ListItemIcon><ListItemText primary={…}/></ListItem>`.

From src/modules/views/OnlyOffice/Scribe/scribeActions.js:
- `SCRIBE_ACTIONS` leaf shape (l.84-91): `{ id, labelKey, icon, children, prompt, mockResult }`.
- `type:'input'` child shape (l.140-149): `{ id, type:'input', placeholderKey, icon, prompt, ... }`.
- `FREE_PROMPT_CONFIG` (l.225-229): defines `id:'free-prompt'`, matching `onSelect('free-prompt', prompt, prompt)` at ScribeActionMenu l.107 — KEEP this id.

i18n: `Scribe.button.open_panel` already defined in all locales (fr "Panneau Scribe", en "Scribe Panel"). NO new key required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate prompt into the list, add open-panel entry, remove the standalone icon button, extend roving</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx, src/modules/views/OnlyOffice/Scribe/scribeActions.js</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx (full file — focus l.21-36, 69-103, 136-163, 165-424, 428-710)
    - src/modules/views/OnlyOffice/Scribe/scribeActions.js (l.84-91 leaf shape, l.140-149 input child, l.225-229 FREE_PROMPT_CONFIG)
    - src/modules/views/OnlyOffice/Scribe/ScribePromptInput.jsx (its onArrow('up'|'down')/onSubmit/onEscape contract)
    - src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx (D-07 — DO NOT modify; read only to avoid confusing it with the removed in-popover icon)
    - .planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-PATTERNS.md (§ ScribeActionMenu — D-04/D-05/D-06; § scribeActions — open-panel injection)
  </read_first>
  <action>
    Per D-04 produce the menu order: the LLM actions (`Corriger la grammaire`, `Traduire ▸`, `Changer le ton ▸`, `Améliorer ▸` — from `SCRIBE_ACTIONS`), then the free-prompt as an integrated list entry at index `PROMPT_INDEX = actions.length`, then a new "Ouvrir le panneau latéral" entry at index `PROMPT_INDEX + 1` (the new last item).

    D-05 (prompt as list entry): relocate the separate prompt `<Paper width:500>` (l.688-707) so the `ScribePromptInput` reads as an integrated entry within the menu list rather than a detached card below it (Claude's Discretion on exact layout: either keep the dedicated `ScribePromptInput` relocated into the list, or model it as a `type:'input'` entry like the translate-custom precedent — keep the `free-prompt` id and the `onSelect('free-prompt', prompt, prompt)` call at l.107 intact). Preserve the focus-highlight keyed on `focusIndex === PROMPT_INDEX` and the `updateFocus(PROMPT_INDEX)` hover wiring.

    D-06 (open-panel entry replaces the icon): REMOVE the standalone `onOpenPanel && (<button><PanelIcon/>…</button>)` block and its tooltip (l.433-480). Add a normal bottom `<ListItem button>` that calls the SAME `onOpenPanel` callback, using `PanelIcon` as `<ListItemIcon>` and `t('Scribe.button.open_panel')` as `<ListItemText primary>`. Apply the last-item `borderRadius '0 0 8px 8px'`; the prompt entry, no longer last among real items, loses that corner. Only render the entry when `onOpenPanel` is provided (mirror the existing `onOpenPanel &&` guard).

    scribeActions.js: do NOT add the open-panel entry to `SCRIBE_ACTIONS` (those feed `buildMessages`/the LLM pipeline). Inject it like the `DEV_MD_ACTION` precedent — e.g. a dedicated constant/flag handled in `ScribeActionMenu` rendering — so it never leaks into the LLM action path. (Optionally export a small constant from scribeActions.js for the entry's id/labelKey/icon; Claude's Discretion whether the constant lives in scribeActions.js or ScribeActionMenu.jsx, as long as it is NOT a `SCRIBE_ACTIONS` member.)

    Extend keyboard roving consistently: the prompt remains at index `n`; the open-panel entry is at `n+1`. Update the main-list wrap from `% (actionCount + 1)` to `% (actionCount + 2)` (l.384-390) so ArrowUp/ArrowDown cycle actions → prompt → open-panel → wrap. Update `handlePromptArrow` (l.136-163) so 'down' from the prompt moves focus to the open-panel entry (index `n+1`) instead of wrapping to index 0, and ensure 'up' from open-panel returns to the prompt and Enter/Space on the open-panel entry calls `onOpenPanel`. Apply the same roving extension to the mobile main-list branch (l.488-562 / l.565-684) so the relocated prompt + open-panel entries participate consistently on mobile. Keep `handleKeyDown`'s `focusIndex === PROMPT_INDEX` early-return (prompt owns its own arrows).

    Do NOT modify `ScribeFloatingButton.jsx` (D-07). Do NOT modify `ScribePopover.jsx` — `onOpenPanel` is already threaded into `ScribeActionMenu` (l.423); the entry reuses that same prop.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/ScribeActionMenu --silent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The standalone in-popover open-panel button is removed: no `<button` wrapping `onClick={onOpenPanel}` remains in ScribeActionMenu.jsx (the former l.433-480 block is gone); `PanelIcon` is now referenced only inside the new ListItem.
    - A bottom open-panel `<ListItem>` calls `onOpenPanel`: ScribeActionMenu.jsx contains a `ListItem` whose onClick is `onOpenPanel` and whose `ListItemText` primary is `t('Scribe.button.open_panel')`.
    - The open-panel entry is NOT a member of `SCRIBE_ACTIONS`: it is injected like `DEV_MD_ACTION`, not pushed into the `SCRIBE_ACTIONS` array in scribeActions.js.
    - The free-prompt id is preserved: `grep -c "'free-prompt'" src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx` is ≥ 1 (the `onSelect('free-prompt', …)` call still present).
    - Roving extended: the main-list wrap modulus is `actionCount + 2` (not `+ 1`) in ScribeActionMenu.jsx; `handlePromptArrow` 'down' targets the open-panel index, not 0.
    - `ScribeFloatingButton.jsx` is unmodified (no diff).
    - `ScribePopover.jsx` is unmodified by this plan (onOpenPanel prop threading already exists).
    - Targeted jest suite for ScribeActionMenu passes (update/extend the suite for the new entry + roving if needed).
  </acceptance_criteria>
  <done>Menu shows the four LLM actions, the integrated free-prompt entry, then the open-panel entry at the bottom; the standalone icon button is gone; the entry calls onOpenPanel with the existing i18n key; keyboard roving cycles actions → prompt → open-panel on desktop and mobile; floating button untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the reworked menu order, open-panel entry, and keyboard roving</name>
  <files>(none — validation only)</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx (the edited render + roving)
    - .planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-CONTEXT.md (D-04 menu schema; D-07 floating button conserved)
  </read_first>
  <action>This is a human verification checkpoint — no source edits. The executor pauses and the user validates the reworked menu in the running app per the steps in <how-to-verify>.</action>
  <what-built>
    The popover action menu was reworked (Task 1): the free-prompt is now an integrated list entry, an "Ouvrir le panneau latéral" entry sits at the bottom (replacing the removed in-popover icon button) and calls the same onOpenPanel; keyboard roving cycles actions → prompt → open-panel. The floating PanelIcon button on the document is unchanged (D-07).
  </what-built>
  <how-to-verify>
    1. Build Drive: `yarn build`, then hard-reload the OO editor tab.
    2. Select text and open the Scribe popover. Confirm the menu order top-to-bottom: Corriger la grammaire, Traduire ▸, Changer le ton ▸, Améliorer ▸, the free-prompt input entry, then "Panneau Scribe" / "Ouvrir le panneau latéral" at the bottom.
    3. Confirm the old separate prompt card and the small floating open-panel icon inside the popover are gone (folded into the menu).
    4. Click the "Ouvrir le panneau latéral" entry → the side panel opens (same behavior as the old icon).
    5. Keyboard: with the menu focused, press ↓ repeatedly — focus should cycle through the actions, into the prompt input, then to the open-panel entry, then wrap. From the prompt, ↓ moves to the open-panel entry; ↑ returns to the prompt. Enter/Space on the open-panel entry opens the panel.
    6. Confirm the separate floating Scribe panel button ON THE DOCUMENT (outside the popover) still appears and still works (D-07 conserved).
  </how-to-verify>
  <verify><human-check>Menu order, integrated prompt, open-panel entry, roving, and the conserved floating document button all behave as described.</human-check></verify>
  <done>The reworked menu order, integrated prompt entry, bottom open-panel entry (calling onOpenPanel), keyboard roving, and the conserved floating document button are all confirmed in the running app.</done>
  <resume-signal>Type "approved" if the menu order, open-panel entry, roving, and the conserved floating button all behave as described; otherwise describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → menu actions | Free-prompt text is user-authored; it flows to the existing LLM pipeline via the unchanged `onSelect('free-prompt', …)` path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-05-04 | Elevation / pipeline leak | open-panel entry injected into the action tree | mitigate | The open-panel entry is NOT a `SCRIBE_ACTIONS` member and does not call `onSelect`/`buildMessages` — it only calls `onOpenPanel`. This prevents a non-LLM UI entry from leaking into the LLM message pipeline (acceptance criteria assert separation). |
| T-v3.1-05-05 | Tampering (XSS) | free-prompt input rendering | accept | No new rendering sink — the prompt input is the existing `ScribePromptInput`/`InputBase`; this plan relocates it, it does not change how its value is rendered or submitted. The LLM output rendering (XSS surface) is owned by plan 01's `MarkdownPreview` path, not this plan. |
| T-v3.1-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (UI reuse only, zero new dependencies). |
</threat_model>

<verification>
- Menu order matches D-04; standalone open-panel icon button removed; new bottom entry calls `onOpenPanel` with `Scribe.button.open_panel`.
- Open-panel entry is not a `SCRIBE_ACTIONS` member (no LLM pipeline leak).
- Keyboard roving cycles actions → prompt → open-panel (desktop + mobile).
- `ScribeFloatingButton.jsx` and `ScribePopover.jsx` unmodified.
- Targeted jest suite green; live UAT (checkpoint) confirms behavior.
</verification>

<success_criteria>
- MENU-01: the free-prompt is an integrated list entry and an "Ouvrir le panneau latéral" entry follows it at the bottom, replacing the removed in-popover open-panel icon; the floating document button is conserved.
- The new entry reuses the existing i18n key and the same `onOpenPanel` callback; keyboard navigation remains coherent.
</success_criteria>

<output>
Create `.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-02-SUMMARY.md` when done.
</output>
