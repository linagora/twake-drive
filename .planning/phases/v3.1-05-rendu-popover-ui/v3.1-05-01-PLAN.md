---
phase: v3.1-05-rendu-popover-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
autonomous: false
requirements: [INLINE-01, INLINE-02]
must_haves:
  truths:
    - "The popover result step renders the single normalized fragment inside a Scribe-violet (#7C3AED) card matching the chat FragmentCard frame"
    - "The card body renders rich markdown with REF/TABLE/footnote markers cleaned for display only (via MarkdownPreview)"
    - "The popover never displays parsed.discussion — only the normalized fragment is visible"
    - "Insert / Replace / Retry still operate on the RAW normalized fragment (markers intact), not the cosmetic display string"
    - "Each inline exchange (prompt + discussion + fragment) is mirrored into shared chat history and appears in the side panel chat"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "Card-aligned (violet frame) result render around MarkdownPreview, fed the RAW fragment"
      contains: "SCRIBE_PURPLE"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "Passes the RAW normalized fragment (rawResult) to the result panel for cosmetic render; Insert/Replace already route on rawResult"
      contains: "rawResult"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      via: "card render receives the RAW normalized fragment for cosmetic display, footer Insert/Replace receives rawResult"
      pattern: "rawResult"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/MarkdownPreview"
      via: "MarkdownPreview runs the single cosmetic marker cleanup on the RAW fragment"
      pattern: "MarkdownPreview"
---

<objective>
Render the popover `result` step's single normalized fragment as a card aligned with v3.1-04's chat card (violet `#7C3AED` frame, rich markdown, clean REF/TABLE/footnote markers) WITHOUT showing the `discussion` (INLINE-01). Validate end-to-end that the inline exchange is mirrored into shared chat history (INLINE-02).

Purpose: Visual + semantic parity between inline and chat surfaces so the user sees the insertable fragment the same way in both places, while the RAW reinjection invariant (D-02) is preserved so rich content (tables/footnotes/cross-refs) still inserts correctly.

Output: Updated `ScribeResultPanel.jsx` (card-framed cosmetic render) and `ScribePopover.jsx` (feeds the RAW fragment to the card; Insert/Replace unchanged on `rawResult`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-CONTEXT.md
@.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx (the chat card to mirror):
```jsx
const SCRIBE_PURPLE = '#7C3AED'
const SCRIBE_PURPLE_08 = 'rgba(124, 58, 237, 0.08)'
const FragmentCard = ({ raw, hasSelection }) => { ... }
// frame: border 1px SCRIBE_PURPLE; bg isDark ? 'rgba(124,58,237,0.12)' : SCRIBE_PURPLE_08;
// borderRadius 8; padding '8px 10px'; <MarkdownPreview>{raw}</MarkdownPreview>; <MessageActions content={raw} .../>
// INVARIANT: `raw` passed UNCHANGED to MarkdownPreview (cosmetic only) — FragmentCard cleans nothing.
```

From src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (current, to align):
- Imports already present: `MarkdownPreview`, `useTheme`. Add `SCRIBE_PURPLE` / `SCRIBE_PURPLE_08` (or import from FragmentCard).
- l.1177 `const isDark = (theme.palette.type || theme.palette.mode) === 'dark'`
- l.1179-1211 `resultContent`: `scribe-result-text` div → `cellWarning` banner (l.1193-1206) + `<MarkdownPreview>{resultText}</MarkdownPreview>` (l.1207).
- l.1268-1277 dev branch: `devData ? <ScribeDevPanels.../> : resultContent` — card alignment applies ONLY to `resultContent` (non-dev).
- l.1279-1316 footer: Insert/Replace/Retry buttons wired to `onInsert`/`onReplace`/`onRetry` — KEEP these.
- propTypes: `resultText` (string, required). Will need a new `rawFragment` (string) prop for the RAW card body.

From src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx:
- l.240-261 normalization → `setRawResult(normalizedFragment)` (RAW, markers intact); `setResult({ text: displayMd, ... })` (cosmetic today).
- l.264-291 `addMessage(...)` user+assistant turns = INLINE-02 mirror (content=normalizedFragment, discussion/fragments/fellBack preserved).
- l.345-351 `handleReplace`/`handleInsert` already call `onReplace(rawResult || result.text)` / `onInsert(rawResult || result.text)` — RAW-correct, do not change.
- l.445-465 `<ScribeResultPanel resultText={result.text} .../>` — the render to feed the RAW fragment into.
- `selectedText` prop available (l.473, required) → `hasSelection = !!selectedText` if needed for any Replace gating in the card (NOTE: the popover footer already owns Replace; card MessageActions are optional/Discretion).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card-align the popover result render and feed it the RAW fragment</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx, src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx (lines 8-53 — the violet frame + RAW invariant to mirror)
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (lines 1-30 imports; 1177-1211 resultContent; 1268-1277 dev branch; 1279-1316 footer; 1326-1378 propTypes/defaultProps)
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (lines 240-291 normalization + chat mirror; 345-351 handleInsert/handleReplace; 445-465 ScribeResultPanel call site)
    - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx (confirm it accepts RAW marker string and cleans internally — the single cosmetic step)
    - .planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-PATTERNS.md (§ ScribeResultPanel — D-01/D-02/D-03)
  </read_first>
  <behavior>
    - Given a normalized RAW fragment containing markers (e.g. `{{REF:scribe-ref-1:Fig.1}}`, `[TABLE:1]`, `[^scribe-fn-1]`), the result panel renders it through MarkdownPreview inside a card with border color `#7C3AED` (SCRIBE_PURPLE), and the rendered DOM contains a violet-framed container — NOT the bare `scribe-result-text` div alone.
    - The component is passed the RAW fragment, NOT the cosmetic `displayMd`: assert the prop wired from ScribePopover is `rawResult` (markers intact). MarkdownPreview is the only place markers are cleaned.
    - `parsed.discussion` is never rendered as a visible element in the result panel (no discussion bubble/text node).
    - The dev-mode branch (`devData` truthy) is unchanged — card framing applies only to the non-dev `resultContent`.
    - The `cellWarning` banner still renders (when set) above the card.
    - Edge: empty/whitespace fragment → card frame still renders, no crash.
  </behavior>
  <action>
    In `ScribeResultPanel.jsx`: add a `rawFragment` prop (string) and align the non-dev `resultContent` (l.1179-1211) so the fragment renders inside a Scribe-violet card frame matching `FragmentCard` (border `1px solid` SCRIBE_PURPLE `#7C3AED`; bg `isDark ? 'rgba(124,58,237,0.12)' : SCRIBE_PURPLE_08`; borderRadius 8; padding '8px 10px'). Define or import `SCRIBE_PURPLE`/`SCRIBE_PURPLE_08` (Claude's Discretion: reuse `FragmentCard` directly as the card frame around `MarkdownPreview`, OR a thin popover-local wrapper — either way the violet frame + RAW invariant must hold; do NOT render `FragmentCard`'s inline `MessageActions` since the popover footer owns Insert/Replace). Feed the card body the RAW fragment: render `<MarkdownPreview>{rawFragment}</MarkdownPreview>` instead of `{resultText}` so MarkdownPreview performs the single cosmetic marker cleanup (matching chat byte-for-byte per D-02). Keep the `cellWarning` banner (l.1193-1206) above the card. Keep `error` handling, the dev-mode `ScribeDevPanels` branch (l.1268-1277), and the existing Insert/Replace/Retry footer (l.1279-1316) exactly as-is — these are popover-specific with no chat analog. Update propTypes/defaultProps for `rawFragment`.
    In `ScribePopover.jsx`: at the `<ScribeResultPanel ... />` call site (l.445-465), pass `rawFragment={rawResult}` (the RAW normalized fragment from l.259) for the cosmetic card render, while keeping `resultText={result.text}` only if still referenced by the dev/error paths. Do NOT alter `handleInsert`/`handleReplace` (l.345-351) — they already route on `rawResult` (RAW). Do NOT alter the normalization (l.240-261) or the `addMessage` chat mirror (l.264-291). Per D-03/INLINE-01, confirm no code path renders `parsed.discussion` in the popover.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/ScribeResultPanel --silent 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ScribeResultPanel.jsx` references `SCRIBE_PURPLE` (or `#7C3AED`) and renders the fragment inside a violet-bordered card frame (grep: `grep -v '^ *//' src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx | grep -c "7C3AED\|SCRIBE_PURPLE"` is ≥ 1).
    - The card body renders `rawFragment` through `MarkdownPreview` (not `resultText`): `ScribeResultPanel.jsx` contains `<MarkdownPreview>{rawFragment}</MarkdownPreview>` (or equivalent passing the raw prop).
    - `ScribePopover.jsx` passes `rawFragment={rawResult}` to `<ScribeResultPanel>`.
    - `ScribePopover.jsx` handleInsert/handleReplace still pass `rawResult` (unchanged): `grep -c "rawResult || result.text" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` is ≥ 2.
    - The dev-mode branch `devData ? <ScribeDevPanels` remains present and unmodified.
    - The `cellWarning` banner block remains present in `ScribeResultPanel.jsx`.
    - No new render of `parsed.discussion` in `ScribePopover.jsx` (the only `discussion` usage stays inside the `addMessage` mirror).
    - Targeted jest suite for ScribeResultPanel passes.
  </acceptance_criteria>
  <done>The popover result renders the RAW normalized fragment in a Scribe-violet card via MarkdownPreview; discussion is never shown; Insert/Replace/Retry still operate on the RAW fragment; dev branch and cellWarning preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate INLINE-02 inline→chat mirror end-to-end</name>
  <files>(none — validation only)</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (lines 264-291 — the addMessage user+assistant mirror; this is the v3.1-02 wiring being validated, NOT re-implemented)
    - .planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-CONTEXT.md (D-03 — "cette phase valide ce miroir de bout en bout sans le ré-implémenter")
    - .planning/REQUIREMENTS.md (INLINE-02 exact wording)
  </read_first>
  <action>This is a human verification checkpoint — no source edits. The executor pauses and the user validates the inline→chat mirror in the running app per the steps in <how-to-verify>.</action>
  <what-built>
    The popover result now renders the single fragment as a violet card without showing the discussion (Task 1). The inline→chat mirror (ScribePopover.jsx l.264-291) was already wired in v3.1-02: each inline exchange pushes a `user` turn (breadcrumb) and an `assistant` turn (content=normalizedFragment, with discussion/fragments/fellBack preserved) into shared chat history. This checkpoint validates that mirror end-to-end in the running app — it is a validation task per D-03, not a re-implementation.
  </what-built>
  <how-to-verify>
    1. Build Drive: `yarn build` (Drive `src/` changes require a build per project memory), then hard-reload the OO editor tab.
    2. Select some text in the OnlyOffice document, open the Scribe popover, run an inline action (e.g. "Corriger la grammaire") and let the result card appear.
    3. Confirm the popover shows ONLY the fragment card (violet frame, clean markers) — NO discussion text.
    4. Open the side panel (chat). Confirm the just-completed inline exchange appears in the chat history: a user turn (the breadcrumb/prompt) followed by an assistant turn rendered with the same fragment card(s) as chat-native turns.
    5. If the assistant turn carried a discussion, confirm the discussion shows in the CHAT (not the popover) and the fragment is in its card.
  </how-to-verify>
  <verify><human-check>Inline exchange appears in chat history; popover hides the discussion.</human-check></verify>
  <done>The inline exchange (prompt + discussion + fragment) is confirmed mirrored into the shared chat history and visible in the side panel; the popover never showed the discussion.</done>
  <resume-signal>Type "approved" if the inline exchange mirrors into the chat history and the popover hides the discussion; otherwise describe what is missing (e.g. no chat turn appeared, discussion leaked into popover).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → popover render | Untrusted model output (markdown + markers) is rendered as rich content in the popover card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-05-01 | Tampering (XSS) | `ScribeResultPanel` card body rendering LLM markdown via `MarkdownPreview` | mitigate | Reuse the EXISTING v3.1-04 `MarkdownPreview` sanitization path unchanged — do NOT introduce `dangerouslySetInnerHTML` or any new HTML-injection sink in the card. The card frame is a plain styled `<div>`; the only content sink is `MarkdownPreview`, which already sanitizes (same component the chat card uses). |
| T-v3.1-05-02 | Information disclosure | `parsed.discussion` leaking into the popover | mitigate | INLINE-01/D-03: the popover renders only `rawFragment`; assert no code path renders `parsed.discussion` in `ScribeResultPanel`/`ScribePopover` (acceptance criteria grep). |
| T-v3.1-05-03 | Tampering (marker corruption) | RAW fragment fed to Insert/Replace | mitigate | D-02 invariant: Insert/Replace/Copy route on the RAW fragment (`rawResult`), markers intact; cosmetic cleanup happens only inside `MarkdownPreview` and never mutates the RAW value passed to the footer. |
| T-v3.1-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (UI reuse only, zero new dependencies). |
</threat_model>

<verification>
- `ScribeResultPanel` renders the fragment in a `#7C3AED` card via `MarkdownPreview` fed the RAW fragment.
- `ScribePopover` passes `rawResult` to the card and still routes Insert/Replace on `rawResult`.
- No `parsed.discussion` rendered in the popover.
- Dev-mode branch and `cellWarning` banner preserved.
- INLINE-02 mirror validated live (checkpoint): inline exchange appears in chat history.
- Targeted jest suites green; no new sanitization path introduced (reuse MarkdownPreview).
</verification>

<success_criteria>
- INLINE-01: popover renders the single normalized fragment as a violet card (rich markdown + clean REF/TABLE/footnote markers), discussion never shown.
- INLINE-02: each inline exchange (prompt + discussion + fragment) appears in the shared chat history on opening the side panel (validated end-to-end).
- D-02 invariant held: Insert/Replace/Copy operate on the RAW fragment.
</success_criteria>

<output>
Create `.planning/phases/v3.1-05-rendu-popover-ui/v3.1-05-01-SUMMARY.md` when done.
</output>
