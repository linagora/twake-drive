---
phase: v3.1-05-rendu-popover-ui
verified: 2026-06-22T14:00:00Z
status: passed
score: 7/7 must-haves verified (automated); live UAT pending
overrides_applied: 0
human_verification:
  - test: "Inline-to-chat mirror (INLINE-02) — open the popover, run an action, confirm exchange appears in chat"
    expected: "Popover shows ONLY the violet fragment card (no discussion text); opening the side panel reveals a user turn (breadcrumb) followed by an assistant turn rendered with the same fragment card(s) as chat-native turns. If the response contained a discussion, it appears in the chat, not the popover."
    why_human: "addMessage wiring at ScribePopover.jsx l.264-291 is code-verified; the actual round-trip through the shared chat state and into ChatMessageList rendering requires a live OO session."
  - test: "Menu visual order and open-panel entry (MENU-01) — open the Scribe popover, inspect the menu list top-to-bottom"
    expected: "Order: Corriger la grammaire, Traduire ▸, Changer le ton ▸, Ameliorer ▸, free-prompt input row, 'Panneau Scribe' / 'Ouvrir le panneau lateral' at the bottom. The old separate prompt card and the small in-popover open-panel icon are gone. Clicking the bottom entry opens the side panel."
    why_human: "Rendering order and visual layout verified via jest assertions on data-attributes; actual list appearance requires a browser."
  - test: "Keyboard roving in the live app — actions -> prompt -> open-panel -> wrap"
    expected: "Pressing ArrowDown from the menu cycles through the 4 actions, into the prompt input, then to the open-panel entry, then wraps to the first action. From the prompt, ArrowDown moves to open-panel; ArrowUp from action 0 wraps to open-panel. Enter/Space on the open-panel entry opens the panel."
    why_human: "Roving logic (rovingCount, handlePromptArrow 'down' -> OPEN_PANEL_INDEX) is unit-tested; live tab-stop/focus behavior in the OO iframe context requires human confirmation."
  - test: "Conserved floating document button (D-07)"
    expected: "The separate floating Scribe PanelIcon button on the OO document (outside the popover) still appears and still opens/closes the side panel."
    why_human: "ScribeFloatingButton.jsx was confirmed unmodified by git diff; visual presence and functionality require a live OO session."
---

# Phase v3.1-05: Rendu popover (UI) — Verification Report

**Phase Goal:** Le popover inline rend le fragment unique pour Inserer/Remplacer en reutilisant le rendu carte de v3.1-04 (MarkdownPreview + marqueurs propres), sans afficher la `discussion` ; le menu d'actions est refondu (prompt libre integre comme entree de liste, acces au panneau lateral deplace dans le menu).
**Verified:** 2026-06-22T14:00:00Z
**Status:** PASS-pending-UAT
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | D-01: ScribeResultPanel renders the fragment inside a SCRIBE_PURPLE (#7C3AED) card frame matching the chat FragmentCard | VERIFIED | `data-scribe-result-card` div with `border: 1px solid SCRIBE_PURPLE` and dark-aware bg at ScribeResultPanel.jsx l.1223-1234. 4 non-comment references to SCRIBE_PURPLE/7C3AED. Jest test confirms card renders with correct hex color. |
| 2 | D-02: MarkdownPreview is fed the RAW fragment (rawFragment / rawResult); Insert/Replace still route on rawResult | VERIFIED | ScribeResultPanel.jsx l.1233 renders `<MarkdownPreview>{rawFragment}</MarkdownPreview>`. ScribePopover.jsx l.449 passes `rawFragment={rawResult}`. handleInsert/handleReplace at l.346/350 still use `rawResult || result.text` (2 occurrences). |
| 3 | D-03 / INLINE-01: parsed.discussion is never rendered in the popover | VERIFIED | All `discussion` references in ScribeResultPanel.jsx are inside: (a) the dev-mode ScribeDevPanels branch (l.751, gated by `devData` at l.1295), (b) propTypes declaration (l.1376 — declaration only, not rendered), (c) dev panel label strings. In ScribePopover.jsx, discussion is only used to build normalizedFragment (l.247) and to populate the addMessage chat mirror (l.286) — never passed to a render output. |
| 4 | INLINE-02 wiring present: addMessage mirror (ScribePopover.jsx l.264-291) intact | VERIFIED (wiring) | ScribePopover.jsx l.264-291 unchanged: pushes user turn (breadcrumb) + assistant turn (content=normalizedFragment, discussion/fragments/fellBack preserved) into shared chat history via `addMessage`. Wiring is structurally correct. Live end-to-end behavior deferred to human UAT. |
| 5 | D-04: menu order = 4 LLM actions -> integrated free-prompt entry -> bottom open-panel entry | VERIFIED | ScribeActionMenu.jsx: SCRIBE_ACTIONS provides 4 LLM actions; PROMPT_INDEX = actions.length places the ScribePromptInput after them (l.80, l.660-677); OPEN_PANEL_INDEX = PROMPT_INDEX+1 places the open-panel ListItem last (l.82, l.679-693). Jest spec asserts DOM order via compareDocumentPosition. |
| 6 | D-06: standalone in-popover open-panel `<button>` removed; new ListItem calls onOpenPanel with Scribe.button.open_panel i18n key | VERIFIED | No `<button` element wrapping onOpenPanel exists in ScribeActionMenu.jsx. New `<ListItem data-scribe-open-panel>` at l.679-693 calls `onClick={onOpenPanel}` and uses `t('Scribe.button.open_panel')` as ListItemText primary. OPEN_PANEL_ENTRY in scribeActions.js (l.243-246) has labelKey: 'Scribe.button.open_panel' and is NOT a SCRIBE_ACTIONS member. Jest spec confirms: `onOpenPanel` called, `onSelect` not called. |
| 7 | D-05: keyboard roving extended — rovingCount = actionCount+2; handlePromptArrow 'down' -> OPEN_PANEL_INDEX | VERIFIED | rovingCount = actions.length + 1 + (hasOpenPanel ? 1 : 0) at l.84. Main-list ArrowDown/Up uses `% rovingCount` at l.395/399. handlePromptArrow 'down' calls `updateFocus(hasOpenPanel ? OPEN_PANEL_INDEX : 0)` at l.167. Enter/Space on OPEN_PANEL_INDEX calls onOpenPanel at l.413-415. Jest spec (9 tests) exercises all three roving paths. |

**Score:** 7/7 must-haves verified (automated code checks)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` | Violet card frame + rawFragment via MarkdownPreview, cellWarning + dev-mode branch preserved | VERIFIED | SCRIBE_PURPLE defined at l.33-34; data-scribe-result-card at l.1224; MarkdownPreview fed rawFragment at l.1233; cellWarning banner at l.1199; devData branch at l.1295; Insert/Replace/Retry footer at l.1309-1343. |
| `src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` | Passes rawFragment={rawResult} to ScribeResultPanel; handleInsert/handleReplace unchanged | VERIFIED | l.449 `rawFragment={rawResult}`; l.346/350 `rawResult || result.text`. No modifications from plan-02 (git diff confirms 0 lines changed). |
| `src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.jsx` | Integrated prompt entry + open-panel ListItem + removed standalone button + extended roving | VERIFIED | data-scribe-prompt-entry at l.661; data-scribe-open-panel ListItem at l.680-693; no standalone `<button>` for open-panel; rovingCount at l.84; OPEN_PANEL_INDEX at l.82. |
| `src/modules/views/OnlyOffice/Scribe/scribeActions.js` | OPEN_PANEL_ENTRY constant; NOT a SCRIBE_ACTIONS member | VERIFIED | OPEN_PANEL_ENTRY exported at l.243-246 with id 'open-panel', labelKey 'Scribe.button.open_panel'. Not in SCRIBE_ACTIONS array (l.83-219). Explicit comment at l.234-240 documenting the no-pipeline-leak boundary. |
| `src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.spec.jsx` | 6 TDD tests covering card frame, RAW display, footer, cellWarning, error | VERIFIED | File exists. All 6 tests pass (jest run: 2026-06-22). |
| `src/modules/views/OnlyOffice/Scribe/ScribeActionMenu.spec.jsx` | 9 tests covering render order, click routing, pipeline-leak guard, free-prompt id, roving | VERIFIED | File exists. All 9 tests pass (jest run: 2026-06-22). |
| `src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx` | Unmodified (D-07) | VERIFIED | git diff 8ec0b1a0e^1 8ec0b1a0e -- ScribeFloatingButton.jsx = 0 lines changed. File present and untouched. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ScribePopover.jsx | ScribeResultPanel.jsx (rawFragment) | `rawFragment={rawResult}` at l.449 | WIRED | rawResult set by normalization at l.259; passed to card for cosmetic display only. |
| ScribeResultPanel.jsx | MarkdownPreview | `<MarkdownPreview>{rawFragment}</MarkdownPreview>` at l.1233 | WIRED | Single cosmetic marker cleanup step; does not mutate rawFragment. |
| ScribeActionMenu.jsx | onOpenPanel callback | `onClick={onOpenPanel}` on ListItem at l.685 | WIRED | ListItem-based; verified to not call onSelect (T-v3.1-05-04 guard holds). |
| ScribeActionMenu.jsx | Scribe.button.open_panel i18n key | ListItemText primary at l.691 | WIRED | Reuses existing key; no new i18n key introduced. |
| ScribePopover.jsx (addMessage) | Shared chat history (INLINE-02) | addMessage at l.270-291 with discussion/fragments/fellBack | WIRED (code) | Structural wiring confirmed; end-to-end requires live UAT. |
| scribeActions.js OPEN_PANEL_ENTRY | ScribeActionMenu (injected at render) | Import at ScribeActionMenu.jsx l.17; NOT in actions[] useMemo | WIRED | Injected like DEV_MD_ACTION; confirmed not a SCRIBE_ACTIONS member. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| ScribeResultPanel.jsx (card body) | rawFragment | ScribePopover rawResult <- normalization of parsed.fragments at l.240-250 | Yes — live LLM response data after normalization | FLOWING |
| ScribeResultPanel.jsx (footer) | onInsert/onReplace | handleInsert/handleReplace in ScribePopover using rawResult | Yes — same rawResult (RAW, markers intact) | FLOWING |
| ScribeActionMenu.jsx (open-panel entry) | onOpenPanel | Callback threaded from ScribePopover at l.423 (pre-existing wiring) | N/A — event callback, not data | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Jest suites for ScribeResultPanel + ScribeActionMenu | `node_modules/.bin/jest ScribeResultPanel ScribeActionMenu --silent` | 2 suites PASS, 15/15 tests | PASS |
| SCRIBE_PURPLE referenced in ScribeResultPanel | `grep -v '^ *//' ScribeResultPanel.jsx \| grep -c '7C3AED\|SCRIBE_PURPLE'` | 4 (>= 1) | PASS |
| rawResult \|\| result.text in ScribePopover | `grep -c 'rawResult \|\| result.text' ScribePopover.jsx` | 2 (>= 2) | PASS |
| rawFragment={rawResult} prop pass | `grep -n 'rawFragment={rawResult}' ScribePopover.jsx` | l.449, 1 match | PASS |
| free-prompt id preserved | `grep -c "'free-prompt'" ScribeActionMenu.jsx` | 1 (l.114) | PASS |
| No standalone `<button>` wrapping onOpenPanel | `grep -n '<button' ScribeActionMenu.jsx` | 0 results | PASS |
| ScribeFloatingButton.jsx unmodified | `git diff 8ec0b1a0e^1 8ec0b1a0e -- ScribeFloatingButton.jsx \| wc -l` | 0 lines | PASS |
| ScribePopover.jsx unmodified by plan 02 | `git diff 8ec0b1a0e^1 8ec0b1a0e -- ScribePopover.jsx \| wc -l` | 0 lines | PASS |
| OPEN_PANEL_ENTRY not in SCRIBE_ACTIONS | scribeActions.js l.83-219 SCRIBE_ACTIONS array; OPEN_PANEL_ENTRY at l.243 outside | Separate export, confirmed | PASS |

### Probe Execution

No probe scripts declared or discovered for this phase. Step 7c SKIPPED (UI-only phase, no runnable probe-*.sh).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| INLINE-01 | v3.1-05-01 | Popover shows only the fragment (not discussion), rendered as a violet card with MarkdownPreview | SATISFIED | ScribeResultPanel renders rawFragment via MarkdownPreview in SCRIBE_PURPLE card; discussion never passed to any render path. |
| INLINE-02 | v3.1-05-01 | Each inline exchange mirrored into shared chat history | SATISFIED (wiring) / PENDING (live UAT) | addMessage at ScribePopover.jsx l.264-291 is intact; live confirmation is the human UAT item. |
| MENU-01 | v3.1-05-02 | Free-prompt as integrated list entry; open-panel entry follows at bottom, replacing removed icon; floating button conserved | SATISFIED (code) / PENDING (live UAT) | ScribeActionMenu.jsx shows the correct structure; live visual + keyboard confirmation is the human UAT item. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| ScribeResultPanel.jsx | 307 | `dangerouslySetInnerHTML={{ __html: html }}` | INFO | Pre-existing dev-mode only (inside ScribeDevPanels / `<pre class="hljs">` highlight). Added by v3.1 dev panels, not by this phase (confirmed by git show e940527f1 commit message: "no new HTML-injection sink"). Not in the result card render path. |

No TBD/FIXME/XXX markers found in modified files. No new dangerouslySetInnerHTML introduced by this phase. No orphaned state or unused variables (the auto-fix in plan 02 removed the now-unused `actionCount` local and `hoveredPanelBtn` state — correctly handled in commit 8ec0b1a0e).

### Human Verification Required

The following live UAT steps were deferred per `workflow.human_verify_mode = end-of-phase`. Both plan summaries explicitly document them as pending, not as failures.

---

#### 1. INLINE-02: Inline exchange appears in chat history; popover hides discussion (Plan 01 Task 2)

**Test:**
1. Build Drive: `yarn build`, then hard-reload the OO editor tab.
2. Select some text in the OnlyOffice document, open the Scribe popover, run an inline action (e.g. "Corriger la grammaire") and let the result card appear.
3. Confirm the popover shows ONLY the fragment card (violet frame, clean markers) — NO discussion text.
4. Open the side panel (chat). Confirm the just-completed inline exchange appears in the chat history: a user turn (the breadcrumb/prompt) followed by an assistant turn rendered with the same fragment card(s) as chat-native turns.
5. If the assistant turn carried a discussion, confirm the discussion shows in the CHAT (not the popover) and the fragment is in its card.

**Expected:** Inline exchange mirrors into chat history; popover hides the discussion.
**Resume signal:** Type "approved" if the inline exchange mirrors into the chat history and the popover hides the discussion; otherwise describe what is missing (e.g. no chat turn appeared, discussion leaked into popover).
**Why human:** addMessage wiring at ScribePopover.jsx l.264-291 is code-verified; the actual round-trip through the shared chat state and into ChatMessageList rendering requires a live OO session.

---

#### 2. Menu order, integrated prompt, open-panel entry, keyboard roving (Plan 02 Task 2)

**Test:**
1. Build Drive: `yarn build`, then hard-reload the OO editor tab.
2. Select text and open the Scribe popover. Confirm the menu order top-to-bottom: Corriger la grammaire, Traduire ▸, Changer le ton ▸, Ameliorer ▸, the free-prompt input entry, then "Panneau Scribe" / "Ouvrir le panneau lateral" at the bottom.
3. Confirm the old separate prompt card and the small floating open-panel icon inside the popover are gone (folded into the menu).
4. Click the "Ouvrir le panneau lateral" entry -> the side panel opens (same behavior as the old icon).
5. Keyboard: with the menu focused, press ArrowDown repeatedly — focus should cycle through the actions, into the prompt input, then to the open-panel entry, then wrap. From the prompt, ArrowDown moves to the open-panel entry; ArrowUp returns to the prompt. Enter/Space on the open-panel entry opens the panel.
6. Confirm the separate floating Scribe panel button ON THE DOCUMENT (outside the popover) still appears and still works (D-07 conserved).

**Expected:** Menu order, integrated prompt, open-panel entry, roving, and the conserved floating document button all behave as described.
**Resume signal:** Type "approved" if the menu order, open-panel entry, roving, and the conserved floating button all behave as described; otherwise describe the discrepancy.
**Why human:** Roving logic unit-tested; visual list order and live keyboard focus behavior in the OO iframe context require human confirmation.

---

### Gaps Summary

No gaps. All 7 must-haves are verified against the merged codebase on `feat/scribe-in-right-panel`. The 2 human verification items are expected pending-UAT items (documented in both SUMMARY files, arising from `workflow.human_verify_mode = end-of-phase`), not failures.

---

## Commits Verified

| Commit | Description | Plans |
|--------|-------------|-------|
| `e940527f1` | feat(v3.1-05-01): render popover result as a Scribe-violet fragment card | Plan 01 Task 1 |
| `8ec0b1a0e` | feat(v3.1-05-02): integrate free-prompt + open-panel menu entries | Plan 02 Task 1 |

Both commits are present in git log on `feat/scribe-in-right-panel` (verified 2026-06-22).

---

_Verified: 2026-06-22T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
