---
phase: v3.1-04-rendu-chat-cartes-clavier
plan: 02
subsystem: Scribe chat rendering (MarkdownPreview cosmetic preprocessor)
tags: [markdown-preview, marker-transform, react-markdown, rehype-raw, tdd]
requires:
  - "transformCellMarkersForPreview (src/modules/views/OnlyOffice/Scribe/tableCellMarkers.js)"
provides:
  - "Cosmetic render-time D-02 marker transforms in MarkdownPreview: REF→text, TABLE/CELL→table, footnote→sup, IMG kept"
affects:
  - "Plan 03 (FragmentCard) and Plan 04 (prose segments) render clean content via MarkdownPreview"
  - "ScribePopover result path (already pre-transforms tables; idempotency gate keeps it safe — no change)"
tech-stack:
  added: []
  patterns:
    - "Render-time-derived string transform (never mutate caller source) — D-03 invariant"
    - "Idempotency gate via includes('[TABLE:'|'[CELL:') before table conversion"
    - "Fixed raw-HTML shell (<sup>) around captured \\d+ only — no model substring in tag/attr"
key-files:
  created:
    - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.spec.jsx
  modified:
    - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx
decisions:
  - "Embed all D-02 transforms inside MarkdownPreview's existing string preprocess stage (not a new module) — extends the same IMG step, keeps rehypeRaw/remarkGfm and component overrides untouched"
  - "Table step gated on raw marker presence so already-converted pipe-tables (popover path) are idempotent"
metrics:
  duration: ~15m
  completed: 2026-06-19
  tasks: 3
  files: 2
---

# Phase v3.1-04 Plan 02: MarkdownPreview D-02 Cosmetic Marker Transforms Summary

Extended `MarkdownPreview`'s existing string preprocess stage to cosmetically render the three
remaining plugin markers (REF→visible text, TABLE/CELL→GFM pipe-table, footnote→discreet `<sup>`)
on top of the existing IMG step, as a render-time-derived transform that never mutates the caller's
raw fragment (D-03).

## What Was Built

- **`MarkdownPreview.jsx`** — replaced the single-step IMG-only `preprocessed` computation with a
  multi-step cosmetic transform applied only when `children` is a string:
  1. **Tables** (multiline-first): when `out.includes('[TABLE:') || out.includes('[CELL:')`, run
     `transformCellMarkersForPreview(out, '').displayMd`. Gated so already-converted pipe-tables pass
     through untouched (idempotent).
  2. **REF** → visible text: `/\{\{REF:scribe-ref-\d+:([^}]*)\}\}/g → '$1'`.
  3. **IMG** (kept): `/\{\{IMG:(scribe-img-\d+)\}\}/g → '![IMG:$1](placeholder)'`.
  4. **Footnote** → discreet sup: `/\[\^scribe-fn-(\d+)\]/g → '<sup style="opacity:0.5">$1</sup>'`
     (rehypeRaw renders the fixed shell; captured group is digit-only).
  The result is a NEW local string — `children` is never reassigned/mutated. `rehypeRaw`/`remarkGfm`
  and the `img`/`table`/`th`/`td`/etc. component overrides are unchanged.
- **`MarkdownPreview.spec.jsx`** (new, RTL) — 9 tests covering REF→text (single + multiple),
  footnote→`<sup>`, TABLE/CELL→`<table>`, idempotency (existing pipe-table rendered once),
  GATE §8 passthrough (unclosed REF/footnote markers never throw), and the D-03 no-mutation contract.
  Mocks `cozy-ui/transpiled/react/styles` `useTheme` to a minimal light theme, mirroring
  `ScribeContainer.spec.jsx`.

## TDD Gate Compliance

- **RED** committed first (`2f7e4880f`, `test(...)`): 5 of 9 assertions failed (REF, footnote, table,
  no-mutation DOM-diff) because the prior preprocessor handled only IMG. The IMG-agnostic idempotency
  and passthrough tests passed pre-implementation, as expected.
- **GREEN** committed after (`8d667918c`, `feat(...)`): 9/9 pass.
- Gate sequence in git log: `test(...)` → `feat(...)` present and ordered. No REFACTOR needed.

## Task 3 — Popover double-transform confirmation (no code change)

`ScribePopover.jsx:180` **does** pre-transform tables: it calls
`transformCellMarkersForPreview(normalizedFragment, enrichedMd)` and passes the resulting `displayMd`
(an already-converted GFM pipe-table with no `[TABLE:`/`[CELL:` markers remaining) to `MarkdownPreview`
via `setResult({ text: displayMd, ... })`. The new idempotency gate (`includes('[TABLE:'|'[CELL:')`)
skips the table step for such input, so there is **no double-transform**. Verified by reading the call
site plus the green idempotency regression assertion in the spec. No edits made to `ScribePopover.jsx`
or `ScribeResultPanel.jsx` (popover render is v3.1-05).

## Deviations from Plan

None — plan executed exactly as written. Task 3 required no new code (the idempotency assertion was
already part of the Task 1 spec); it is documented above rather than producing a separate commit.

## Security — pre-existing rehype-raw XSS exposure (flagged for human, deferred to v3.1-05)

Per threat T-v3.1-04-04: `rehypeRaw` is **already enabled** today (`MarkdownPreview.jsx`, unchanged at
the `<Markdown rehypePlugins={[rehypeRaw]}>` call) and renders model-influenceable raw HTML on the
popover path. This plan does **not** introduce it; it only routes chat fragments through the same
preview. The new footnote step emits a **fixed** `<sup style="opacity:0.5">` shell around a captured
`\d+` only — **no model-controlled substring is interpolated into any HTML tag or attribute**, and the
REF visible text is placed as plain markdown text (not raw HTML). Existing escaping is not regressed.

**Recommendation (do not auto-resolve):** whether to add a `rehype-sanitize` pass is an out-of-scope
hardening decision deferred to **v3.1-05** (the dedicated hardening phase per ROADMAP / RESEARCH
Security Domain). Surfaced here for human confirmation of the deferral.

T-v3.1-04-05 (ReDoS) mitigated: both new regexes are linear with bounded classes
(`\{\{REF:scribe-ref-\d+:([^}]*)\}\}`, `\[\^scribe-fn-(\d+)\]`) — no nested quantifiers.
T-v3.1-04-06 (double table transform) mitigated by the `includes('[TABLE:'|'[CELL:')` gate.

## Known Stubs

None.

## Verification

`env NODE_ENV='test' npx jest src/modules/views/OnlyOffice/Scribe/MarkdownPreview.spec.jsx --silent`
→ **9 passed, 9 total** (1 suite passed).

## Self-Check: PASSED

- FOUND: src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx
- FOUND: src/modules/views/OnlyOffice/Scribe/MarkdownPreview.spec.jsx
- FOUND commit 2f7e4880f (RED), 8d667918c (GREEN)
