---
phase: v3.1-04-rendu-chat-cartes-clavier
plan: 03
type: execute
wave: 2
depends_on: [v3.1-04-02]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx
  - src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx
  - src/modules/views/OnlyOffice/Scribe/MessageActions.jsx
autonomous: true
requirements: [FRAG-02, FRAG-03, FRAG-04, KBD-02, KBD-04]
must_haves:
  truths:
    - "The card is bordered (fine border + light fill, Scribe purple #7C3AED) and renders the fragment as rich markdown via MarkdownPreview, not raw source, with no source toggle (D-01)"
    - "Each fragment card renders three actions: Copy, Insert, Replace, reusing the MessageActions logic per card (D-06)"
    - "Replace is hidden unless a document selection is active (hasSelection false → only Copy + Insert) (D-07)"
    - "Clicking Insert/Replace passes the RAW fragment string (markers intact) to panelActions, NOT the cleaned display string"
    - "Copy writes the RAW fragment to the clipboard (markdown+HTML), with the green-check confirmation"
    - "The focused card button shows a visible accent focus ring (D-09)"
    - "Card buttons are reachable/activatable so the keyboard controller can focus and Enter/Space-activate them"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx"
      provides: "Bordered Scribe-purple card: MarkdownPreview(raw cosmetic) + per-card Copy/Insert/Replace on raw fragment"
      exports: ["FragmentCard"]
    - path: "src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx"
      provides: "RTL coverage: 3 buttons, Replace gating, RAW-fragment-to-action spy (D-03/FRAG-03)"
    - path: "src/modules/views/OnlyOffice/Scribe/MessageActions.jsx"
      provides: "Reusable handlers + focus-ring affordance for the card variant"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (panelActions.insert/replace)"
      via: "MessageActions handlers → panelActions on the RAW fragment"
      pattern: "panelActions"
    - from: "src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx"
      via: "cosmetic display of the raw fragment"
      pattern: "MarkdownPreview"
---

<objective>
Create `<FragmentCard raw hasSelection>`: a bordered Scribe-purple card that displays the raw fragment
through the (Plan 02) extended `MarkdownPreview` (cosmetic cleanup happens inside the preview) and
carries its OWN Copy / Insert / Replace buttons that operate on the RAW fragment string (markers
intact). Reuse the existing `MessageActions` handler logic verbatim — it already takes a raw `content`
string and routes Copy → clipboard (markdown+HTML), Insert/Replace → `panelActions`, with a 1.5 s
green-check confirmation, and gates Replace on `hasSelection`. Add a visible accent focus ring (D-09)
and ensure the card's buttons are DOM-queryable focusable `<button>` elements so Plan 05's keyboard
controller can focus and activate them.

The governing invariant (D-03 / FRAG-03): the buttons MUST receive the RAW fragment, never the
cosmetically cleaned display string. The OO reinjection pipeline parses `{{REF:}}`/`[TABLE:]`/`[^scribe-fn-]`
to restore rich content; stripping them before insert silently breaks FRAG-03.

Purpose: FRAG-02 (3 buttons per card), FRAG-03 (rich reinjection per fragment), FRAG-04 (Replace
gated), and the focusable-button + focus-ring substrate for KBD-02/KBD-04.
Output: `FragmentCard.jsx` + `FragmentCard.spec.jsx` + light `MessageActions.jsx` edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-CONTEXT.md
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-RESEARCH.md
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md

<interfaces>
From src/modules/views/OnlyOffice/Scribe/MessageActions.jsx (existing — reuse, do not re-implement):
- default export MessageActions({ content, hasSelection }): renders Copy + Insert buttons always,
  Replace button only when hasSelection truthy. handleCopy uses markdownToHtml + stripParagraphWrapper
  + ClipboardItem (text/html + text/plain) with writeText fallback; handleInsert/handleReplace call
  panelActions.insert(content)/panelActions.replace(content); showConfirmation('copy'|'insert'|'replace')
  shows a green CheckIcon for 1500ms. Buttons use btnStyle (24x24, no outline today). aria-labels are
  the i18n keys Scribe.button.copy / .insert / .replace.

From src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx:
- useScribe() provides { panelActions: { insert, replace }, currentSelection, ... }. panelActions are
  wired by View.jsx to markdownToHtml + castPanelAction → OO plugin rich reinjection (FRAG-03, unchanged).

From src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx (extended in Plan 02):
- named export MarkdownPreview({ children }) — children is the RAW fragment string; the preview does
  cosmetic REF/TABLE/footnote/IMG cleanup INTERNALLY and never mutates the input.

Accent constants (ChatMessageList.jsx:13,98): SCRIBE_PURPLE='#7C3AED', SCRIBE_PURPLE_08='rgba(124, 58, 237, 0.08)'.
SelectionQuote (ChatMessageList.jsx:100-119) shows the bordered-surface idiom; FragmentCard wants a FULL
border per FRAG-01 "encadrée". getFocusables idiom: ScribeResultPanel.jsx:587-631 collects buttons via DOM query.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Light MessageActions edits — focus ring + DOM-queryable buttons</name>
  <files>src/modules/views/OnlyOffice/Scribe/MessageActions.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/MessageActions.jsx (full — handlers 56-86, btnStyle 88-100, render 111-145, hasSelection gate 133)
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (587-631 getFocusables/handleKeyDown — how the repo queries focusables from DOM)
    - .planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md (MessageActions.jsx section, lines 287-298)
  </read_first>
  <action>
    Make two minimal, backward-compatible edits to MessageActions so it doubles as the card action bar:
    1. Focus-ring affordance (D-09): give each action `<button>` a visible accent ring when focused.
       Define SCRIBE_PURPLE = '#7C3AED' locally (matches ChatMessageList). Implement via onFocus/onBlur
       inline-style handlers (`e.currentTarget.style.outline = '2px solid ' + SCRIBE_PURPLE` on focus,
       reset to 'none' on blur) so it works under the existing inline-style convention; add a
       `:focus-visible` fallback rule in scribe.styl if a className is used. Keep the existing hoverProps
       behavior intact.
    2. DOM-queryable buttons: leave the buttons as plain focusable `<button>` elements with aria-labels
       (already true) so a parent controller can collect them via `node.querySelectorAll('button')`.
       No prop API change required.
    Do NOT change handler logic, the hasSelection gate (line 133), or the 1500ms confirmation. The
    component file stays (D-08: only its message-level call site is removed later, in Plan 04).
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').readFileSync('src/modules/views/OnlyOffice/Scribe/MessageActions.jsx','utf8').includes('7C3AED')?0:1)" && echo OUTLINE-OK</automated>
  </verify>
  <acceptance_criteria>
    - MessageActions.jsx references SCRIBE_PURPLE / '#7C3AED' and sets a visible outline on focus.
    - Handler logic, hasSelection gate, and 1500ms confirmation are unchanged (grep shows panelActions.insert/replace and 1500 still present).
    - Buttons remain plain `<button>` with aria-labels (queryable by getByLabelText / querySelectorAll).
  </acceptance_criteria>
  <done>MessageActions shows an accent focus ring on the focused button; buttons DOM-queryable; handler behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing FragmentCard spec (RTL)</name>
  <files>src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContainer.spec.jsx (repo RTL idiom: jest.mock of cozy-ui + ScribeContext, render, queries)
    - src/modules/views/OnlyOffice/Scribe/MessageActions.jsx (handlers + aria-labels to query)
    - .planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md (FragmentCard.jsx section, lines 31-95)
  </read_first>
  <behavior>
    - Test (FRAG-02): rendering <FragmentCard raw="hello" hasSelection /> shows exactly three buttons (Copy, Insert, Replace) queryable by aria-label.
    - Test (FRAG-04): rendering with hasSelection={false} shows only Copy + Insert (no Replace button).
    - Test (FRAG-03 / D-03 — the critical guard): provide a raw fragment containing markers, e.g.
      'See {{REF:scribe-ref-1:Fig 1}} and [^scribe-fn-2]'. Mock useScribe panelActions.insert as a jest spy.
      Click Insert → the spy is called with the EXACT raw string (markers intact: still contains
      '{{REF:scribe-ref-1:Fig 1}}' and '[^scribe-fn-2]'), NOT a cleaned/stripped variant.
    - Test (FRAG-03 / D-03 replace): with hasSelection, click Replace → panelActions.replace spy called with the exact raw string.
    - Test (D-01 framing): the card root has a border + Scribe-purple accent (assert inline style border contains '7C3AED' or a className applied).
    - Test (display vs raw): the rendered card shows the cosmetic display (e.g. 'Fig 1' visible text from the REF) while the action spy still received the raw marker — proving the display/raw split.
  </behavior>
  <action>
    Create `FragmentCard.spec.jsx` with @testing-library/react. Mock `cozy-ui/transpiled/react/styles`
    (useTheme) and `cozy-ui/transpiled/react/Tooltip`, and mock the `useScribe` hook (or wrap in a
    ScribeContext provider stub) to supply `panelActions: { insert: jest.fn(), replace: jest.fn() }`.
    Mock `MarkdownPreview` to a passthrough that renders its children text if real-rendering complicates
    queries — but ensure the display-vs-raw test still validates the split (assert the spy arg, not the
    DOM, for raw-ness). Cover every behavior above. Run and confirm RED (FragmentCard does not exist).
  </action>
  <verify>
    <automated>yarn test src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx 2>&1 | grep -qiE "cannot find module|fail|✕" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Spec imports FragmentCard from './FragmentCard' and fails RED before implementation.
    - Contains the D-03 spy assertion that panelActions.insert receives the verbatim raw (marker substrings present).
    - Contains FRAG-02 (3 buttons) and FRAG-04 (Replace gated) assertions.
  </acceptance_criteria>
  <done>Failing RTL spec covering FRAG-02, FRAG-03/D-03 raw-arg spy, FRAG-04, D-01 framing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement FragmentCard (GREEN)</name>
  <files>src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx (Task 2 spec)
    - src/modules/views/OnlyOffice/Scribe/MessageActions.jsx (the handlers/component to embed)
    - src/modules/views/OnlyOffice/Scribe/MarkdownPreview.jsx (named export to render the raw cosmetically)
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx (SelectionQuote 100-119 for the bordered-surface idiom; SCRIBE_PURPLE/_08 constants 13,98)
  </read_first>
  <action>
    Implement and export (named) `FragmentCard({ raw, hasSelection })`. Render a bordered card div:
    `border: '1px solid ' + SCRIBE_PURPLE`, `background: SCRIBE_PURPLE_08` (or 0.04), `borderRadius: 8`,
    `padding: '8px 10px'` (radii/spacing are Claude's discretion per D-01; theme-aware via useTheme like
    AssistantBubble). Inside, render `<MarkdownPreview>{raw}</MarkdownPreview>` (the preview does the
    cosmetic REF/TABLE/footnote/IMG cleanup INTERNALLY — pass the RAW fragment, never a pre-cleaned
    string). Below the preview, render `<MessageActions content={raw} hasSelection={hasSelection} />`
    (Approach (a) from PATTERNS: embed MessageActions directly — zero new handler code; it already
    operates on raw `content` and emits the 3 gated buttons with the focus ring from Task 1). Do NOT
    pre-clean `raw` before passing to either the preview or the actions (D-03). Keep the module a simple
    functional component. Run the spec; iterate to GREEN.
  </action>
  <verify>
    <automated>yarn test src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx` passes (all green incl. the D-03 raw-arg spy).
    - `grep -nE "MessageActions content=\{raw\}|content=\{raw\}" src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx` — actions receive raw.
    - `grep -n "MarkdownPreview" src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx` — display via MarkdownPreview on raw.
    - No string-cleaning of `raw` in FragmentCard.jsx before passing to actions/preview (grep shows no `.replace(/\{\{REF` or `.replace(/\[\^scribe-fn` in this file).
  </acceptance_criteria>
  <done>FragmentCard renders a bordered card showing cosmetic markdown of raw + 3 gated raw-driven actions; D-03 split proven by spec spy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Card action → OO document | Insert/Replace cross the panelActions bridge into the OO editor. The string crossing must be the raw fragment (markers drive rich reinjection). |
| LLM fragment → clipboard/DOM | Copy writes LLM content to the OS clipboard; MarkdownPreview renders it (rehypeRaw). Model-influenceable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-04-07 | Tampering | cleaned display string accidentally sent to panelActions/clipboard (D-03 violation) | mitigate | FragmentCard passes `raw` to BOTH MessageActions and MarkdownPreview; cosmetic cleanup is internal to the preview only. The FRAG-03/D-03 spec spy asserts the action receives the verbatim raw (marker substrings present) — a regression turns the test red. |
| T-v3.1-04-08 | Tampering / Elevation (XSS) | MarkdownPreview rehypeRaw renders model HTML inside the card | accept (pre-existing) | Same exposure as Plan 02 T-v3.1-04-04 — pre-existing, deferred to v3.1-05. FragmentCard adds no new sink; it reuses MarkdownPreview unchanged. |
| T-v3.1-04-09 | Information Disclosure | Copy places fragment on OS clipboard | accept | User-initiated action on already-displayed content; no new secret exposure. |

No package installs in this plan → no T-{phase}-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `yarn test src/modules/views/OnlyOffice/Scribe/FragmentCard.spec.jsx` green.
- FragmentCard: bordered Scribe-purple card; MarkdownPreview(raw) for display; MessageActions(content=raw) for actions.
- D-03 enforced: action spy receives verbatim raw with markers intact; no marker-cleaning in FragmentCard.
- MessageActions shows a focus ring; buttons DOM-queryable.
</verification>

<success_criteria>
- FRAG-02/D-06: each card renders Copy/Insert/Replace, reusing MessageActions per card (clipboard markdown+HTML, panelActions.insert/replace, green-check 1.5s) acting on its own raw fragment.
- FRAG-04/D-07: Replace hidden without selection (hasSelection/currentSelection gating).
- FRAG-03/D-03: insert/replace/copy operate on the RAW fragment (markers intact).
- D-01: card is bordered (fine border + light fill, Scribe purple #7C3AED) with rich-markdown fragment render via MarkdownPreview (no raw source, no source toggle).
- D-09: focused button shows accent ring (substrate for KBD-02/KBD-04 in Plan 05).
</success_criteria>

<output>
Create `.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-03-SUMMARY.md` when done.
</output>
