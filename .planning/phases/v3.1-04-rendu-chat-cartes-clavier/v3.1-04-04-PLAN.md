---
phase: v3.1-04-rendu-chat-cartes-clavier
plan: 04
type: execute
wave: 3
depends_on: [v3.1-04-01, v3.1-04-03]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx
  - src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx
autonomous: true
requirements: [CONTRACT-02, FRAG-01, FRAG-03]
must_haves:
  truths:
    - "An assistant message renders one bubble: discussion prose interleaved with fragment cards at their {{fragment:N}} positions"
    - "Orphan (unreferenced) fragments render as cards at the end of the bubble (D-04)"
    - "A 0-fragment assistant response renders the discussion only — no card, no insertion UI (CONTRACT-02/D-05)"
    - "A 0-fragment + empty-discussion message renders nothing (no empty bubble)"
    - "The message-level MessageActions call is removed for assistant messages (D-08); actions live only on cards"
    - "Legacy messages without discussion/fragments still render as a single prose bubble (backward-compat)"
    - "composeAssistantDisplay is deleted"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx"
      provides: "Segment-based assistant render: prose via MarkdownPreview, cards via FragmentCard; composeAssistantDisplay removed; message-level MessageActions removed"
      contains: "buildAssistantSegments"
    - path: "src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx"
      provides: "RTL coverage for CONTRACT-02, FRAG-01 placement/orphans, no-empty-bubble, backward-compat, D-08"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/assistantSegments.js"
      via: "buildAssistantSegments(discussion, fragments)"
      pattern: "buildAssistantSegments"
    - from: "src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/FragmentCard.jsx"
      via: "render card segments as FragmentCard"
      pattern: "FragmentCard"
---

<objective>
Rewrite the assistant-message render branch of `ChatMessageList` to use the segment model. Delete the
throwaway `composeAssistantDisplay` (its own JSDoc says delete in v3.1-04). For each contract assistant
message, call `buildAssistantSegments(msg.discussion ?? msg.content, msg.fragments)` and render the
ordered segments inside a single assistant container: prose segment to `MarkdownPreview` (swap the bare
`Markdown` in AssistantBubble so prose gets the same cosmetic marker treatment + theme styling); card
segment to `FragmentCard raw=segment.raw hasSelection`. Remove the message-level `MessageActions` for
assistant messages (D-08 — actions now live only on cards). Keep the `hasContractFields` backward-compat
path (legacy messages render as a single prose bubble, no cards) and the "no empty bubble" discipline
(0 fragments + empty discussion render nothing).

Purpose: FRAG-01 (cards at marker positions, orphans at end), CONTRACT-02/D-05 (0-fragment to
discussion only, no UI), D-04 placement, D-08 (message-level actions removed). FRAG-03 is realized
end-to-end here because cards now carry the raw fragment to the reinjection pipeline.
Output: rewritten `ChatMessageList.jsx` + `ChatMessageList.render.spec.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-CONTEXT.md
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-RESEARCH.md
@.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md

<interfaces>
From assistantSegments.js (Plan 01): buildAssistantSegments(discussion, fragments) returns an ordered
array of prose/card segments. 0 fragments returns a single prose segment.
From FragmentCard.jsx (Plan 03): named export FragmentCard taking raw + hasSelection.
From MarkdownPreview.jsx (Plan 02): named export MarkdownPreview taking children (markdown string).

Current ChatMessageList assistant branch (lines 239-253): hasContractFields check; composeAssistantDisplay
(DELETE, lines 15-73); renders AssistantBubble + message-level MessageActions (REMOVE the actions line,
D-08). AssistantBubble (140-159) uses bare Markdown — swap to MarkdownPreview. extractChannelMarkers
import (line 11) becomes unused here — remove it. useScribe provides messages, isLoading, currentSelection.
Auto-scroll useEffect keys on messages.length (207-214) — leave unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing render spec for segment-based assistant render</name>
  <files>src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx (full — assistant branch 232-254, AssistantBubble 140-159)
    - src/modules/views/OnlyOffice/Scribe/ScribeContainer.spec.jsx (RTL idiom + how to mock useScribe / cozy-ui)
    - .planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md (ChatMessageList.jsx section, lines 152-219)
  </read_first>
  <behavior>
    - Test (FRAG-01 placement): message discussion 'A {{fragment:0}} B' with fragments ['FRAG0'] renders a card for FRAG0 between prose 'A' and 'B' (card queryable by its action buttons appears in document order between the two prose texts).
    - Test (D-04 orphans): discussion 'just prose' with fragments ['X','Y'] renders two cards after the prose.
    - Test (CONTRACT-02/D-05): discussion 'pure discussion' with fragments [] renders the discussion text and ZERO action buttons for that message and no FragmentCard.
    - Test (no empty bubble): discussion '' with fragments [] renders no assistant container.
    - Test (D-08): for any assistant message there is no message-level action bar separate from cards; a 0-fragment message shows no action buttons; a carded message's only action buttons are inside cards.
    - Test (backward-compat): legacy message with content 'old text' and no discussion/fragments renders 'old text' as a single prose bubble, no cards, no actions.
    - Test (user/error unchanged): a user message and an error message still render their existing bubbles.
  </behavior>
  <action>
    Create `ChatMessageList.render.spec.jsx` with @testing-library/react. Mock `useScribe` (from
    ScribeContext) to supply a controllable `messages` array, `isLoading:false`, and `currentSelection`
    (null in most tests; one test sets a selection to assert Replace appears inside cards). Mock cozy-ui
    useTheme + Tooltip + Spinner + Typography like ScribeContainer.spec.jsx. Let FragmentCard and
    MarkdownPreview render for real to prove integration; action handlers being no-ops in jsdom is fine
    for render assertions. Query action buttons by their aria-labels (Scribe.button.copy/insert/replace
    — the i18n mock can echo the key). Cover every behavior above. Run; confirm RED.
  </action>
  <verify>
    <automated>yarn test src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx</automated>
  </verify>
  <acceptance_criteria>
    - Spec renders ChatMessageList via a mocked useScribe with configurable messages.
    - Assertions present for FRAG-01 placement, D-04 orphans, CONTRACT-02 (no action UI on 0-fragment), no-empty-bubble, D-08, backward-compat.
    - Fails RED before implementation (segment render not present yet).
  </acceptance_criteria>
  <done>Failing render spec covering CONTRACT-02, FRAG-01, D-04, D-08, no-empty-bubble, backward-compat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite assistant render to segments; delete composeAssistantDisplay; remove message-level actions</name>
  <files>src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx (Task 1 spec)
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx (full)
    - .planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-PATTERNS.md (ChatMessageList.jsx section)
  </read_first>
  <action>
    Edit ChatMessageList.jsx:
    1. Delete `composeAssistantDisplay` (lines 15-73). Remove the now-unused `extractChannelMarkers`
       import (line 11) and the `MessageActions` import if no assistant render uses it (it is removed by
       D-08 — confirm no other use remains; if none, drop the import). Add imports: `buildAssistantSegments`
       from '@/modules/views/OnlyOffice/Scribe/assistantSegments', `FragmentCard` from
       '@/modules/views/OnlyOffice/Scribe/FragmentCard', `MarkdownPreview` from
       '@/modules/views/OnlyOffice/Scribe/MarkdownPreview'.
    2. In AssistantBubble (140-159) replace bare `Markdown` with `MarkdownPreview` for the content prose.
    3. Rewrite the assistant branch (239-253): keep the `hasContractFields` check
       (msg.discussion !== undefined && msg.fragments !== undefined). Legacy (non-contract) message:
       render a single AssistantBubble with msg.content, NO MessageActions (D-08). Contract message:
       compute segments via buildAssistantSegments(msg.discussion ?? msg.content, msg.fragments). If
       segments is a single prose segment whose md is empty/whitespace-only, render nothing for that
       message (CONTRACT-02 no-empty-bubble). Otherwise render ONE assistant container (alignSelf
       flex-start, maxWidth 85%, theme-aware surface matching AssistantBubble) mapping over segments:
       prose to MarkdownPreview with seg.md; card to FragmentCard with raw=seg.raw and
       hasSelection={!!currentSelection}. Use stable keys (msg.id plus segment position/index). DROP the
       message-level MessageActions for assistant role entirely (D-08).
    4. Leave user/error branches, WelcomeMessage, and the auto-scroll useEffect unchanged.
    Run the render spec to GREEN, then run the whole Scribe dir to catch sibling-spec fallout (note:
    the pre-existing red ScribeContainer "Drawer PaperProps fullscreen on mobile" test is UNRELATED —
    do not fix it here).
  </action>
  <verify>
    <automated>yarn test src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx` passes.
    - `grep -c composeAssistantDisplay src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx` returns 0.
    - `grep -n "buildAssistantSegments" src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx` shows the call.
    - `grep -n "FragmentCard" src/modules/views/OnlyOffice/Scribe/ChatMessageList.jsx` shows card rendering.
    - No `MessageActions` rendered in the assistant branch (grep shows it is not used for assistant role).
    - `yarn test src/modules/views/OnlyOffice/Scribe` is green except the documented pre-existing ScribeContainer mobile-drawer red.
  </acceptance_criteria>
  <done>Assistant messages render as prose + fragment cards; composeAssistantDisplay deleted; message-level actions removed; legacy + no-empty-bubble paths intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM message model → DOM | discussion + fragments rendered into the chat thread via MarkdownPreview/FragmentCard. Model-influenceable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-04-10 | Tampering / Elevation (XSS) | prose + card rendered via MarkdownPreview (rehypeRaw) | accept (pre-existing) | Same exposure as Plan 02 T-v3.1-04-04; swapping AssistantBubble's bare Markdown for MarkdownPreview keeps the existing rehypeRaw behavior (the popover already renders LLM markdown the same way). No new sink introduced; sanitize decision deferred to v3.1-05. |
| T-v3.1-04-11 | DoS | many fragments inflating render | accept | Fragment count bounded by upstream contract; React handles list render; no unbounded loop. |
| T-v3.1-04-12 | Tampering | empty/malformed message rendering a broken bubble | mitigate | CONTRACT-02 no-empty-bubble guard + backward-compat path covered by render spec. |

No package installs in this plan → no T-{phase}-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `yarn test src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx` green.
- composeAssistantDisplay deleted; buildAssistantSegments + FragmentCard wired; AssistantBubble uses MarkdownPreview.
- Message-level MessageActions removed for assistant role (D-08).
- Whole Scribe dir green except the documented unrelated pre-existing ScribeContainer mobile-drawer red.
</verification>

<success_criteria>
- FRAG-01: cards at {{fragment:N}} positions; orphans at end.
- CONTRACT-02/D-05: 0-fragment renders discussion only, no insertion UI; empty message renders nothing.
- D-08: actions only on cards.
- Backward-compat: legacy messages render as a single prose bubble.
</success_criteria>

<output>
Create `.planning/phases/v3.1-04-rendu-chat-cartes-clavier/v3.1-04-04-SUMMARY.md` when done.
</output>
