---
phase: v3.1-06-durcissement-contrat
verified: 2026-06-24T00:00:00Z
status: passed
score: 9/9
overrides_applied: 0
---

# Phase v3.1-06: Durcissement contrat — Verification Report

**Phase Goal:** Durcissement contrat — Re-ask sur parse invalide (1x avant repli), corpus de régression des réponses malformées, décision documentée du défaut `response_format` (sans UI OO).
**Verified:** 2026-06-24
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | On a parse-invalid first response (fellBack===true OR valid===false), the system issues exactly one corrective re-ask before applying the contextual fallback | VERIFIED | `callScribeAIWithReask` in scribeAI.js:343 — branches on `first.valid && !first.fellBack`; second call is unconditional (hard cap); spec tests confirm exactly 2 calls on fellBack and on valid===false triggers |
| 2 | A first response that parses cleanly (valid && !fellBack) issues NO second call | VERIFIED | scribeAI.js:351-353 early-returns `first` on `first.valid && !first.fellBack`; scribeAI.spec.js tests assert fetchJSON called exactly 1 time on clean response |
| 3 | Both chat (ScribeContext.jsx) and popover (ScribePopover.jsx) surfaces use the SAME shared re-ask implementation (no divergence) | VERIFIED | ScribeContext.jsx:198 `callScribeAIWithReask(client, aiMessages, { surface: 'chat' })`; ScribePopover.jsx:218 `callScribeAIWithReask(client, messages, { signal: controller.signal, surface: 'popover' })`; old two-step pattern (callScribeAI + parseScribeResponse) absent from both files |
| 4 | The re-ask reuses the caller's AbortController signal; aborting cancels the second call too | VERIFIED | scribeAI.js:348 and :359 both pass `{ signal }`; abort spec asserts AbortError propagates; ScribePopover.jsx:219 forwards `controller.signal` into the helper |
| 5 | After at most one re-ask, the final result is always a normalized parseScribeResponse object (re-ask never leaves the caller without a result) | VERIFIED | scribeAI.js:361 unconditionally returns `parseScribeResponse(secondText, { surface })`; spec asserts the second parse is returned even when second response is also invalid (never 3 calls) |
| 6 | A static malformed-response corpus drives deterministic Jest assertions over parseScribeResponse for all 6 D-07 categories | VERIFIED | scribeResponse.fixtures.js exports `SCRIBE_RESPONSE_FIXTURES` with keys `brokenRef,fence,nonJsonProse,preamble,splitTable,trailingComma` (confirmed by `node -e` key dump); scribeResponse.corpus.spec.js asserts all 6 categories are non-empty and verifies per-fixture fellBack/valid outcomes; 41/41 tests pass |
| 7 | Any model response whose ONLY defect is a wrapping code fence parses successfully (fellBack===false), not via fallback | VERIFIED | `fellBack rate` describe block in corpus spec asserts every `fence` fixture yields `fellBack===false`; includes verbatim real-reliquat sample from STATE.md (fixture name: "fence: VERBATIM real-reliquat sample observed in STATE.md") anchoring the 0% claim to real model output; 8/8 fence-rate tests pass |
| 8 | The ~2% code-fence fellBack reliquat is re-measured against the corpus and confirmed closed by existing stripFence | VERIFIED | Re-measured at 0% — existing `stripFence` (scribeResponse.js:88) handles all 3 required shapes (language-tag, trailing-whitespace, no-trailing-newline) plus the verbatim reliquat; confirming comment added to stripFence definition; no behavior change; ReDoS gate clean (grep -nE nested quantifiers returns 0 matches) |
| 9 | The response_format default decision (keep json_object, defer json_schema) is documented with the proxy-forwarding fact | VERIFIED | `RESPONSE_FORMAT_DECISION.md` exists; records: `json_object` default, `chat.go ~l.446-464` forwards body unchanged via `bytes.NewReader(payload)`, json_schema deferral is a model-capability question not a proxy block, SCRIBE_OUTPUT_SCHEMA is the ready payload; `grep -c "json_object"` returns 5; `grep -c "chat.go"` returns 1 |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/scribeAI.js` | callScribeAIWithReask + REASK_CORRECTION_NUDGE | VERIFIED | Both exported at lines 307 and 343; implementation is substantive (not a stub): 19-line function with documented trigger logic, non-mutating retryMessages, signal forwarding |
| `src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js` | Re-ask behavior tests (call-count, trigger, no-trigger, abort, surface fallback) | VERIFIED | 8 new tests under `describe('callScribeAIWithReask …')` cover clean/fellBack/valid===false/cap/abort/nudge-length/surface/nudge-content; 21/21 tests pass |
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` | Chat call site wired to callScribeAIWithReask | VERIFIED | Lines 15 (import) + 198 (await) + 206/235 (parsed.raw); no stale `responseText` (grep -c returns 0); no direct `callScribeAI(` call |
| `src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` | Popover call site wired to callScribeAIWithReask | VERIFIED | Lines 18 (import) + 218 (await with controller.signal) + 222 (setRawResponse(parsed.raw)); stale deferral comment removed |
| `src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js` | Static malformed-response fixtures, 6 D-07 categories | VERIFIED | Exports SCRIBE_RESPONSE_FIXTURES with exactly 6 keys; multiple fixtures per category; no gate-harness import |
| `src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js` | Deterministic regression assertions + fellBack rate re-measure | VERIFIED | describe.each loop over all 6 categories; splitTable invariants block; nonJsonProse surface-specific fallback block; `fellBack rate` describe block with verbatim real-reliquat check; 41/41 pass |
| `src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md` | response_format decision + proxy-forwarding fact | VERIFIED | Substantive document; covers json_object default, proxy fact (chat.go), json_schema deferral rationale, fence re-measure table with 5 shapes |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| ScribeContext.jsx | callScribeAIWithReask | import at line 15, await at line 198 | WIRED | `parsed` used throughout chat handler (discussion, fragments, fellBack, parsed.raw) |
| ScribePopover.jsx | callScribeAIWithReask | import at line 18, await at line 218 with controller.signal | WIRED | `parsed.raw` at line 222, parsed used by logScribeExchange, setParsedResponse |
| scribeResponse.corpus.spec.js | parseScribeResponse | import from scribeResponse.js at line 13 | WIRED | Every fixture run through parseScribeResponse, results asserted |
| scribeResponse.corpus.spec.js | SCRIBE_RESPONSE_FIXTURES | import from scribeResponse.fixtures.js at line 14 | WIRED | Used in describe.each, it.each loops across all 6 categories |

### Data-Flow Trace (Level 4)

Not applicable — this phase produces logic modules (a helper function and a test corpus), not UI components that render dynamic data from a remote source. The helper's data flow is: LLM response text → parseScribeResponse → normalized object returned to the caller. This is synchronous within the helper and verified by the call-count and output-shape specs.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| callScribeAIWithReask exported at correct line | `grep -n "export async function callScribeAIWithReask" scribeAI.js` | line 343: 1 match | PASS |
| REASK_CORRECTION_NUDGE exported | `grep -n "export const REASK_CORRECTION_NUDGE" scribeAI.js` | line 307: 1 match | PASS |
| callScribeAIWithReask count in ScribeContext.jsx | `grep -c "callScribeAIWithReask" ScribeContext.jsx` | 3 | PASS |
| callScribeAIWithReask count in ScribePopover.jsx | `grep -c "callScribeAIWithReask" ScribePopover.jsx` | 3 | PASS |
| No stale responseText in ScribeContext.jsx | `grep -c "responseText" ScribeContext.jsx` | 0 | PASS |
| Fixture keys match 6 D-07 categories | `node -e` key dump | `brokenRef,fence,nonJsonProse,preamble,splitTable,trailingComma` | PASS |
| Three spec files pass | `yarn jest scribeAI.spec.js corpus.spec.js scribeResponse.spec.js` | 100/100 tests pass | PASS |
| Full Scribe suite | `yarn jest src/modules/views/OnlyOffice/Scribe/` | 235/236 pass — 1 failure is the known pre-existing ScribeContainer Drawer PaperProps test, unrelated to this phase (ScribeContainer.jsx not touched: `git log d737cbc9d..HEAD -- ScribeContainer.jsx` returns empty) | PASS |
| ReDoS gate | `grep -nE nested quantifiers scribeResponse.js` | 0 matches | PASS |
| RESPONSE_FORMAT_DECISION.md content | `grep -c "json_object"` / `grep -c "chat.go"` | 5 / 1 | PASS |
| Commits exist | git log 79e2cdf5b b58af1d04 b9333ae3b 9f1c29151 | All 4 present on branch | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| HARDEN-01 | v3.1-06-01 | Sur une réponse au parse invalide, le système re-sollicite le LLM une seule fois avant d'appliquer le repli contextuel | SATISFIED | callScribeAIWithReask in scribeAI.js; both call sites wired; spec green (21/21) |
| HARDEN-02 | v3.1-06-02 | Corpus de régression de réponses malformées + tests de cas limites passe au vert ; taux de fellBack code-fence re-mesuré ; défaut response_format décidé et documenté | SATISFIED | scribeResponse.fixtures.js + corpus.spec.js (41/41 green); fence rate 0%; RESPONSE_FORMAT_DECISION.md |

Both requirements are accounted for with implementation evidence. REQUIREMENTS.md traceability table already reflects HARDEN-01 and HARDEN-02 as complete.

### Anti-Patterns Found

No anti-patterns found.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TBD/FIXME/XXX markers | — | — |
| — | — | No TODO/HACK/PLACEHOLDER comments | — | — |
| — | — | No stub patterns (return null / return [] / empty handlers) | — | — |

### Human Verification Required

None. All deliverables are deterministic code and tests — no visual rendering, real-time behavior, or external service integration that requires human testing.

### Gaps Summary

No gaps. All 9 observable truths are VERIFIED, all 7 required artifacts exist and are substantive and wired, all 4 key links are wired, both requirement IDs are satisfied, all behavioral spot-checks pass, and no anti-patterns are present. The phase goal is fully achieved.

---

_Verified: 2026-06-24T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
