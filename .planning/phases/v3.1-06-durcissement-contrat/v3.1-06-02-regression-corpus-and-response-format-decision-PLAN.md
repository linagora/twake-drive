---
phase: v3.1-06-durcissement-contrat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js
  - src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js
  - src/modules/views/OnlyOffice/Scribe/scribeResponse.js
  - src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md
autonomous: true
requirements: [HARDEN-02]
must_haves:
  truths:
    - "A static malformed-response corpus drives deterministic Jest assertions over parseScribeResponse for all 6 D-07 categories"
    - "The ~2% code-fence fellBack reliquat is re-measured against the corpus and either confirmed closed by existing stripFence or closed by an extension"
    - "Any model response whose ONLY defect is a wrapping code fence parses successfully (fellBack===false), not via fallback"
    - "The response_format default decision (keep json_object, defer json_schema) is documented with the proxy-forwarding fact"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js"
      provides: "static malformed-response fixtures grouped by the 6 D-07 categories"
      exports: ["SCRIBE_RESPONSE_FIXTURES"]
    - path: "src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js"
      provides: "deterministic regression assertions + fellBack rate measurement over the fixtures"
      contains: "SCRIBE_RESPONSE_FIXTURES"
    - path: "src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md"
      provides: "documented response_format default decision + proxy-forwarding fact + json_schema deferral rationale"
      contains: "json_object"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js"
      to: "parseScribeResponse"
      via: "import from scribeResponse.js, run each fixture through it"
      pattern: "parseScribeResponse"
    - from: "src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js"
      to: "SCRIBE_RESPONSE_FIXTURES"
      via: "import from scribeResponse.fixtures.js"
      pattern: "SCRIBE_RESPONSE_FIXTURES"
---

<objective>
Build a static regression corpus of malformed LLM responses with deterministic Jest assertions over `parseScribeResponse`, re-measure the code-fence `fellBack` reliquat and confirm-or-extend `stripFence` to close it, and document the `response_format` default decision (HARDEN-02).

Purpose: Lock parser tolerance against regression with reproducible fixtures (separate from the live GATE harness), prove the ~2% fenced-JSON tail is closed, and record the structured-output decision with the now-confirmed proxy-forwarding fact.
Output: fixtures module, corpus spec, optional stripFence extension, RESPONSE_FORMAT_DECISION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-06-durcissement-contrat/v3.1-06-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live code; use directly. -->

From src/modules/views/OnlyOffice/Scribe/scribeResponse.js:
- export function parseScribeResponse(raw, { surface } = {}): { discussion, fragments, valid, fellBack, warnings, raw }  (NEVER throws)
- function stripFence(str): string  (NOT currently exported; l.88-96)
    Strips ONE leading ```/```json fence and matching trailing ```. leadingFence = /^```[a-zA-Z0-9_-]*[ \t]*\r?\n?/ ; trailingFence = /\r?\n?[ \t]*```$/ . Both linear/ReDoS-safe.
    Applied inside tolerantParse(str) (l.156-175) BEFORE JSON.parse, then balanced-object extraction, then trailing-comma repair.
- export const SCRIBE_OUTPUT_SCHEMA  (documented json_schema artifact; see SCRIBE_OUTPUT_SCHEMA.md)
- export const MAX_RAW_LENGTH = 512 * 1024

fellBack semantics: parseScribeResponse returns fellBack:true ONLY when the tolerant parse pipeline failed and buildFallback ran (raw treated as fallback). A response that parses after stripFence has fellBack:false.

valid semantics: valid:false on split-table (unbalanced [TABLE:N]/[CELL:r,c]) even when JSON parsed.

Existing parser spec (do NOT duplicate, EXTEND-by-corpus): src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js already covers single examples of fenced JSON, prose-wrapped JSON, trailing comma, split-table, fallback, prototype-pollution, ReDoS.
</interfaces>

<proxy_fact>
VERIFIED (record in RESPONSE_FORMAT_DECISION.md): the cozy-stack RAG proxy CallRAGQuery (/home/ben/Dev-local/cozy-stack/model/rag/chat.go ~l.446-464) forwards the POST body UNCHANGED (bytes.NewReader(payload)) to the upstream server — it does NOT parse, filter, or strip response_format / json_schema. So response_format DOES reach the model today; a future json_schema would too. CONTEXT.md (l.95) listed proxy-forwarding as the open dependency — it is now CONFIRMED forwarded. The decision to keep json_object (D-06) is therefore the low-risk choice, NOT a proxy block; json_schema activation is deferred pending confirmation the UPSTREAM MODEL (Mistral-Small-3.2-24B behind the proxy) honors json_schema structured output — a model-capability question, not a proxy question.
</proxy_fact>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static malformed-response fixtures + deterministic corpus spec (6 D-07 categories)</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js (parseScribeResponse l.234-322, tolerantParse l.156-175, stripFence l.88-96, hasUnbalancedStructureMarkers l.186-192, buildFallback l.204-223)
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js (existing coverage — fixtures must NOT duplicate single cases but broaden each category; same import style, same toBe assertions)
    - .planning/phases/v3.1-06-durcissement-contrat/v3.1-06-CONTEXT.md (D-07 categories, D-08)
  </read_first>
  <action>
    Create src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js exporting `SCRIBE_RESPONSE_FIXTURES` — a plain object keyed by the SIX D-07 categories: `fence` (```json-wrapped valid JSON), `preamble` (localized prose preface then JSON, fr/en at least), `trailingComma` (trailing comma before } and ]), `splitTable` (unbalanced [TABLE:N]/[CELL:r,c] inside a fragment), `brokenRef` (a fragment containing a malformed {{REF:scribe-ref-N:…}} / [^scribe-fn-N] marker), `nonJsonProse` (pure prose, no JSON object). Each category value is an array of fixtures, each fixture = { name, raw, surface, expect } where expect describes the asserted parse outcome (at minimum fellBack and valid). Use only static string literals (no live network, no model). Keep fixtures English/French literals; do NOT import the GATE harness or scripts/gate-harness/.
    Create src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js that imports SCRIBE_RESPONSE_FIXTURES and parseScribeResponse and, for every fixture, asserts parseScribeResponse(fixture.raw, { surface: fixture.surface }) matches fixture.expect. fence fixtures: assert fellBack===false plus the parsed discussion/fragments. splitTable fixtures: assert valid===false and warnings includes 'split-table'. nonJsonProse fixtures: assert fellBack===true and the surface-specific fallback shape (popover => fragments=[raw]; chat => discussion=raw). Iterate categories with a data-driven loop and assert each category array is non-empty.
    Keep this spec SEPARATE from scribeResponse.spec.js (D-07: corpus is its own module/spec) and SEPARATE from the live GATE harness.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js'); console.log(Object.keys(f.SCRIBE_RESPONSE_FIXTURES).sort().join(','))"` prints exactly: `brokenRef,fence,nonJsonProse,preamble,splitTable,trailingComma`.
    - The spec asserts every category array is non-empty (`expect(SCRIBE_RESPONSE_FIXTURES[cat].length).toBeGreaterThan(0)` for all six).
    - The spec asserts, per fixture, the documented fellBack/valid outcome; splitTable fixtures assert `warnings` contains `'split-table'`; nonJsonProse fixtures assert surface-specific fallback (popover fragments=[raw], chat discussion=raw).
    - `grep -n "gate-harness" src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js` returns NO match (corpus is independent of the live harness).
    - `yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js` exits 0.
  </acceptance_criteria>
  <done>scribeResponse.fixtures.js exports SCRIBE_RESPONSE_FIXTURES with all 6 non-empty D-07 categories; scribeResponse.corpus.spec.js asserts the parse outcome of every fixture and is green; corpus is independent of scripts/gate-harness/.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure code-fence fellBack reliquat, confirm-or-extend stripFence, document response_format decision</name>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.js (stripFence l.88-96, tolerantParse l.156-175)
    - src/modules/views/OnlyOffice/Scribe/scribeResponse.fixtures.js (the `fence` category from Task 1)
    - src/modules/views/OnlyOffice/Scribe/SCRIBE_OUTPUT_SCHEMA.md (existing schema doc — the decision doc cross-references it)
    - .planning/phases/v3.1-06-durcissement-contrat/v3.1-06-CONTEXT.md (D-06, D-08)
    - .planning/STATE.md (the ~2% fellBack / ```json-wrapping observation, l.47-49)
  </read_first>
  <action>
    Re-measure (D-08): in scribeResponse.corpus.spec.js add a `fellBack rate` describe block that runs every `fence`-category fixture through parseScribeResponse and asserts ALL yield fellBack===false (a fence-only defect must NEVER fall back). Include the known reliquat shapes the model emits: leading ```json + trailing ``` with a language tag, trailing whitespace/newlines, and a ```json fence with NO trailing newline before the closing ```. ANCHOR to reality: at least one `fence` fixture MUST be a VERBATIM copy of the actual model-emitted reliquat observed in STATE.md (the exact ```json-wrapped payload that produced the ~2% fellBack), tagged in the fixture `name` as the real-world sample — this prevents the re-measure passing trivially over only author-invented shapes that existing stripFence already happens to handle. If any such shape currently yields fellBack===true, EXTEND stripFence (scribeResponse.js l.88) to handle it — keeping the regexes linear/anchored (no nested quantifiers, ReDoS-safe per the module security contract) — until every fence fixture parses (fellBack===false). If existing stripFence already covers all shapes, add a code comment in scribeResponse.js confirming the reliquat is closed by the existing implementation (no behavior change) and record that finding in the decision doc.
    Create src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md documenting (D-06): the default stays `response_format: { type: 'json_object' }` (scribeAI.js l.279); the cozy-stack proxy CallRAGQuery forwards the POST body UNCHANGED (cite model/rag/chat.go ~l.446-464) so response_format and a future json_schema DO reach the upstream model; therefore the deferral of json_schema is NOT a proxy limitation but pending confirmation the upstream model (Mistral-Small-3.2-24B) honors json_schema structured output; SCRIBE_OUTPUT_SCHEMA is the ready json_schema payload (cross-reference SCRIBE_OUTPUT_SCHEMA.md); record the re-measured fence fellBack result (closed by existing stripFence vs extended).
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js -t "fellBack rate" && yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - The `fellBack rate` describe block asserts every `fence`-category fixture yields `fellBack===false`; running it exits 0.
    - The fence category includes at least these 3 shapes: ```json+language-tag, fence+trailing whitespace/newlines, fence with no trailing newline before closing ```.
    - At least one `fence` fixture is a verbatim real-reliquat sample (its `name` marks it as the STATE.md-observed model output), so the 0% re-measure is anchored to a real shape, not only author-invented ones.
    - RESPONSE_FORMAT_DECISION.md enumerates which fence shapes were tested in the re-measure (including the verbatim real-reliquat sample) so the "fellBack re-measured at 0%" claim is auditable against the originally-observed reliquat.
    - `grep -nE '\(\.\*\)\+|\(\.\+\)\+|\(\.\*\)\*' src/modules/views/OnlyOffice/Scribe/scribeResponse.js` returns NO match (no nested-quantifier regex introduced — ReDoS contract preserved).
    - If stripFence was unchanged, scribeResponse.js contains a comment confirming the fence reliquat is closed by the existing implementation; if changed, the existing scribeResponse.spec.js still passes (`yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js` exits 0).
    - `test -f src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md` succeeds and `grep -c "json_object" src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md` >= 1 and `grep -c "chat.go" src/modules/views/OnlyOffice/Scribe/RESPONSE_FORMAT_DECISION.md` >= 1.
  </acceptance_criteria>
  <done>The fence fellBack rate is re-measured at 0% fallback over the corpus (stripFence confirmed or extended, ReDoS-safe); RESPONSE_FORMAT_DECISION.md documents the json_object default, the proxy-forwarding fact, and the json_schema deferral rationale; scribeResponse.spec.js stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/proxy -> parser | Malformed/adversarial model text (the corpus simulates it) crosses into parseScribeResponse. |
| fixture authoring -> test runtime | Static fixtures are trusted developer-authored literals (no network, no model at test time). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-06-04 | Denial of Service | stripFence extension | mitigate | Any stripFence change keeps anchored, linear regexes (no nested quantifiers) per the module ReDoS contract; the grep gate `grep -nE '\(\.\*\)\+\|\(\.\+\)\+\|\(\.\*\)\*'` must return zero matches. The existing ReDoS spec in scribeResponse.spec.js (~50k adversarial input < 1s) must still pass. |
| T-v3.1-06-05 | Tampering | brokenRef / split-table fixtures | mitigate | Fixtures assert the parser's existing prototype-pollution-safe + split-table guards still hold (e.g. unbalanced [TABLE]/[CELL] => valid:false). The corpus is a regression net preserving these invariants, not new attack surface. |
| T-v3.1-06-06 | Information Disclosure | RESPONSE_FORMAT_DECISION.md | accept | The doc records that response_format reaches the upstream model and that the JWT-gated proxy forwards it; no secrets are written (JWT secret is NOT included). Low risk: documentation of existing behavior. |
| T-v3.1-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (fixtures + Jest only, both already present). No legitimacy gate required. |
</threat_model>

<verification>
- `yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.corpus.spec.js` green (all 6 categories + fellBack rate).
- `yarn jest src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js` green (no regression to existing parser tests).
- ReDoS grep gate returns zero matches; RESPONSE_FORMAT_DECISION.md exists with json_object + chat.go references.
</verification>

<success_criteria>
- ROADMAP v3.1-06 criterion 2 met: a malformed-response regression corpus + edge-case tests pass green and the code-fence fellBack rate is re-measured (stripFence confirmed or extended).
- ROADMAP v3.1-06 criterion 3 met: the response_format default (json_object vs json_schema) is decided and documented, with the cozy-stack proxy forwarding fact taken into account.
</success_criteria>

<output>
Create `.planning/phases/v3.1-06-durcissement-contrat/v3.1-06-02-SUMMARY.md` when done.
</output>
