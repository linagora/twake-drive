---
phase: 27-references-documentaires
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/onlyoffice-scribe/scripts/code.js
autonomous: false
requirements:
  - REF-01
  - REF-02

must_haves:
  truths:
    - "Diagnostic callCommand reveals exactly how footnote reference marks appear (or do not appear) when iterating paragraph elements via GetElement(i)"
    - "Diagnostic callCommand reveals exactly how cross-reference fields appear (or do not appear) when iterating paragraph elements via GetElement(i)"
    - "Copy() behavior on footnote/cross-ref elements is tested and documented"
    - "A concrete detection strategy is chosen based on empirical results"
  artifacts:
    - path: ".planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md"
      provides: "Empirical findings for footnote and cross-reference element detection"
      contains: "Detection Strategy"
  key_links: []
---

<objective>
Empirical diagnostic of OO footnote and cross-reference element visibility through the builder API.

Purpose: The research (27-RESEARCH.md) confirmed there is NO dedicated GetClassType for footnotes or cross-references. Before implementing extraction/injection, we MUST determine empirically how these elements appear (or fail to appear) when iterating paragraph elements via GetElement(i). This diagnostic gates all subsequent implementation work.

Output: 27-DIAGNOSTIC-RESULTS.md with concrete findings and chosen detection strategy.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-references-documentaires/27-CONTEXT.md
@.planning/phases/27-references-documentaires/27-RESEARCH.md
@plugins/onlyoffice-scribe/scripts/code.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and run diagnostic callCommand for footnote and cross-reference element detection</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js</files>
  <read_first>
    - plugins/onlyoffice-scribe/scripts/code.js (lines 1858-1975 for paragraphToMarkdown element iteration pattern)
    - .planning/phases/27-references-documentaires/27-RESEARCH.md (detection strategy section, diagnostic code example)
  </read_first>
  <action>
Add a temporary diagnostic function `_diagnosticFootnotesCrossRefs()` near the end of code.js (before the closing IIFE brace), invoked via a temporary intent handler or a manual call. This function must:

1. Use `Asc.plugin.callCommand` (not executeMethod) to run inside the document context.
2. Inside the callCommand (ES5 only — var, function, no arrow functions, no const/let):
   a. Get the document: `var doc = Api.GetDocument();`
   b. Get all paragraphs: `var paras = doc.GetAllParagraphs();`
   c. For each paragraph, iterate elements via `para.GetElementsCount()` and `para.GetElement(i)`:
      - Log `GetClassType()` return value
      - Log `GetText()` return value
      - If classType is "run": check `GetTextPr()` for style name (via `GetStyle()` if available), VertAlign (superscript), and any other properties
      - Try `el.GetInlineDrawings()`, `el.GetElementsCount()`, `el.GetFootnotesFirstParagraphs` (checking if method exists on different objects)
   d. Also call `doc.GetFootnotesFirstParagraphs()` and log the count and first paragraph text of each footnote
   e. Also call `doc.GetAllHeadingParagraphs()` and log count for cross-ref context
   f. Also call `doc.GetAllBookmarksNames()` and log bookmark names
   g. Try `doc.GetAllFields()` or similar method to detect cross-reference fields
   h. Store all results in `Asc.scope.diagnosticResults = JSON.stringify(results)`
3. After callCommand completes, read `Asc.scope.diagnosticResults` and log it to console
4. Also add a second diagnostic that specifically tests Copy() on any detected non-standard elements:
   - If an element has classType "unsupported" or has footnote-like properties, try `el.Copy()` and log success/failure

The diagnostic should be triggered by adding a temporary `window.scribeDiagnostic = function() { ... }` that can be called from the browser console while a document with footnotes and cross-references is open.

IMPORTANT: All code inside callCommand must be ES5 (var, function, no arrow functions, no const/let, no template literals). Code outside callCommand (in the plugin iframe context) can use modern JS since it runs in the browser.

After writing the diagnostic, restart the OO container:
```
cd /home/ben/Dev-local/cozy-drive && ./scripts/oo-dev-setup.sh
```

Then test by:
1. Opening a test document in OO that contains footnotes and cross-references
2. Running `window.scribeDiagnostic()` from the browser console (in the plugin iframe)
3. Collecting the console output
  </action>
  <verify>
    <automated>grep -c "_diagnosticFootnotesCrossRefs\|scribeDiagnostic" plugins/onlyoffice-scribe/scripts/code.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "_diagnosticFootnotesCrossRefs" plugins/onlyoffice-scribe/scripts/code.js returns at least 1 match
    - grep "scribeDiagnostic" plugins/onlyoffice-scribe/scripts/code.js returns at least 1 match
    - grep "GetFootnotesFirstParagraphs" plugins/onlyoffice-scribe/scripts/code.js returns at least 1 match
    - grep "diagnosticResults" plugins/onlyoffice-scribe/scripts/code.js returns at least 1 match
  </acceptance_criteria>
  <done>Diagnostic function exists in code.js, OO container restarted, ready for manual testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run diagnostic and collect findings</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js</files>
  <action>
Human runs the diagnostic function in a document with footnotes and cross-references, then shares the console output. The executor will use this output to determine the detection strategy.

Steps for human:
1. Open a document in OO that contains at least one footnote and at least one cross-reference
2. Open browser DevTools console, select the plugin iframe context
3. Run: window.scribeDiagnostic()
4. Copy and paste the full console output
  </action>
  <what-built>A diagnostic function that reveals how OO exposes footnote references and cross-reference fields through the builder API element iteration.</what-built>
  <how-to-verify>
    1. Open a document in OO that contains at least one footnote (superscript number referencing a footnote at the bottom of the page) and at least one cross-reference (e.g., "See Chapter 3" field that links to a heading)
    2. Open the browser DevTools console
    3. Navigate to the plugin iframe context (select the correct iframe in the console dropdown)
    4. Run: `window.scribeDiagnostic()`
    5. Copy the full console output and share it here
    6. If you don't have a test document with footnotes/cross-refs, create one:
       - Type some text, then Insert > Footnote to add a footnote
       - Type a heading ("Chapter 1"), then elsewhere Insert > Cross-reference to reference it
       - Save the document
       - Then run the diagnostic
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: awaiting human diagnostic output"</automated>
  </verify>
  <done>Human has shared the diagnostic console output showing how footnote and cross-reference elements appear in OO API element iteration</done>
  <resume-signal>Paste the diagnostic output, then type "continue" — the executor will analyze results and write 27-DIAGNOSTIC-RESULTS.md</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Analyze findings and write diagnostic results document, then clean up</name>
  <files>.planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md, plugins/onlyoffice-scribe/scripts/code.js</files>
  <read_first>
    - .planning/phases/27-references-documentaires/27-RESEARCH.md (the three possible outcomes table at line 122-128)
    - plugins/onlyoffice-scribe/scripts/code.js (the diagnostic function added in Task 1)
  </read_first>
  <action>
Based on the diagnostic output from the checkpoint:

1. Create `.planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md` with:
   - **Findings:** What GetClassType values appeared for footnote ref elements, what GetText returned, whether they were visible or invisible in GetElement iteration
   - **Cross-Reference Findings:** Same for cross-reference fields
   - **Copy() Results:** Whether Copy() worked on the detected elements
   - **Detection Strategy:** Based on findings, which of the three strategies from research applies:
     a. Elements visible as "run" with specific style → detect via style/VertAlign
     b. Elements visible as "unsupported" → detect via classType + Copy()
     c. Elements invisible → use GetFootnotesFirstParagraphs() count + position correlation
   - **Implementation Implications:** What the Plan 02 and Plan 03 executors need to know

2. Remove the temporary diagnostic code from code.js:
   - Delete the `_diagnosticFootnotesCrossRefs` function
   - Delete the `window.scribeDiagnostic` assignment
   - Ensure no other temporary code remains

3. Restart OO container to pick up cleaned code:
   ```
   cd /home/ben/Dev-local/cozy-drive && ./scripts/oo-dev-setup.sh
   ```
  </action>
  <verify>
    <automated>test -f .planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md && grep -c "Detection Strategy" .planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md && ! grep -q "scribeDiagnostic" plugins/onlyoffice-scribe/scripts/code.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/27-references-documentaires/27-DIAGNOSTIC-RESULTS.md exists
    - grep "Detection Strategy" 27-DIAGNOSTIC-RESULTS.md returns at least 1 match
    - grep "scribeDiagnostic" plugins/onlyoffice-scribe/scripts/code.js returns 0 matches (diagnostic removed)
  </acceptance_criteria>
  <done>Diagnostic results documented with concrete detection strategy chosen, temporary diagnostic code removed from code.js, OO container restarted clean</done>
</task>

</tasks>

<verification>
- 27-DIAGNOSTIC-RESULTS.md exists and contains a concrete detection strategy
- code.js has no remaining diagnostic code (no scribeDiagnostic, no _diagnosticFootnotesCrossRefs)
- The detection strategy document addresses both footnotes and cross-references
</verification>

<success_criteria>
- Empirical data collected on how footnote refs and cross-ref fields appear in OO builder API element iteration
- A concrete detection strategy is chosen and documented (one of the three options from research, or a new approach based on findings)
- Plan 02 and Plan 03 executors can implement without guessing about API behavior
</success_criteria>

<output>
After completion, create `.planning/phases/27-references-documentaires/27-01-SUMMARY.md`
</output>
