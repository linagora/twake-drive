---
phase: v3.1-03-sonde-dev-hard-gate-before-render
plan: 02
type: execute
wave: 2
depends_on: ["v3.1-03-01"]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
autonomous: false
requirements: [PROBE-01]

must_haves:
  truths:
    - "Inline (popover) responses are captured into the probe corpus after parse, dev-mode only"
    - "Chat responses (0..N fragments) are captured into the probe corpus after parse, dev-mode only"
    - "A dev sees the live parsed response {discussion, fragments, valid, fellBack, warnings} in a DevPanelGrid panel"
    - "A dev sees conformance metrics + coverage (rates vs thresholds, 0/1/N dist, counters) with export/import controls"
    - "Production pays zero cost: every capture is gated behind isScribeDevMd()"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "Inline capture call after parse at line ~149"
      contains: "recordProbeSample"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "Chat capture call after parse at line ~161 (new devData flow, D-11)"
      contains: "recordProbeSample"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "Two new DevPanelGrid panels: parsedResponse + probeMetrics"
      contains: "parsedResponse"
  key_links:
    - from: "ScribePopover.jsx"
      to: "scribeProbe.js"
      via: "recordProbeSample import + gated call"
      pattern: "recordProbeSample"
    - from: "ScribeContext.jsx"
      to: "scribeProbe.js"
      via: "recordProbeSample import + isScribeDevMd import (new) + gated call"
      pattern: "recordProbeSample"
    - from: "ScribeResultPanel.jsx"
      to: "scribeProbe.js"
      via: "aggregate/exportCorpus/importCorpus for the metrics panel"
      pattern: "aggregate|exportCorpus|importCorpus"
---

<objective>
Wire the probe into both surfaces and surface it in the existing DevPanelGrid. Add one gated
`recordProbeSample` call at each post-parse point (inline popover + chat), then extend
`ScribeResultPanel`'s DevPanelGrid with two new panels: a live parsed-response viewer and a
conformance-metrics + coverage view with export/import controls.

Purpose: D-10 (reuse DevPanelGrid) + D-11 (observe BOTH surfaces). This makes the engine from
Plan 01 visible and fed by real usage — the live half of the hybrid sample (D-07a).
Output: edits to ScribePopover.jsx, ScribeContext.jsx, ScribeResultPanel.jsx. No new module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-CONTEXT.md
@.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-PATTERNS.md
@.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01's scribeProbe.js (consume directly — no exploration needed) -->
- `recordProbeSample(parsed, meta)` -> sampleCount; meta = { surface, inputMd, ts, tags? }
- `exportCorpus()` -> JSON string
- `importCorpus(json)` -> sampleCount (throws on bad shape/version)
- `replay(corpus?)` -> samples augmented with metrics
- `aggregate(samples)` -> { total, dupRate, preambleRate, splitTableCount, refBrokenCount, fragDist:{0,1,N}, coverage:{perLocale, tableCases, refCases} }

<!-- Existing capture points (verified line numbers) -->
ScribePopover.jsx line 149: `const parsed = parseScribeResponse(text, { surface: 'popover' })`
  - `isScribeDevMd` ALREADY imported (line 19); `inputMd` already exists (line 99, fed to setDevData).
ScribeContext.jsx line 161: `const parsed = parseScribeResponse(responseText, { surface: 'chat' })`
  - does NOT import scribeDevMode today — must ADD `isScribeDevMd` + `recordProbeSample` imports.
  - per-turn selection markdown available as `m.selection.markdown || m.selection.text` (history mapping lines 132-133).

<!-- Existing DevPanelGrid machinery to extend (ScribeResultPanel.jsx) -->
- line 20: `const DEV_PANEL_KEYS = ['htmlSource', 'htmlNorm', 'mdConverted', 'llmRaw', 'llmDisplay', 'rendered']`
- line 21-31: `getDevPanelLabels(source)` returns label map
- line 32: `const DEV_PANEL_DEFAULTS = { ... }`
- line 34-50: `loadDevPanelPrefs`/`saveDevPanelPrefs` (merge-with-defaults makes new keys auto-visible)
- line 77+: `DevPanelGrid` component — one `if (devPanelPrefs.<key> !== false) panels.push(<div .../>)` block per key
- line 556+: `<DevPanelGrid ... />` call site (pass new data props here)
- line 16: `loadHighlightJs` / `loadBeautify` imported from scribeDevMode (reuse for JSON panel)
- line 525: checkbox toolbar maps DEV_PANEL_KEYS (new keys appear automatically)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture probe samples at both surfaces (dev-gated)</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx, src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (lines 1-19 imports, 99 inputMd, 116-149 parse point)
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (lines 8-9 imports, 132-133 selection mapping, 156-161 parse point)
    - src/modules/views/OnlyOffice/Scribe/scribeProbe.js (recordProbeSample signature from Plan 01)
    - src/modules/views/OnlyOffice/Scribe/scribeDevMode.js (isScribeDevMd — lines 16-25)
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-PATTERNS.md (sections "ScribePopover.jsx" + "ScribeContext.jsx" capture-at-parse-point)
  </read_first>
  <action>
    In `ScribePopover.jsx`: add the import `recordProbeSample` from
    `@/modules/views/OnlyOffice/Scribe/scribeProbe` (match the `@/` alias style used lines 11-20).
    Immediately after the existing parse at line ~149, insert a single call gated behind the
    already-imported `isScribeDevMd()`: `recordProbeSample(parsed, { surface: 'popover', inputMd,
    ts: Date.now() })` using the existing `inputMd` variable (line 99). Optionally pass
    `tags: { locale: <account/browser locale if cheaply available> }` — discretion; do not add new
    state to obtain it. In `ScribeContext.jsx`: add TWO imports (this file does not import
    scribeDevMode today) — `isScribeDevMd` from `@/modules/views/OnlyOffice/Scribe/scribeDevMode`
    and `recordProbeSample` from `@/modules/views/OnlyOffice/Scribe/scribeProbe`. Immediately after
    the existing parse at line ~161, insert the gated call `recordProbeSample(parsed, { surface:
    'chat', inputMd: <per-turn selection markdown via the same `selection.markdown || selection.text`
    accessor used at lines 132-133, or '' if absent>, ts: Date.now() })`. Per RESEARCH A4, an absent
    inputMd degrades REF missing-detection to N/A for that sample — acceptable. Do NOT change any
    rendering, message-model, or control flow; these are additive capture calls only.
  </action>
  <verify>
    <automated>grep -n "recordProbeSample" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx && grep -n "isScribeDevMd" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recordProbeSample" src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` >= 1 and the call is inside an `isScribeDevMd()` guard
    - `grep -c "recordProbeSample" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` >= 1 and the call is inside an `isScribeDevMd()` guard
    - `grep -c "isScribeDevMd" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` >= 2 (new import + guarded call)
    - Both capture calls pass `surface: 'popover'` and `surface: 'chat'` respectively
    - `npx jest scribeResponse.spec.js scribeProbe.spec.js --silent` still exits 0 (no regression to existing modules)
    - No change to render output, message model, or existing control flow (diff is import + guarded call only)
  </acceptance_criteria>
  <done>Both surfaces record samples post-parse, gated behind isScribeDevMd(); ScribeContext gains the two new imports; zero production-path cost; no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DevPanelGrid with parsedResponse + probeMetrics panels</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (lines 1-50 keys/labels/prefs, 73-170 DevPanelGrid panel blocks, 300-388 highlight useEffect + state, 504-583 toolbar + DevPanelGrid call site)
    - src/modules/views/OnlyOffice/Scribe/scribeProbe.js (aggregate / replay / exportCorpus / importCorpus signatures from Plan 01)
    - src/modules/views/OnlyOffice/Scribe/scribeDevMode.js (loadHighlightJs / loadBeautify usage)
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-PATTERNS.md (section "ScribeResultPanel.jsx" — exact extension points + line numbers)
  </read_first>
  <action>
    Extend the existing DevPanelGrid (D-10, purely additive). Append `'parsedResponse'` and
    `'probeMetrics'` to `DEV_PANEL_KEYS` (line 20), add a label for each in `getDevPanelLabels`
    (e.g. "Réponse parsée" and "Métriques + couverture") and `true` for each in
    `DEV_PANEL_DEFAULTS` (line 32) — the merge-with-defaults in `loadDevPanelPrefs` (line 40) makes
    them auto-visible without migration, and the checkbox toolbar (line 525) picks them up
    automatically. Add two `if (devPanelPrefs.<key> !== false) panels.push(<div .../>)` blocks
    inside `DevPanelGrid` (after the existing blocks): the `parsedResponse` block renders the
    current `{discussion, fragments, valid, fellBack, warnings}` as pretty-printed + syntax-colored
    JSON (reuse `loadBeautify()` / `loadHighlightJs()` already wired in the highlight `useEffect`
    lines 327-363; add a `setHighlightedParsedJson` state branch mirroring the existing highlight
    state declared line 321-325). The `probeMetrics` block calls `aggregate(replay())` from
    scribeProbe to render: dup/preamble RATES vs thresholds (D-04), splitTable/refBroken COUNTS
    (D-03, zero-tolerance — visually flag any non-zero), the 0/1/N fragDist (D-05 indicative), and
    coverage counters per locale + table + REF (D-09) with a warn indicator when below the
    [ASSUMED] minimums documented in GATE.md. Add two buttons in that panel: Export (calls
    `exportCorpus()`, copies/downloads the JSON as GATE.md evidence) and Import (file input ->
    `importCorpus(text)`, wrapped in try/catch to surface a panel error on bad shape, never crash).
    Pass any new data props through the `<DevPanelGrid ... />` call site (line 556). The grid
    already adapts columns up to its layout breakpoints, so 8 panels lay out fine. Keep all probe
    logic in scribeProbe.js — the component only renders aggregate output (anti-pattern: no metric
    math in the component). Follow PATTERNS section "ScribeResultPanel.jsx" for the exact insert
    points.
  </action>
  <verify>
    <automated>grep -n "parsedResponse\|probeMetrics" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx && grep -n "aggregate\|exportCorpus\|importCorpus" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `DEV_PANEL_KEYS` includes both `'parsedResponse'` and `'probeMetrics'`
    - `getDevPanelLabels` returns a label for each new key and `DEV_PANEL_DEFAULTS` sets each to `true`
    - DevPanelGrid contains two new `panels.push` blocks keyed `parsedResponse` and `probeMetrics`
    - probeMetrics panel calls `aggregate` and exposes `exportCorpus` (Export) + `importCorpus` (Import) controls; import is wrapped in try/catch
    - No metric computation lives in the component (all math via scribeProbe.js exports)
    - `npx jest --silent` exits 0 (existing Scribe specs still green)
  </acceptance_criteria>
  <done>DevPanelGrid renders a live parsed-response panel and a metrics+coverage panel with working export/import; new keys auto-visible via prefs merge; no metric logic in the component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — probe captures both surfaces and panels render</name>
  <files>(none — runtime verification)</files>
  <action>
    Human verification of the probe wiring (cannot be fully asserted by automated tests: requires
    the live OO/Drive runtime and visual confirmation of the new panels). Built: probe capture
    wired to both surfaces (popover + chat) and two new DevPanelGrid panels (live parsed response +
    conformance metrics/coverage with export/import), all dev-mode gated. Execute the steps in
    how-to-verify below and report results.
  </action>
  <what-built>
    Probe capture wired to both surfaces (popover + chat) and two new DevPanelGrid panels
    (live parsed response + conformance metrics/coverage with export/import), all dev-mode gated.
  </what-built>
  <how-to-verify>
    1. Restart the OO container per project convention (`./scripts/oo-dev-setup.sh`) if needed for the rebuilt Drive bundle.
    2. Enable dev mode: in the Drive app console run `localStorage.setItem('SCRIBE_DEV_MD','true')`, reload.
    3. Run an inline Scribe action on a selection -> open the result panel -> confirm the new "Réponse parsée" panel shows `{discussion, fragments, valid, fellBack, warnings}` syntax-highlighted, and "Métriques + couverture" shows total=1 with rates/dist/coverage.
    4. Open the chat side panel, send a message -> reopen the popover result panel -> confirm the metrics total incremented (chat capture works, D-11).
    5. Click Export -> confirm a JSON envelope `{v:1,samples:[...]}` is produced. Edit it to a bad shape and Import -> confirm a panel error appears and the existing corpus is NOT wiped (try/catch, Pitfall 5).
    6. Confirm the panel checkboxes toggle the two new panels (prefs persist across reload).
    7. Sanity: with `SCRIBE_DEV_MD` unset, confirm no panels appear and no samples recorded (production-zero-cost).
  </how-to-verify>
  <verify>
    <human-check>All seven how-to-verify steps confirmed: both surfaces increment the corpus in dev mode, both new panels render, export/import works without wiping on bad shape, panels gated off in production.</human-check>
  </verify>
  <done>Probe verified working on both surfaces with rendering panels and safe export/import; production shows no probe UI/recording.</done>
  <resume-signal>Type "approved" or describe issues (e.g. chat capture not incrementing, import wipes corpus, panel not gated)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev UI -> importCorpus | Untrusted JSON file chosen by the dev, parsed and written to localStorage |
| LLM response -> recordProbeSample | Already-parsed model output captured into corpus (text only) |
| production runtime -> probe | Probe must be invisible/inactive in production (no UI, no recording) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-03-05 | Information Disclosure | probe panels + capture | mitigate | All capture calls AND panel rendering gated behind `isScribeDevMd()` (SCRIBE_DEV_MD localStorage flag); production renders nothing and records nothing. Verified by the unset-flag step in the checkpoint. |
| T-v3.1-03-06 | Tampering | Import button -> importCorpus | mitigate | Import wrapped in try/catch; importCorpus (Plan 01) validates shape/version before replacing and throws on mismatch without mutating the existing corpus; UI surfaces the error instead of crashing. |
| T-v3.1-03-07 | Elevation of Privilege | dangerouslySetInnerHTML in parsedResponse panel | mitigate | JSON rendered through the existing `escapeHtml` + highlight.js pipeline already used by sibling panels (lines 167-169); never inject raw model text as HTML — reuse the vetted highlight path. |
| T-v3.1-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (UI wiring + reuse of existing lazy chunks highlight.js/js-beautify, vetted in earlier phases). Nothing to slopcheck. |
</threat_model>

<verification>
- Inline + chat responses both increment the corpus when dev mode is on
- Parsed-response panel renders the structured payload; metrics panel renders rates/counts/dist/coverage
- Export produces valid JSON; Import rejects bad shape without wiping corpus
- With SCRIBE_DEV_MD unset: no probe UI, no recording
- `npx jest` exits 0 (no regression)
</verification>

<success_criteria>
- D-11 satisfied: both surfaces captured post-parse, dev-gated
- D-10 satisfied: DevPanelGrid extended additively with parsed-response + metrics/coverage panels
- Export/import operational for GATE.md evidence + offline corpus replay
- Production pays zero cost
</success_criteria>

<output>
Create `.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-02-SUMMARY.md` when done
</output>
