---
phase: v3.1-03-sonde-dev-hard-gate-before-render
verified: 2026-06-17T14:30:00Z
status: human_needed
score: 10/10
overrides_applied: 0
human_verification:
  - test: "Confirm panels render and probe captures on both surfaces in the live runtime"
    expected: "Popover and chat both increment corpus; parsedResponse + probeMetrics panels visible; export/import works without wiping on bad shape; panels absent when SCRIBE_DEV_MD unset"
    why_human: "Runtime UI behavior — requires the live OO/Drive stack and browser interaction; cannot be asserted by grep or Jest"
    note: "ALREADY APPROVED 2026-06-17 by the developer (7/7 how-to-verify steps confirmed, documented in v3.1-03-02-SUMMARY.md Task 3)"
---

# Phase v3.1-03: Sonde dev (HARD GATE before render) — Verification Report

**Phase Goal:** Build the dev conformance probe + HARD GATE apparatus: confirm empirically (downstream, by the human) that the real model separates discussion vs fragments well enough BEFORE any card rendering is built — a go/no-go gate for phases v3.1-04/05.

**Verified:** 2026-06-17T14:30:00Z
**Status:** human_needed (runtime UI check already approved; recorded for completeness)
**Re-verification:** No — initial verification

---

## Goal Achievement

The phase delivers the GATE *apparatus*, not the verdict. The three deliverables are: (1) the pure measurement engine (`scribeProbe.js` + spec), (2) both-surface capture wired into the live dev panels (`ScribePopover`, `ScribeContext`, `ScribeResultPanel`), and (3) the durable gate artefacts (`v3.1-03-GATE.md`, `probe-corpus.json`, ROADMAP edit). The empirical verdict is a downstream human activity — intentionally out of scope and correctly absent.

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each conformance metric is a pure function of stored text (no React, no network, no LLM) | VERIFIED | `scribeProbe.js` has no React import, no fetch/axios. All five metric exports are plain JS functions. Confirmed by grep: zero matches for React/fetch/network. |
| 2 | Duplication is a 0..1 word-bigram Dice score; max over discussion vs each fragment | VERIFIED | `maxFragmentDuplication` + `diceCoefficient` + `normalizeForDup` at lines 140-198 of scribeProbe.js. Spec: 6 it-blocks in `describe('maxFragmentDuplication')`, all green. |
| 3 | Preamble detection covers fr/en/de/es/it and returns the matched locale or null | VERIFIED | `PREAMBLE_PATTERNS` (9 entries) + `detectPreamble` at lines 80-126. Spec: 9 it-blocks in `describe('detectPreamble')` including one per locale + null + non-string + anchor test, all green. |
| 4 | Split-table flags markers leaking into discussion AND tables spread across fragments | VERIFIED | `hasSplitTable` at lines 262-296 implements all 4 cases: (a) parser warning, (b) discussion leak, (c) intra-fragment imbalance, (d) global-balanced but per-fragment-split. Spec: 6 it-blocks, all green. |
| 5 | REF integrity compares the scribe-ref-N identifier SET (input vs fragments), ignoring visible-text edits | VERIFIED | `refIntegrity` at lines 329-337. Spec: 6 it-blocks covering preserved, dropped, fabricated, visible-text-change, footnote ids, and non-array defensive case. All green. |
| 6 | Samples persist in localStorage under SCRIBE_PROBE_v1 with versioned envelope, ring-buffer trim, and corrupt-corpus reset | VERIFIED | `readCorpus`/`writeCorpus`/`recordProbeSample` at lines 377-439. `PROBE_KEY = 'SCRIBE_PROBE_v1'`. Spec: version-mismatch reset, corrupt JSON reset, ring-buffer trim (205 inserts → <=200), all green. |
| 7 | exportCorpus/importCorpus round-trip JSON; importCorpus rejects shape/version mismatch | VERIFIED | `exportCorpus`/`importCorpus` at lines 447-470. Spec: round-trip test + import-throws-without-mutating test, both green. |
| 8 | replay recomputes metrics over stored samples with no LLM call (idempotent vs record-time) | VERIFIED | `replay` at lines 479-483: maps samples through `computeSampleMetrics`. Spec: replay-idempotence test asserts `replayed[0].metrics` equals record-time `computeSampleMetrics` output. Green. |
| 9 | Probe captures at BOTH surfaces (popover + chat) are gated behind isScribeDevMd() | VERIFIED | `ScribePopover.jsx` lines 120, 159-160: `isScribeDevMd()` guard + `recordProbeSample(parsed, { surface: 'popover', ... })`. `ScribeContext.jsx` lines 171-178: same pattern with `surface: 'chat'` and per-turn selection markdown. Both confirmed by grep. |
| 10 | DevPanelGrid has parsedResponse + probeMetrics panels; probeMetrics calls aggregate/exportCorpus/importCorpus; production pays zero cost | VERIFIED | `ScribeResultPanel.jsx` line 27: `DEV_PANEL_KEYS` includes both. Line 41: `DEV_PANEL_DEFAULTS` sets both to true. Lines 340-376: `parsedResponse` panel with highlight.js JSON. Lines 370-376: `ProbeMetricsPanel` calls `aggregate(replay())`, export/import wired. `ScribePanel.jsx` Sonde button gated behind `devMode` (line 89: `{devMode && ...}`). |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/scribeProbe.js` | Pure metrics + corpus + export/import/replay/aggregate + PREAMBLE_PATTERNS/PROBE_KEY/DUP_THRESHOLD | VERIFIED | 542 lines, 10 exports confirmed (5 metrics + 5 persistence). No React, no network, no new deps. |
| `src/modules/views/OnlyOffice/Scribe/scribeProbe.spec.js` | Jest spec locking all metric semantics | VERIFIED | 41 tests in `describe('scribeProbe')`, 5 nested describe blocks + corpus block. 79 total Scribe tests green. |
| `src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` | Inline capture call after parse, dev-gated | VERIFIED | `recordProbeSample` imported + called inside `isScribeDevMd()` guard at parse point. `parsedResponse` state threaded to DevPanelGrid. |
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` | Chat capture call after parse, dev-gated | VERIFIED | Two new imports (`isScribeDevMd`, `recordProbeSample`) + gated call with per-turn selection markdown. |
| `src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` | parsedResponse + probeMetrics panels in DevPanelGrid | VERIFIED | Both keys in `DEV_PANEL_KEYS`, labels, defaults=true. `ProbeMetricsPanel` exported. `parsedResponse` JSON panel with highlight.js. export/import controls. No metric math in component. |
| `.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md` | Durable go/no-go doc with [ASSUMED] thresholds, empty verdict, A/B-retired note | VERIFIED | Exists. 12 ASSUMED tags (>= 8 required). Verdict section has blank PASS/NO-GO placeholder. A/B RETIRED note present in Section 4. Empty evidence section. |
| `.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json` | Versioned {v:1,samples:[]} covering 5 locales + table + REF + broken-REF + preamble | VERIFIED | v=1, 11 curated samples. Locales: fr(×2), en(×3), de(×2), es(×2), it(×2). hasTable=2, hasRef=3. Sample index 9: scribe-ref-7 in inputMd but absent from fragment (broken-REF). Sample index 10: "Voici le texte réécrit" in fragment (preamble). |
| `.planning/ROADMAP.md` | Criterion 2 no longer mentions A/B | VERIFIED | `grep "A/B" ROADMAP.md` returns only line 115 (plan-list description meta-text, not criterion text). Criterion 2 lists: duplication, préambule par locale, table non scindée, REF préservés, répartition 0/1/N. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scribeProbe.spec.js` | `scribeProbe.js` | `from './scribeProbe'` | VERIFIED | Named import of all 10 exports confirmed at top of spec file. |
| `scribeProbe.js` | `scribeResponse.js` marker grammar | marker reuse (TABLE_OPEN_RE etc.) | VERIFIED | Marker constants defined at lines 205-208 of scribeProbe.js mirror scribeResponse.js grammar exactly; note `// Marker grammar reused EXACTLY from scribeResponse.js`. |
| `ScribePopover.jsx` | `scribeProbe.js` | `recordProbeSample` import + gated call | VERIFIED | Import at line 20, gated call at lines 159-160 inside `isScribeDevMd()`. |
| `ScribeContext.jsx` | `scribeProbe.js` | `recordProbeSample` import + gated call | VERIFIED | Import at line 13, gated call at lines 171-178 inside `isScribeDevMd()`. |
| `ScribeResultPanel.jsx` | `scribeProbe.js` | `aggregate`/`exportCorpus`/`importCorpus` in probeMetrics panel | VERIFIED | All three imported at lines 18-21; `ProbeMetricsPanel` calls `aggregate(replay())` at line 129; `exportCorpus()` at line 133; `importCorpus()` at line 154 with try/catch. |
| `v3.1-03-GATE.md` | `scribeProbe.js` thresholds | documented DUP_THRESHOLD/rate/coverage knobs | VERIFIED | GATE.md Section 2 thresholds table maps directly to `DUP_THRESHOLD`, `dupRate`, `preambleRate`, `splitTableCount`, `refBrokenCount`, `coverage` as confirmed in the GATE.md footer note. |
| `probe-corpus.json` | `scribeProbe.replay`/`importCorpus` | versioned {v,samples} envelope | VERIFIED | `node -e` verify script passes: v===1, Array.isArray(samples) true, all 5 locales present. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `ProbeMetricsPanel` | `stats` (aggregate output) | `aggregate(replay())` — reads localStorage corpus via `readCorpus()` | Yes — real accumulated samples from both surfaces | FLOWING |
| `parsedResponse` panel in `ScribeResultPanel` | `parsedResponse` prop | Set in `ScribePopover` via `setParsedResponse(parsed)` after real LLM parse, dev-gated | Yes — real parsed LLM response | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Jest spec exits 0 (all 41 probe tests + 38 response tests) | `npx jest scribeProbe.spec.js scribeResponse.spec.js --silent` | 79 passed, 0 failed, 0.531s | PASS |
| probe-corpus.json parses with correct shape and all 5 locales | `node -e "const c=require('...')"` verify script | "corpus OK 11 samples" — v=1, all locales present | PASS |
| Zero new npm dependencies | `grep -E "string-similarity\|fast-dice\|leven\|natural" scribeProbe.js \| wc -l` | 0 | PASS |
| 10 metric + persistence function exports (5+5) | `grep -c "export function ..."` | 5 metrics + 5 persistence = 10 | PASS |
| recordProbeSample gated in ScribePopover | `grep -n "recordProbeSample\|isScribeDevMd" ScribePopover.jsx` | import + guard at line 120 + call at line 159-160 | PASS |
| recordProbeSample gated in ScribeContext | `grep -n "recordProbeSample\|isScribeDevMd" ScribeContext.jsx` | two new imports + guard at line 171 | PASS |
| GATE.md has >= 8 ASSUMED tags | `grep -c "ASSUMED" v3.1-03-GATE.md` | 12 | PASS |

---

### Probe Execution

Step 7c: SKIPPED — this phase delivers a measurement library and dev panel, not a standalone executable script. No `probe-*.sh` declared in plan or discoverable at `scripts/*/tests/`. The behavioral analog (Jest spec) was run directly and passed in Step 7b.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PROBE-01 | Plans 01, 02, 03 | Conformance probe: five metrics over parsed Scribe responses, corpus persistence/replay, dev panel on both surfaces, GATE.md with documented thresholds | SATISFIED | scribeProbe.js (10 exports), scribeProbe.spec.js (41 tests), both-surface capture in ScribePopover + ScribeContext, DevPanelGrid panels in ScribeResultPanel, v3.1-03-GATE.md, probe-corpus.json |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | No TODO/FIXME/XXX/TBD/placeholder in any phase-modified file. No stub returns in probe code. No hardcoded empty data passed to rendering. |

One pre-existing test failure noted in SUMMARY: `ScribeContainer.spec.jsx` fails due to Phase 16 responsive-drawer changes — not introduced by this phase, out of scope. The phase-relevant specs (scribeProbe.spec.js, scribeResponse.spec.js) all pass.

---

### Human Verification Required

The following item requires human confirmation. It is already documented as APPROVED (2026-06-17, 7/7 steps) in `v3.1-03-02-SUMMARY.md` but is listed here per the verification process:

#### 1. Probe panels render and capture on both surfaces (runtime)

**Test:** Enable `SCRIBE_DEV_MD=true`, run an inline Scribe action, open the result panel. Confirm "Réponse parsée (contrat)" shows syntax-highlighted `{discussion, fragments, valid, fellBack, warnings}`; "Métriques + couverture (sonde)" shows total=1 with rates/dist/coverage. Then use the chat panel, confirm corpus increments. Export produces `{v:1,samples:[...]}`. Import of a bad shape shows an error without wiping the corpus. Panels absent with `SCRIBE_DEV_MD` unset.

**Expected:** All 7 how-to-verify steps from the plan pass.

**Why human:** Runtime UI behavior — requires the live OO/Drive stack and browser interaction.

**Note:** ALREADY APPROVED by the developer on 2026-06-17 (documented in v3.1-03-02-SUMMARY.md, Task 3 section). A refinement commit (`a96206f4d`) followed, adding the side-panel Sonde button in `ScribePanel.jsx` (dev-gated behind `devMode`) and clearer metric labels. The 79 Scribe specs remain green after that commit.

---

## Gaps Summary

No gaps found. All 10 observable truths verified, all artifacts exist and are substantively implemented, all key links wired, all data flows confirmed. The phase goal — delivering the GATE apparatus — is achieved. The verdict slot in GATE.md is intentionally blank, which is correct per the phase design (verdict is a downstream human activity, explicitly out of scope).

The `human_needed` status reflects process protocol (runtime UI was human-verified during execution), not a code deficiency.

---

_Verified: 2026-06-17T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
