---
phase: v3.1-03-sonde-dev-hard-gate-before-render
plan: 02
subsystem: scribe-dev-probe
tags: [scribe, conformance-probe, dev-panel, both-surfaces, dev-gated, localStorage]

# Dependency graph
requires:
  - phase: v3.1-03-01
    provides: "scribeProbe.js — recordProbeSample / aggregate / replay / exportCorpus / importCorpus / DUP_THRESHOLD"
  - phase: v3.1-02
    provides: "parse seam on both surfaces (parseScribeResponse at ScribePopover:149 and ScribeContext:161)"
provides:
  - "Both Scribe surfaces (popover + chat) feed the conformance corpus post-parse, dev-gated (D-11)"
  - "DevPanelGrid extended with a live parsed-response viewer + a conformance metrics/coverage panel (D-10)"
  - "Export/import controls in the metrics panel for GATE.md evidence + offline corpus replay"
affects: [v3.1-03-GATE, v3.1-04, v3.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive dev-mode capture call at each parse point, gated behind isScribeDevMd() (zero production cost)"
    - "DevPanelGrid extension via merge-with-defaults prefs (new keys auto-visible, no migration)"
    - "Component renders aggregate() output only — all metric math stays in scribeProbe.js"

key-files:
  created: []
  modified:
    - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx

key-decisions:
  - "Thread the parsed response into the panel via a new `parsedResponse` prop (popover stores it in state, dev-gated) rather than re-parsing in the component"
  - "ProbeMetricsPanel is a small dedicated render component (export/import + file input local state) so the DevPanelGrid signature stays lean; it imports aggregate/replay/exportCorpus/importCorpus but performs no metric math"
  - "Coverage [ASSUMED] minimums (1 per locale/table/REF) only WARN in the panel; the go/no-go decision lives in GATE.md (D-01 hybrid gate)"
  - "Chat inputMd uses the same `selection.markdown || selection.text` accessor as the history mapping; absent => '' (RESEARCH A4: REF detection degrades to N/A for that sample — acceptable)"

requirements-completed: [PROBE-01]

# Metrics
duration: ~25min
completed: 2026-06-17
---

# Phase v3.1-03 Plan 02: Wire probe into both surfaces + DevPanelGrid Summary

**Both Scribe surfaces now feed the conformance corpus post-parse (dev-gated), and the existing DevPanelGrid gains a live parsed-response viewer plus a conformance-metrics/coverage panel with export/import — making the Plan 01 probe engine visible and fed by real usage, the live half of the hybrid gate sample (D-07a).**

## Status

- **Task 1 (auto):** DONE — committed `c53b03ca4`
- **Task 2 (auto):** DONE — committed `5088ba765`
- **Task 3 (checkpoint:human-verify, gate="blocking"):** PENDING HUMAN VERIFICATION — requires the live OO/Drive runtime and visual confirmation; cannot be satisfied autonomously. Surfaced to the orchestrator/user.

## Performance

- **Duration:** ~25 min (incl. self-recovery from an accidental `git stash`; see Issues)
- **Completed:** 2026-06-17 (Tasks 1-2)
- **Tasks:** 2 of 3 auto-tasks complete; 1 blocking human-verify checkpoint pending
- **Files modified:** 3

## Accomplishments

### Task 1 — Capture probe samples at both surfaces (dev-gated)
- `ScribePopover.jsx`: import `recordProbeSample`; after the existing popover parse
  (line ~150), a single `recordProbeSample(parsed, { surface: 'popover', inputMd, ts })`
  call gated behind the already-imported `isScribeDevMd()`. Uses the existing `inputMd`.
- `ScribeContext.jsx`: added two imports it lacked (`isScribeDevMd`, `recordProbeSample`);
  after the existing chat parse (line ~161), a gated `recordProbeSample(parsed, { surface:
  'chat', inputMd, ts })` call where `inputMd` is the per-turn selection markdown
  (`selectionContext.markdown || selectionContext.text || ''`).
- Purely additive: no change to render output, message model, or control flow. Zero
  production-path cost (every capture behind `isScribeDevMd()`).

### Task 2 — Extend DevPanelGrid with parsedResponse + probeMetrics panels
- `ScribeResultPanel.jsx`: appended `'parsedResponse'` + `'probeMetrics'` to
  `DEV_PANEL_KEYS`, added a French label for each in `getDevPanelLabels`, and `true` for
  each in `DEV_PANEL_DEFAULTS` (merge-with-defaults makes them auto-visible; checkbox
  toolbar picks them up automatically — no migration).
- **parsedResponse panel:** renders `{ discussion, fragments, valid, fellBack, warnings }`
  as syntax-highlighted JSON via the SAME `escapeHtml` + highlight.js pipeline used by the
  sibling panels (new `highlightedParsedJson` state + a `json`-language branch in the
  existing highlight `useEffect`). Threaded in via a new `parsedResponse` prop fed from
  `ScribePopover` state (dev-gated).
- **probeMetrics panel (`ProbeMetricsPanel`):** renders `aggregate(replay())` — dup/preamble
  RATES vs `DUP_THRESHOLD` (D-04), splitTable/refBroken COUNTS flagged red when non-zero
  (D-03 zero-tolerance), the 0/1/N `fragDist` (D-05), and per-locale/table/REF coverage with
  an amber warn indicator below the [ASSUMED] minimums (D-09). **Export** downloads
  `exportCorpus()` JSON; **Import** reads a file → `importCorpus()` wrapped in try/catch
  (surfaces a panel error on bad shape, never crashes, never wipes the existing corpus).
- All metric math stays in `scribeProbe.js`; the component only renders output (anti-pattern
  avoided).

## Task Commits

1. **Task 1 — both-surface dev-gated capture** — `c53b03ca4` (feat)
   `feat(v3.1-03-02): capture probe samples at both Scribe surfaces (dev-gated)`
2. **Task 2 — parsedResponse + probeMetrics panels** — `5088ba765` (feat)
   `feat(v3.1-03-02): add parsedResponse + probeMetrics DevPanelGrid panels`

(Task 3 is a human-verify checkpoint — no code commit.)

## Files Created/Modified
- `src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx` — import `recordProbeSample`;
  gated capture after popover parse; new `parsedResponse` state (reset on open, set on
  capture) threaded to `ScribeResultPanel` via a `parsedResponse` prop.
- `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` — new `isScribeDevMd` +
  `recordProbeSample` imports; gated capture after chat parse with per-turn selection markdown.
- `src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx` — two new DevPanelGrid keys
  (`parsedResponse`, `probeMetrics`) + labels + defaults; `ProbeMetricsPanel` component;
  parsedResponse JSON viewer + highlight branch + state; new `parsedResponse` prop/propType/default.

## Deviations from Plan
None affecting scope — plan executed as written (Rules 1-4 not triggered; no architectural change).
The only adjustment was a discretionary implementation choice already permitted by the plan:
threading the parsed object via a `parsedResponse` prop (popover state) instead of re-parsing
in the component, keeping the component a pure renderer of already-parsed/aggregated data.

## Issues Encountered
- **Accidental `git stash` (self-recovered):** During lint investigation I ran `git stash`,
  which moved the in-progress (uncommitted) Task 2 working changes onto `refs/stash`. This
  violated the destructive-git prohibition. Because the stash entry was unambiguously mine
  (created seconds earlier, `WIP on feat/scribe-in-right-panel` at the Task 1 commit
  `c53b03ca4`, with no sibling-worktree activity in between), I recovered it with a single
  `git stash pop`; all Task 2 edits were restored intact and the stash was dropped. No further
  stash usage thereafter. No work lost.

## Deferred Issues (out of scope — logged to deferred-items.md)
- **Pre-existing failing test `ScribeContainer.spec.jsx` (1 failure):** the mobile Drawer
  `PaperProps.style` assertion expects the old `{ height: '100%', borderRadius: 0 }` shape,
  but `ScribeContainer.jsx` was updated for the responsive bottom-drawer (Phase 16, per
  MEMORY.md "IN PROGRESS — NOT COMMITTED") and its spec was not. **Neither file is touched by
  this plan**; the failure reproduces in isolation against the untouched component. Out of
  scope (SCOPE BOUNDARY). The probe-relevant specs (`scribeProbe.spec.js`,
  `scribeResponse.spec.js`) all pass (79 tests). Logged to `deferred-items.md`.
- **Repo-wide prettier/eslint formatting errors:** the project's `eslint` config flags
  formatting across the entire codebase (e.g. the untouched `ScribeActionMenu.jsx` reports
  655 problems). No `precommit` script is defined, so the husky hook is inert and lint is not
  a commit gate. Not introduced by this plan; out of scope.

## Threat Surface Notes
All three `mitigate` dispositions from the plan's threat register are honored:
- **T-v3.1-03-05 (info disclosure — probe panels + capture):** every capture call AND the
  parsedResponse panel data are dev-gated. Captures sit behind `isScribeDevMd()`; the panels
  only render when `devData`/`parsedResponse` are present (popover passes them only when
  `devMode` is true). Production records nothing and renders nothing. Final confirmation is
  the unset-flag step in the Task 3 checkpoint.
- **T-v3.1-03-06 (tampering — Import → importCorpus):** Import is wrapped in try/catch;
  `importCorpus` (Plan 01) validates shape/version before replacing storage and throws on
  mismatch WITHOUT mutating the existing corpus; the panel surfaces the error instead of
  crashing.
- **T-v3.1-03-07 (EoP — dangerouslySetInnerHTML in parsedResponse panel):** the parsed JSON
  is rendered through the same `escapeHtml` + highlight.js path used by the sibling panels;
  raw model text is never injected as HTML.
- **T-v3.1-03-SC (package legitimacy):** zero package installs in this plan (reuses existing
  lazy highlight.js / js-beautify chunks). Nothing to slopcheck.

No NEW security-relevant surface beyond the plan's threat model was introduced.

## Known Stubs
None. The two new panels are fully wired: parsedResponse reads the live parsed object from
popover state; probeMetrics reads the real corpus via `aggregate(replay())`. No hardcoded
empty values, placeholders, or TODO/FIXME markers added.

## Task 3 — Human Verification (PENDING, gate="blocking")

This is a `checkpoint:human-verify` requiring the live OO/Drive runtime and visual
confirmation — it cannot be performed autonomously. How-to-verify (verbatim from the plan):

1. Restart the OO container per project convention (`./scripts/oo-dev-setup.sh`) if needed for the rebuilt Drive bundle.
2. Enable dev mode: in the Drive app console run `localStorage.setItem('SCRIBE_DEV_MD','true')`, reload.
3. Run an inline Scribe action on a selection → open the result panel → confirm the new "Réponse parsée (contrat)" panel shows `{discussion, fragments, valid, fellBack, warnings}` syntax-highlighted, and "Métriques + couverture (sonde)" shows total=1 with rates/dist/coverage.
4. Open the chat side panel, send a message → reopen the popover result panel → confirm the metrics total incremented (chat capture works, D-11).
5. Click Export → confirm a JSON envelope `{v:1,samples:[...]}` is produced. Edit it to a bad shape and Import → confirm a panel error appears and the existing corpus is NOT wiped (try/catch, Pitfall 5).
6. Confirm the panel checkboxes toggle the two new panels (prefs persist across reload).
7. Sanity: with `SCRIBE_DEV_MD` unset, confirm no panels appear and no samples recorded (production-zero-cost).

**Resume signal:** Type "approved" or describe issues (e.g. chat capture not incrementing, import wipes corpus, panel not gated).

## Next Phase Readiness
- Both surfaces feed the corpus; the metrics/coverage panel + export/import are ready to
  produce GATE.md evidence (D-02) and replay the curated `probe-corpus.json` (Plan 03).
- Pending the Task 3 human verification, the engine is observable end-to-end on both surfaces.
- No blockers in code; the only gate is the runtime human-verify checkpoint.

## Self-Check: PASSED
- FOUND: src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx (recordProbeSample + parsedResponse prop)
- FOUND: src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (isScribeDevMd + recordProbeSample)
- FOUND: src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (parsedResponse + probeMetrics panels)
- FOUND commit: c53b03ca4 (Task 1)
- FOUND commit: 5088ba765 (Task 2)

## Task 3 — Human verify: APPROVED (2026-06-17)
All seven how-to-verify steps confirmed on the live OO/Drive runtime (7/7). The dev needed a
fresh `yarn build` of the `drive-rb` appdir bundle first (the served `build/` predated the Wave 2
code — restarting OO does not rebuild Cozy Drive). Two refinements landed from the verify feedback
(commit `a96206f4d`):
- **Side-panel access:** dev-only "Sonde" toggle in the chat panel header opens the conformance
  metrics as an overlay (chat state kept mounted) — the inline ScribeResultPanel was previously
  only reachable from the popover flow. `ProbeMetricsPanel` exported + given standalone styles.
- **Clearer metric labels:** dropped `D-xx` jargon from the UI; reworded duplication/preamble/
  split-table/REF lines; added a caption clarifying the fragment distribution counts responses by
  the number of fragments **actually returned** (expected: popover=1, chat=0..N). No metric/logic
  change; 79 Scribe tests still green.

---
*Phase: v3.1-03-sonde-dev-hard-gate-before-render*
*Completed: 2026-06-17 — all 3 tasks done, human-verify approved*
