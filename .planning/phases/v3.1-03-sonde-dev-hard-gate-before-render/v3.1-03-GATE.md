# v3.1-03 GATE — Sonde dev conformance go/no-go

**Status:** PASS (verdict pronounced 2026-06-19 — see §6)
**Created:** 2026-06-17
**Source of truth (D-02):** This file is the durable, versioned record of the gate's passing
criteria AND its verdict. It is cited as `Depends on` by Plans v3.1-04 and v3.1-05 and survives
`/clear`. STATE.md / ROADMAP.md track *progress*; this file holds the *decision*.

---

## 1. Gate model (D-01 — hybrid)

The gate is **hybrid**:

- The **probe** (`scribeProbe.js`, dev-only) computes each conformance metric over an accumulated
  sample and raises **flags against the documented thresholds in §2** for objective failures.
- The **user pronounces the final go/no-go** (§6) after reviewing an evidence sample (§7).

Thresholds catch the objective defects (broken REF, split table, systemic duplication, systemic
preamble leakage). Human judgment covers the residual subjective quality that no numeric threshold
can capture. This is the appropriate model for a one-shot gate against a non-deterministic generic
model.

**Every numeric value in this document is tagged `[ASSUMED]` until the user freezes it against
real observed data.** The "Frozen value" column is intentionally left blank for the user to fill.

---

## 2. Thresholds (the knobs to freeze)

| # | Knob | Proposed (`[ASSUMED]`) | Frozen value | Type | Decision rule |
|---|------|------------------------|--------------|------|---------------|
| 1 | Duplication per-response threshold (word-bigram Dice, 0..1) | `0.6` `[ASSUMED]` | **`0.6`** (frozen 2026-06-19) | per-response | A response counts as "duplicating" when `maxFragmentDuplication(discussion, fragments) >= threshold` |
| 2 | Duplication NO-GO rate (D-04) | `> 15%` of sample `[ASSUMED]` | **`> 15%`** (frozen) | rate over sample | NO-GO if the fraction of duplicating responses exceeds this rate |
| 3 | Preamble NO-GO rate (D-04) | `> 15%` of sample `[ASSUMED]` | **`> 15%`** (frozen) | rate over sample | NO-GO if the fraction of preamble-leaking responses exceeds this rate |
| 4 | Split table (D-03.2) | `0` tolerance `[ASSUMED]` | **`0`** (frozen) | per-sample count | NO-GO on **any** breach (hard blocker) |
| 5 | Broken REF / cross-ref (D-03.1) | `0` tolerance `[ASSUMED]` | **`0`** (frozen) | per-sample count | NO-GO on **any** breach (hard blocker) |
| 6 | Coverage — `N` total (D-09) | `>= 30` `[ASSUMED]` | **`>= 30`** (frozen) | precondition | Verdict not credible below this total sample size |
| 7 | Coverage — per locale fr/en/de/es/it (D-09) | `>= 3` each `[ASSUMED]` | **`>= 3`** each (frozen) | precondition | Each of the 5 locales must reach this count |
| 8 | Coverage — `M` table cases (D-09) | `>= 5` `[ASSUMED]` | **`>= 5`** (frozen) | precondition | Tables are highest split-risk content |
| 9 | Coverage — `M` REF cases (D-09) | `>= 5` `[ASSUMED]` | **`>= 5`** (frozen) | precondition | REF integrity is a hard blocker — needs real exposure |

> **Frozen 2026-06-19:** all 9 knobs frozen at their proposed `[ASSUMED]` values — the v2 evidence
> (§7) cleared each by a wide margin (0% on both rate blockers, 0 on both hard blockers, coverage
> well above every minimum), so no recalibration was warranted.

**Rationale (from RESEARCH "Proposed initial numeric values"):**
- Dice `0.6`: above ~0.6 bigram overlap usually means substantive copy, not shared stock phrases.
- `>15%` rate on the two quantitative blockers tolerates an isolated non-deterministic miss while
  flagging systemic copying / preamble leakage.
- `0` tolerance on split-table and broken-REF: these are structural / data-corruption failures.
- Coverage `>=30` total makes a 15% rate threshold statistically meaningful; per-locale `>=3` is the
  minimum to observe per-locale preamble behavior; `>=5` table and `>=5` REF cases force real
  exposure of the two hard-blocker surfaces.

These knobs map directly to `scribeProbe.js` constants and `aggregate()` outputs
(`DUP_THRESHOLD`, `dupRate`, `preambleRate`, `splitTableCount`, `refBrokenCount`, `coverage`).

---

## 3. Hard blockers (D-03)

Four metrics are **hard blockers** — a breach (per the rule in §2) is a NO-GO regardless of the
subjective human judgment:

1. **Broken REF / cross-refs** (D-03.1) — a `scribe-ref-N` (or `[^scribe-fn-N]` footnote)
   identifier is dropped from the output or fabricated. Identifier corruption = data corruption.
   **Zero tolerance** (threshold #5). Only the identifier is immutable; visible text may legitimately
   change (translation/edit) and is NOT counted as a breach.
2. **Split table** (D-03.2) — a table that should stay whole is cut between `discussion` and a
   fragment, or spread across fragments, or any `[TABLE:|[/TABLE]|[CELL:|[/CELL]` marker leaks into
   `discussion` (markers must live inside fragments only). **Zero tolerance** (threshold #4).
3. **Duplication discussion↔fragment** (D-03.3, quantitative D-04) — a fragment recopies all/part of
   the discussion. Measured per-response by word-bigram Dice; NO-GO is **rate-based** (threshold #2).
4. **Preamble in fragment** (D-03.4, quantitative D-04) — a fragment opens with a meta-referential
   preamble ("Voici le texte…", "Here is…", per locale) instead of pure content. NO-GO is
   **rate-based** (threshold #3).

---

## 4. Indicative metrics (D-05)

These are **descriptive** — displayed in the probe panel, NOT blocking:

- **Fragment distribution 0 / 1 / N** — the count of responses with 0, 1, or 2+ fragments. Useful to
  read how the model behaves per surface (popover should trend to 1; chat 0..N), but it never on its
  own forces a verdict.

**RETIRED metric (D-06):** The **A/B prose-quality comparison** ("comparaison A/B qualité de prose
vs ancien chemin plain", originally ROADMAP criterion 2) is **RETIRED** and intentionally NOT
implemented. Reasons: (a) no stable "old level" baseline exists to compare against — the old plain
path never measured or froze any prose-quality level, so the target is a moving, non-deterministic
one; (b) the real, concrete degradations (preamble, duplication, split table, broken REF) are already
covered by the objective hard blockers in §3. Building baseline machinery for an advisory, ill-defined
metric is not worth the cost. Side benefit: corpus replay (D-08) is LLM-free, because every surviving
metric is a pure function of stored text.

---

## 5. Coverage precondition (D-09)

The verdict in §6 is **not credible** until the accumulated sample reaches the minimum coverage in
thresholds #6–#9. The probe **displays achieved coverage** (per-locale counters, table-case count,
REF-case count, total) and **warns/blocks** if a verdict is pronounced below these minimums.

| Coverage dimension | Required (`[ASSUMED]`) | Achieved | Met? |
|--------------------|------------------------|----------|------|
| Total responses `N` | `>= 30` | **46** | ✅ yes |
| Locale `fr` | `>= 3` | **9** | ✅ yes |
| Locale `en` | `>= 3` | **17** | ✅ yes |
| Locale `de` | `>= 3` | **7** | ✅ yes |
| Locale `es` | `>= 3` | **7** | ✅ yes |
| Locale `it` | `>= 3` | **6** | ✅ yes |
| Table cases `M` | `>= 5` | **8** | ✅ yes |
| REF cases `M` | `>= 5` | **7** | ✅ yes |

The curated seed corpus (`probe-corpus.json`, D-07b) guarantees the critical-case coverage that
spontaneous live usage (D-07a) tends to miss — tables, REF, and the 5 locales.

---

## 6. Verdict

> **DO NOT pre-fill.** The user pronounces PASS or NO-GO after reviewing §7 and confirming the
> coverage precondition (§5) is met and no hard blocker (§3) is breached.

- **Verdict:** **PASS**
- **Decided by:** Benibur
- **Date:** 2026-06-19
- **Frozen thresholds confirmed:** yes — all 9 values entered in §2 "Frozen value" column.
- **Decision rests on the hardened `v2` contract**, NOT the old `v1` baseline (a measured NO-GO on
  the same real data). **`v2` is now ADOPTED in production** via the unification refactor (2026-06-19):
  `scribeAI.RESPONSE_CONTRACT_CORE` (the shared hardened core) + per-surface cardinality clauses
  replace the old `RESPONSE_CONTRACT_BLOCK` and the divergent hand-written chat prompt. Both surfaces
  now compose the SAME gate-validated separation rules. See §8 for the chat re-validation.
- **Chat-surface verdict: PASS conditional on the v3.1-05 re-ask** (see §8). The unification gives the
  chat the marker-preservation rules it previously lacked entirely, but a non-deterministic tail
  remains (a footnote glued to an edited word is occasionally dropped). The probe already DETECTS this
  (`refIntegrity`); the planned v3.1-05 re-ask is the mitigation. Card rendering may proceed.
- **Notes / subjective judgment:**
  - The decisive evidence is the v1→v2 contrast on the **same real table/REF inputs** captured via
    the IHM (§7): v1 leaked preamble (21.4%) and split a table (1); v2 produced 0% / 0% / 0 / 0
    across all four blockers with full coverage. The fix is the v2 contract's explicit separation
    of `discussion` from `fragments`.
  - **One residual quality issue (non-blocking):** 1/46 v2 responses (~2%, a REF case) wrapped its
    JSON in a ```` ```json ```` fence, so the strict parser fell back to the contextual fallback
    (no crash, REF preserved, no threshold breached). `fellBack` is not a gate threshold, so this
    does not block PASS — but it is a candidate for a v3 contract hardening ("never wrap the JSON in
    a code fence") and/or the re-ask path planned for v3.1-05.

If the verdict is **PASS**, Plans v3.1-04 and v3.1-05 may proceed to build fragment cards / popover
render. If **NO-GO**, the contract or prompts must be revisited before any card rendering.

---

## 7. Evidence sample

The full accumulated sample (46 responses, both surfaces, prompt `v2`) is stored alongside this
doc as **`gate-evidence-v2.json`** (versioned envelope `{v:1, samples:[…]}`, re-scorable LLM-free
via `scribeProbe.replay/aggregate`). The aggregates below are the durable summary.

**How the evidence was produced (2026-06-19):**
- Real table/REF/footnote inputs captured via the IHM dev panel (`SCRIBE_DEV_MD`) on a live
  OnlyOffice/Drive session → exported with `exportCorpus()`.
- Real inputs extracted into `scripts/gate-harness/captured/live-table-ref.json` (`source:"live"`),
  kept alongside the synthetic `table-ref-seed.json` seeds.
- Replayed through the gate harness against the live cozy-stack LLM proxy
  (`Mistral-Small-3.2-24B-Instruct-2506-FP8`) under both prompt variants:
  `node scripts/gate-harness/run.mjs --variant {v1|v2} --domain alice.localhost:8080`.
- Scored by the **production** `parseScribeResponse` + `scribeProbe` (loaded from `src/` at runtime
  — zero drift; `assertPromptInSync()` confirmed the harness prompt matches live source).

**`v2` aggregate (the PASS evidence) — N=46, 0 failures:**

```
model          : Mistral-Small-3.2-24B-Instruct-2506-FP8
samples        : 46   (failures: 0)
dupRate        : 0.0%    (frozen NO-GO if > 15%)   → PASS
preambleRate   : 0.0%    (frozen NO-GO if > 15%)   → PASS
splitTableCount: 0       (frozen NO-GO if > 0)     → PASS
refBrokenCount : 0       (frozen NO-GO if > 0)     → PASS
fragDist       : 0=0 1=46 N=0
coverage       : perLocale={en:17, fr:9, de:7, es:7, it:6}  tables=8  refs=7   → all minimums met
fellBack       : 1/46 (~2%, a REF case wrapped in a ```json fence) — non-blocking, see §6 notes
```

**`v1` aggregate (the deployed baseline — measured NO-GO) — real IHM session, N=14:**

```
samples        : 14
dupRate        : 14.3%
preambleRate   : 21.4%   → NO-GO (> 15%)
splitTableCount: 1       → NO-GO (> 0)   [the same real table input v2 handles cleanly]
refBrokenCount : 0
coverage       : tables=4  refs=2
```

The contrast is the core of the verdict: on the **same real table/REF inputs**, `v1` breaches two
hard/rate blockers while `v2` clears all four with margin. Hence PASS — conditional on adopting `v2`
as the production prompt (§6).

---

## 8. Chat-surface re-validation (unification, 2026-06-19)

The original §6/§7 verdict rested on **inline (popover)** evidence only — the chat surface used a
SEPARATE, divergent prompt (`CHAT_SYSTEM_PROMPT`) that the harness never exercised and that carried
**no marker-preservation rules at all**. To close that gap, the contract was unified: a single shared
`RESPONSE_CONTRACT_CORE` (the hardened v2 separation) + a per-surface cardinality clause
(`CARDINALITY_INLINE` = exactly 1 fragment / `CARDINALITY_CHAT` = 0..N) + shared
`markerPreservationClauses()`, consumed by both `buildMessages` (inline) and `buildChatSystemPrompt`
(chat). The gate harness was extended with a chat path + 6 chat fixtures; `assertPromptInSync` updated.

**Unified re-validation — N=52 (6 chat), prompt = unified prod, `Mistral-Small-3.2-24B`:**

```
dupRate        : 0.0%    → PASS
preambleRate   : 0.0%    → PASS
splitTableCount: 0       → PASS   (incl. chat table edit)
refBrokenCount : 1       → 1 flaky chat breach (see below)
fragDist       : 0=0 1=51 N=1
coverage       : perLocale={en:19, fr:13, de:7, es:7, it:6}  tables=9  refs=9
fellBack       : 1/52 (popover REF wrapped in ```json fence; REF preserved) — non-blocking
```

Evidence: `gate-evidence-unified.json` (alongside this doc).

**The single refBroken (chat `chat-fn-fr-1`):** on "Corrige les fautes", the model corrected
`essai`→`essais` and dropped the `[^scribe-fn-1]` footnote glued to that word. A 6× determinism probe
on this exact case: **1/6 dropped, 5/6 preserved** — a non-deterministic tail at temp 0.3, NOT a
systemic failure. The marker clause works the large majority of the time; a footnote attached to an
edited token is the residual hard case.

**Disposition (user-approved 2026-06-19):** the zero-tolerance refBroken rule (#5) is met
deterministically for tables and REF; the footnote-on-edited-word tail is accepted as the **motivating
case for the v3.1-05 re-ask** (the probe already detects the dropped identifier — the re-ask re-prompts
on breach). The unification is a strict improvement over the prior chat prompt (which had zero
protection), so it ships now; card rendering (v3.1-04/05) may proceed, with the re-ask as the safety net.

---

*Phase: v3.1-03-sonde-dev-hard-gate-before-render*
*Gate doc (D-02). Knobs map to scribeProbe.js: DUP_THRESHOLD, MAX_SAMPLES, aggregate() rates/coverage.*
