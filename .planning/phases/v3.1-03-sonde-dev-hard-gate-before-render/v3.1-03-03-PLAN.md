---
phase: v3.1-03-sonde-dev-hard-gate-before-render
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md
  - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json
  - .planning/ROADMAP.md
autonomous: true
requirements: [PROBE-01]

must_haves:
  truths:
    - "A durable, versioned GATE.md exists with documented (numeric, [ASSUMED]) thresholds, a verdict slot, and an evidence-sample slot"
    - "A curated probe-corpus.json seed exists covering tables, REF, and the 5 locales (fr/en/de/es/it)"
    - "ROADMAP criterion 2 for Phase v3.1-03 no longer mentions the retired A/B prose-quality metric"
  artifacts:
    - path: ".planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md"
      provides: "Durable go/no-go gate doc cited as Depends-on by v3.1-04/05"
      contains: "NO-GO"
    - path: ".planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json"
      provides: "Replayable curated seed corpus (versioned envelope)"
      contains: "\"v\": 1"
    - path: ".planning/ROADMAP.md"
      provides: "Criterion-2 aligned with real scope (A/B removed)"
      contains: "v3.1-03"
  key_links:
    - from: "v3.1-03-GATE.md"
      to: "scribeProbe.js thresholds"
      via: "documented DUP_THRESHOLD / rate / coverage knobs"
      pattern: "threshold|coverage|rate"
    - from: "probe-corpus.json"
      to: "scribeProbe.replay/import"
      via: "versioned {v,samples} envelope shape"
      pattern: "samples"
---

<objective>
Produce the durable gate artifacts: scaffold `v3.1-03-GATE.md` (the source-of-truth go/no-go doc,
D-02) with documented `[ASSUMED]` thresholds for the user to freeze against real data, seed a
curated `probe-corpus.json` (D-07b) covering tables/REF/5-locales for offline replay, and make the
one-line ROADMAP criterion-2 edit retiring the A/B prose-quality metric (D-06).

Purpose: GATE.md is the citable `Depends on` for v3.1-04/05 and survives `/clear`. The curated
corpus guarantees critical-case coverage (D-09). The ROADMAP edit aligns the documented success
criteria with the real scope.
Output: GATE.md + probe-corpus.json (new), ROADMAP.md (edited). Docs only — no source code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-CONTEXT.md
@.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-RESEARCH.md

<interfaces>
<!-- Curated corpus must match the versioned envelope scribeProbe.importCorpus/replay expects -->
Envelope shape (RESEARCH section "Curated corpus JSON schema"):
  { "v": 1, "samples": [ { ts, surface, inputMd, discussion, fragments, valid, fellBack,
    warnings, tags: { locale, hasTable, hasRef, curated } } ] }
  - tags.curated:true distinguishes deliberately-run cases (D-07b) from live accumulation (D-07a)
  - REF marker grammar in inputMd/fragments: {{REF:scribe-ref-N:visible text}}
  - table markers (inside fragments only): [TABLE:N] ... [/TABLE], [CELL:r,c] ... [/CELL]
  - fragment position marker in discussion: {{fragment:0}}

<!-- Proposed [ASSUMED] thresholds to document in GATE.md (RESEARCH "Proposed initial numeric values") -->
  Duplication per-response threshold (Dice): 0.6
  Duplication NO-GO rate: >15% of sample
  Preamble NO-GO rate: >15% of sample
  Split table / broken REF: 0 tolerance (any breach = NO-GO)
  Coverage N total: >=30 ; per locale: >=3 each ; table cases M: >=5 ; REF cases M: >=5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold v3.1-03-GATE.md (durable go/no-go doc, D-02)</name>
  <files>.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md</files>
  <read_first>
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-CONTEXT.md (D-01 hybrid gate, D-02 recording, D-03 hard blockers, D-04 quantitative severity, D-05 indicative, D-09 coverage)
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-RESEARCH.md (section "Proposed initial numeric values" — the [ASSUMED] knobs)
  </read_first>
  <action>
    Create `v3.1-03-GATE.md` as the durable source-of-truth gate doc (D-02). Structure it with:
    (1) a header explaining the hybrid model (D-01): the probe raises flags against documented
    thresholds for objective failures, the user pronounces the final go/no-go after reviewing a
    sample. (2) A "Thresholds" table listing every knob with its proposed value tagged `[ASSUMED]`
    and a "frozen value" column left blank for the user: duplication per-response Dice threshold
    (0.6), duplication NO-GO rate (>15%), preamble NO-GO rate (>15%), split-table (0 tolerance),
    broken-REF (0 tolerance), and the D-09 coverage minimums (N total >=30, >=3 per locale across
    fr/en/de/es/it, >=5 table cases, >=5 REF cases). (3) A "Hard blockers (D-03)" section naming
    the four: broken REF, split table, duplication (rate, D-04), preamble (rate, D-04). (4) An
    "Indicative metrics (D-05)" section noting the 0/1/N fragment distribution is descriptive, and
    explicitly recording that the A/B prose-quality metric is RETIRED (D-06). (5) A "Coverage
    precondition (D-09)" section. (6) An empty "Verdict" section with a PASS / NO-GO placeholder and
    a date/decider line for the user to fill. (7) An empty "Evidence sample" section to paste the
    `exportCorpus()` JSON + aggregate output. Every numeric value MUST carry `[ASSUMED]` until the
    user freezes it. Do not invent a verdict — leave it blank. Write directive prose + tables; the
    only fenced block needed is the empty evidence placeholder.
  </action>
  <verify>
    <automated>test -f .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md && grep -c "ASSUMED" .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md && grep -c "NO-GO" .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path `.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md`
    - Contains a thresholds table with the four hard blockers + coverage minimums, every numeric value tagged `[ASSUMED]`
    - Contains an explicit note that the A/B prose-quality metric is RETIRED (D-06)
    - Contains an empty Verdict section (PASS / NO-GO placeholder, no verdict pronounced) and an empty Evidence-sample section
    - `grep -c "ASSUMED"` returns >= 8 (one per documented knob)
  </acceptance_criteria>
  <done>GATE.md scaffolded with documented [ASSUMED] thresholds, hard-blocker + coverage sections, A/B-retired note, and empty verdict/evidence slots for the user to freeze against real data.</done>
</task>

<task type="auto">
  <name>Task 2: Seed curated probe-corpus.json + retire A/B in ROADMAP criterion 2</name>
  <files>.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-RESEARCH.md (section "Curated corpus JSON schema"; "Code Examples" REF/table grammars)
    - .planning/ROADMAP.md (Phase v3.1-03 Success Criteria, criterion 2 — line ~109)
    - .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-CONTEXT.md (D-06 A/B retired action; D-07b curated corpus; D-09 coverage)
  </read_first>
  <action>
    Create `probe-corpus.json` as a versioned envelope `{ "v": 1, "samples": [...] }` matching the
    exact shape scribeProbe.importCorpus/replay expects. Seed deliberately-curated samples
    (`tags.curated: true`) covering the critical cases live usage misses (D-07b/D-09): at least one
    sample per locale fr/en/de/es/it (set `tags.locale`), at least two table cases
    (`tags.hasTable: true`, with `[TABLE:N]...[/TABLE]` and `[CELL:r,c]...[/CELL]` markers inside a
    fragment), and at least two REF cases (`tags.hasRef: true`, with
    `{{REF:scribe-ref-N:visible text}}` present in both `inputMd` and a fragment so REF-preserved is
    exercised). Include one deliberately broken-REF case (input id missing from the fragment) and
    one deliberate preamble case (a fragment starting "Voici le texte…") so the spec/replay has
    positive detections to assert against. Keep each sample small but realistic; include
    `{{fragment:0}}` in the discussion where a fragment exists; set `surface` to a mix of
    `'popover'`/`'chat'`. These are seed fixtures, not real model output — they make the corpus a
    replayable regression asset for v3.1-04/05 and enable a deterministic LLM-free replay. Then
    edit `.planning/ROADMAP.md` Phase v3.1-03 Success Criterion 2: remove the clause "comparaison
    A/B qualité de prose vs ancien chemin plain" so the criterion lists only the surviving metrics
    (duplication discussion-fragment, préambule par locale fr/en/de/es/it, table non scindée, REF
    préservés, répartition 0/1/N). Make ONLY that minimal edit to the criterion line; do not touch
    other phases or criteria.
  </action>
  <verify>
    <automated>node -e "const c=require('./.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json'); if(c.v!==1||!Array.isArray(c.samples)) throw new Error('bad shape'); const ls=new Set(c.samples.map(s=>s.tags&&s.tags.locale)); ['fr','en','de','es','it'].forEach(l=>{if(!ls.has(l))throw new Error('missing locale '+l)}); console.log('corpus OK', c.samples.length, 'samples')"</automated>
  </verify>
  <acceptance_criteria>
    - `probe-corpus.json` parses as JSON with `v === 1` and `Array.isArray(samples)` true
    - Samples cover all five locales fr/en/de/es/it (via `tags.locale`), >=2 `tags.hasTable`, >=2 `tags.hasRef`, plus one broken-REF and one preamble case
    - ROADMAP Phase v3.1-03 criterion 2 no longer contains the substring "A/B" (verify: `grep -n "A/B" .planning/ROADMAP.md` returns nothing within the v3.1-03 criterion block)
    - ROADMAP criterion 2 still lists duplication, préambule par locale, table non scindée, REF préservés, répartition 0/1/N
    - No other ROADMAP phase/criterion modified
  </acceptance_criteria>
  <done>Curated replayable corpus seeded with full locale + table + REF + broken-REF + preamble coverage; ROADMAP criterion 2 aligned with real scope (A/B retired); no collateral ROADMAP edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON fixture -> scribeProbe.importCorpus | The curated corpus is loaded by importCorpus/replay (consumed in Plans 01/02 + future phases) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.1-03-08 | Tampering | probe-corpus.json shape | mitigate | Corpus written to the exact versioned `{v:1,samples:[...]}` envelope importCorpus validates; the verify node script asserts shape before the artifact is accepted, so a malformed fixture cannot silently pass. |
| T-v3.1-03-09 | Information Disclosure | curated fixture content | accept | Fixtures are synthetic illustrative text (no secrets/PII); they live in versioned .planning/ as gate evidence by design (D-02/D-07b). |
| T-v3.1-03-SC | Tampering | npm/pip/cargo installs | mitigate | Docs/data only; no package installs in this plan. Nothing to slopcheck. |
</threat_model>

<verification>
- GATE.md exists with [ASSUMED] thresholds, hard-blocker + coverage sections, A/B-retired note, empty verdict/evidence slots
- probe-corpus.json parses, version 1, covers 5 locales + table + REF + broken-REF + preamble
- ROADMAP criterion 2 no longer mentions A/B; surviving metrics intact
</verification>

<success_criteria>
- D-02 satisfied: durable citable GATE.md scaffold exists for v3.1-04/05 to depend on
- D-07b/D-09 satisfied: curated replayable corpus guarantees critical-case coverage
- D-06 satisfied: ROADMAP criterion 2 aligned with real scope
</success_criteria>

<output>
Create `.planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-03-SUMMARY.md` when done
</output>
