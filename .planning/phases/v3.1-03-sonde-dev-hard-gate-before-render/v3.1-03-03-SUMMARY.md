---
phase: v3.1-03-sonde-dev-hard-gate-before-render
plan: 03
subsystem: Scribe conformance gate (docs/data artifacts)
tags: [gate, corpus, roadmap, docs-only]
requires:
  - "v3.1-03-01: scribeProbe.js metrics + versioned corpus envelope (importCorpus/replay shape)"
provides:
  - "v3.1-03-GATE.md: durable go/no-go gate doc (D-02), citable Depends-on for v3.1-04/05"
  - "probe-corpus.json: replayable curated seed corpus (5 locales + table + REF + broken-REF + preamble)"
  - "ROADMAP criterion-2 aligned with real scope (A/B prose-quality metric retired)"
affects:
  - ".planning/ROADMAP.md (Phase v3.1-03 Success Criterion 2)"
tech-stack:
  added: []
  patterns:
    - "Versioned JSON envelope {v:1, samples:[...]} matching scribeProbe.importCorpus/replay"
    - "[ASSUMED]-tagged thresholds with blank frozen-value column for hybrid-gate (D-01) human freeze"
key-files:
  created:
    - ".planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md"
    - ".planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json"
  modified:
    - ".planning/ROADMAP.md"
decisions:
  - "Curated corpus stored under .planning/phases/v3.1-03-.../probe-corpus.json (Open Question 1 resolution): gate evidence belongs with GATE.md"
  - "Broken-REF seed case = scribe-ref-7 present in inputMd but absent from fragment (input id missing from output)"
  - "Preamble seed case = fragment opening 'Voici le texte :' (fr opener), warnings tagged preamble-suspected"
  - "All GATE.md numeric knobs tagged [ASSUMED] with blank Frozen-value column — user freezes against real data (D-01 hybrid)"
metrics:
  duration: ~14min
  completed: 2026-06-17
---

# Phase v3.1-03 Plan 03: Durable Gate Artifacts (GATE.md + curated corpus + ROADMAP A/B retirement) Summary

Scaffolded the durable, versioned `v3.1-03-GATE.md` go/no-go doc (D-02) with documented `[ASSUMED]` thresholds and empty verdict/evidence slots, seeded a replayable `probe-corpus.json` covering all 5 locales plus table/REF/broken-REF/preamble critical cases (D-07b/D-09), and made the one-line ROADMAP criterion-2 edit retiring the A/B prose-quality metric (D-06). Docs/data only — no source code.

## What Was Built

### Task 1 — v3.1-03-GATE.md (durable go/no-go doc, D-02)
- **Gate model (D-01 hybrid):** header explains the probe raises flags against documented thresholds for objective failures; the user pronounces the final go/no-go after reviewing a sample.
- **Thresholds table:** 9 knobs (dup per-response Dice 0.6, dup NO-GO rate >15%, preamble NO-GO rate >15%, split-table 0-tol, broken-REF 0-tol, coverage N>=30, per-locale >=3, table cases >=5, REF cases >=5), every numeric value tagged `[ASSUMED]`, with a blank "Frozen value" column for the user.
- **Hard blockers (D-03):** the four — broken REF, split table, duplication (rate D-04), preamble (rate D-04).
- **Indicative metrics (D-05):** 0/1/N fragment distribution is descriptive; explicit note that the A/B prose-quality metric is RETIRED (D-06).
- **Coverage precondition (D-09):** counter table for total/per-locale/table/REF minimums.
- **Verdict section:** empty PASS / NO-GO placeholder + decider/date lines (no verdict pre-filled).
- **Evidence sample section:** empty fenced placeholder for `exportCorpus()` + `aggregate()` output.

### Task 2 — probe-corpus.json + ROADMAP criterion-2 edit
- **probe-corpus.json:** versioned `{ "v": 1, "samples": [...] }` envelope matching the exact shape `scribeProbe.importCorpus/replay` expects. 11 deliberately-curated samples (`tags.curated: true`):
  - Locale coverage: fr (×2), en (×3), de (×2), es (×2), it (×2) — all 5 locales.
  - 2 table cases (`tags.hasTable: true`) with `[TABLE:N]...[/TABLE]` / `[CELL:r,c]...[/CELL]` markers inside a fragment.
  - 3 REF cases (`tags.hasRef: true`) with `{{REF:scribe-ref-N:visible text}}` in both inputMd and a fragment (REF-preserved exercised).
  - 1 deliberately broken-REF case (`scribe-ref-7` in inputMd, missing from the fragment).
  - 1 deliberate preamble case (fragment opening "Voici le texte :").
  - `{{fragment:0}}` present in each discussion; `surface` mixed across `'popover'`/`'chat'`.
- **ROADMAP edit:** removed the clause "comparaison A/B qualité de prose vs ancien chemin plain" from Phase v3.1-03 Success Criterion 2; surviving metrics (duplication discussion↔fragment, préambule par locale fr/en/de/es/it, table non scindée, REF préservés, répartition 0/1/N) left intact. Minimal one-line edit; no other phase/criterion touched.

## Verification Results

- **GATE.md (Task 1):** `test -f` passes; `grep -c ASSUMED` = 12 (>= 8 required); `grep -c NO-GO` = 10; RETIRED note present (×2). Verdict + Evidence sections empty as required.
- **probe-corpus.json (Task 2):** node verify script: parses with `v === 1` and `Array.isArray(samples)`; all 5 locales present; hasTable=2, hasRef=3; broken-REF detection (set-diff of scribe-ref ids) finds exactly 1 case (ts ...010); preamble (`^\s*voici`) finds exactly 1 case (ts ...011).
- **ROADMAP (Task 2):** "A/B" no longer appears inside the criterion-2 block (only remaining occurrence is line 115, the plan-list description of this very plan — correct meta-text, outside the criterion). Surviving metrics intact. Diff is exactly the single criterion-2 line; no collateral edits.

## Deviations from Plan

None - plan executed exactly as written.

## Notes

- Pre-existing unrelated working-tree change `.planning/config.json` (a trailing-newline addition from an earlier process) was left untouched and NOT included in any commit — out of scope for this plan.
- Plan v3.1-03-01's ROADMAP plan-progress tracking was not reverted or conflicted; only the criterion-2 content edit was made here. The final-commit plan-progress tracking update is applied via `roadmap update-plan-progress`.

## Self-Check: PASSED

- FOUND: .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/v3.1-03-GATE.md
- FOUND: .planning/phases/v3.1-03-sonde-dev-hard-gate-before-render/probe-corpus.json
- FOUND commit defa0f8f2 (Task 1 GATE.md)
- FOUND commit 446372c35 (Task 2 corpus + ROADMAP)
