---
phase: v3.2-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/locales/fr.json
  - src/locales/en.json
  - src/locales/de.json
  - src/locales/es.json
  - src/locales/it.json
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx
  - src/modules/views/OnlyOffice/Scribe/ChatInput.jsx
  - src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js
  - src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.spec.jsx
autonomous: true
requirements: [CTX-UX-01, CTX-UX-02, CTX-UX-03, CTX-UX-04, CTX-UX-05]
must_haves:
  truths:
    - "CTX-UX-01: A discreet « Inclure » zone renders in the side panel directly above the prompt input (inside ChatInput's footer cluster, above the textarea row), showing the caption + a checkbox row"
    - "CTX-UX-02: « document » and « discussion » checkboxes are always rendered and unchecked by default"
    - "CTX-UX-03: the « sélection » checkbox mounts ONLY when currentSelection != null, and is checked by default the moment a selection exists (opt-OUT model)"
    - "CTX-UX-04: checking « sélection » shows the existing SelectionChip; unchecking it hides the chip WITHOUT calling dismissSelection() (currentSelection is preserved, so re-checking restores the same chip)"
    - "CTX-UX-05: the zone is visually discreet — cozy-ui Checkbox size='small' style padding:2, all text 11/12px text.secondary (muted), no Scribe-purple recolor on the checkboxes, theme-aware via useTheme()"
    - "SC#5 (state-only): the three include booleans live in ScribeContext (NOT component-local useState) and toggling them produces ZERO change to the LLM call — sendMessage/encodeSelectionForPrompt/prompt assembly are byte-for-byte identical to v3.1"
    - "i18n: Scribe.include.{label,document,discussion,selection} exist in ALL 5 locales (fr/en/de/es/it) with native values; every visible label and aria-label renders via t() — zero hard-coded strings"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx"
      provides: "The « Inclure » zone: muted caption + role=group checkbox row (document/discussion/sélection), always-mounted aria-live wrapper for the conditional « sélection » checkbox; reads/writes the three include booleans from ScribeContext"
      contains: "Scribe.include.label"
      min_lines: 60
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "Three include booleans (includeDocument, includeDiscussion, includeSelection) in shared context with a read-seam comment for v3.2-02/03; selection auto-checks on null→selection transition"
      contains: "includeSelection"
    - path: "src/modules/views/OnlyOffice/Scribe/ChatInput.jsx"
      provides: "Mounts ScribeIncludeZone above the textarea row; SelectionChip visibility now gated by (currentSelection && includeSelection)"
      contains: "ScribeIncludeZone"
    - path: "src/locales/fr.json"
      provides: "Scribe.include.* group (label/document/discussion/selection) in French"
      contains: "include"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.spec.jsx"
      provides: "Component test: default states, conditional+default-checked sélection, toggle-chip-without-wiping-selection, state-lives-in-context"
      contains: "ScribeIncludeZone"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      via: "useScribe() reads currentSelection + the three include booleans and their setters (NO component-local useState for the booleans)"
      pattern: "useScribe"
    - from: "src/modules/views/OnlyOffice/Scribe/ChatInput.jsx"
      to: "src/modules/views/OnlyOffice/Scribe/SelectionChip.jsx"
      via: "SelectionChip render is gated by currentSelection && includeSelection"
      pattern: "includeSelection"
    - from: "src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx"
      to: "twake-i18n"
      via: "every label + aria-label resolved through t('Scribe.include.*')"
      pattern: "Scribe.include"
---

<objective>
Build the static « Inclure » zone above the Scribe prompt: a discreet caption + three checkboxes (« document », « discussion », « sélection ») whose state lives in `ScribeContext`. « document »/« discussion » default unchecked; « sélection » renders only when a selection exists and defaults to checked, coupling its checked-state to the existing `SelectionChip`'s visibility. Add the `Scribe.include.*` i18n group to all 5 target locales.

Purpose: deliver the v3.2 context-enrichment UX FIRST, with the three include booleans parked in shared context so v3.2-02 (discussion + selection wiring) and v3.2-03 (document wiring) can read them at the prompt-assembly seam. **This phase is state-only and presentational — the LLM call is byte-for-byte identical to v3.1. NO prompt injection here.**

Output: new `ScribeIncludeZone.jsx`, three booleans added to `ScribeContext.jsx`, `ChatInput.jsx` wiring, the `Scribe.include.*` group in fr/en/de/es/it, a new component spec, and the literal-audit gate extended to scan the new surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-UI-SPEC.md
@.planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly, no exploration needed. -->

From src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (the shared state to extend):
```jsx
// l.60  const [currentSelection, setCurrentSelectionState] = useState(null)
// l.75  const selectionDismissedRef = useRef(null)  // "dismiss THIS exact text" — SEPARATE concern, do not touch
// l.93  const setCurrentSelection = useCallback((text, html, enrichedMd, tableSnapshots) => { ... })
//        -> sets currentSelectionState (or null). Honors selectionDismissedRef.
// l.114 const dismissSelection = useCallback(() => { selectionDismissedRef.current = currentSelection.text; setCurrentSelectionState(null) })
//        -> the chip's × button. DO NOT call this from the « sélection » checkbox.
// l.130 const sendMessage = useCallback(async (text, selectionContext) => { ... }, [client, t])
//        -> the prompt-assembly seam. DO NOT MODIFY in this phase. v3.2-02/03 will read the include booleans here.
// l.283 const value = useMemo(() => ({ ...currentSelection, setCurrentSelection, dismissSelection, ... }), [ ...deps ])
//        -> ADD the 3 booleans + setters to BOTH the object and the deps array.
```

From src/modules/views/OnlyOffice/Scribe/ChatInput.jsx (the mount site):
```jsx
// l.20-28 const { sendMessage, isLoading, currentSelection, dismissSelection, pendingDraft, clearPendingDraft } = useScribe()
// l.149-161 footer container (flexShrink:0; borderTop divider; padding '8px 12px'; flexDirection column)
//   l.159-161:  {currentSelection && (<SelectionChip selection={currentSelection} onDismiss={dismissSelection} />)}
//   l.162+:     <div style={{ display:'flex', alignItems:'flex-end', gap:8 }}> <textarea.../> <button.../> </div>
// The « Inclure » zone mounts at the TOP of this footer column, ABOVE the SelectionChip line.
```

From src/modules/views/OnlyOffice/Scribe/SelectionChip.jsx:
```jsx
export const SelectionChip = ({ selection, onDismiss }) => { ... }  // reused UNCHANGED. Renders null if !selection.
```

The dev-panel checkbox pattern to mirror (src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx l.933-956):
```jsx
import Checkbox from 'cozy-ui/transpiled/react/Checkbox'   // l.6 — same import path
<label style={{ display:'inline-flex', alignItems:'center', fontSize:10, cursor:'pointer', whiteSpace:'nowrap', userSelect:'none', marginRight:4 }}
       onMouseDown={e => e.stopPropagation()}>
  <Checkbox size="small" checked={...} onChange={...} style={{ padding: 2 }} />
  <span style={{ color: theme.palette.text.secondary }}>{label}</span>
</label>
```

i18n helpers: `import { useI18n } from 'twake-i18n'` then `const { t } = useI18n()`. Theme: `import { useTheme } from 'cozy-ui/transpiled/react/styles'`.

NOTE: project memory — the ES5-only rule applies ONLY to `plugins/onlyoffice-scribe/scripts/code.js`. `src/` React is modern JSX (arrow/const/let fine). DO NOT touch the plugin in this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scribe.include.* i18n group to all 5 locales</name>
  <files>src/locales/fr.json, src/locales/en.json, src/locales/de.json, src/locales/es.json, src/locales/it.json</files>
  <read_first>
    - src/locales/scribeI18nParity.spec.js (the parity gate — flattens Scribe.* and requires the key set to be IDENTICAL across fr/en/de/es/it with non-empty trimmed string leaves)
    - src/locales/fr.json (find the existing `Scribe` object and its `prompt` group — `Scribe.prompt.placeholder` = "Aidez-moi à écrire" — to match casing/placement)
    - .planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-UI-SPEC.md (§ Copywriting Contract — the exact i18n value table)
  </read_first>
  <action>
    Add a new `include` group under the existing `Scribe` object in each of the 5 target locale files (fr, en, de, es, it). The group has exactly four keys: `label`, `document`, `discussion`, `selection`. Use the researcher-approved native values from the UI-SPEC Copywriting Contract:
    - fr: label "Inclure", document "Document", discussion "Conversation", selection "Sélection"
    - en: label "Include", document "Document", discussion "Conversation", selection "Selection"
    - de: label "Einbeziehen", document "Dokument", discussion "Unterhaltung", selection "Auswahl"
    - es: label "Incluir", document "Documento", discussion "Conversación", selection "Selección"
    - it: label "Includi", document "Documento", discussion "Conversazione", selection "Selezione"
    The key NAMES stay stable English (`discussion` even though its visible label reads "Conversation"/"Unterhaltung"/etc.) — apply one label set across all 5 (CTX-UX i18n decision). Place the `include` group inside `Scribe` alongside the existing `prompt`/`button`/`menu` groups (ordering is cosmetic; the parity gate sorts keys). Do NOT add this group to any non-target locale (ar/ja/ko/nl/nl_NL/pl/ru/vi/zh_*) — they are out of parity-gate scope. Keep valid JSON (commas, no trailing comma).
  </action>
  <verify>
    <automated>yarn jest src/locales/scribeI18nParity.spec.js --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 5 target locales contain `Scribe.include.label`, `Scribe.include.document`, `Scribe.include.discussion`, `Scribe.include.selection` with non-empty native strings. Quick check: `for l in fr en de es it; do node -e "const f=require('./src/locales/'+process.argv[1]+'.json'); const i=f.Scribe.include; if(!i||!i.label||!i.document||!i.discussion||!i.selection){console.error(process.argv[1]+' MISSING'); process.exit(1)} console.log(process.argv[1], JSON.stringify(i))" $l || exit 1; done`
    - The parity gate `scribeI18nParity.spec.js` passes (Scribe.* key set identical across all 5; no empty leaves).
    - No `Scribe.include` group added to any non-target locale file.
    - Each file remains valid JSON (`node -e "require('./src/locales/fr.json')"` etc. exit 0 for all 5).
  </acceptance_criteria>
  <done>The Scribe.include.{label,document,discussion,selection} group exists in fr/en/de/es/it with native values and the parity gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the three include booleans to ScribeContext (shared state, read-seam comment, no LLM wiring)</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (l.56-128 provider state + setCurrentSelection/dismissSelection; l.130-281 sendMessage — DO NOT MODIFY; l.283-322 the value useMemo object + deps array)
    - .planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-UI-SPEC.md (§ Conditional « sélection » behavior — opt-OUT model; § Static-state guarantee — the seam comment requirement)
    - .planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-CONTEXT.md (DESIGN NOTE: prefer a NEW "selection included" boolean over reusing dismissSelection())
  </read_first>
  <action>
    In `ScribeProvider`, add three boolean state values to shared context: `includeDocument` (default `false`), `includeDiscussion` (default `false`), `includeSelection` (default `true`). Expose setters `setIncludeDocument`, `setIncludeDiscussion`, `setIncludeSelection` (plain `useState` booleans are fine — what matters is they live HERE, in the provider, not in a leaf component).

    Implement the opt-OUT auto-check for « sélection » (CTX-UX-03): when `currentSelection` transitions from `null` → non-null, set `includeSelection` to `true` (a fresh selection is included by default). Use a `useEffect` keyed on `currentSelection` that tracks the previous value (e.g. a ref or `useEffect` comparing to a prevRef): on a null→non-null edge, `setIncludeSelection(true)`. Do NOT force it back to a value while a selection persists — once shown, the user's manual uncheck must stick until the selection itself changes. (Acceptable simpler equivalent: in the same place `setCurrentSelection` actually transitions state from null→value, also set `includeSelection` true — but the leaf component must never own this.)

    Add the three booleans + their setters to BOTH the `value` useMemo object (l.283-302) AND its dependency array (l.303-321).

    **Add a code comment at the state seam** (immediately above the three boolean declarations, AND a one-liner inside/just above `sendMessage` near the prompt assembly) reading exactly: `// read by v3.2-02/03; no prompt injection in v3.2-01`. This marks where the wiring phases will consume the booleans.

    **HARD SCOPE GUARD:** do NOT modify `sendMessage` body, `encodeSelectionForPrompt`, `buildChatSystemPrompt`, `callScribeAIWithReask`, the `[Selected text from document]` block assembly, or any message-mapping logic. The booleans are declared and exposed ONLY — they feed NO value into the prompt in this phase. The LLM request must stay byte-for-byte identical to v3.1. Do NOT touch `selectionDismissedRef` or `dismissSelection` (the chip × is a separate concern).
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/scribeAI.spec.js src/modules/views/OnlyOffice/Scribe/scribeResponse.spec.js --silent 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `ScribeContext.jsx` declares `includeDocument` (default false), `includeDiscussion` (default false), `includeSelection` (default true), with setters, in the provider. Grep: `grep -v '^ *//' src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx | grep -c "includeSelection"` is ≥ 2.
    - All three booleans + setters appear in the `value` useMemo object AND its deps array.
    - The null→non-null selection edge sets `includeSelection` true (opt-OUT default).
    - The seam comment `read by v3.2-02/03; no prompt injection in v3.2-01` is present. Grep: `grep -c "no prompt injection in v3.2-01" src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` is ≥ 1.
    - **Scope guard:** the `sendMessage` body, `encodeSelectionForPrompt(...)` call, the `[Selected text from document]` block, and `buildChatSystemPrompt(...)` are UNCHANGED. `git diff src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` shows NO edits inside the `sendMessage` async body (l.130-281) other than the single seam comment line, and NO reference to any include boolean inside the aiMessages assembly.
    - `dismissSelection` / `selectionDismissedRef` are untouched.
    - The existing scribeAI / scribeResponse suites still pass (no prompt-assembly regression).
  </acceptance_criteria>
  <done>ScribeContext exposes the three include booleans (sélection opt-OUT auto-checks on a fresh selection), the seam comment marks the v3.2-02/03 read point, and the LLM call path is provably unchanged from v3.1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build ScribeIncludeZone, wire it into ChatInput, gate the chip, extend the literal-audit gate, and test</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx, src/modules/views/OnlyOffice/Scribe/ChatInput.jsx, src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js, src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.spec.jsx</files>
  <read_first>
    - src/modules/views/OnlyOffice/Scribe/ChatInput.jsx (l.20-28 useScribe destructure; l.149-161 footer + the SelectionChip render line; l.162+ textarea row)
    - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx (l.933-956 — the EXACT cozy-ui Checkbox size="small" + label + muted span pattern to mirror; l.6 Checkbox import path; l.894-902 the dev micro-caption treatment)
    - src/modules/views/OnlyOffice/Scribe/SelectionChip.jsx (reused unchanged)
    - src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js (l.32-39 SURFACE_FILES list to extend)
    - src/modules/views/OnlyOffice/Scribe/ChatMessageList.render.spec.jsx (l.1-70 — the mock pattern: useTheme/twake-i18n/useScribe mocks + testing-library render — mirror this for the new spec)
    - .planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-UI-SPEC.md (§§ Layout & Interaction Contract, Accessibility, Typography, Color — the discreet styling + aria-live wrapper + role=group contract)
  </read_first>
  <behavior>
    - Default states: « document » and « discussion » render and are UNCHECKED; the zone caption renders via t('Scribe.include.label').
    - No selection (currentSelection == null): the « sélection » checkbox is ABSENT (not disabled/greyed — absent), but the aria-live wrapper div is STILL mounted (renders empty children).
    - Selection exists (currentSelection != null): the « sélection » checkbox is present and CHECKED by default; the SelectionChip (in ChatInput) is visible.
    - Toggle « sélection » off: SelectionChip becomes hidden, but currentSelection is NOT wiped (dismissSelection is NOT called) — toggling back on restores the same chip.
    - State location: toggling any checkbox calls the ScribeContext setter (the spec asserts the context setter is invoked / the value comes from context) — NOT a component-local useState. Mocking useScribe with a setter spy proves the leaf reads/writes context.
    - i18n: every visible label and every aria-label resolves through t('Scribe.include.*') — when t echoes the key, the rendered text equals the key (no hard-coded « Inclure »/«Document»/etc.).
    - Static-state: no test asserts any change to sendMessage/the prompt (out of scope); but the spec confirms toggling does not call dismissSelection and does not clear currentSelection.
  </behavior>
  <action>
    **Create `ScribeIncludeZone.jsx`** in `src/modules/views/OnlyOffice/Scribe/`. It reads from `useScribe()`: `currentSelection`, `includeDocument`, `includeDiscussion`, `includeSelection`, `setIncludeDocument`, `setIncludeDiscussion`, `setIncludeSelection`. It is a pure presentational consumer — NO local useState for the booleans.

    Structure (per UI-SPEC § Layout):
    - A container `<div role="group" aria-label={t('Scribe.include.label')}>` with zone padding `'8px 12px'` (mirrors ChatInput's horizontal inset) and `flexDirection: column`, `gap: 4`.
    - A muted caption row rendering `t('Scribe.include.label')` as a `<span>` styled `fontSize: 11`, `fontWeight: 600`, `textTransform: 'uppercase'`, `letterSpacing: '0.5px'`, `color: theme.palette.text.secondary` (the dev-micro-caption treatment).
    - A checkbox row `<div style={{ display:'flex', flexWrap:'wrap', gap:8, rowGap:4 }}>` containing, in order:
      1. « document » — a `<label>`-wrapped `<Checkbox size="small" style={{padding:2}} checked={includeDocument} onChange={() => setIncludeDocument(!includeDocument)} />` + muted 12px `<span>{t('Scribe.include.document')}</span>`. Always rendered.
      2. « discussion » — same pattern, `includeDiscussion` / `Scribe.include.discussion`. Always rendered.
      3. An **always-mounted** aria-live wrapper `<div aria-live="polite">{ currentSelection ? <label>…« sélection » checkbox…</label> : null }</div>`. The inner « sélection » `<label>`+`<Checkbox>` (checked={includeSelection}, onChange toggles setIncludeSelection, label t('Scribe.include.selection')) mounts ONLY when `currentSelection != null`. Do NOT move focus to it on appearance.
    - Each `<Checkbox>` carries `aria-label={t('Scribe.include.<key>')}` (the `<label>` wrap already associates the visible span, but resolve aria from t() too so no literal strings exist). Wrap labels in `<label>` for full-width hit area (dev-toggle pattern). Use cozy-ui default `primary` checked color — do NOT recolor to Scribe purple. Theme via `useTheme()`; all text via `theme.palette.text.secondary`; NO hard-coded hex for text/surfaces.

    **Wire into `ChatInput.jsx`:** import `ScribeIncludeZone` and the `includeSelection` boolean (from `useScribe()`). Render `<ScribeIncludeZone />` at the TOP of the footer column (l.149-161), ABOVE the SelectionChip line. Change the SelectionChip gate from `{currentSelection && (...)}` to `{currentSelection && includeSelection && (...)}` so unchecking « sélection » hides the chip. Keep `onDismiss={dismissSelection}` on the chip's × (separate concern — the × still wipes the selection). Do NOT add any extra border around the zone (the existing footer borderTop is the only divider). Do NOT insert the zone's checkboxes into the thread roving-keyboard nav (`onArrowUp`) — they are plain Tab-reachable controls.

    **Extend the literal-audit gate:** in `scribeLiteralAudit.spec.js`, add `'ScribeIncludeZone.jsx'` (and `'ChatInput.jsx'`) to the `SURFACE_FILES` array (l.32-39) so the new user-facing surface is scanned for hard-coded strings.

    **Write `ScribeIncludeZone.spec.jsx`** mirroring the mock pattern in `ChatMessageList.render.spec.jsx`: mock `cozy-ui/transpiled/react/styles` (useTheme), `cozy-ui/transpiled/react/Checkbox` (a passthrough `<input type="checkbox">` is fine), and `twake-i18n` (t echoes the key); mock `@/modules/views/OnlyOffice/Scribe/ScribeContext` `useScribe` with a controllable `scribeState` carrying the booleans + setter spies + currentSelection. Cover the behaviors listed in `<behavior>`: default unchecked document/discussion; « sélection » absent when no selection / present+checked when a selection exists; the aria-live wrapper present in BOTH states; toggling « sélection » calls `setIncludeSelection` (NOT `dismissSelection`); toggling a checkbox invokes the context setter (proving state lives in context); caption/labels rendered via the echoed t() keys.
  </action>
  <verify>
    <automated>yarn jest src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.spec.jsx src/modules/views/OnlyOffice/Scribe/scribeLiteralAudit.spec.js src/locales/scribeI18nParity.spec.js --silent 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `ScribeIncludeZone.jsx` exists, reads the three booleans + currentSelection from `useScribe()`, and contains NO `useState` for the include booleans (grep: `grep -c "useState" src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx` is 0, OR any useState present is unrelated to the include booleans — verified by the spec's context-setter assertion).
    - Every label/aria-label uses t('Scribe.include.*'): `grep -c "Scribe.include" src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx` is ≥ 4; no hard-coded « Inclure »/«Document»/«Conversation»/«Sélection» literal in the JSX (the extended literal-audit gate enforces this).
    - The aria-live wrapper is ALWAYS mounted; only the inner « sélection » label/checkbox is conditional. Grep: `grep -c "aria-live" src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx` is ≥ 1.
    - cozy-ui `Checkbox size="small"` with `padding: 2` is used (no custom toggle); no Scribe-purple/`#7C3AED` applied to the checkboxes (grep: `grep -c "7C3AED\|SCRIBE_PURPLE" src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx` is 0).
    - `ChatInput.jsx` renders `<ScribeIncludeZone />` above the chip and gates the chip on `currentSelection && includeSelection` (grep: `grep -c "ScribeIncludeZone" src/modules/views/OnlyOffice/Scribe/ChatInput.jsx` is ≥ 2 (import + render); `grep -c "includeSelection" src/modules/views/OnlyOffice/Scribe/ChatInput.jsx` is ≥ 1).
    - `dismissSelection` is NOT called by the « sélection » checkbox onChange (the spec asserts `dismissSelection` spy is NOT called when toggling « sélection »).
    - `scribeLiteralAudit.spec.js` `SURFACE_FILES` now includes `'ScribeIncludeZone.jsx'` and `'ChatInput.jsx'`.
    - All three suites green: the new `ScribeIncludeZone.spec.jsx`, the extended `scribeLiteralAudit.spec.js`, and `scribeI18nParity.spec.js`.
  </acceptance_criteria>
  <done>The « Inclure » zone renders above the prompt with discreet cozy-ui checkboxes; « sélection » is conditional+default-checked and toggles the chip without wiping currentSelection; all strings go through t(); the literal-audit + parity gates and the new component spec are green; the LLM call is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OO editor selection → ScribeContext | Selected document text already flows into `currentSelection` (pre-existing boundary, unchanged here) |
| User checkbox input → ScribeContext booleans | Only toggles in-memory booleans; this phase feeds them into NO outbound request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v3.2-01-01 | Elevation of privilege (scope creep) | The three include booleans accidentally wired into the prompt | mitigate | HARD scope guard: `sendMessage`/`encodeSelectionForPrompt`/`buildChatSystemPrompt`/the `[Selected text from document]` block are unchanged (Task 2 acceptance grep + git-diff check). The booleans feed NO value into the LLM call in v3.2-01; the seam comment marks the v3.2-02/03 read point only. |
| T-v3.2-01-02 | Information disclosure | « sélection » uncheck wiping currentSelection or leaking dismissed selections | mitigate | Unchecking « sélection » sets `includeSelection=false` (chip hidden) but NEVER calls `dismissSelection()` and NEVER clears `currentSelection`; the separate `selectionDismissedRef` × behavior is untouched. Spec asserts dismissSelection is not invoked on toggle. |
| T-v3.2-01-03 | Tampering (i18n drift) | Missing locale key breaking the parity/literal gate | mitigate | Task 1 adds all four keys to all 5 target locales; the parity gate + extended literal-audit gate are run in CI and as plan verification. |
| T-v3.2-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — all components are first-party (cozy-ui Checkbox, twake-i18n). Zero new dependencies. |
</threat_model>

<verification>
- Parity gate `scribeI18nParity.spec.js` green with the new `Scribe.include.*` group in all 5 locales.
- Literal-audit gate `scribeLiteralAudit.spec.js` (extended to scan `ScribeIncludeZone.jsx` + `ChatInput.jsx`) green — no hard-coded user-facing string in the new surface.
- New `ScribeIncludeZone.spec.jsx` green: default unchecked document/discussion; conditional+default-checked « sélection »; aria-live wrapper always mounted; toggle hides chip without calling dismissSelection; state lives in ScribeContext (setter spies invoked).
- State-location check: include booleans declared in `ScribeContext` (NOT component-local); present in the `value` useMemo + deps.
- Scope-guard check: `git diff` of `ScribeContext.jsx` shows the `sendMessage` async body unchanged except the single seam comment; no include boolean referenced in `aiMessages` assembly. `scribeAI.spec.js` / `scribeResponse.spec.js` still pass.
- Discreet-styling check: cozy-ui `Checkbox size="small"` padding:2, 11/12px text.secondary, no `#7C3AED` on the checkboxes, theme via `useTheme()`.
</verification>

<success_criteria>
- CTX-UX-01: « Inclure » zone renders above the prompt with caption + 3 checkboxes.
- CTX-UX-02: « document » and « discussion » unchecked by default.
- CTX-UX-03: « sélection » mounts only when a selection exists and is checked by default.
- CTX-UX-04: checking « sélection » shows the SelectionChip; unchecking hides it without wiping currentSelection.
- CTX-UX-05: the zone is visually discreet (small muted controls, theme-aware, no purple recolor).
- SC#5 (state-only): the three booleans live in ScribeContext; toggling produces zero change to the LLM call — v3.1 behavior preserved byte-for-byte.
- i18n: `Scribe.include.*` present in all 5 locales, every label via t(); parity + literal-audit gates green.
</success_criteria>

<output>
Create `.planning/phases/v3.2-01-zone-inclure-discrete-ux-statique/v3.2-01-01-SUMMARY.md` when done.
</output>
