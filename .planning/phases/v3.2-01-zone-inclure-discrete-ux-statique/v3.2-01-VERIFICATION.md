---
phase: v3.2-01-zone-inclure-discrete-ux-statique
verified: 2026-06-24T00:00:00Z
status: human_needed
score: 7/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Open the Scribe side panel with no selection active. Verify the « Inclure » zone renders above the prompt: muted uppercase caption 'Inclure', two unchecked checkboxes (Document, Conversation), no third checkbox, all text visually smaller and muted vs the prompt text."
    expected: "Caption at ~11px uppercase text.secondary, two unchecked checkboxes at ~12px muted. No sélection checkbox. Zone fits compactly above the prompt with no heavy border."
    why_human: "Visual sizing and muted appearance vs the 14px prompt body text requires human eye — color tokens and font-size values are in code but their rendered visual weight cannot be verified by grep."
  - test: "Make a text selection in the OnlyOffice document. Open the side panel. Verify: (a) a third checkbox 'Sélection' appears pre-checked; (b) the SelectionChip is visible below the checkboxes; (c) unchecking 'Sélection' hides the chip but the chip content returns when you re-check it."
    expected: "Sélection checkbox appears pre-checked; chip visible when checked, hidden when unchecked, re-visible when re-checked. currentSelection is NOT wiped (chip content preserved on toggle)."
    why_human: "Requires a live OO document with an actual selection — can't be replicated in a jsdom test environment."
  - test: "Verify the checkbox checked-state color uses the cozy-ui default (blue/primary) and NOT Scribe purple (#7C3AED)."
    expected: "Checked checkbox renders in the library's default primary color, not purple."
    why_human: "Color rendering is a visual check that requires browser inspection — the code correctly omits any color override, but its runtime appearance needs a live check."
  - test: "Verify that sending a message with all three checkboxes checked produces the identical LLM request to what v3.1 sent (only the user text and selection are in the prompt — no 'document'/'discussion' context is injected)."
    expected: "Network request to the AI endpoint shows the same prompt structure as before v3.2-01. The include booleans have no observable effect on the outgoing request."
    why_human: "Network-level confirmation that the scope guard holds at runtime (the code analysis confirms it, but a live network trace provides final assurance for the state-only contract)."
---

# Phase v3.2-01 Verification Report

**Phase Goal:** A discreet « Inclure » zone above the prompt with 3 checkboxes (document/discussion/sélection), correct defaults, conditional+default-checked « sélection » coupled to the existing SelectionChip — STATE-ONLY, NO LLM wiring (the LLM call must be byte-for-byte identical to v3.1).
**Verified:** 2026-06-24
**Status:** human_needed (all mechanical checks PASS; 4 recommended live UX checks listed below)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CTX-UX-01: « Inclure » zone renders above prompt with caption + 3-checkbox row | VERIFIED | `ScribeIncludeZone.jsx` (127 lines) mounts in `ChatInput.jsx` line 161 (`<ScribeIncludeZone />`), above the chip and textarea; role=group container with caption `{t('Scribe.include.label')}` |
| 2 | CTX-UX-02: document and discussion unchecked by default | VERIFIED | ScribeContext declares `const [includeDocument, setIncludeDocument] = useState(false)` and `const [includeDiscussion, setIncludeDiscussion] = useState(false)` (lines 65-66); spec test "renders document and discussion checkboxes UNCHECKED by default" PASS |
| 3 | CTX-UX-03: sélection checkbox mounts ONLY when currentSelection != null, default-checked | VERIFIED | `ScribeIncludeZone.jsx` line 108: `{currentSelection ? (<label>…</label>) : null}` inside always-mounted aria-live wrapper; `useState(true)` default in ScribeContext; `prevSelectionRef` useEffect auto-checks on null→selection edge (lines 133-140); spec "OMITS the « sélection » checkbox when there is no selection" PASS; spec "shows « sélection » CHECKED by default when a selection exists" PASS |
| 4 | CTX-UX-04: unchecking sélection hides SelectionChip WITHOUT calling dismissSelection / wiping currentSelection | VERIFIED | `ChatInput.jsx` line 162: `{currentSelection && includeSelection && (<SelectionChip .../>)}`; spec "toggling « sélection » calls setIncludeSelection (context setter), NOT dismissSelection" PASS (`dismissSelection` spy asserted not called) |
| 5 | CTX-UX-05: discreet styling — size="small", 11/12px text.secondary, no SCRIBE_PURPLE recolor, useTheme | VERIFIED | `grep -n "7C3AED\|SCRIBE_PURPLE" ScribeIncludeZone.jsx` returns empty; `Checkbox size="small" style={{ padding: 2 }}`; fontSize 11 (caption) + 12 (labels); all text uses `theme.palette.text.secondary`; `useTheme()` imported from `cozy-ui/transpiled/react/styles` |
| 6 | SC#5: three booleans live in ScribeContext (NOT component-local useState); LLM call unchanged | VERIFIED | `ScribeIncludeZone.jsx` has zero `useState` calls — booleans read via `useScribe()`; `git diff a392ac993..HEAD -- ScribeContext.jsx` shows include booleans referenced ONLY in declaration (lines 65-67), seam comment (lines 164-166), and useMemo object+deps (lines 320-349) — NOT in `aiMessages` assembly; `scribeAI.spec.js` + `scribeResponse.spec.js` 59/59 PASS |
| 7 | i18n: Scribe.include.{label,document,discussion,selection} in all 5 locales with native values; all labels via t() | VERIFIED | All 5 locale files have the group (node verified: fr/en/de/es/it); DE/ES/IT values are native (label/discussion/selection all differ from English); `grep -c "Scribe.include" ScribeIncludeZone.jsx` = 8 (all 4 keys appear as t() calls at least twice each — aria-label + visible span); `scribeI18nParity.spec.js` 9/9 PASS |

**Score:** 7/7 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.jsx` | Inclure zone component; reads 3 booleans from useScribe(); aria-live wrapper always mounted; no local useState for booleans | VERIFIED | 127 lines; reads from useScribe(); 0 useState calls; aria-live="polite" wrapper present; Scribe.include keys used ≥8 times via t() |
| `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` | 3 include booleans in provider; in value useMemo + deps; seam comment; opt-OUT auto-check on null→selection | VERIFIED | `includeDocument` (false), `includeDiscussion` (false), `includeSelection` (true) declared lines 65-67; all 6 (booleans + setters) in useMemo object lines 319-325 and deps lines 344-350; seam comment at line 64 + inside sendMessage lines 164-166; prevSelectionRef useEffect lines 133-140 |
| `src/modules/views/OnlyOffice/Scribe/ChatInput.jsx` | Mounts ScribeIncludeZone above chip; SelectionChip gated by `currentSelection && includeSelection` | VERIFIED | Line 14: import; line 161: `<ScribeIncludeZone />`; line 162: `{currentSelection && includeSelection && (<SelectionChip .../>)}` |
| `src/locales/fr.json` | Scribe.include.* group in French | VERIFIED | label:"Inclure", document:"Document", discussion:"Conversation", selection:"Sélection" |
| `src/modules/views/OnlyOffice/Scribe/ScribeIncludeZone.spec.jsx` | 10 behavior tests | VERIFIED | 10/10 tests PASS: default states, conditional sélection, aria-live in both states, toggle-hides-chip-without-wiping, state-in-context setter spies |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ScribeIncludeZone.jsx` | `ScribeContext.jsx` | `useScribe()` reads currentSelection + 3 booleans + setters; NO component-local useState | WIRED | Confirmed: lines 29-37 of ScribeIncludeZone.jsx; zero useState in the file |
| `ChatInput.jsx` | `SelectionChip.jsx` | `currentSelection && includeSelection` gate | WIRED | Line 162 of ChatInput.jsx: `{currentSelection && includeSelection && (<SelectionChip .../>)}` |
| `ScribeIncludeZone.jsx` | `twake-i18n` | every label + aria-label via `t('Scribe.include.*')` | WIRED | 8 occurrences of `Scribe.include` in component; grep found no hardcoded locale strings |

### Data-Flow Trace (Level 4)

ScribeIncludeZone renders state from ScribeContext (not dynamic API data). State flows from:
- `useState` declarations in ScribeContext provider (lines 65-67)
- Context `value` useMemo exposes them (lines 319-325)
- `useScribe()` in ScribeIncludeZone consumes them (lines 29-37)
- Setters flow back from leaf to context on checkbox onChange

This is synchronous local state with no API calls — no data-flow stub risk. The booleans intentionally produce no change to the LLM call in this phase (confirmed by scope guard: no include boolean appears inside `aiMessages` construction, verified via git diff).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| ScribeIncludeZone spec (10 behaviors) | `yarn jest ScribeIncludeZone.spec.jsx --silent` | 10/10 PASS | PASS |
| Literal audit gate (no hardcoded strings in ScribeIncludeZone + ChatInput) | `yarn jest scribeLiteralAudit.spec.js --silent` | 4/4 PASS | PASS |
| i18n parity gate (5 locales, 4 keys each) | `yarn jest scribeI18nParity.spec.js --silent` | 9/9 PASS | PASS |
| Scope guard — LLM call unchanged | `yarn jest scribeAI.spec.js scribeResponse.spec.js --silent` | 59/59 PASS | PASS |
| ScribeContainer not modified (pre-existing failing test excluded) | `git log a392ac993..HEAD -- ScribeContainer.jsx` | empty — file untouched | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CTX-UX-01 | v3.2-01-01 | « Inclure » zone with 3 checkboxes above prompt | SATISFIED | ScribeIncludeZone.jsx exists and wired in ChatInput.jsx line 161 |
| CTX-UX-02 | v3.2-01-01 | document and discussion unchecked by default | SATISFIED | useState(false) for both; spec asserts unchecked |
| CTX-UX-03 | v3.2-01-01 | sélection conditional on currentSelection, checked by default | SATISFIED | Conditional render in ScribeIncludeZone.jsx line 108; prevSelectionRef auto-check in ScribeContext.jsx lines 133-140 |
| CTX-UX-04 | v3.2-01-01 | sélection toggle shows/hides chip without wiping currentSelection | SATISFIED | ChatInput.jsx line 162 gate; spec asserts dismissSelection NOT called on toggle |
| CTX-UX-05 | v3.2-01-01 | Visually discreet — small, muted, theme-aware, no purple recolor | SATISFIED (mechanical) | size="small"; padding:2; 11/12px; text.secondary; no #7C3AED; useTheme() — live visual check is the human item |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `ChatInput.jsx` | 171 | `placeholder=` | Info | This is the textarea HTML `placeholder` attribute set via `t('Scribe.prompt.placeholder')` — a proper i18n call, not a hardcoded string. Not a stub. |

No TBD/FIXME/XXX markers found in any phase-modified file. No return null/return []/return {} stubs found in implementation files. The "booleans not yet wired to LLM" is the deliberate and documented phase boundary (seam comment present), not a stub.

### Human Verification Required

These are recommended live checks — all mechanical gates pass. They do NOT indicate implementation gaps; they confirm visual and runtime behavior that grep cannot assert.

#### 1. Discreet Visual Appearance

**Test:** Open the Scribe side panel with no selection. Observe the « Inclure » zone above the prompt.
**Expected:** Muted uppercase caption "Inclure" at ~11px, two unchecked checkboxes with 12px muted labels (Document, Conversation), visually lighter than the 14px prompt body. Zone does not feel heavy or competitive with the prompt area.
**Why human:** Font size values and color token names are verified in code; their rendered visual weight relative to adjacent elements requires a browser.

#### 2. Sélection Checkbox — Conditional Appearance and Chip Coupling

**Test:** Select text in the OO document, open the panel. Verify (a) "Sélection" checkbox appears pre-checked and the SelectionChip is visible; (b) unchecking "Sélection" hides the chip; (c) re-checking restores the same chip (content preserved, confirming currentSelection was not wiped).
**Expected:** Opt-OUT behavior works end-to-end with the live plugin selection flow.
**Why human:** Requires a live OO document and actual plugin selection event — jsdom tests mock currentSelection.

#### 3. Checkbox Checked-State Color

**Test:** Check the "Document" checkbox and inspect the rendered color.
**Expected:** Checked state uses cozy-ui default primary color (blue), NOT Scribe purple (#7C3AED). Confirms no accidental color override at runtime.
**Why human:** The code has no color override (grep confirms), but browser-level cozy-ui default rendering needs a live check.

#### 4. LLM Scope Guard at Runtime

**Test:** Check all three boxes, type a message, send it. Inspect the network request in DevTools.
**Expected:** The prompt payload is identical to a v3.1 request — no document/discussion/selection context injected beyond what the selection already provided. The include booleans are invisible to the backend.
**Why human:** Network-level runtime confirmation of the scope guard that code analysis already supports.

---

## Gaps Summary

No gaps. All 7 must-haves are VERIFIED. The 4 human verification items are recommended live UAT checks for a phase that delivers visible UI — they do not indicate missing implementation.

The one known pre-existing failing test (`ScribeContainer › configures Drawer PaperProps for fullscreen on mobile`) is confirmed pre-existing: `git log a392ac993..HEAD -- src/modules/views/OnlyOffice/Scribe/ScribeContainer.jsx` returns empty (file untouched by this phase).

---

_Verified: 2026-06-24_
_Verifier: Claude (gsd-verifier)_
