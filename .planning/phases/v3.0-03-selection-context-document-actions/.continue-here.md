---
phase: v3.0-03-selection-context-document-actions
task: 4
total_tasks: 4
status: awaiting_manual_test
last_updated: 2026-04-07T19:40:19.281Z
---

<current_state>
Plan v3.0-03-04 (ToJSON/FromJSON table snapshots) — Tasks 2-4 implemented, automated verifies all PASS. Code is NOT committed — awaiting manual testing before commit.

OO container has been restarted (`./scripts/oo-dev-setup.sh`). User needs Ctrl+Shift+R in browser to pick up React changes (View.jsx, ScribeContext, protocol.js) and plugin changes (code.js).
</current_state>

<completed_work>

**This session (2026-04-05 → 2026-04-07):**

- v3.0-04-02: Fix resize handle drag over OO iframe via pointer capture — DONE, committed (65fe01a5f)
- v3.0-03-03 Tasks 1-3: PANEL_ACTION intent for panel Replace/Insert — DONE, committed (c48573ef4)
- v3.0-03-03 Task 4: Enrich SELECTION_CHANGED with enrichedMd — DONE, committed (c48573ef4)
- v3.0-03-04 Task 1: Research ToJSON/FromJSON feasibility — DONE, committed (f9b7d0ea4)
- v3.0-03-04 Tasks 2-4: ToJSON extraction + transport + FromJSON injection — DONE, NOT COMMITTED

**Files modified (uncommitted):**
- `plugins/onlyoffice-scribe/scripts/code.js` (+141/-69) — ToJSON capture in extraction, FromJSON in injection, tableSnapshots in buildEditIntentData/PANEL_ACTION
- `src/modules/views/OnlyOffice/View.jsx` (+20) — tableSnapshotsRef, pass through respond/castPanelAction
- `src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx` (+4/-1) — setCurrentSelection accepts tableSnapshots
- `src/lib/cozy-bridge/protocol.js` (+4/-1) — createPanelActionIntent includes tableSnapshots
</completed_work>

<remaining_work>

**Immediate — manual testing of v3.0-03-04:**
1. Table basique: sélectionner un tableau formaté (borders, couleurs, bold) → panel → LLM modifie contenu → Replace → formatting original conservé avec contenu modifié
2. Table modifiée entre-temps: après envoi message, modifier le tableau dans OO avant Replace → formatting du snapshot, pas du tableau modifié
3. Insert mode: même test avec Insert
4. Inline popover: régression check (Ctrl+Shift+I → Replace/Insert)
5. Sans table (texte simple): pas de régression

**After v3.0-03-04 is tested and committed:**
- v3.0-03-05: Copy rich HTML via round-trip injection (plan written, not started, depends on 03-04)
</remaining_work>

<decisions_made>

- **ToJSON/FromJSON over HTML/PasteHtml**: PasteHtml is async (can't run in callCommand), ruled out. ToJSON(true,true) is synchronous, lossless, captures all formatting including images. FromJSON returns ApiTable directly.
- **ToJSON/FromJSON over structured JSON**: manual property enumeration misses images, requires maintenance when OO adds properties. ToJSON captures everything automatically.
- **Fallback to Clone path**: when tableSnapshots is absent or FromJSON fails, fall back to old doc.GetAllTables()[index].Copy() path. Marked with comment "FALLBACK: legacy clone path — remove once ToJSON snapshots are proven stable".
- **PANEL_ACTION intent (v3.0-03-03)**: new one-way intent bypasses pending-intent/respond mechanism for panel actions. Plugin sees no difference between inline and panel flows.
- **Enriched SELECTION_CHANGED (v3.0-03-03)**: SELECTION_CHANGED now calls buildEditIntentData() to carry enrichedMd, tableAmbiguity, partialTableInfo alongside text/html.
</decisions_made>

<blockers>
- None. Code is ready for manual testing.
</blockers>

<context>
The v3.0-03-04 plan went through 3 approach iterations:
1. First: structured JSON (manual property extraction) — rejected because images not captured, too much maintenance
2. Second: HTML via GetSelectedContent + PasteHtml reconstruction — rejected because PasteHtml is async
3. Final: ToJSON/FromJSON — lossless, synchronous, simplest code

Known concern: the executor makes a double Api.FromJSON() call per table (once for font reading, once for reconstruction). Not a blocker — optimize later if performance is an issue.

Research file at `plugins/onlyoffice-scribe/scripts/table-snapshot-research.md` documents all API findings.
</context>

<next_action>
1. User does Ctrl+Shift+R in browser
2. Run the 5 manual tests listed in remaining_work
3. If tests pass: commit v3.0-03-04 code + SUMMARY, then move to v3.0-03-05
4. If tests fail: debug based on console logs ([Scribe] prefixed)
</next_action>
