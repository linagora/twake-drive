---
phase: 14-navigation-clavier-micro-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/onlyoffice-scribe/scripts/code.js
  - src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx
autonomous: true
requirements: [NAV-01, NAV-02]

must_haves:
  truths:
    - "Ctrl+Shift+I opens Scribe when text is selected"
    - "Ctrl+I still triggers italic in OO (not intercepted)"
    - "In the result panel, Tab moves focus left-to-right: Insert then Replace then Close"
    - "Shift+Tab moves focus right-to-left: Close then Replace then Insert"
  artifacts:
    - path: "plugins/onlyoffice-scribe/scripts/code.js"
      provides: "Ctrl+Shift+I shortcut (was Ctrl+I)"
      contains: "e.shiftKey"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx"
      provides: "Updated tooltip showing Ctrl+Shift+I"
      contains: "Ctrl+Shift+I"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx"
      provides: "Correct visual button order: Insert (left), Replace (right)"
  key_links:
    - from: "plugins/onlyoffice-scribe/scripts/code.js"
      to: "useCozyBridge"
      via: "castIntent AI_TEXT_EDIT on Ctrl+Shift+I"
      pattern: "e\\.shiftKey.*e\\.key.*i"
---

<objective>
Fix keyboard shortcut conflict and result panel focus order.

Purpose: Ctrl+I currently conflicts with OO's native italic shortcut. Changing to Ctrl+Shift+I eliminates the conflict. The result panel button order is visually backwards (Replace left, Insert right) which makes Tab navigation counterintuitive.

Output: Updated plugin shortcut, updated tooltip, corrected button visual order.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@plugins/onlyoffice-scribe/scripts/code.js
@src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx
@src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx

<interfaces>
<!-- Plugin code.js shortcut registration (lines 384-406) -->
<!-- Currently checks: (e.ctrlKey || e.metaKey) && e.key === "i" -->
<!-- Must add: e.shiftKey to the condition -->

<!-- ScribeFloatingButton tooltip (line 70) -->
<!-- Currently shows: (Ctrl+I) -->
<!-- Must show: (Ctrl+Shift+I) -->

<!-- ScribeResultPanel button order (lines 296-307) -->
<!-- Current visual order: Replace (replaceRef) then Insert (insertRef) -->
<!-- getFocusables returns: [insertRef, replaceRef, closeRef] -->
<!-- Fix: Swap visual order so Insert renders first (left), Replace second (right) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change shortcut to Ctrl+Shift+I and update tooltip</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js, src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx</files>
  <action>
In `plugins/onlyoffice-scribe/scripts/code.js`:

1. Line ~387: Change the shortcut condition from `(e.ctrlKey || e.metaKey) && e.key === "i"` to `(e.ctrlKey || e.metaKey) && e.shiftKey && e.key === "I"`. Note: when Shift is held, `e.key` is uppercase "I" not lowercase "i".
2. Apply the same change to the fallback handler at line ~399.
3. Update the log messages from "Ctrl+I" to "Ctrl+Shift+I".
4. IMPORTANT: This file must use ES5 syntax only (no arrow functions, no const/let, no template literals).

In `src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx`:

1. Line 70: Change the tooltip text from `(Ctrl+I)` to `(Ctrl+Shift+I)`.
  </action>
  <verify>
    <automated>grep -n "e.shiftKey" plugins/onlyoffice-scribe/scripts/code.js | head -5 && grep -n "Ctrl+Shift+I" src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx</automated>
  </verify>
  <done>Plugin registers Ctrl+Shift+I (not Ctrl+I) as the Scribe shortcut. Tooltip shows the updated shortcut.</done>
</task>

<task type="auto">
  <name>Task 2: Fix result panel button visual order (Insert left, Replace right)</name>
  <files>src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx</files>
  <action>
In `ScribeResultPanel.jsx`, in the success action buttons section (lines ~296-307):

1. Swap the render order of the two buttons so that `insertRef` (Insert button) renders FIRST (left) and `replaceRef` (Replace button) renders SECOND (right). Currently Replace renders first and Insert second.

The current code:
```jsx
<Buttons ref={replaceRef} variant="text" label={t('Scribe.button.replace')} onClick={onReplace} />
<Buttons ref={insertRef} label={t('Scribe.button.insert')} onClick={onInsert} />
```

Change to:
```jsx
<Buttons ref={insertRef} label={t('Scribe.button.insert')} onClick={onInsert} />
<Buttons ref={replaceRef} variant="text" label={t('Scribe.button.replace')} onClick={onReplace} />
```

This makes the visual order match the focus order (getFocusables already returns [insertRef, replaceRef, closeRef]), so Tab moves left-to-right naturally.

Do NOT change the getFocusables function or the handleKeyDown logic -- the focus order array is already correct, only the visual order was wrong.
  </action>
  <verify>
    <automated>grep -A4 "insertRef\|replaceRef" src/modules/views/OnlyOffice/Scribe/ScribeResultPanel.jsx | head -20</automated>
  </verify>
  <done>Insert button is visually left of Replace button. Tab moves Insert -> Replace -> Close (left-to-right). Shift+Tab reverses.</done>
</task>

</tasks>

<verification>
- `grep "shiftKey" plugins/onlyoffice-scribe/scripts/code.js` shows shiftKey check in shortcut handler
- `grep "Ctrl+Shift+I" src/modules/views/OnlyOffice/Scribe/ScribeFloatingButton.jsx` shows updated tooltip
- In ScribeResultPanel.jsx, the Insert button (`ref={insertRef}`) appears before the Replace button (`ref={replaceRef}`) in JSX
</verification>

<success_criteria>
1. Ctrl+Shift+I triggers Scribe, Ctrl+I does NOT trigger Scribe (allows OO italic)
2. Tooltip shows "Ctrl+Shift+I"
3. Result panel buttons render left-to-right: Insert, Replace, Close
4. Tab cycles: Insert -> Replace -> Close; Shift+Tab reverses
</success_criteria>

<output>
After completion, create `.planning/phases/14-navigation-clavier-micro-interactions/14-01-SUMMARY.md`
</output>
