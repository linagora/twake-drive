---
phase: v3.0-02-chat-core
plan: 02
type: execute
wave: 2
depends_on: [v3.0-02-01]
files_modified:
  - src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx
  - src/modules/views/OnlyOffice/Scribe/ScribePanel.jsx
autonomous: false
requirements: [CHAT-01, CHAT-03, CHAT-04]

must_haves:
  truths:
    - "Popover Scribe actions (rephrase, translate, etc.) write to the shared conversation history"
    - "When user opens the panel after a popover action, they see the action and its result in the chat"
    - "Panel chat and popover actions share the same message thread"
  artifacts:
    - path: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      provides: "Popover that writes user actions + AI responses to shared ScribeContext messages"
      contains: "addMessage"
    - path: "src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx"
      provides: "addMessage used by popover to inject action messages"
      contains: "addMessage"
  key_links:
    - from: "src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx"
      to: "ScribeContext.addMessage"
      via: "useScribe() hook"
      pattern: "addMessage"
---

<objective>
Refactor ScribePopover so its actions (rephrase, translate, summarize, etc.) write through the shared conversation in ScribeContext. The popover breadcrumb becomes the user message, and the AI result becomes the assistant message, making the panel chat a unified history of all Scribe interactions.

Purpose: Unify the popover and panel as two views of the same conversation, per user decision.
Output: Popover actions appear in the panel chat history. Panel shows all interactions regardless of origin.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v3.0-02-chat-core/v3.0-02-CONTEXT.md
@.planning/phases/v3.0-02-chat-core/v3.0-02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- ScribeContext extended API -->
From src/modules/views/OnlyOffice/Scribe/ScribeContext.jsx (after Plan 01):
```jsx
// value shape: { isPanelOpen, togglePanel, openPanel, closePanel, messages, isLoading, sendMessage, addMessage }
// addMessage(msg) -- appends a message { id, role, content, timestamp } to the conversation
// Message roles: 'user' | 'assistant' | 'error'
```

From src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx:
```jsx
// handleActionSelect(actionId, label, breadcrumb) is the core action handler
// breadcrumb: display string like "Reformuler > Anglais"
// Currently calls callScribeAI internally and manages its own state machine
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire popover actions into shared conversation history</name>
  <files>
    src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx
  </files>
  <action>
In ScribePopover.jsx, add conversation history integration:

1. Import `useScribe` from ScribeContext
2. Destructure `{ addMessage }` from `useScribe()` (with null-safe fallback: `const scribe = useScribe(); const addMessage = scribe?.addMessage`)
3. In `handleActionSelect`, after the AI call succeeds (where `setResult({ text, ...})` is called):
   - Call `addMessage({ id: Date.now(), role: 'user', content: breadcrumb, timestamp: new Date() })` -- the breadcrumb (e.g., "Reformuler > Anglais") serves as the user message per user decision
   - Call `addMessage({ id: Date.now() + 1, role: 'assistant', content: text, timestamp: new Date() })` -- the AI response
4. In the error catch block (where `setResult({ error: ... })` is called):
   - Call `addMessage({ id: Date.now(), role: 'user', content: breadcrumb, timestamp: new Date() })`
   - Call `addMessage({ id: Date.now() + 1, role: 'error', content: classified.messageKey ? t(classified.messageKey) : 'Error', timestamp: new Date() })`
5. Do NOT change the popover's own state machine (menu -> loading -> result flow stays intact). The popover continues to work exactly as before for its own UI. The addMessage calls are side effects that mirror the interaction into the shared conversation.
6. Guard addMessage calls with `if (addMessage)` so the popover still works if ScribeContext is not in the tree.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive-scribe-in-right-panel && node -e "
      const fs = require('fs');
      const pop = fs.readFileSync('src/modules/views/OnlyOffice/Scribe/ScribePopover.jsx','utf8');
      const checks = [
        ['imports useScribe', pop.includes('useScribe')],
        ['calls addMessage', pop.includes('addMessage')],
        ['uses breadcrumb as user message', pop.includes('breadcrumb') && pop.includes('addMessage')],
        ['null-safe addMessage guard', pop.includes('addMessage)') || pop.includes('addMessage &&') || pop.includes('if (addMessage')],
      ];
      const failed = checks.filter(c => !c[1]);
      if (failed.length) { console.log('FAILED:', failed.map(c=>c[0]).join(', ')); process.exit(1); }
      console.log('All checks passed');
    "
    </automated>
  </verify>
  <done>Popover actions write breadcrumb as user message and AI response as assistant message to shared ScribeContext conversation. Popover's own UI flow is unchanged. addMessage calls are null-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete chat core end-to-end</name>
  <action>Human verifies the full chat experience: panel chat with multi-turn AI, markdown rendering, and popover actions mirrored to chat history.</action>
  <what-built>Complete chat core: panel chat with multi-turn AI, markdown rendering, popover actions mirrored to chat history</what-built>
  <how-to-verify>
    1. Open the OO editor with Scribe enabled
    2. Open the Scribe panel -- should show empty welcome state
    3. Type "Hello, can you help me improve my writing?" and press Enter
    4. Verify: user message appears right-aligned, loading indicator shows, AI response appears with markdown
    5. Send a follow-up message -- verify conversation context is maintained (AI references prior exchange)
    6. Close the panel, reopen it -- verify conversation is still visible
    7. Now use the inline popover: select text, click floating button, choose "Correct grammar"
    8. After result shows in popover, open the panel
    9. Verify: the panel chat now shows the popover action ("Correct grammar" or its breadcrumb) as a user message and the AI result as an assistant message
    10. Test Shift+Enter creates newline in chat input
    11. Test that Send button is disabled when input is empty
  </how-to-verify>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>Full chat core verified: panel chat works with multi-turn AI, markdown rendering, popover integration, keyboard shortcuts, and loading states.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Popover action (e.g., "Correct grammar") creates entries in the panel chat
2. Panel chat shows the breadcrumb as user message and AI response as assistant message
3. Multi-turn context works across both popover and panel messages
4. Popover UI flow is unchanged (menu -> loading -> result still works)
5. Panel conversation persists across open/close
</verification>

<success_criteria>
- Popover Scribe actions mirror into the panel conversation
- Breadcrumb text appears as user message in chat
- AI result appears as assistant message in chat
- Popover's own state machine (menu/loading/result) is unchanged
- Full conversation history (panel + popover) available in the chat
</success_criteria>

<output>
After completion, create `.planning/phases/v3.0-02-chat-core/v3.0-02-02-SUMMARY.md`
</output>
