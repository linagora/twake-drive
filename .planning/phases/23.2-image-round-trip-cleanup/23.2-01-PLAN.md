---
phase: 23.2-image-round-trip-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/onlyoffice-scribe/scripts/code.js
  - scripts/oo-dev-setup.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "No console.log debug statements remain in code.js except the log() helper definition"
    - "getInlineDrawings wrapper is removed; apiRun.GetInlineDrawings() is called directly with graceful fallback"
    - "paragraphToMarkdown is clean — no dead branches or unused variables"
    - "oo-dev-setup.sh has clean comments for SDK patch mounting"
    - "Image round-trip still works end-to-end (extraction, preview, reinjection)"
  artifacts:
    - path: "plugins/onlyoffice-scribe/scripts/code.js"
      provides: "Cleaned plugin code without debug logs or dead wrappers"
    - path: "scripts/oo-dev-setup.sh"
      provides: "Clean dev setup script"
  key_links:
    - from: "paragraphToMarkdown"
      to: "apiRun.GetInlineDrawings()"
      via: "direct call with fallback check"
      pattern: "GetInlineDrawings"
---

<objective>
Clean up code.js and oo-dev-setup.sh after the image round-trip implementation (Phase 23).

Purpose: Remove debug logs, dead code, and trivial wrappers accumulated during exploratory development. The image round-trip is working — this makes the code maintainable.

Output: Clean code.js and oo-dev-setup.sh with no functional changes.
</objective>

<execution_context>
@/home/ben/.claude/get-shit-done/workflows/execute-plan.md
@/home/ben/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23.2-image-round-trip-cleanup/23.2-CONTEXT.md
@plugins/onlyoffice-scribe/scripts/code.js
@scripts/oo-dev-setup.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove debug logs and inline getInlineDrawings wrapper in code.js</name>
  <files>plugins/onlyoffice-scribe/scripts/code.js</files>
  <read_first>
    - plugins/onlyoffice-scribe/scripts/code.js (full file — 1694 lines)
  </read_first>
  <action>
**Debug log removal:**
- Remove the `console.log` on line 1124 (GetInlineDrawings NOT available fallback log) — keep the `return []` fallback behavior
- Remove the `console.log` on line 1140 (GetInlineDrawings found N drawings debug log) — the entire details-building loop (lines 1128-1141) is pure debug, remove it, keep only `return result`
- Remove the `console.log` on line 1265 (paragraphToMarkdown debug log for drawings) — the entire `if (hasScribeDrawings)` block wrapping the log at lines 1264-1266 should be removed; keep `return result` at line 1267
- Keep the `log()` helper function (line 32-34) and its existing usage in `castIntent` and `pasteInProgress` — those are operational logs, not debug

**Inline the getInlineDrawings wrapper (lines 1119-1143):**
The `getInlineDrawings(apiRun)` function is now just:
```
function getInlineDrawings(apiRun) {
  if (!apiRun.GetInlineDrawings) { return []; }
  // debug logging removed
  return apiRun.GetInlineDrawings();
}
```
This is a trivial pass-through. Replace each call site with inline code:

1. Line 1216: `var emptyRunDrawings = getInlineDrawings(el);`
   Replace with:
   ```
   var emptyRunDrawings = el.GetInlineDrawings ? el.GetInlineDrawings() : [];
   ```

2. Line 1230: `var inlineDrawings = getInlineDrawings(el);`
   Replace with:
   ```
   var inlineDrawings = el.GetInlineDrawings ? el.GetInlineDrawings() : [];
   ```

3. Delete the `getInlineDrawings` function definition entirely (lines 1119-1143 after debug removal).

**IMPORTANT: ES5 only** — no arrow functions, no const/let, no template literals.
**IMPORTANT: Do NOT modify** any of: image reinjection code (Copy approach), SetName extraction logic, MarkdownPreview, flattenTokens, getDrawingMarker, restoreImage.
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && grep -c "console.log" plugins/onlyoffice-scribe/scripts/code.js | grep -q "^1$" && echo "PASS: exactly 1 console.log (the log helper)" || echo "FAIL: wrong console.log count"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "console.log" plugins/onlyoffice-scribe/scripts/code.js returns exactly 1 (the log() helper on line ~33)
    - grep -c "function getInlineDrawings" plugins/onlyoffice-scribe/scripts/code.js returns 0
    - grep -c "GetInlineDrawings" plugins/onlyoffice-scribe/scripts/code.js returns at least 2 (the two direct call sites)
    - No const/let/arrow functions in the file (ES5 compliance)
  </acceptance_criteria>
  <done>All debug console.log statements removed except log() helper. getInlineDrawings wrapper inlined. No functional changes to extraction or injection logic.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up oo-dev-setup.sh comments</name>
  <files>scripts/oo-dev-setup.sh</files>
  <read_first>
    - scripts/oo-dev-setup.sh (full file — 198 lines)
  </read_first>
  <action>
Review `oo-dev-setup.sh` lines 48-56 (the patched SDK mounting section). The current code is already functional and clean. The CONTEXT.md mentions cleanup of "old comments/approaches" — review and ensure:

1. The comment on line 48 ("Patched SDK: mount non-minified sdkjs word bundles with GetInlineDrawings patch") is accurate and clear
2. The echo on line 54 ("Patched SDK: mounting non-minified word bundles") is concise
3. No stale TODOs or commented-out code remains in the SDK section

If the section is already clean (which it appears to be from inspection), no changes needed — document in summary that oo-dev-setup.sh was reviewed and found clean.

**Do NOT add** version checking (explicitly deferred per CONTEXT.md).
**Do NOT add** production deployment docs (explicitly deferred per CONTEXT.md).
  </action>
  <verify>
    <automated>cd /home/ben/Dev-local/cozy-drive && bash -n scripts/oo-dev-setup.sh && echo "PASS: script syntax valid" || echo "FAIL: syntax error"</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/oo-dev-setup.sh exits 0 (valid syntax)
    - No version check code added (deferred)
  </acceptance_criteria>
  <done>oo-dev-setup.sh reviewed, comments are accurate, no stale code found (or cleaned if needed).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -c "console.log" plugins/onlyoffice-scribe/scripts/code.js` returns exactly 1
2. `grep -c "function getInlineDrawings" plugins/onlyoffice-scribe/scripts/code.js` returns 0
3. `grep "GetInlineDrawings" plugins/onlyoffice-scribe/scripts/code.js` shows direct API calls with fallback
4. No ES5 violations: `grep -P "(const |let |=>)" plugins/onlyoffice-scribe/scripts/code.js` returns nothing
5. `bash -n scripts/oo-dev-setup.sh` passes
</verification>

<success_criteria>
- code.js has zero debug console.log statements (only the log() helper remains)
- getInlineDrawings wrapper removed, replaced with inline ternary fallback at each call site
- paragraphToMarkdown has no debug logging blocks
- oo-dev-setup.sh is syntactically valid with clean comments
- No functional changes to image round-trip behavior
</success_criteria>

<output>
After completion, create `.planning/phases/23.2-image-round-trip-cleanup/23.2-01-SUMMARY.md`
</output>
